---
phase: quick
plan: 260402-v3j
type: execute
wave: 1
depends_on: []
files_modified:
  - app/tasks/position_tasks.py
  - app/services/serp_parser_service.py
  - tests/test_position_tasks_engine.py
autonomous: true
requirements: [bugfix-engine-hardcode]
must_haves:
  truths:
    - "Position checks use kw.engine instead of hardcoded 'google'"
    - "Yandex keywords get engine='yandex' written to keyword_positions"
    - "Keywords with engine=None default to 'google'"
    - "SERP parser receives correct engine per keyword"
  artifacts:
    - path: "app/tasks/position_tasks.py"
      provides: "Engine-aware position writing"
      contains: "kw.engine"
    - path: "app/services/serp_parser_service.py"
      provides: "Engine passed from caller, not hardcoded"
    - path: "tests/test_position_tasks_engine.py"
      provides: "Unit tests for engine propagation"
  key_links:
    - from: "app/tasks/position_tasks.py"
      to: "app/services/position_service.py:write_position_sync"
      via: "engine parameter from kw.engine"
      pattern: "kw\\.engine"
    - from: "app/tasks/position_tasks.py"
      to: "app/services/serp_parser_service.py:parse_serp_sync"
      via: "engine parameter from kw.engine"
      pattern: "engine=.*kw\\.engine"
---

<objective>
Fix hardcoded "google" engine in position check tasks. Keywords imported from KeyCollector have engine="yandex" but positions are always written as engine="google" because `_check_via_dataforseo` and `_check_via_serp_parser` hardcode the engine string.

Purpose: Yandex keyword positions must be stored with the correct engine so analytics and position history are accurate.
Output: Patched position_tasks.py and serp_parser_service.py, plus unit tests.
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/tasks/position_tasks.py
@app/models/keyword.py (SearchEngine enum: google/yandex, line 67-69)
@app/services/position_service.py (write_position_sync takes engine: str)
@app/services/serp_parser_service.py (parse_serp_sync takes engine: str)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix hardcoded engine in position_tasks.py and serp_parser_service.py</name>
  <files>app/tasks/position_tasks.py, app/services/serp_parser_service.py, tests/test_position_tasks_engine.py</files>
  <behavior>
    - Test 1: _check_via_dataforseo passes kw.engine.value (not "google") to write_position_sync when kw.engine is set
    - Test 2: _check_via_dataforseo defaults to "google" when kw.engine is None
    - Test 3: _check_via_serp_parser passes kw.engine.value to both parse_serp_sync and write_position_sync
    - Test 4: _check_via_serp_parser defaults to "google" when kw.engine is None
  </behavior>
  <action>
    **In `app/tasks/position_tasks.py`:**

    1. Line 112-114 (`_check_via_dataforseo`): Replace hardcoded `"google"` with `kw.engine.value if kw.engine else "google"`:
       ```python
       engine_str = kw.engine.value if kw.engine else "google"
       write_position_sync(
           db, kw.id, uuid.UUID(site_id), engine_str, position, url=url
       )
       ```

    2. Line 142 (`_check_via_serp_parser`): Replace hardcoded `engine="google"` with keyword's engine:
       ```python
       engine_str = kw.engine.value if kw.engine else "google"
       serp_data = parse_serp_sync(kw.phrase, engine=engine_str)
       ```

    3. Line 153-154 (`_check_via_serp_parser`): Replace hardcoded `"google"` with the same engine_str variable:
       ```python
       write_position_sync(
           db, kw.id, uuid.UUID(site_id), engine_str, position, url=url
       )
       ```

    **In `tests/test_position_tasks_engine.py`:**

    Create unit tests that mock `get_sync_db`, `write_position_sync`, `parse_serp_sync`, and `fetch_serp_batch` to verify:
    - Engine value propagation for yandex keywords
    - Default to "google" for keywords with engine=None
    - Use `unittest.mock.patch` and create mock Keyword objects with `.engine` set to `SearchEngine.yandex`, `SearchEngine.google`, and `None`
  </action>
  <verify>
    <automated>cd /projects/test && python -m pytest tests/test_position_tasks_engine.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `_check_via_dataforseo` writes engine from kw.engine (or "google" default)
    - `_check_via_serp_parser` passes engine from kw.engine to parse_serp_sync and write_position_sync
    - All 4 test cases pass
  </done>
</task>

</tasks>

<verification>
- `grep -n '"google"' app/tasks/position_tasks.py` should show NO hardcoded "google" in write_position_sync or parse_serp_sync calls (only in fallback default expressions)
- `grep -n 'kw.engine' app/tasks/position_tasks.py` should show engine being read from keyword model in both functions
- All tests pass: `python -m pytest tests/test_position_tasks_engine.py -x -v`
</verification>

<success_criteria>
- Position tasks read engine from Keyword model instead of hardcoding "google"
- Keywords with engine=None gracefully default to "google"
- SERP parser receives the correct engine per keyword
- Unit tests confirm engine propagation for yandex, google, and None cases
</success_criteria>

<output>
After completion, create `.planning/quick/260402-v3j-fix-position-check-engine-bug/260402-v3j-SUMMARY.md`
</output>
