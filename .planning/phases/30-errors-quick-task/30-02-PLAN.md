---
phase: 30-errors-quick-task
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - app/routers/mobile.py
  - app/templates/mobile/errors/index.html
  - app/templates/mobile/errors/partials/errors_content.html
  - app/templates/mobile/errors/partials/section.html
  - app/templates/mobile/errors/partials/brief_form.html
  - app/templates/mobile/errors/partials/brief_result.html
  - app/templates/mobile/errors/partials/sync_progress.html
  - app/templates/base_mobile.html
autonomous: true
requirements:
  - ERR-01
  - ERR-02

must_haves:
  truths:
    - "User opens /m/errors and sees 3 error sections (Indexing/Crawl/Sanctions) with count badges"
    - "User selects a site from dropdown — sections reload via HTMX with that site's errors"
    - "User clicks Обновить — Celery sync task starts, HTMX polls every 3s, sections reload on done"
    - "User clicks Составить ТЗ on an error row — inline form expands via HTMX outerHTML swap"
    - "User submits inline brief form — SeoTask created with source_error_id FK, success confirmation shown inline"
    - "Bottom nav has Ошибки tab linking to /m/errors"
  artifacts:
    - path: "app/templates/mobile/errors/index.html"
      provides: "Main errors page with site dropdown, sync button, 3 sections"
      contains: "errors-content"
    - path: "app/templates/mobile/errors/partials/section.html"
      provides: "Single error section with rows and Показать все link"
      contains: "error-row"
    - path: "app/templates/mobile/errors/partials/brief_form.html"
      provides: "Inline brief form that replaces error row"
      contains: "hx-post"
    - path: "app/templates/mobile/errors/partials/sync_progress.html"
      provides: "HTMX polling partial for sync status"
      contains: "every 3s"
    - path: "app/routers/mobile.py"
      provides: "7 new endpoints for /m/errors/*"
      contains: "mobile_errors"
  key_links:
    - from: "app/routers/mobile.py"
      to: "app/services/yandex_errors_service.py"
      via: "list_errors, count_errors, get_error, last_fetched_at calls"
      pattern: "from app.services.yandex_errors_service import"
    - from: "app/routers/mobile.py"
      to: "app/tasks/yandex_errors_tasks.py"
      via: "sync_yandex_errors.delay() call"
      pattern: "sync_yandex_errors.delay"
    - from: "app/templates/mobile/errors/partials/brief_form.html"
      to: "app/routers/mobile.py"
      via: "hx-post=/m/errors/{error_id}/brief"
      pattern: "hx-post"
---

<objective>
Errors UI: /m/errors page with site dropdown, sync, 3 error sections, inline brief form, bottom nav tab.

Purpose: ERR-01 needs the errors page showing Yandex Webmaster errors grouped by type. ERR-02 needs the inline ТЗ creation flow from error rows. This plan creates all UI and router endpoints for the errors feature.
Output: 7 router endpoints, 6 Jinja2 templates, updated bottom nav.
</objective>

<execution_context>
@/opt/seo-platform/.claude/get-shit-done/workflows/execute-plan.md
@/opt/seo-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-errors-quick-task/30-CONTEXT.md
@.planning/phases/30-errors-quick-task/30-RESEARCH.md
@.planning/phases/30-errors-quick-task/30-UI-SPEC.md
@.planning/phases/30-errors-quick-task/30-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From app/services/yandex_errors_service.py:
```python
async def list_errors(db, site_id, error_type, status_filter=None, limit=5, offset=0) -> list[YandexError]
async def count_errors(db, site_id, error_type) -> int
async def get_error(db, error_id) -> YandexError | None
async def last_fetched_at(db, site_id) -> str | None
```

From app/models/yandex_errors.py:
```python
class YandexErrorType(str, PyEnum): indexing, crawl, sanction
class YandexErrorStatus(str, PyEnum): open, ignored, resolved
class YandexError(Base): id, site_id, error_type, subtype, url, title, detail, detected_at, fetched_at, status
```

From app/models/task.py:
```python
class TaskType(str, PyEnum): ..., yandex_indexing, yandex_crawl, yandex_sanction
class SeoTask(Base): ..., source_error_id (nullable FK)
```

From app/tasks/yandex_errors_tasks.py:
```python
sync_yandex_errors.delay(str(site_id))  # returns AsyncResult with task_id
# Stores result in Redis key yandex_sync:{task_id} = {"status": "done"|"error", "message": "..."}
```

From app/templates/base_mobile.html:
- Bottom nav: 4 tabs (Дайджест, Сайты, Позиции, Ещё)
- `active_tab` variable, `showToast(msg, type)` JS helper
- Extends pattern: `{% extends "base_mobile.html" %}`

From app/routers/mobile.py:
- `router = APIRouter(prefix="/m", tags=["mobile"])`
- Uses `user: User = Depends(get_current_user)`, `db: AsyncSession = Depends(get_db)`
- `mobile_templates = Jinja2Templates(directory="app/templates")`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Router endpoints for /m/errors + bottom nav update</name>
  <files>
    app/routers/mobile.py,
    app/templates/base_mobile.html
  </files>
  <read_first>
    app/routers/mobile.py,
    app/templates/base_mobile.html,
    app/services/yandex_errors_service.py,
    app/tasks/yandex_errors_tasks.py,
    app/models/yandex_errors.py,
    app/models/task.py,
    app/models/site.py
  </read_first>
  <action>
**1. Add 7 new endpoints to `app/routers/mobile.py`** (append after existing endpoints). Add imports at top:
```python
from app.services.yandex_errors_service import list_errors, count_errors, get_error, last_fetched_at
from app.models.yandex_errors import YandexErrorType, YandexErrorStatus
from app.tasks.yandex_errors_tasks import sync_yandex_errors
```

**Endpoint 1: `GET /m/errors`** — main page
```python
@router.get("/errors", response_class=HTMLResponse)
async def mobile_errors(
    request: Request,
    db: AsyncSession = Depends(get_db),
    user: User = Depends(get_current_user),
    site_id: str | None = None,
):
```
- Get all user's sites via `select(Site).where(Site.user_id == user.id).order_by(Site.domain)`.
- If no sites, render index.html with `sites=[]`, `has_sites=False`.
- Determine `selected_site_id`: from `site_id` query param, else from cookie `m_errors_site_id`, else first site.
- For selected site, fetch error counts (3x `count_errors`) and top-5 lists (3x `list_errors`). Also `last_fetched_at`.
- If request has `HX-Request` header, return only `errors_content.html` partial (HTMX swap from site dropdown).
- Else return full `mobile/errors/index.html` with context: `sites`, `selected_site_id`, `indexing_errors`, `crawl_errors`, `sanction_errors`, `indexing_count`, `crawl_count`, `sanction_count`, `last_synced`, `active_tab='errors'`.
- Set cookie `m_errors_site_id` on response.

**Endpoint 2: `POST /m/errors/sync`** — trigger Celery sync
```python
@router.post("/errors/sync", response_class=HTMLResponse)
async def mobile_errors_sync(
    request: Request,
    db: AsyncSession = Depends(get_db),
    user: User = Depends(get_current_user),
    site_id: str = Form(...),
):
```
- Call `sync_yandex_errors.delay(site_id)` — get `task_id = result.id`.
- Return `sync_progress.html` partial with `task_id`, `site_id`, initial state "running".

**Endpoint 3: `GET /m/errors/sync/status/{task_id}`** — HTMX polling for sync progress
```python
@router.get("/errors/sync/status/{task_id}", response_class=HTMLResponse)
async def mobile_errors_sync_status(
    request: Request,
    task_id: str,
    site_id: str,
    user: User = Depends(get_current_user),
):
```
- Check Redis key `yandex_sync:{task_id}` for status.
- If not found, check Celery AsyncResult state.
- Return `sync_progress.html` with current state. If done, include `hx-trigger="load"` + `hx-get="/m/errors?site_id={site_id}"` + `hx-target="#errors-content"` to reload sections.

**Endpoint 4: `GET /m/errors/content`** — reload sections (after sync done)
```python
@router.get("/errors/content", response_class=HTMLResponse)
async def mobile_errors_content(
    request: Request,
    db: AsyncSession = Depends(get_db),
    user: User = Depends(get_current_user),
    site_id: str = "",
):
```
- Fetch all 3 error types counts and top-5 lists. Return `errors_content.html` partial.

**Endpoint 5: `GET /m/errors/{error_type}/all`** — "Показать все" expand
```python
@router.get("/errors/{error_type}/all", response_class=HTMLResponse)
async def mobile_errors_show_all(
    request: Request,
    error_type: str,
    db: AsyncSession = Depends(get_db),
    user: User = Depends(get_current_user),
    site_id: str = "",
    page: int = 1,
):
```
- Validate error_type is one of "indexing", "crawl", "sanction".
- Fetch errors with `limit=20, offset=(page-1)*20`.
- Return `section.html` with `show_all=True`, `errors`, `total_count`, `page`, `has_more`.

**Endpoint 6: `GET /m/errors/{error_id}/brief/form`** — inline brief form (per D-09)
```python
@router.get("/errors/{error_id}/brief/form", response_class=HTMLResponse)
async def mobile_error_brief_form(
    request: Request,
    error_id: str,
    db: AsyncSession = Depends(get_db),
    user: User = Depends(get_current_user),
):
```
- Load error via `get_error(db, UUID(error_id))`. 404 if not found.
- Load user's projects: `select(Project).where(Project.user_id == user.id)`.
- Return `brief_form.html` with `error`, `projects`.

**Endpoint 7: `POST /m/errors/{error_id}/brief`** — create SeoTask from error (per D-08, D-09)
```python
@router.post("/errors/{error_id}/brief", response_class=HTMLResponse)
async def mobile_error_brief_submit(
    request: Request,
    error_id: str,
    db: AsyncSession = Depends(get_db),
    user: User = Depends(get_current_user),
    description: str = Form(""),
    priority: str = Form("p3"),
    project_id: str | None = Form(None),
):
```
- Load error. Map `error.error_type` to TaskType: `indexing→yandex_indexing`, `crawl→yandex_crawl`, `sanction→yandex_sanction`.
- Create SeoTask:
  - `site_id = error.site_id`
  - `url = error.url or ""`
  - `title = error.title`
  - `description = description`
  - `task_type = mapped TaskType`
  - `priority = TaskPriority(priority)`
  - `project_id = UUID(project_id) if project_id else None`
  - `source_error_id = error.id`
  - `status = TaskStatus.open`
- `db.add(task)`, `await db.commit()`.
- Return `brief_result.html` with `task_id=task.id`.

**2. Update `app/templates/base_mobile.html`** — replace "Ещё" button with "Ошибки" link tab per RESEARCH recommendation:

Replace the `<button>` for "Ещё" (the 4th nav item, around line 89) with:
```html
<a href="/m/errors" style="display:flex;flex-direction:column;align-items:center;gap:4px;min-height:44px;justify-content:center;text-decoration:none;padding:0 8px;{% if active_tab == 'errors' %}color:#818cf8;{% else %}color:white;{% endif %}">
  <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" width="24" height="24">
    <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126ZM12 15.75h.007v.008H12v-.008Z"/>
  </svg>
  <span style="font-size:0.75rem;{% if active_tab == 'errors' %}font-weight:600;{% endif %}">Ошибки</span>
</a>
```
This uses the Heroicons `exclamation-triangle` icon (24x24, stroke-width 1.5). Keep all other tabs exactly as is.
  </action>
  <verify>
    <automated>cd /opt/seo-platform && python -c "
from app.routers.mobile import router
routes = [r.path for r in router.routes]
assert '/errors' in routes or '/m/errors' in routes or any('errors' in str(r.path) for r in router.routes), f'Missing /errors route in {routes}'
print('Router OK')
" && grep -q 'errors' app/templates/base_mobile.html && echo "Nav OK"</automated>
  </verify>
  <acceptance_criteria>
    - app/routers/mobile.py contains `async def mobile_errors(`
    - app/routers/mobile.py contains `async def mobile_errors_sync(`
    - app/routers/mobile.py contains `async def mobile_errors_sync_status(`
    - app/routers/mobile.py contains `async def mobile_error_brief_form(`
    - app/routers/mobile.py contains `async def mobile_error_brief_submit(`
    - app/routers/mobile.py contains `sync_yandex_errors.delay(`
    - app/routers/mobile.py contains `from app.services.yandex_errors_service import`
    - app/routers/mobile.py contains `source_error_id`
    - app/routers/mobile.py contains `m_errors_site_id` (cookie name)
    - app/templates/base_mobile.html contains `/m/errors`
    - app/templates/base_mobile.html contains `active_tab == 'errors'`
  </acceptance_criteria>
  <done>7 router endpoints handle /m/errors page, sync, polling, inline brief form, brief submit. Bottom nav updated with Ошибки tab replacing Ещё.</done>
</task>

<task type="auto">
  <name>Task 2: Jinja2 templates for errors page and partials</name>
  <files>
    app/templates/mobile/errors/index.html,
    app/templates/mobile/errors/partials/errors_content.html,
    app/templates/mobile/errors/partials/section.html,
    app/templates/mobile/errors/partials/brief_form.html,
    app/templates/mobile/errors/partials/brief_result.html,
    app/templates/mobile/errors/partials/sync_progress.html
  </files>
  <read_first>
    app/templates/base_mobile.html,
    app/templates/mobile/tools/partials/tool_progress.html,
    app/templates/mobile/positions.html,
    .planning/phases/30-errors-quick-task/30-UI-SPEC.md
  </read_first>
  <action>
Create directory structure: `app/templates/mobile/errors/` and `app/templates/mobile/errors/partials/`.

**1. `app/templates/mobile/errors/index.html`** — main page per UI-SPEC Screen 1:
```html
{% extends "base_mobile.html" %}
{% block title %}Ошибки сайта{% endblock %}
{% block content %}
<div style="padding:16px;padding-bottom:64px;">
  <h1 class="text-xl font-semibold mb-4">Ошибки сайта</h1>

  {% if not has_sites %}
  <div class="bg-white rounded-lg p-4 text-sm text-gray-400 text-center">
    Нет сайтов. Добавьте сайт в полной версии платформы.
  </div>
  {% else %}
  <!-- Site dropdown -->
  <select name="site_id" class="w-full min-h-[44px] border border-gray-300 rounded-lg px-3 py-2 text-sm mb-4"
    hx-get="/m/errors" hx-target="#errors-content" hx-swap="innerHTML"
    hx-include="this" hx-push-url="false">
    {% for site in sites %}
    <option value="{{ site.id }}" {% if site.id|string == selected_site_id|string %}selected{% endif %}>{{ site.domain }}</option>
    {% endfor %}
  </select>

  <!-- Sync row -->
  <div class="flex items-center justify-between mb-4">
    <span class="text-xs text-gray-400">
      {% if last_synced %}Последнее обновление: {{ last_synced }}{% else %}Ещё не синхронизировано{% endif %}
    </span>
    <button hx-post="/m/errors/sync" hx-include="[name=site_id]" hx-target="#sync-progress" hx-swap="innerHTML"
      class="border border-gray-300 text-gray-700 bg-white rounded-lg px-4 min-h-[44px] text-sm font-semibold">
      Обновить
    </button>
  </div>

  <!-- Sync progress slot -->
  <div id="sync-progress" aria-live="polite"></div>

  <!-- Error sections -->
  <div id="errors-content">
    {% include "mobile/errors/partials/errors_content.html" %}
  </div>
  {% endif %}
</div>
{% endblock %}
```

**2. `app/templates/mobile/errors/partials/errors_content.html`** — 3 sections:
Render 3 includes of `section.html` with variables:
- Section 1: `section_title="Ошибки индексации"`, `section_type="indexing"`, `errors=indexing_errors`, `count=indexing_count`, badge color `bg-blue-100 text-blue-700`, icon = `document-magnifying-glass` SVG (Heroicons 20x20)
- Section 2: `section_title="Ошибки краулинга"`, `section_type="crawl"`, `errors=crawl_errors`, `count=crawl_count`, badge color `bg-orange-100 text-orange-700`, icon = `arrow-path` SVG
- Section 3: `section_title="Санкции"`, `section_type="sanction"`, `errors=sanction_errors`, `count=sanction_count`, badge color `bg-red-100 text-red-700`, icon = `shield-exclamation` SVG

Each section separated by `mb-8` (32px vertical rhythm per UI-SPEC).

If `host_not_linked` is True, show instead: `<div class="bg-white rounded-lg p-4 text-sm text-gray-500">Хост не привязан к Yandex Webmaster. Верифицируйте сайт и нажмите Обновить.</div>`.

**3. `app/templates/mobile/errors/partials/section.html`** — single error section:
```html
<div class="mb-8">
  <!-- Section heading -->
  <div class="flex items-center justify-between mb-3">
    <div class="flex items-center">
      <span class="text-sm font-semibold">{{ section_title }}</span>
      <span class="{{ badge_class }} text-xs font-semibold px-2 py-0.5 rounded-full ml-2">{{ count }}</span>
    </div>
    <!-- Section icon (20x20) -->
    {{ section_icon|safe }}
  </div>

  {% if errors|length == 0 %}
  <div class="bg-white rounded-lg p-4 text-sm text-gray-400 text-center">Ошибок не обнаружено</div>
  {% else %}
  <div class="bg-white rounded-lg divide-y divide-gray-100 error-list">
    {% for error in errors %}
    <div class="error-row p-3 min-h-[44px] flex items-center justify-between">
      <div class="flex-1 min-w-0">
        <span class="text-sm truncate block">{{ error.title }}</span>
        {% if error.url %}
        <span class="text-xs text-gray-400 truncate block">{{ error.url }}</span>
        {% endif %}
      </div>
      <button
        hx-get="/m/errors/{{ error.id }}/brief/form"
        hx-target="closest .error-row"
        hx-swap="outerHTML"
        class="text-xs font-semibold text-indigo-600 underline min-h-[44px] px-2 flex items-center shrink-0">
        Составить ТЗ
      </button>
    </div>
    {% endfor %}
  </div>

  {% if count > 5 and not show_all %}
  <a hx-get="/m/errors/{{ section_type }}/all?site_id={{ selected_site_id }}&page=1"
     hx-target="closest .mb-8 .error-list"
     hx-swap="innerHTML"
     class="text-sm text-indigo-600 underline mt-2 block text-right cursor-pointer">
    Показать все ({{ count }})
  </a>
  {% endif %}

  {% if show_all and has_more %}
  <a hx-get="/m/errors/{{ section_type }}/all?site_id={{ selected_site_id }}&page={{ page + 1 }}"
     hx-target="closest .error-list"
     hx-swap="beforeend"
     class="text-sm text-indigo-600 underline mt-2 block text-right cursor-pointer">
    Загрузить ещё
  </a>
  {% endif %}
  {% endif %}
</div>
```

**4. `app/templates/mobile/errors/partials/brief_form.html`** — inline form per D-09, UI-SPEC Screen 2:
```html
<div class="bg-white rounded-lg p-4 space-y-3 border-l-2 border-indigo-600" aria-live="polite">
  <div class="text-sm font-semibold text-gray-700 mb-2">{{ error.title }}</div>
  <form hx-post="/m/errors/{{ error.id }}/brief" hx-target="closest div[aria-live]" hx-swap="outerHTML">
    <div class="mb-3">
      <label class="text-xs text-gray-500 mb-1 block">Описание работ</label>
      <textarea name="description" class="w-full min-h-[100px] border border-gray-300 rounded-lg px-3 py-2 text-sm" placeholder="Опишите, что нужно исправить..."></textarea>
    </div>
    <div class="mb-3">
      <label class="text-xs text-gray-500 mb-1 block">Приоритет</label>
      <div class="flex gap-3">
        {% for p, label, color in [("p1","P1","red"),("p2","P2","orange"),("p3","P3","gray"),("p4","P4","blue")] %}
        <label class="cursor-pointer">
          <input type="radio" name="priority" value="{{ p }}" class="sr-only peer" {% if p == "p3" %}checked{% endif %}>
          <span class="px-3 py-1 rounded-full text-xs font-semibold border-2 block
            peer-checked:bg-{{ color }}-100 peer-checked:border-{{ color }}-500 peer-checked:text-{{ color }}-700
            border-gray-200 text-gray-400">{{ label }}</span>
        </label>
        {% endfor %}
      </div>
    </div>
    <div class="mb-3">
      <select name="project_id" class="w-full min-h-[44px] border border-gray-300 rounded-lg px-3 py-2 text-sm">
        <option value="">— Проект (необязательно) —</option>
        {% for project in projects %}
        <option value="{{ project.id }}">{{ project.name }}</option>
        {% endfor %}
      </select>
    </div>
    <div class="flex gap-2">
      <button type="submit" class="flex-1 min-h-[44px] rounded-lg text-white font-semibold text-sm" style="background:#4f46e5;">Сохранить</button>
      <button type="button" hx-get="/m/errors/{{ error.id }}/row" hx-target="closest div[aria-live]" hx-swap="outerHTML"
        class="flex-1 min-h-[44px] rounded-lg text-sm font-semibold text-gray-600 bg-gray-100">Отмена</button>
    </div>
  </form>
</div>
```

Note: The "Отмена" button should restore the original error row. Add an endpoint `GET /m/errors/{error_id}/row` in the router that returns just the error-row div (single row from `section.html` pattern). OR use a simpler approach: make "Отмена" reload the full page via `window.location.reload()` with `onclick`. Choose the simpler approach: `<button type="button" onclick="window.location.reload()" ...>Отмена</button>`.

**5. `app/templates/mobile/errors/partials/brief_result.html`** — success confirmation:
```html
<div class="bg-green-50 rounded-lg p-3 flex items-center gap-2" aria-live="polite">
  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" width="20" height="20" class="text-green-600 shrink-0">
    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.857-9.809a.75.75 0 00-1.214-.882l-3.483 4.79-1.88-1.88a.75.75 0 10-1.06 1.06l2.5 2.5a.75.75 0 001.137-.089l4-5.5z" clip-rule="evenodd"/>
  </svg>
  <span class="text-sm text-green-800">ТЗ создано — <a href="/ui/tasks/{{ task_id }}" class="text-indigo-600 underline text-sm">открыть</a></span>
</div>
```

**6. `app/templates/mobile/errors/partials/sync_progress.html`** — polling partial (same as tool_progress.html pattern):
```html
{% if sync_status == "running" %}
<div class="flex items-center gap-2 p-3 bg-white rounded-lg mb-4"
  hx-get="/m/errors/sync/status/{{ task_id }}?site_id={{ site_id }}"
  hx-trigger="every 3s"
  hx-swap="outerHTML">
  <svg class="animate-spin h-4 w-4 text-indigo-600" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
    <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"/>
    <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"/>
  </svg>
  <span class="text-sm text-gray-600">Синхронизируем ошибки...</span>
</div>
{% elif sync_status == "done" %}
<div class="flex items-center gap-2 p-3 bg-green-50 rounded-lg mb-4"
  hx-get="/m/errors/content?site_id={{ site_id }}"
  hx-trigger="load"
  hx-target="#errors-content"
  hx-swap="innerHTML">
  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" width="20" height="20" class="text-green-600">
    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.857-9.809a.75.75 0 00-1.214-.882l-3.483 4.79-1.88-1.88a.75.75 0 10-1.06 1.06l2.5 2.5a.75.75 0 001.137-.089l4-5.5z" clip-rule="evenodd"/>
  </svg>
  <span class="text-sm text-green-800">Обновлено</span>
</div>
{% elif sync_status == "error" %}
<div class="flex items-center gap-2 p-3 bg-red-50 rounded-lg mb-4">
  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" width="20" height="20" class="text-red-600">
    <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-8-5a.75.75 0 01.75.75v4.5a.75.75 0 01-1.5 0v-4.5A.75.75 0 0110 5zm0 10a1 1 0 100-2 1 1 0 000 2z" clip-rule="evenodd"/>
  </svg>
  <span class="text-sm text-red-800">Ошибка синхронизации. Попробуйте ещё раз.</span>
</div>
{% endif %}
```

HTMX polling stops in done/error branches (no `hx-trigger="every 3s"` in those branches). Done branch triggers section reload via `hx-trigger="load"`.
  </action>
  <verify>
    <automated>cd /opt/seo-platform && test -f app/templates/mobile/errors/index.html && test -f app/templates/mobile/errors/partials/errors_content.html && test -f app/templates/mobile/errors/partials/section.html && test -f app/templates/mobile/errors/partials/brief_form.html && test -f app/templates/mobile/errors/partials/brief_result.html && test -f app/templates/mobile/errors/partials/sync_progress.html && echo "All templates exist"</automated>
  </verify>
  <acceptance_criteria>
    - app/templates/mobile/errors/index.html contains `extends "base_mobile.html"`
    - app/templates/mobile/errors/index.html contains `id="errors-content"`
    - app/templates/mobile/errors/index.html contains `id="sync-progress"`
    - app/templates/mobile/errors/index.html contains `hx-post="/m/errors/sync"`
    - app/templates/mobile/errors/index.html contains `name="site_id"`
    - app/templates/mobile/errors/partials/errors_content.html contains `section_title`
    - app/templates/mobile/errors/partials/section.html contains `error-row`
    - app/templates/mobile/errors/partials/section.html contains `hx-get="/m/errors/`
    - app/templates/mobile/errors/partials/section.html contains `Составить ТЗ`
    - app/templates/mobile/errors/partials/section.html contains `Показать все`
    - app/templates/mobile/errors/partials/brief_form.html contains `hx-post="/m/errors/`
    - app/templates/mobile/errors/partials/brief_form.html contains `name="priority"`
    - app/templates/mobile/errors/partials/brief_form.html contains `name="description"`
    - app/templates/mobile/errors/partials/brief_result.html contains `ТЗ создано`
    - app/templates/mobile/errors/partials/brief_result.html contains `bg-green-50`
    - app/templates/mobile/errors/partials/sync_progress.html contains `every 3s`
    - app/templates/mobile/errors/partials/sync_progress.html contains `Синхронизируем ошибки`
  </acceptance_criteria>
  <done>6 Jinja2 templates implement the full /m/errors UI: page layout, 3 error sections, inline brief form with priority radio, success confirmation, and HTMX polling for sync progress</done>
</task>

</tasks>

<verification>
- `curl -s http://localhost:8000/m/errors` returns 200 (or 302 if auth required — test with session cookie)
- All 6 template files exist in `app/templates/mobile/errors/`
- `grep -q "errors" app/templates/base_mobile.html` confirms bottom nav update
- `grep -c "async def mobile_error" app/routers/mobile.py` returns 5+ (multiple error endpoints)
</verification>

<success_criteria>
- /m/errors page renders with site dropdown, sync button, 3 error sections per D-06
- Site dropdown change triggers HTMX reload of sections per D-07
- Sync button triggers Celery task with HTMX polling per D-03
- "Составить ТЗ" button opens inline form per D-09
- Inline form submit creates SeoTask with source_error_id and shows success confirmation per D-08
- Bottom nav shows "Ошибки" tab with exclamation-triangle icon
</success_criteria>

<output>
After completion, create `.planning/phases/30-errors-quick-task/30-02-SUMMARY.md`
</output>
