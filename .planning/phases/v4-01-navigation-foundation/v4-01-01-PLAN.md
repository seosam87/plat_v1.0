---
phase: v4-01-navigation-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/base.html
  - app/templates/components/sidebar.html
  - app/templates/components/breadcrumb.html
  - app/templates/components/site_selector.html
  - app/navigation.py
  - app/main.py
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05, NAV-06, VIS-01, VIS-03]

must_haves:
  truths:
    - "User sees a vertical sidebar with 6 named sections, each with icon and collapsible sub-items"
    - "User selects a site in the sticky site selector and the selection persists across page changes"
    - "Current sidebar section and sub-item are visually highlighted"
    - "User sees a breadcrumb trail on every page"
    - "All existing pages render with new sidebar layout"
    - "Sidebar collapses to icons on screens < 1024px and shows as overlay on mobile < 768px"
  artifacts:
    - path: "app/templates/base.html"
      provides: "New sidebar layout replacing top nav"
      contains: "sidebar"
    - path: "app/templates/components/sidebar.html"
      provides: "Sidebar navigation component with 6 sections"
      contains: "nav_sections"
    - path: "app/templates/components/breadcrumb.html"
      provides: "Breadcrumb trail component"
      contains: "breadcrumb"
    - path: "app/templates/components/site_selector.html"
      provides: "Sticky site selector dropdown"
      contains: "selected_site"
    - path: "app/navigation.py"
      provides: "Navigation config and active-state logic"
      contains: "NAV_SECTIONS"
    - path: "app/main.py"
      provides: "Navigation context injection middleware"
      contains: "nav_context"
  key_links:
    - from: "app/templates/base.html"
      to: "app/templates/components/sidebar.html"
      via: "Jinja2 include"
      pattern: "include.*sidebar"
    - from: "app/templates/components/sidebar.html"
      to: "app/navigation.py"
      via: "nav_sections template variable"
      pattern: "nav_sections"
    - from: "app/main.py"
      to: "app/navigation.py"
      via: "middleware imports nav config"
      pattern: "from app.navigation import"
---

<objective>
Build the new sidebar-based layout that replaces the current top nav in base.html. This includes the sidebar component with 6 sections, sticky site selector with cookie persistence, breadcrumb system, active-state highlighting, and responsive behavior. All 34 existing templates that extend base.html will automatically inherit the new layout.

Purpose: Foundation for the entire v4.0 UI overhaul — every subsequent phase depends on this layout being in place.
Output: New base.html with sidebar, site selector, breadcrumbs, navigation config module, and context injection middleware.
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v4.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@app/templates/base.html
@app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Navigation config module and context injection middleware</name>
  <files>app/navigation.py, app/main.py</files>
  <read_first>app/main.py, app/templates/base.html</read_first>
  <action>
Create `app/navigation.py` with:

1. A `NAV_SECTIONS` list defining the sidebar structure. Each section is a dict with keys: `id` (str), `label` (str, Russian), `icon` (str, Heroicon SVG name), `url` (str or None), `admin_only` (bool), `children` (list of dicts with `id`, `label`, `url`). The 6 sections:

```python
NAV_SECTIONS = [
    {
        "id": "overview",
        "label": "Обзор",
        "icon": "chart-bar",
        "url": "/ui/dashboard",
        "admin_only": False,
        "children": [],
    },
    {
        "id": "sites",
        "label": "Сайты",
        "icon": "globe-alt",
        "url": None,
        "admin_only": False,
        "children": [
            {"id": "sites-list", "label": "Список сайтов", "url": "/ui/sites"},
            {"id": "sites-detail", "label": "Детали сайта", "url": "/ui/sites/{site_id}"},
            {"id": "sites-crawls", "label": "Краулы", "url": "/ui/sites/{site_id}/crawls"},
            {"id": "sites-schedules", "label": "Расписания", "url": "/ui/sites/{site_id}#schedules"},
        ],
    },
    {
        "id": "positions",
        "label": "Позиции и ключи",
        "icon": "key",
        "url": None,
        "admin_only": False,
        "children": [
            {"id": "keywords", "label": "Ключевые слова", "url": "/ui/keywords/{site_id}"},
            {"id": "positions-list", "label": "Позиции", "url": "/ui/positions/{site_id}"},
            {"id": "clusters", "label": "Кластеры", "url": "/ui/clusters/{site_id}"},
            {"id": "cannibalization", "label": "Каннибализация", "url": "/ui/cannibalization/{site_id}"},
            {"id": "intent", "label": "Интент", "url": "/intent/{site_id}"},
            {"id": "bulk", "label": "Массовые операции", "url": "/bulk/{site_id}"},
        ],
    },
    {
        "id": "analytics",
        "label": "Аналитика",
        "icon": "magnifying-glass",
        "url": None,
        "admin_only": False,
        "children": [
            {"id": "workspace", "label": "Воркспейс", "url": "/analytics/sites/{site_id}"},
            {"id": "gap", "label": "Gap-анализ", "url": "/gap/{site_id}"},
            {"id": "architecture", "label": "Архитектура", "url": "/architecture/{site_id}"},
            {"id": "metrika", "label": "Трафик (Metrika)", "url": "/ui/metrika/{site_id}"},
            {"id": "traffic-analysis", "label": "Анализ трафика", "url": "/traffic-analysis/{site_id}"},
            {"id": "competitors", "label": "Конкуренты", "url": "/ui/competitors/{site_id}"},
        ],
    },
    {
        "id": "content",
        "label": "Контент",
        "icon": "document-text",
        "url": None,
        "admin_only": False,
        "children": [
            {"id": "audit", "label": "Аудит", "url": "/audit/{site_id}"},
            {"id": "pipeline", "label": "Pipeline", "url": "/ui/pipeline/{site_id}"},
            {"id": "publish", "label": "Публикация", "url": "/ui/content-publish/{site_id}"},
            {"id": "projects", "label": "Проекты", "url": "/ui/projects"},
            {"id": "kanban", "label": "Kanban", "url": "/ui/tasks"},
            {"id": "content-plan", "label": "Контент-план", "url": "/ui/projects/{project_id}/plan"},
            {"id": "monitoring", "label": "Мониторинг", "url": "/monitoring/{site_id}"},
        ],
    },
    {
        "id": "settings",
        "label": "Настройки",
        "icon": "cog-6-tooth",
        "url": None,
        "admin_only": True,
        "children": [
            {"id": "users", "label": "Пользователи", "url": "/ui/admin/users"},
            {"id": "groups", "label": "Группы", "url": "/ui/admin/groups"},
            {"id": "datasources", "label": "Источники данных", "url": "/ui/admin/datasources"},
            {"id": "proxy", "label": "Прокси", "url": "/ui/admin/settings"},
            {"id": "parameters", "label": "Параметры", "url": "/ui/admin/issues"},
            {"id": "audit-log", "label": "Журнал аудита", "url": "/ui/admin/audit"},
        ],
    },
]
```

2. A `URL_TO_NAV` dict mapping URL path patterns to `(section_id, child_id)` tuples. Build this by iterating NAV_SECTIONS and creating regex patterns from the URL templates (replacing `{site_id}` and `{project_id}` with `\d+`). This enables active-state highlighting.

3. A function `resolve_nav_context(request_path: str) -> dict` that returns:
   - `active_section`: the section id that matches the current path
   - `active_child`: the child id (or None for top-level sections)
   - `breadcrumb_section`: the section label
   - `breadcrumb_child`: the child label (or None)

4. A function `build_sidebar_sections(site_id: int | None, is_admin: bool) -> list[dict]` that takes the selected site_id, replaces `{site_id}` placeholders in URLs with the actual ID (or "#" if None), and filters out `admin_only` sections for non-admin users. Returns the sections list ready for template rendering.

Then modify `app/main.py`:

5. In the `_HelpAwareTemplates.TemplateResponse` method, inject navigation context into every template response:
   - Import `resolve_nav_context` and `build_sidebar_sections` from `app.navigation`
   - Get `selected_site_id` from `request.cookies.get("selected_site_id")` — parse as int or None
   - Call `resolve_nav_context(request.url.path)` to get active state
   - Call `build_sidebar_sections(selected_site_id, is_admin)` — for is_admin, check if JWT payload has admin role (use try/except around decode, default False)
   - Inject into context: `nav_sections`, `active_section`, `active_child`, `breadcrumb_section`, `breadcrumb_child`, `selected_site_id`

6. Add a new route `GET /ui/api/sites` that returns an HTML fragment (not JSON) of site `<option>` elements for the site selector dropdown. Query all sites from DB, return `<option value="{id}">{name}</option>` for each. This is used by the site selector via HTMX on page load.

7. Add a new route `POST /ui/api/select-site` that reads `site_id` from form body, sets a cookie `selected_site_id` with value `{site_id}`, `path=/`, `max_age=31536000` (1 year), `samesite=lax`, and returns 204. This is called by the site selector dropdown on change via HTMX.

Per VIS-03: Use indigo (#4f46e5) as the sidebar active-state color, emerald (#059669) for success badges, consistent with existing palette.
  </action>
  <verify>
    <automated>cd /projects/test && python -c "from app.navigation import NAV_SECTIONS, resolve_nav_context, build_sidebar_sections; ctx = resolve_nav_context('/ui/keywords/1'); assert ctx['active_section'] == 'positions', ctx; assert ctx['active_child'] == 'keywords', ctx; secs = build_sidebar_sections(1, True); assert len(secs) == 6; secs2 = build_sidebar_sections(1, False); assert len(secs2) == 5; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "NAV_SECTIONS" app/navigation.py` succeeds
    - `grep -q "resolve_nav_context" app/navigation.py` succeeds
    - `grep -q "build_sidebar_sections" app/navigation.py` succeeds
    - `grep -q "selected_site_id" app/main.py` succeeds
    - `grep -q "nav_sections" app/main.py` succeeds
    - `grep -q "/ui/api/sites" app/main.py` succeeds
    - `grep -q "/ui/api/select-site" app/main.py` succeeds
    - NAV_SECTIONS has exactly 6 top-level entries
    - Settings section has admin_only=True
    - resolve_nav_context returns correct active_section for known paths
  </acceptance_criteria>
  <done>Navigation module defines all 6 sections with children, URL-to-nav mapping resolves active states, main.py injects nav context into all template responses, site selector API endpoints exist</done>
</task>

<task type="auto">
  <name>Task 2: New base.html with sidebar layout, site selector, breadcrumbs, and responsive behavior</name>
  <files>app/templates/base.html, app/templates/components/sidebar.html, app/templates/components/site_selector.html, app/templates/components/breadcrumb.html</files>
  <read_first>app/templates/base.html, app/navigation.py, app/main.py</read_first>
  <action>
Create `app/templates/components/` directory and the three component templates, then replace `app/templates/base.html`.

**app/templates/components/site_selector.html:**
A sticky site selector at the top of the sidebar. Structure:
- A `<div>` with class `site-selector` containing a `<select>` element
- The select has `id="site-selector"`, loads options via `hx-get="/ui/api/sites"` with `hx-trigger="load"` and `hx-swap="innerHTML"`
- On change, POST to `/ui/api/select-site` with the selected site_id via HTMX: `hx-post="/ui/api/select-site"` `hx-vals="js:{site_id: document.getElementById('site-selector').value}"` `hx-trigger="change"` `hx-swap="none"`
- After successful site change, reload the current page by adding `hx-on::after-request="if(event.detail.successful) window.location.reload()"` to the select
- Style: full width, indigo-50 background, border-indigo-200, rounded, padding 0.5rem
- The currently selected_site_id (from template context) should be set as selected after options load. Add a small `<script>` that after htmx:afterSettle on the select, sets `select.value = '{{ selected_site_id or "" }}'`

**app/templates/components/breadcrumb.html:**
- A `<nav aria-label="Breadcrumb">` with an `<ol>` styled as horizontal flex with Tailwind-like inline styles
- Renders breadcrumb segments: Home icon link to `/ui/dashboard`, then `breadcrumb_section` (if set), then `breadcrumb_child` (if set)
- Segments separated by `>` chevron character
- Style: text-sm, text-gray-500, links in indigo-600

**app/templates/components/sidebar.html:**
The main sidebar navigation component. Structure:
- Outer `<aside>` with id `sidebar`, classes for width (w-64 on desktop), bg color (#1e1b4b — indigo-950), text white, fixed height (h-screen), fixed position, flex column
- Contains: site selector include at top, then scrollable nav section, then footer with logout link
- Nav sections: iterate over `nav_sections` variable. For each section:
  - A clickable section header with icon (inline SVG from Heroicons outline set — use the actual SVG path data for: chart-bar, globe-alt, key, magnifying-glass, document-text, cog-6-tooth) + label
  - If section has children: a collapsible `<ul>` (hidden by default, visible if `active_section == section.id`). Toggle via Alpine.js-free vanilla JS: clicking the header toggles a `data-open` attribute
  - Each child `<li>` contains an `<a>` with href to the child URL
  - Active state: if `active_section == section.id`, the section header gets bg-indigo-800. If `active_child == child.id`, the child link gets bg-indigo-700 and font-semibold
  - Section without children (Обзор): the header itself is a link
- Footer: a div at the bottom with logout link (`/ui/logout`) styled as text-indigo-300

Responsive behavior (per NAV-03):
- Desktop (>= 1024px): sidebar is visible at w-64, main content has margin-left: 16rem
- Tablet (768-1023px): sidebar collapses to w-16 showing only icons, expand on hover. Add `onmouseenter`/`onmouseleave` JS to toggle between w-16 and w-64
- Mobile (< 768px): sidebar is hidden by default (translate-x: -100%), shown as overlay when hamburger button clicked. Add a hamburger button visible only on mobile in the top bar area

Icon SVG paths (Heroicons outline 24x24 viewBox, use `<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="sidebar-icon" style="width:1.25rem;height:1.25rem;flex-shrink:0">`):
- chart-bar: `<path stroke-linecap="round" stroke-linejoin="round" d="M3 13.125C3 12.504 3.504 12 4.125 12h2.25c.621 0 1.125.504 1.125 1.125v6.75C7.5 20.496 6.996 21 6.375 21h-2.25A1.125 1.125 0 0 1 3 19.875v-6.75ZM9.75 8.625c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125v11.25c0 .621-.504 1.125-1.125 1.125h-2.25a1.125 1.125 0 0 1-1.125-1.125V8.625ZM16.5 4.125c0-.621.504-1.125 1.125-1.125h2.25C20.496 3 21 3.504 21 4.125v15.75c0 .621-.504 1.125-1.125 1.125h-2.25a1.125 1.125 0 0 1-1.125-1.125V4.125Z" />`
- globe-alt: `<path stroke-linecap="round" stroke-linejoin="round" d="M12 21a9.004 9.004 0 0 0 8.716-6.747M12 21a9.004 9.004 0 0 1-8.716-6.747M12 21c2.485 0 4.5-4.03 4.5-9S14.485 3 12 3m0 18c-2.485 0-4.5-4.03-4.5-9S9.515 3 12 3m0 0a8.997 8.997 0 0 1 7.843 4.582M12 3a8.997 8.997 0 0 0-7.843 4.582m15.686 0A11.953 11.953 0 0 1 12 10.5c-2.998 0-5.74-1.1-7.843-2.918m15.686 0A8.959 8.959 0 0 1 21 12c0 .778-.099 1.533-.284 2.253m0 0A17.919 17.919 0 0 1 12 16.5a17.92 17.92 0 0 1-8.716-2.247m0 0A9.015 9.015 0 0 1 3 12c0-1.605.42-3.113 1.157-4.418" />`
- key: `<path stroke-linecap="round" stroke-linejoin="round" d="M15.75 5.25a3 3 0 0 1 3 3m3 0a6 6 0 0 1-7.029 5.912c-.563-.097-1.159.026-1.563.43L10.5 17.25H8.25v2.25H6v2.25H2.25v-2.818c0-.597.237-1.17.659-1.591l6.499-6.499c.404-.404.527-1 .43-1.563A6 6 0 1 1 21.75 8.25Z" />`
- magnifying-glass: `<path stroke-linecap="round" stroke-linejoin="round" d="m21 21-5.197-5.197m0 0A7.5 7.5 0 1 0 5.196 5.196a7.5 7.5 0 0 0 10.607 10.607Z" />`
- document-text: `<path stroke-linecap="round" stroke-linejoin="round" d="M19.5 14.25v-2.625a3.375 3.375 0 0 0-3.375-3.375h-1.5A1.125 1.125 0 0 1 13.5 7.125v-1.5a3.375 3.375 0 0 0-3.375-3.375H8.25m0 12.75h7.5m-7.5 3H12M10.5 2.25H5.625c-.621 0-1.125.504-1.125 1.125v17.25c0 .621.504 1.125 1.125 1.125h12.75c.621 0 1.125-.504 1.125-1.125V11.25a9 9 0 0 0-9-9Z" />`
- cog-6-tooth: `<path stroke-linecap="round" stroke-linejoin="round" d="M9.594 3.94c.09-.542.56-.94 1.11-.94h2.593c.55 0 1.02.398 1.11.94l.213 1.281c.063.374.313.686.645.87.074.04.147.083.22.127.325.196.72.257 1.075.124l1.217-.456a1.125 1.125 0 0 1 1.37.49l1.296 2.247a1.125 1.125 0 0 1-.26 1.431l-1.003.827c-.293.241-.438.613-.43.992a7.723 7.723 0 0 1 0 .255c-.008.378.137.75.43.991l1.004.827c.424.35.534.955.26 1.43l-1.298 2.247a1.125 1.125 0 0 1-1.369.491l-1.217-.456c-.355-.133-.75-.072-1.076.124a6.47 6.47 0 0 1-.22.128c-.331.183-.581.495-.644.869l-.213 1.281c-.09.543-.56.94-1.11.94h-2.594c-.55 0-1.019-.398-1.11-.94l-.213-1.281c-.062-.374-.312-.686-.644-.87a6.52 6.52 0 0 1-.22-.127c-.325-.196-.72-.257-1.076-.124l-1.217.456a1.125 1.125 0 0 1-1.369-.49l-1.297-2.247a1.125 1.125 0 0 1 .26-1.431l1.004-.827c.292-.24.437-.613.43-.991a6.932 6.932 0 0 1 0-.255c.007-.38-.138-.751-.43-.992l-1.004-.827a1.125 1.125 0 0 1-.26-1.43l1.297-2.247a1.125 1.125 0 0 1 1.37-.491l1.216.456c.356.133.751.072 1.076-.124.072-.044.146-.086.22-.128.332-.183.582-.495.644-.869l.214-1.28Z" /><path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 1 1-6 0 3 3 0 0 1 6 0Z" />`

**app/templates/base.html** (complete replacement):
New layout structure:
```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% block title %}SEO Platform{% endblock %}</title>
  <script src="https://unpkg.com/htmx.org@2.0.3/dist/htmx.min.js"></script>
  <style>
    /* --- Reset & Base --- */
    body { font-family: system-ui, sans-serif; margin: 0; padding: 0; background: #f5f5f5; }

    /* --- Sidebar --- */
    #sidebar { ... w-64, fixed, h-screen, bg #1e1b4b, color white, flex column, z-index 40 ... }
    #sidebar .sidebar-section-header { ... padding, flex, align-items center, gap, cursor pointer, rounded, hover bg-indigo-800 ... }
    #sidebar .sidebar-section-header.active { background: #3730a3; /* indigo-800 */ }
    #sidebar .sidebar-child { ... pl-10, py-1.5, text-sm, rounded ... }
    #sidebar .sidebar-child.active { background: #4338ca; /* indigo-700 */ font-weight: 600; }
    #sidebar .sidebar-child:hover { background: #4338ca; }
    .site-selector select { ... full width, bg indigo-50, border indigo-200, rounded, padding ... }

    /* --- Main content --- */
    .main-content { margin-left: 16rem; padding: 1.5rem 2rem; min-height: 100vh; }

    /* --- Responsive --- */
    @media (max-width: 1023px) {
      #sidebar { width: 4rem; }  /* collapsed to icons */
      #sidebar .sidebar-label, #sidebar .sidebar-child-label, #sidebar .site-selector, #sidebar .sidebar-footer-text { display: none; }
      #sidebar:hover { width: 16rem; }
      #sidebar:hover .sidebar-label, #sidebar:hover .sidebar-child-label, #sidebar:hover .site-selector, #sidebar:hover .sidebar-footer-text { display: block; }
      .main-content { margin-left: 4rem; }
    }
    @media (max-width: 767px) {
      #sidebar { transform: translateX(-100%); width: 16rem; position: fixed; z-index: 50; transition: transform 0.3s; }
      #sidebar.mobile-open { transform: translateX(0); }
      .main-content { margin-left: 0; }
      .mobile-hamburger { display: flex !important; }
      .sidebar-overlay { display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.5); z-index: 45; }
      .sidebar-overlay.active { display: block; }
    }

    /* Preserve ALL existing styles from current base.html */
    .card { background: white; border-radius: 8px; padding: 1.5rem; box-shadow: 0 1px 3px rgba(0,0,0,.1); }
    table { width: 100%; border-collapse: collapse; }
    th, td { text-align: left; padding: .75rem 1rem; border-bottom: 1px solid #e5e7eb; }
    th { font-weight: 600; color: #6b7280; font-size: .875rem; text-transform: uppercase; }
    .badge { display: inline-block; padding: .2rem .6rem; border-radius: 9999px; font-size: .8rem; font-weight: 600; }
    .badge-connected { background: #d1fae5; color: #065f46; }
    .badge-failed { background: #fee2e2; color: #991b1b; }
    .badge-unknown { background: #f3f4f6; color: #6b7280; }
    .btn { padding: .4rem .9rem; border: none; border-radius: 4px; cursor: pointer; font-size: .875rem; }
    .btn-primary { background: #4f46e5; color: white; }
    .btn-sm { padding: .25rem .6rem; font-size: .8rem; }
    .btn-verify { background: #0ea5e9; color: white; }
    form.inline { display: inline; }
    .htmx-indicator { opacity: 0; }
    .htmx-request .htmx-indicator { opacity: 1; }
    .breadcrumbs { font-size:.85rem; color:#6b7280; margin-bottom:1rem; }
    .breadcrumbs a { color:#4f46e5; text-decoration:none; }
    .breadcrumbs a:hover { text-decoration:underline; }
    .breadcrumbs span { margin:0 .4rem; }
    #toast-container { position:fixed; bottom:1.5rem; right:1.5rem; z-index:999; display:flex; flex-direction:column; gap:.5rem; }
    .toast { ... same as current ... }
    .toast-success { background:#ecfdf5; color:#065f46; border:1px solid #a7f3d0; }
    .toast-error { background:#fef2f2; color:#991b1b; border:1px solid #fecaca; }
    .toast-info { background:#eff6ff; color:#1e40af; border:1px solid #bfdbfe; }
    @keyframes toastIn { from{opacity:0;transform:translateY(1rem)} to{opacity:1;transform:translateY(0)} }
  </style>
</head>
<body>
  <!-- Mobile hamburger (hidden on desktop) -->
  <button class="mobile-hamburger" style="display:none;position:fixed;top:0.75rem;left:0.75rem;z-index:55;background:#1e1b4b;color:white;border:none;border-radius:6px;padding:0.5rem;cursor:pointer" onclick="toggleMobileSidebar()">
    <svg width="24" height="24" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" /></svg>
  </button>
  <div id="sidebar-overlay" class="sidebar-overlay" onclick="toggleMobileSidebar()"></div>

  {% include "components/sidebar.html" %}

  <div class="main-content">
    {% include "components/breadcrumb.html" %}
    {% block breadcrumbs %}{% endblock %}
    {% block content %}{% endblock %}
  </div>

  <!-- Server log panel (preserved from current base.html) -->
  <div id="log-panel" style="position:fixed;bottom:0;left:0;right:0;z-index:50;background:#1a1a2e;color:#e5e7eb;transition:height .3s">
    <div style="display:flex;justify-content:space-between;align-items:center;padding:.4rem 1rem;cursor:pointer;border-top:1px solid #374151" onclick="toggleLogPanel()">
      <span style="font-size:.8rem;font-weight:600">Server Log</span>
      <span id="log-toggle-icon" style="font-size:.8rem">&#9650;</span>
    </div>
    <div id="log-content" style="display:none;max-height:200px;overflow-y:auto;padding:.5rem 1rem"
         hx-get="/ui/logs?lines=40" hx-trigger="revealed" hx-swap="innerHTML">
    </div>
  </div>
  <div style="height:32px"></div>
  <div id="toast-container"></div>
  <script>
  /* Toast system — preserved */
  function showToast(msg, type) { ... same as current ... }
  document.body.addEventListener('htmx:responseError', function(e) { ... });
  function toggleLogPanel() { ... same as current ... }
  document.body.addEventListener('showToast', function(e) { ... });

  /* Sidebar section toggle */
  function toggleSection(sectionId) {
    var children = document.getElementById('children-' + sectionId);
    if (children) {
      children.style.display = children.style.display === 'none' ? 'block' : 'none';
    }
  }

  /* Mobile sidebar toggle */
  function toggleMobileSidebar() {
    var sidebar = document.getElementById('sidebar');
    var overlay = document.getElementById('sidebar-overlay');
    sidebar.classList.toggle('mobile-open');
    overlay.classList.toggle('active');
  }
  </script>
  {% if help_module %}
  {% with hm=help_module %}
  {% include "help/_modal.html" %}
  {% endwith %}
  {% endif %}
</body>
</html>
```

CRITICAL: Preserve ALL existing CSS classes (.card, .badge-*, .btn-*, table styles, toast styles, breadcrumbs class) exactly as they are in the current base.html. Child templates depend on these classes. The `{% block breadcrumbs %}` and `{% block content %}` blocks MUST remain in the same order.

CRITICAL: The `lang` attribute changes from "en" to "ru" since UI labels are Russian.

CRITICAL: The old `<nav>` top bar with horizontal links is REMOVED entirely. No top nav remains.

Per VIS-03: sidebar bg uses indigo-950 (#1e1b4b), active section uses indigo-800 (#3730a3), active child uses indigo-700 (#4338ca), site selector uses indigo-50/indigo-200 borders.
  </action>
  <verify>
    <automated>cd /projects/test && python -c "
import os
assert os.path.exists('app/templates/components/sidebar.html'), 'sidebar.html missing'
assert os.path.exists('app/templates/components/breadcrumb.html'), 'breadcrumb.html missing'
assert os.path.exists('app/templates/components/site_selector.html'), 'site_selector.html missing'
base = open('app/templates/base.html').read()
assert 'sidebar' in base, 'no sidebar in base.html'
assert 'block content' in base, 'block content missing'
assert 'block breadcrumbs' in base, 'block breadcrumbs missing'
assert 'nav_sections' in open('app/templates/components/sidebar.html').read(), 'nav_sections not used in sidebar'
assert '.card' in base, 'card CSS class missing'
assert '.badge-connected' in base, 'badge-connected CSS missing'
assert 'showToast' in base, 'showToast function missing'
assert 'toggleMobileSidebar' in base, 'mobile toggle missing'
assert 'translateX' in base, 'responsive transform missing'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "sidebar" app/templates/base.html` succeeds
    - `grep -q "nav_sections" app/templates/components/sidebar.html` succeeds
    - `grep -q "selected_site_id" app/templates/components/site_selector.html` succeeds
    - `grep -q "breadcrumb_section" app/templates/components/breadcrumb.html` succeeds
    - `grep -q "block content" app/templates/base.html` succeeds
    - `grep -q "block breadcrumbs" app/templates/base.html` succeeds
    - `grep -q ".card" app/templates/base.html` succeeds (existing CSS preserved)
    - `grep -q ".badge-connected" app/templates/base.html` succeeds
    - `grep -q "showToast" app/templates/base.html` succeeds
    - `grep -q "toggleMobileSidebar" app/templates/base.html` succeeds
    - `grep -q "1023px" app/templates/base.html` succeeds (tablet breakpoint)
    - `grep -q "767px" app/templates/base.html` succeeds (mobile breakpoint)
    - Old top nav `<nav>` with horizontal links is NOT present
    - `grep -q "active_section" app/templates/components/sidebar.html` succeeds
    - All 6 section labels appear in sidebar.html: Обзор, Сайты, Позиции и ключи, Аналитика, Контент, Настройки
  </acceptance_criteria>
  <done>New base.html has sidebar layout instead of top nav, all 3 component templates exist, all existing CSS classes preserved, responsive breakpoints at 1024px and 768px work, active states highlight current section/child, site selector persists via cookie, breadcrumbs render on every page</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of new sidebar layout</name>
  <files>app/templates/base.html</files>
  <action>Human verifies the sidebar layout visually in the browser. No automated action — this is a visual/functional checkpoint.</action>
  <what-built>Complete sidebar layout replacement: vertical sidebar with 6 sections, site selector, breadcrumbs, active states, responsive behavior (tablet icon-only, mobile overlay)</what-built>
  <how-to-verify>
    1. Visit /ui/dashboard — verify sidebar appears on left with 6 sections
    2. Click a section (e.g. Сайты) — verify sub-items expand
    3. Select a site in the site selector dropdown — verify it persists when navigating to another page
    4. Navigate to /ui/keywords/{site_id} — verify active state highlighting
    5. Verify breadcrumb shows section and page name
    6. Resize browser to < 1024px — verify sidebar collapses to icons only
    7. Resize to < 768px — verify hamburger button and overlay sidebar
    8. Verify /ui/sites, /ui/positions/{site_id}, /ui/admin/users all render correctly
    9. Verify no old top nav bar is visible on any page
  </how-to-verify>
  <verify>
    <automated>cd /projects/test && python -c "print('Checkpoint — requires human visual verification')"</automated>
  </verify>
  <done>User confirms sidebar layout looks correct, all sections visible, site selector works, active states highlight, responsive behavior functions at both breakpoints, no regressions</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. All 34 templates that extend base.html render without Jinja2 errors
2. Sidebar displays 6 sections with correct Russian labels
3. Site selector persists selection in cookie across page loads
4. Active state correctly highlights current section and child on all tested pages
5. Breadcrumb renders Section > Page on all pages
6. Responsive: collapses to icons < 1024px, overlay on mobile < 768px
7. All existing CSS classes (.card, .badge-*, .btn-*, table styles) still work
8. Toast system, log panel, help modal all still function
</verification>

<success_criteria>
- User sees vertical sidebar with 6 sections replacing old top nav
- Site selector persists across navigation
- Active states highlight correctly
- Breadcrumbs show on every page
- All existing pages render without errors
- Responsive behavior works at both breakpoints
</success_criteria>

<output>
After completion, create `.planning/phases/v4-01-navigation-foundation/v4-01-01-SUMMARY.md`
</output>
