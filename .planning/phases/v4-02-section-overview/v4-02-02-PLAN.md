---
phase: v4-02-section-overview
plan: "02"
type: execute
wave: 2
depends_on:
  - v4-02-01
files_modified:
  - app/templates/dashboard/index.html
autonomous: false
requirements:
  - OVR-01
  - OVR-02
  - OVR-03

must_haves:
  truths:
    - "User sees TOP-3, TOP-10, TOP-100 counts and trend arrows (up/down) on the dashboard"
    - "User sees a list of today's overdue and in-progress tasks with priority badge and due date"
    - "Overdue tasks are visually distinct from in-progress tasks (red accent or overdue label)"
    - "Dashboard uses Tailwind CSS classes consistently (indigo primary, emerald success, red danger)"
    - "Login redirect still lands on /ui/dashboard — confirmed by checking main.py"
  artifacts:
    - path: "app/templates/dashboard/index.html"
      provides: "Enhanced dashboard template with position summary and tasks widgets"
      contains: "pos_summary"
  key_links:
    - from: "app/templates/dashboard/index.html"
      to: "pos_summary template variable"
      via: "{{ pos_summary.top3 }}, {{ pos_summary.top10 }}, {{ pos_summary.top100 }}"
      pattern: "pos_summary\\.top"
    - from: "app/templates/dashboard/index.html"
      to: "tasks_today template variable"
      via: "{% for task in tasks_today %}"
      pattern: "tasks_today"
---

<objective>
Rewrite the dashboard/index.html template to display the new cross-site position summary widget and today's tasks widget, using the Tailwind CSS palette established in Phase v4-01.

Purpose: OVR-01 and OVR-02 are visible to users only through the template. This plan renders the data from overview_service (added in v4-02-01) as clear, scannable widgets with trend indicators.

Output: Enhanced dashboard/index.html. Followed by a human visual verification checkpoint.
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP-v4.md

<interfaces>
<!-- Template variables available from ui_dashboard handler after v4-02-01 -->

pos_summary dict:
  top3: int       — keywords in positions 1–3 across all sites
  top10: int      — keywords in positions 1–10 across all sites
  top100: int     — keywords in positions 1–100 across all sites
  trend_up: int   — keywords that improved this week (delta > 0)
  trend_down: int — keywords that declined this week (delta < 0)

tasks_today list[dict]:
  id: str
  title: str
  url: str         — page URL associated with the task
  priority: str    — "p1" | "p2" | "p3" | "p4"
  status: str      — "open" | "assigned" | "in_progress" | "review"
  site_id: str
  due_date: str | None  — ISO date string e.g. "2026-04-02"
  is_overdue: bool

Existing template variables (unchanged):
  stats.sites, stats.projects, stats.total_keywords, stats.open_tasks, stats.in_progress_tasks
  sites_overview: list[{id, name, keyword_count, top3, top10, top30, open_tasks}]
  projects: list[{id, name, status, site_id, site_name}]

Color palette (from v4-01, Tailwind):
  Primary: indigo-600, indigo-700
  Success: emerald-600
  Danger:  red-600
  Warning: amber-500
  Text:    gray-700, gray-500
  Background cards: white, with shadow-sm border border-gray-200

<!-- existing base.html extends sidebar layout from v4-01 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite dashboard/index.html with position summary and tasks widgets</name>
  <files>app/templates/dashboard/index.html</files>
  <read_first>
    - app/templates/dashboard/index.html (current template — already read, 81 lines)
    - app/templates/base.html (check block names: block title, block content)
  </read_first>
  <action>
Rewrite app/templates/dashboard/index.html. Keep `{% extends "base.html" %}` and `{% block title %}` / `{% block content %}`.

Layout (top-to-bottom):

**Section 1: Cross-Site Position Summary (OVR-01)**
A row of 3 stat cards (Tailwind grid, 3 cols on md+, 1 col on mobile):

Card A — TOP-3: emerald background (bg-emerald-50 border-emerald-200)
  - Large number: {{ pos_summary.top3 }} in emerald-700 font-bold text-3xl
  - Label: "TOP-3" in gray-500 text-sm

Card B — TOP-10: indigo background (bg-indigo-50 border-indigo-200)
  - Large number: {{ pos_summary.top10 }} in indigo-700
  - Label: "TOP-10"

Card C — TOP-100: gray background (bg-gray-50 border-gray-200)
  - Large number: {{ pos_summary.top100 }} in gray-700
  - Label: "TOP-100"

Below the cards, one trend row inside a white card (bg-white rounded-lg shadow-sm border border-gray-200 p-4 mt-3):
  - "Тренд за неделю:" label
  - Arrow up (▲) in emerald-600: {{ pos_summary.trend_up }} выросли
  - Arrow down (▼) in red-600: {{ pos_summary.trend_down }} упали
  Both on the same line, space-x-4.

**Section 2: Задачи на сегодня (OVR-02)**
A white card (bg-white rounded-lg shadow-sm border border-gray-200 p-4 mt-4) with:
  - Header: "Задачи на сегодня" h3 font-semibold text-gray-800
  - If tasks_today is empty: `<p class="text-gray-400 text-sm mt-2">Нет задач на сегодня</p>`
  - If not empty: ul list, each li is a row with:
    - Priority badge: p1=red-600, p2=amber-500, p3=indigo-500, p4=gray-400 — small inline span with priority text
    - Overdue badge (only if is_overdue): `<span class="text-xs font-semibold text-red-600 bg-red-50 px-1.5 py-0.5 rounded mr-1">Просрочено</span>`
    - Task title (truncated to 60 chars if needed, use Jinja2 truncate filter)
    - Status badge: in_progress="В работе" (indigo), open/assigned="Открыто" (gray), review="Проверка" (amber)
    - Due date (right-aligned): due_date or "—"
    - Link: entire row clickable to /ui/tasks (generic kanban link since we don't have per-task URL in this phase)
  Each li: `flex items-center gap-3 py-2 border-b border-gray-100 last:border-0 hover:bg-gray-50`

**Section 3: Keep existing widgets**
Keep the existing "Sites Overview" table (sites_overview loop) and Projects table EXACTLY as they are — just move them below the two new sections. Do NOT remove or modify them.

**Important style notes:**
- Use Tailwind CSS classes throughout — no inline style="" attributes (the existing template used inline styles; replace them with Tailwind equivalents)
- The `sites_overview` table columns: keep existing columns but replace inline styles with Tailwind: `class="font-semibold text-indigo-600 hover:underline"` on site name links, `class="text-emerald-700 font-bold"` on TOP-3, `class="text-indigo-600 font-bold"` on TOP-10
- Stats header row (sites count, projects, keywords, tasks): keep the existing 5 stat cards but replace inline styles with Tailwind grid and color classes matching the palette
  </action>
  <verify>
    <automated>cd /projects/test && python -c "
from jinja2 import Environment, FileSystemLoader
env = Environment(loader=FileSystemLoader('app/templates'))
tmpl = env.get_template('dashboard/index.html')
html = tmpl.render(
    request=None,
    stats={'sites':2,'projects':1,'total_keywords':50,'open_tasks':3,'in_progress_tasks':2},
    pos_summary={'top3':5,'top10':20,'top100':80,'trend_up':10,'trend_down':4},
    tasks_today=[
        {'id':'abc','title':'Fix 404','url':'/x','priority':'p1','status':'in_progress','site_id':'s1','due_date':'2026-04-01','is_overdue':True}
    ],
    sites_overview=[],
    projects=[],
)
assert 'pos_summary' not in html or True  # rendered — check for actual content
assert 'TOP-3' in html, 'Missing TOP-3'
assert 'TOP-10' in html, 'Missing TOP-10'
assert 'TOP-100' in html, 'Missing TOP-100'
assert 'Тренд за неделю' in html, 'Missing trend row'
assert 'Задачи на сегодня' in html, 'Missing tasks section'
assert 'Просрочено' in html, 'Missing overdue badge'
print('Template renders OK')
"
</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pos_summary\.top3\|pos_summary\.top10\|pos_summary\.top100" /projects/test/app/templates/dashboard/index.html` shows all 3 position variables rendered
    - `grep -n "trend_up\|trend_down\|Тренд" /projects/test/app/templates/dashboard/index.html` shows trend row
    - `grep -n "tasks_today\|Задачи на сегодня\|is_overdue" /projects/test/app/templates/dashboard/index.html` shows tasks widget
    - `grep -n "style=" /projects/test/app/templates/dashboard/index.html | wc -l` returns 0 (no inline styles)
    - Template renders without error via the automated verify command above
  </acceptance_criteria>
  <done>
    dashboard/index.html renders position summary (TOP-3/10/100 + trend) and today's tasks. All Tailwind, no inline styles.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Обзор dashboard</name>
  <action>Verify the enhanced dashboard visually: confirm position summary cards, trend row, tasks widget, and that all existing widgets are preserved. No code changes in this step — purely confirmation of automated work from Task 1.</action>
  <what-built>
    Enhanced Обзор dashboard with:
    1. Cross-site position summary: TOP-3, TOP-10, TOP-100 stat cards + weekly trend row (▲ up / ▼ down)
    2. Today's tasks widget: overdue (red "Просрочено" badge) + in-progress tasks, with priority and status badges
    3. Existing sites overview table and projects table preserved below new widgets
  </what-built>
  <how-to-verify>
    1. Open http://localhost:8000/ui/dashboard in a browser
    2. Confirm you see three position stat cards labeled TOP-3, TOP-10, TOP-100 with numeric values
    3. Confirm trend row below cards shows "Тренд за неделю: ▲ N выросли ▼ M упали"
    4. Confirm "Задачи на сегодня" section appears; if no tasks are due, shows "Нет задач на сегодня"
    5. If tasks exist: confirm overdue tasks show red "Просрочено" badge; in-progress tasks show indigo "В работе" badge
    6. Confirm Sites Overview table still appears below
    7. Log out and log in — confirm redirect goes to /ui/dashboard (not a splash page)
    8. Confirm page loads in < 3 seconds (check browser Network tab for the /ui/dashboard request)
  </how-to-verify>
  <resume-signal>Type "approved" to complete the phase, or describe any visual issues found.</resume-signal>
</task>

</tasks>

<verification>
After human approval:
```bash
grep -n "pos_summary\.top3\|pos_summary\.top10\|pos_summary\.top100" /projects/test/app/templates/dashboard/index.html
grep -n "tasks_today\|Задачи на сегодня" /projects/test/app/templates/dashboard/index.html
grep -c "style=" /projects/test/app/templates/dashboard/index.html  # should be 0
```
</verification>

<success_criteria>
1. Position summary widget shows TOP-3, TOP-10, TOP-100 with correct Tailwind color coding (emerald, indigo, gray)
2. Weekly trend row shows ▲ trend_up and ▼ trend_down counts
3. Tasks widget shows today's overdue + in-progress tasks, max 20 rows
4. Overdue tasks have red "Просрочено" badge
5. All styling is Tailwind classes — zero inline style="" attributes
6. Existing sites_overview and projects tables still visible below new widgets
7. Login redirect confirmed at /ui/dashboard
8. User (human) has approved the visual output
</success_criteria>

<output>
After human approval, create `.planning/phases/v4-02-section-overview/v4-02-02-SUMMARY.md` following the summary template.
</output>
