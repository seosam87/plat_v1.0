---
wave: 1
depends_on: []
requirements: [INFRA-01, INFRA-06, INFRA-07]
files_modified:
  - .gitignore
  - requirements.txt
  - Dockerfile
  - .env.example
  - app/__init__.py
  - app/config.py
  - app/database.py
  - app/dependencies.py
  - app/celery_app.py
  - app/tasks/__init__.py
  - app/tasks/crawl_tasks.py
  - app/tasks/wp_tasks.py
  - app/tasks/default_tasks.py
  - app/main.py
  - docker-compose.yml
  - pyproject.toml
  - tests/__init__.py
  - tests/conftest.py
autonomous: true
---

## Goal

Full Docker Compose stack (FastAPI + PostgreSQL 16 + Redis 7 + Celery 5.4 + Beat) starts from scratch with `docker-compose up --build`. Uses lifespan pattern, async SQLAlchemy session with try/commit/except rollback, and three Celery queues (`crawl`, `wp`, `default`) wired with routing. No manual steps required after `cp .env.example .env`.

<task id="01-01-01">
<title>Directory scaffolding and .gitignore</title>
<action>
Create the following directory structure (empty __init__.py files as needed):

  app/
  app/tasks/
  app/models/
  app/routers/
  app/services/
  app/auth/
  logs/   (empty dir, tracked via .gitkeep)
  tests/

Write .gitignore with exactly these entries:
```
.env
__pycache__/
*.pyc
*.pyo
.pytest_cache/
logs/*.log
celerybeat-schedule
.coverage
htmlcov/
dist/
*.egg-info/
.mypy_cache/
.ruff_cache/
```

Write logs/.gitkeep as empty file.
Write app/__init__.py, app/tasks/__init__.py, app/models/__init__.py, app/routers/__init__.py, app/services/__init__.py, app/auth/__init__.py, tests/__init__.py — all empty.
</action>
<read_first>None — first task, no existing files.</read_first>
<acceptance_criteria>
grep -q "^\.env$" .gitignore
grep -q "__pycache__/" .gitignore
grep -q "\*.pyc" .gitignore
grep -q "\.pytest_cache/" .gitignore
grep -q "logs/\*\.log" .gitignore
grep -q "celerybeat-schedule" .gitignore
test -f logs/.gitkeep
test -f app/__init__.py
test -f app/tasks/__init__.py
test -f tests/__init__.py
</acceptance_criteria>
</task>

<task id="01-01-02">
<title>requirements.txt with pinned versions</title>
<action>
Write requirements.txt with the following pinned dependencies:

```
# Web framework
fastapi>=0.115,<0.116
uvicorn[standard]>=0.30,<0.31
python-multipart>=0.0.9,<0.1

# Database
sqlalchemy>=2.0.30,<2.1
asyncpg>=0.29,<0.30
alembic>=1.13,<1.14
greenlet>=3.0,<4.0

# Task queue
celery[redis]>=5.4,<5.5
redis>=5.0,<5.1
redbeat>=2.2,<2.3

# Auth & security
python-jose[cryptography]>=3.3,<3.4
passlib[bcrypt]>=1.7,<1.8
cryptography>=42.0,<43.0

# Config
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0,<2.0

# HTTP client
httpx>=0.27,<0.28

# Logging
loguru>=0.7,<0.8

# Testing
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<0.24
pytest-cov>=5.0,<6.0
respx>=0.21,<0.22
```
</action>
<read_first>None</read_first>
<acceptance_criteria>
grep -q "fastapi>=0.115" requirements.txt
grep -q "celery\[redis\]>=5.4" requirements.txt
grep -q "asyncpg>=0.29" requirements.txt
grep -q "redbeat>=2.2" requirements.txt
grep -q "python-jose\[cryptography\]>=3.3" requirements.txt
grep -q "passlib\[bcrypt\]>=1.7" requirements.txt
grep -q "pydantic-settings>=2.3" requirements.txt
grep -q "loguru>=0.7" requirements.txt
grep -q "pytest-asyncio>=0.23" requirements.txt
</acceptance_criteria>
</task>

<task id="01-01-03">
<title>Dockerfile</title>
<action>
Write Dockerfile:

```dockerfile
FROM python:3.12-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    libpq-dev \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

RUN mkdir -p logs

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```
</action>
<read_first>requirements.txt</read_first>
<acceptance_criteria>
grep -q "FROM python:3.12-slim" Dockerfile
grep -q "gcc" Dockerfile
grep -q "libpq-dev" Dockerfile
grep -q "pip install --no-cache-dir -r requirements.txt" Dockerfile
grep -q "uvicorn app.main:app" Dockerfile
grep -q "mkdir -p logs" Dockerfile
</acceptance_criteria>
</task>

<task id="01-01-04">
<title>.env.example</title>
<action>
Write .env.example:

```bash
# Database
POSTGRES_USER=seo_user
POSTGRES_PASSWORD=changeme
POSTGRES_DB=seo_platform
DATABASE_URL=postgresql+asyncpg://seo_user:changeme@postgres:5432/seo_platform
SYNC_DATABASE_URL=postgresql+psycopg2://seo_user:changeme@postgres:5432/seo_platform

# Redis
REDIS_URL=redis://redis:6379/0

# JWT — generate with: openssl rand -hex 32
SECRET_KEY=replace-with-openssl-rand-hex-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Celery
CELERY_WORKER_CONCURRENCY=8

# App
DB_ECHO=false
LOG_LEVEL=INFO
```
</action>
<read_first>None</read_first>
<acceptance_criteria>
grep -q "DATABASE_URL=postgresql+asyncpg://" .env.example
grep -q "SYNC_DATABASE_URL=postgresql+psycopg2://" .env.example
grep -q "REDIS_URL=redis://redis:6379/0" .env.example
grep -q "ALGORITHM=HS256" .env.example
grep -q "ACCESS_TOKEN_EXPIRE_MINUTES=1440" .env.example
grep -q "CELERY_WORKER_CONCURRENCY=8" .env.example
grep -q "LOG_LEVEL=INFO" .env.example
</acceptance_criteria>
</task>

<task id="01-01-05">
<title>app/config.py — Pydantic BaseSettings</title>
<action>
Write app/config.py:

```python
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        extra="ignore",
    )

    # Database
    DATABASE_URL: str
    SYNC_DATABASE_URL: str

    # Redis
    REDIS_URL: str

    # JWT
    SECRET_KEY: str
    ALGORITHM: str = "HS256"
    ACCESS_TOKEN_EXPIRE_MINUTES: int = 1440

    # Celery
    CELERY_WORKER_CONCURRENCY: int = 8

    # App
    DB_ECHO: bool = False
    LOG_LEVEL: str = "INFO"


settings = Settings()
```
</action>
<read_first>.env.example</read_first>
<acceptance_criteria>
grep -q "from pydantic_settings import BaseSettings, SettingsConfigDict" app/config.py
grep -q "model_config = SettingsConfigDict" app/config.py
grep -q 'env_file=".env"' app/config.py
grep -q "DATABASE_URL: str" app/config.py
grep -q "SYNC_DATABASE_URL: str" app/config.py
grep -q "REDIS_URL: str" app/config.py
grep -q "SECRET_KEY: str" app/config.py
grep -q 'ALGORITHM: str = "HS256"' app/config.py
grep -q "ACCESS_TOKEN_EXPIRE_MINUTES: int = 1440" app/config.py
grep -q "CELERY_WORKER_CONCURRENCY: int = 8" app/config.py
grep -q "settings = Settings()" app/config.py
</acceptance_criteria>
</task>

<task id="01-01-06">
<title>app/database.py — async engine and session factory</title>
<action>
Write app/database.py:

```python
from sqlalchemy.ext.asyncio import (
    AsyncSession,
    async_sessionmaker,
    create_async_engine,
)
from sqlalchemy.orm import DeclarativeBase

from app.config import settings

engine = create_async_engine(
    settings.DATABASE_URL,
    pool_size=10,
    max_overflow=5,
    pool_timeout=30,
    pool_pre_ping=True,
    echo=settings.DB_ECHO,
)

AsyncSessionLocal = async_sessionmaker(
    engine,
    class_=AsyncSession,
    expire_on_commit=False,
)


class Base(DeclarativeBase):
    pass
```
</action>
<read_first>app/config.py</read_first>
<acceptance_criteria>
grep -q "from sqlalchemy.ext.asyncio import" app/database.py
grep -q "create_async_engine" app/database.py
grep -q "pool_size=10" app/database.py
grep -q "pool_pre_ping=True" app/database.py
grep -q "async_sessionmaker" app/database.py
grep -q "expire_on_commit=False" app/database.py
grep -q "class Base(DeclarativeBase):" app/database.py
</acceptance_criteria>
</task>

<task id="01-01-07">
<title>app/dependencies.py — get_db with commit/rollback pattern</title>
<action>
Write app/dependencies.py:

```python
from typing import AsyncGenerator

from sqlalchemy.ext.asyncio import AsyncSession

from app.database import AsyncSessionLocal


async def get_db() -> AsyncGenerator[AsyncSession, None]:
    async with AsyncSessionLocal() as session:
        try:
            yield session
            await session.commit()
        except Exception:
            await session.rollback()
            raise
```
</action>
<read_first>app/database.py</read_first>
<acceptance_criteria>
grep -q "async def get_db" app/dependencies.py
grep -q "async with AsyncSessionLocal() as session:" app/dependencies.py
grep -q "await session.commit()" app/dependencies.py
grep -q "await session.rollback()" app/dependencies.py
grep -q "AsyncGenerator\[AsyncSession, None\]" app/dependencies.py
</acceptance_criteria>
</task>

<task id="01-01-08">
<title>app/celery_app.py — Celery with 3 queues and redbeat</title>
<action>
Write app/celery_app.py:

```python
from celery import Celery

from app.config import settings

celery_app = Celery(
    "seo_platform",
    broker=settings.REDIS_URL,
    backend=settings.REDIS_URL,
    include=[
        "app.tasks.crawl_tasks",
        "app.tasks.wp_tasks",
        "app.tasks.default_tasks",
    ],
)

celery_app.conf.update(
    task_serializer="json",
    result_serializer="json",
    accept_content=["json"],
    timezone="UTC",
    enable_utc=True,
    result_expires=3600,
    task_acks_late=True,
    task_track_started=True,
    worker_prefetch_multiplier=1,
    task_routes={
        "app.tasks.crawl_tasks.*": {"queue": "crawl"},
        "app.tasks.wp_tasks.*": {"queue": "wp"},
        "app.tasks.default_tasks.*": {"queue": "default"},
    },
    task_default_queue="default",
    redbeat_redis_url=settings.REDIS_URL,
)
```

Write app/tasks/crawl_tasks.py:

```python
from app.celery_app import celery_app


@celery_app.task(name="app.tasks.crawl_tasks.crawl_site")
def crawl_site(site_id: str) -> dict:
    """Stub: crawl a site. Implemented in Phase 3."""
    return {"status": "stub", "site_id": site_id}
```

Write app/tasks/wp_tasks.py:

```python
from app.celery_app import celery_app


@celery_app.task(name="app.tasks.wp_tasks.process_wp_content")
def process_wp_content(site_id: str, post_id: int) -> dict:
    """Stub: process WP content. Implemented in Phase 8."""
    return {"status": "stub", "site_id": site_id, "post_id": post_id}
```

Write app/tasks/default_tasks.py:

```python
from app.celery_app import celery_app


@celery_app.task(name="app.tasks.default_tasks.send_notification")
def send_notification(message: str) -> dict:
    """Stub: send notification. Implemented in Phase 6."""
    return {"status": "stub", "message": message}
```
</action>
<read_first>app/config.py</read_first>
<acceptance_criteria>
grep -q "celery_app = Celery(" app/celery_app.py
grep -q "result_expires=3600" app/celery_app.py
grep -q "task_acks_late=True" app/celery_app.py
grep -q "worker_prefetch_multiplier=1" app/celery_app.py
grep -q '"app.tasks.crawl_tasks.\*": {"queue": "crawl"}' app/celery_app.py
grep -q '"app.tasks.wp_tasks.\*": {"queue": "wp"}' app/celery_app.py
grep -q '"app.tasks.default_tasks.\*": {"queue": "default"}' app/celery_app.py
grep -q "redbeat_redis_url=settings.REDIS_URL" app/celery_app.py
grep -q "def crawl_site" app/tasks/crawl_tasks.py
grep -q "def process_wp_content" app/tasks/wp_tasks.py
grep -q "def send_notification" app/tasks/default_tasks.py
</acceptance_criteria>
</task>

<task id="01-01-09">
<title>app/main.py — lifespan pattern with DB startup check</title>
<action>
Write app/main.py:

```python
from contextlib import asynccontextmanager

from fastapi import FastAPI
from sqlalchemy import text

from app.database import engine


@asynccontextmanager
async def lifespan(app: FastAPI):
    # Startup: verify DB connectivity
    async with engine.begin() as conn:
        await conn.execute(text("SELECT 1"))
    yield
    # Shutdown: dispose engine cleanly
    await engine.dispose()


app = FastAPI(
    title="SEO Management Platform",
    lifespan=lifespan,
)


@app.get("/")
async def root():
    return {"status": "ok", "service": "SEO Management Platform"}
```

NOTE: setup_logging() will be prepended to this file in task 01-04-05 as the first statement. Do not add it here — 01-04 adds it.
</action>
<read_first>app/database.py</read_first>
<acceptance_criteria>
grep -q "from contextlib import asynccontextmanager" app/main.py
grep -q "async def lifespan(app: FastAPI):" app/main.py
grep -q 'await conn.execute(text("SELECT 1"))' app/main.py
grep -q "await engine.dispose()" app/main.py
grep -q 'FastAPI(' app/main.py
grep -q 'title="SEO Management Platform"' app/main.py
grep -q "lifespan=lifespan" app/main.py
</acceptance_criteria>
</task>

<task id="01-01-10">
<title>docker-compose.yml — full 5-service stack</title>
<action>
Write docker-compose.yml:

```yaml
version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7.2-alpine
    command: >
      redis-server
      --appendonly yes
      --maxmemory 512mb
      --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  api:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./logs:/app/logs
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  worker:
    build: .
    command: >
      celery -A app.celery_app worker
      --loglevel=info
      --queues=crawl,wp,default
      --concurrency=${CELERY_WORKER_CONCURRENCY:-8}
      --max-tasks-per-child=50
    env_file: .env
    volumes:
      - ./logs:/app/logs
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  beat:
    build: .
    command: >
      celery -A app.celery_app beat
      --loglevel=info
      --scheduler redbeat.RedBeatScheduler
    env_file: .env
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
```
</action>
<read_first>.env.example</read_first>
<acceptance_criteria>
grep -q "image: postgres:16-alpine" docker-compose.yml
grep -q "pg_isready -U" docker-compose.yml
grep -q "image: redis:7.2-alpine" docker-compose.yml
grep -q "appendonly yes" docker-compose.yml
grep -q "maxmemory 512mb" docker-compose.yml
grep -q "maxmemory-policy allkeys-lru" docker-compose.yml
grep -q "redis-cli ping" docker-compose.yml
grep -q "condition: service_healthy" docker-compose.yml
grep -q "CELERY_WORKER_CONCURRENCY:-8" docker-compose.yml
grep -q "max-tasks-per-child=50" docker-compose.yml
grep -q "redbeat.RedBeatScheduler" docker-compose.yml
grep -q "crawl,wp,default" docker-compose.yml
</acceptance_criteria>
</task>

<task id="01-01-11">
<title>pyproject.toml — pytest configuration</title>
<action>
Write pyproject.toml:

```toml
[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = []

[tool.mypy]
python_version = "3.12"
strict = false
ignore_missing_imports = true
```
</action>
<read_first>None</read_first>
<acceptance_criteria>
grep -q 'asyncio_mode = "auto"' pyproject.toml
grep -q 'testpaths = \["tests"\]' pyproject.toml
</acceptance_criteria>
</task>

<task id="01-01-12">
<title>tests/conftest.py — async engine fixture with rollback isolation and AsyncClient</title>
<action>
Write tests/conftest.py:

```python
import pytest
import pytest_asyncio
from httpx import AsyncClient, ASGITransport
from sqlalchemy.ext.asyncio import (
    AsyncSession,
    async_sessionmaker,
    create_async_engine,
)

from app.database import Base
from app.dependencies import get_db
from app.main import app

# Derive test DB URL from main DB URL (replace db name)
# Falls back gracefully if DATABASE_URL is not set during collection
try:
    from app.config import settings
    TEST_DATABASE_URL = settings.DATABASE_URL.replace(
        f"/{settings.DATABASE_URL.split('/')[-1]}",
        "/seo_platform_test",
    )
except Exception:
    TEST_DATABASE_URL = "postgresql+asyncpg://seo_user:changeme@postgres:5432/seo_platform_test"


@pytest_asyncio.fixture(scope="session")
async def test_engine():
    engine = create_async_engine(TEST_DATABASE_URL, echo=False)
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
    yield engine
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.drop_all)
    await engine.dispose()


@pytest_asyncio.fixture
async def db_session(test_engine):
    """Per-test async session with rollback for isolation."""
    async with test_engine.begin() as conn:
        session = AsyncSession(bind=conn)
        try:
            yield session
        finally:
            await session.close()
            await conn.rollback()


@pytest_asyncio.fixture
async def client(db_session):
    """AsyncClient with DB dependency overridden to use the test session."""
    async def override_get_db():
        yield db_session

    app.dependency_overrides[get_db] = override_get_db
    async with AsyncClient(
        transport=ASGITransport(app=app), base_url="http://test"
    ) as ac:
        yield ac
    app.dependency_overrides.clear()
```
</action>
<read_first>app/main.py, app/database.py, app/dependencies.py</read_first>
<acceptance_criteria>
grep -q "pytest_asyncio.fixture" tests/conftest.py
grep -q "scope=\"session\"" tests/conftest.py
grep -q "Base.metadata.create_all" tests/conftest.py
grep -q "await conn.rollback()" tests/conftest.py
grep -q "ASGITransport" tests/conftest.py
grep -q "app.dependency_overrides\[get_db\]" tests/conftest.py
grep -q "app.dependency_overrides.clear()" tests/conftest.py
</acceptance_criteria>
</task>

## Must Have

1. `docker-compose up --build` completes without errors and all five services (postgres, redis, api, worker, beat) reach healthy/running state on a fresh clone with only `.env` copied from `.env.example`.
2. `GET http://localhost:8000/` returns `{"status": "ok"}` — confirming lifespan startup (DB `SELECT 1`) succeeded.
3. `docker compose logs worker` shows `crawl`, `wp`, and `default` queues registered — confirming three-queue topology is wired.
4. Celery Beat container starts and logs show `RedBeatScheduler` is active without errors.
5. `CELERY_WORKER_CONCURRENCY` value in `.env` changes worker concurrency without any code modification — confirmed by restarting the worker container.
