---
phase: 09.1-fix-project-ui-bugs
verified: 2026-04-05T00:00:00Z
status: passed
score: 6/6 must-haves verified
re_verification: false
---

# Phase 09.1: Fix Project UI Bugs — Verification Report

**Phase Goal:** Fix 6 user-reported UI bugs in project management pages — task creation, breadcrumbs, comments, plan page, pipeline feedback
**Verified:** 2026-04-05
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | User can create a task from the kanban page via a form that POSTs to the backend | VERIFIED | `id="new-task-form"` form with `hx-post="/projects/{{ project_id }}/tasks/create"` and `hx-ext="json-enc"` present at kanban.html lines 13-34 |
| 2 | Kanban page breadcrumbs show clickable 'Projects' link to /ui/projects | VERIFIED | `{% block breadcrumbs %}` at line 3 with `<a href="/ui/projects">Projects</a>` at line 4 |
| 3 | Comment form on kanban page submits successfully without 422 error | VERIFIED | Form uses `hx-ext="json-enc"` (line 106), no `hx-headers` attribute, `hx-swap="none"` with reload; json-enc script loaded at line 88 |
| 4 | Plan page has clickable breadcrumbs, an 'Add Item' form, and project_id context | VERIFIED | `{% block breadcrumbs %}` at line 3, `id="new-plan-item"` form at lines 13-34, `project_id` passed in main.py line 948 |
| 5 | User sees feedback after clicking Run Batch — job count or informative message | VERIFIED | `id="batch-result"` div at line 20, `hx-target="#batch-result"` on button, `htmx:afterRequest` listener parses `jobs_created` at lines 160-183 |
| 6 | Empty pipeline page provides guidance on prerequisites and next steps | VERIFIED | Ordered list with "Prerequisites:" heading and 3 workflow steps at lines 81-95 in jobs.html |

**Score: 6/6 truths verified**

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/templates/projects/kanban.html` | Task creation form, breadcrumbs block, fixed comment form | VERIFIED | All three fixes present; 114 lines, substantive implementation |
| `app/templates/projects/plan.html` | Breadcrumbs, add-item form, helpful empty state | VERIFIED | All fixes present; 89 lines, full implementation including back-to-kanban link |
| `app/templates/pipeline/jobs.html` | Batch feedback UI, helpful empty state | VERIFIED | Both fixes present; 185 lines including JS event handler |
| `app/main.py` | project_id passed to plan template context | VERIFIED | Line 948: `{"project_name": project.name, "project_id": project_id, "items": items_data}` |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| kanban.html | /projects/{project_id}/tasks/create | hx-post + json-enc | WIRED | Line 14: `hx-post="/projects/{{ project_id }}/tasks/create"`, json-enc loaded at line 88 |
| kanban.html | /projects/{project_id}/comments | hx-post + json-enc | WIRED | Line 104: `hx-post="/projects/{{ project_id }}/comments"`, `hx-ext="json-enc"` at line 106 |
| plan.html | /projects/{project_id}/plan | hx-post + json-enc | WIRED | Line 14: `hx-post="/projects/{{ project_id }}/plan"`, `hx-ext="json-enc"` at line 16 |
| jobs.html | /pipeline/sites/{site_id}/batch | hx-post + hx-target | WIRED | Line 11: `hx-post="/pipeline/sites/{{ site_id }}/batch"`, `hx-target="#batch-result"` at line 12 |

---

### Data-Flow Trace (Level 4)

These are Jinja2 server-rendered templates; data flows from backend to template at render time, not via client-side fetch. The forms issue POSTs to existing backend endpoints verified in plan context. No client-side data-flow issues found.

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| kanban.html | tasks, comments | ui_kanban handler (main.py ~line 924) | DB query via SQLAlchemy | FLOWING |
| plan.html | items | ui_plan handler (main.py line 929) | DB query via SQLAlchemy select | FLOWING |
| jobs.html | jobs | ui_pipeline handler (main.py ~line 952) | DB query | FLOWING |
| jobs.html batch response | jobs_created | /pipeline/sites/{id}/batch POST | Returns `{"jobs_created": N}` JSON | FLOWING |

---

### Behavioral Spot-Checks

Step 7b: SKIPPED for template rendering checks — templates require running server. Git commit verification performed instead.

| Check | Result | Status |
|-------|--------|--------|
| Commit 27d26e6 (kanban fixes) exists | `git cat-file -t 27d26e6` → `commit` | PASS |
| Commit 7ce9c36 (plan page fixes) exists | `git cat-file -t 7ce9c36` → `commit` | PASS |
| Commit f39973b (pipeline fixes) exists | `git cat-file -t f39973b` → `commit` | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| BUG-01 | 09.1-01 | No "Create Task" button on kanban page | SATISFIED | `id="new-task-form"` with toggle button at kanban.html lines 10-34 |
| BUG-02 | 09.1-01 | Breadcrumb "Проекты" not clickable on kanban | SATISFIED | `{% block breadcrumbs %}` with `<a href="/ui/projects">Projects</a>` at kanban.html lines 3-5 |
| BUG-03 | 09.1-01 | Comment form 422 error on kanban (JSON encoding) | SATISFIED | `hx-ext="json-enc"` on form; old `hx-headers` removed; json-enc CDN script loaded |
| BUG-04 | 09.1-01 | Plan page dead end (no breadcrumbs, no buttons, no hints) | SATISFIED | plan.html has breadcrumbs, add-item form, helpful empty state, back-to-kanban link; main.py passes project_id |
| BUG-05 | 09.1-02 | Pipeline "Run Batch" silent failure (no feedback) | SATISFIED | `#batch-result` div + `hx-target` on button + `htmx:afterRequest` JS handler parsing `jobs_created` |
| BUG-06 | 09.1-02 | Pipeline empty state unhelpful | SATISFIED | Replaced with SVG icon + description + ordered Prerequisites list (3 steps) |

All 6 requirement IDs from both plans are accounted for. No orphaned requirements.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| jobs.html | 119 | `alert('Выберите jobs')` — Russian-language alert in JS | Info | Minor UX inconsistency; not a blocker |

No blockers or warnings found. The `alert` string is pre-existing code from a prior phase, not introduced by this phase's changes.

---

### Human Verification Required

#### 1. Task Creation Form — End-to-End POST

**Test:** Navigate to a project's kanban page. Click "+ Create Task", fill in a title, submit.
**Expected:** Page reloads showing the new task in the "Open" column.
**Why human:** HTMX json-enc serialization and the backend 200 response chain require a live server.

#### 2. Comment Form — 422 Regression

**Test:** Navigate to a project's kanban page. Type a comment and click Post.
**Expected:** Page reloads showing the new comment; no 422 error in network tab.
**Why human:** json-enc encoding behavior is only observable in a live browser with network inspection.

#### 3. Pipeline Run Batch — Feedback Display

**Test:** Navigate to a pipeline page for a site with crawled pages. Click "Run Batch".
**Expected:** Button shows "Running..." during request, then result div appears with job count (emerald) or "no pages need enrichment" (amber).
**Why human:** Requires live server + Celery + crawled site data.

---

### Gaps Summary

No gaps found. All 6 bugs have been addressed with substantive implementations that are correctly wired to their respective backend endpoints. The three templates are fully rewritten with the required forms, breadcrumbs, and feedback mechanisms. The `app/main.py` `ui_plan` handler correctly passes `project_id` to the template context.

---

_Verified: 2026-04-05_
_Verifier: Claude (gsd-verifier)_
