---
status: diagnosed
phase: 999.8-playbook-builder-reusable-promotion-plan-blocks
source:
  - 999.8-01-data-foundation-SUMMARY.md
  - 999.8-02-admin-crud-SUMMARY.md
  - 999.8-03-playbook-builder-SUMMARY.md
  - 999.8-04-apply-and-project-tab-SUMMARY.md
  - 999.8-05-banner-and-seed-SUMMARY.md
  - 999.8-VERIFICATION.md
started: 2026-04-11T19:30:00Z
updated: 2026-04-11T20:45:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: |
  Остановить все запущенные контейнеры/сервисы. Запустить стек с нуля
  (docker compose down && docker compose up -d). API-контейнер стартует
  без ошибок, Alembic миграция 0054 применяется (или уже на head=0054),
  `app.main` импортируется с 468 routes, главная страница /ui загружается
  без 500, /ui/playbooks возвращает список шаблонов.
result: pass
note: |
  Initial run hit 502 Bad Gateway — ModuleNotFoundError: 'markdown' in
  app/template_engine.py:12. Root cause: Plan 01 added Markdown>=3.6,<4.0
  to requirements.txt but docker image was not rebuilt; volume mounts only
  cover ./app, not site-packages. Fix: `docker compose up -d --build`.
  After rebuild: api Uvicorn startup complete, no errors.

### 2. Админ создаёт PlaybookBlock в библиотеке
expected: |
  После входа → Плейбуки → Библиотека блоков → + Создать блок → форма
  с полями title/category/summary_md/checklist_md/action_kind/estimated_days
  + инлайн-медиа → Сохранить → карточка появляется в сетке. Категорийный
  фильтр работает. Валидация URL на медиа работает.
result: issue
reported: "pass. Фильтрация по категориями не работает"
severity: major
note: |
  Форма создания блока работает (карточка появляется в сетке),
  но категорийный фильтр в списке библиотеки не фильтрует.

### 3. Админ собирает Playbook drag-drop билдером
expected: |
  /ui/playbooks → + Создать шаблон → заполнить мету → открыть билдер →
  перетащить блоки из левой панели в правую (Sortable.js), проверить
  ↑/↓ клавиатурно, авто-сохранение позиций через /steps/reorder,
  клон через + (копия).
result: pass

### 4. Админ применяет Playbook к реальному проекту
expected: |
  Открыть любой проект → вкладка Плейбуки → Применить плейбук → модал
  с селектом → выбрать 'SEO для коммерческих страниц (демо)' → Применить
  → HX-Redirect на /ui/projects/{id}/kanban#playbook → карточка
  ProjectPlaybook с 0/6 прогрессом и 6 шагами.
result: issue
reported: "После нажатия на кнопку 'Применить' тишина. Несмотря на то что окно не закрылось, шаблон к проекту применился."
severity: major
note: |
  Backend copy-on-apply работает (ProjectPlaybook создаётся в БД, карточка
  появляется на /ui/projects/{id}/kanban#playbook при ручной перезагрузке).
  Сломано только UI-поведение: модал не закрывается, HX-Redirect не
  срабатывает, пользователь не видит реакции на клик. Гипотеза:
  /ui/projects/{id}/playbooks/apply возвращает HTML вместо HX-Redirect
  header, либо нет hx-target на модале, либо modal close по событию.

### 5. Пользователь жмёт 'Перейти к шагу' → баннер появляется на целевой странице
expected: |
  Клик по 'Перейти к шагу' на шаге 1 (run_crawl) → sessionStorage.active_playbook_step
  заполнен → POST /open-action → fetch /api/playbook-step-route → редирект на
  /ui/sites/{site_id}/crawls → на целевой странице появляется sticky indigo баннер
  с 'Шаг 1/6 · Первичный краулинг сайта'.
result: issue
reported: "Перейти к шагу - не работает. Попытка нажать на кружок - окошко с ошибкой 'Server error 500'."
severity: blocker
note: |
  Две проблемы в одном наблюдении:
  1) Клик по 'Перейти к шагу' не перенаправляет — баннер не появляется
     (либо /api/playbook-step-route возвращает 500/ошибку, либо JS-handler
     openPlaybookStep не отрабатывает).
  2) Клик по кружку статуса (status toggle → /api/project-playbook-steps/{id}/status)
     возвращает HTTP 500. Это же сломает Test 6 (отметить выполненным).
  Нужны docker compose logs api для стектрейса.

### 6. Пользователь жмёт 'Отметить выполненным' → возврат на вкладку с done
expected: |
  В баннере нажать 'Отметить выполненным' → POST /api/project-playbook-steps/{id}/complete
  → редирект на /ui/projects/{id}/kanban#playbook → шаг 1 с emerald галкой и
  зачёркнутым заголовком, прогресс 1/6.
result: blocked
blocked_by: prior-phase
reason: "Баннер не появляется (Test 5 сломан), физически нельзя нажать 'Отметить выполненным'. Также endpoint /api/project-playbook-steps/{id}/complete рендерит тот же partial что упал с MissingGreenlet — фикс Test 5 скорее всего закроет и этот путь."

### 7. Hint-badges загружаются на вкладке после выполнения действий в системе
expected: |
  После возврата на вкладку плейбука → для open-шагов с action_kind in
  {run_crawl, open_keywords, open_competitors, open_content_plan}, если
  соответствующая запись появилась после step.opened_at → amber pill
  'Похоже, выполнено ✓' + кнопка 'Подтвердить'. Для open_commercial_check/open_brief
  — badges не должны появляться (отложено в 999.8.1).
result: issue
reported: "Нет понимания, что нажимать. Кружок не кликабельный, перейти к шагу - ничего не происходит. Есть кнопка Архивировать на плейбуке, она скрывает лишние плейбуки"
severity: major
note: |
  Пользователь не видит hint-badges на вкладке — либо они не рендерятся,
  либо пользователь их не замечает (UX affordance проблема).
  Побочные наблюдения:
  - Кружок статуса НЕ кликабельный на этой view (в Test 5 при клике был 500 —
    возможно разные состояния/типы шагов, или баг hx-on:click / pointer-events).
  - 'Перейти к шагу' тихо ничего не делает (подтверждение Test 5 issue).
  - ✓ Положительное наблюдение: кнопка 'Архивировать' работает — скрывает
    лишние плейбуки (это побочная функция, не в scope UAT).
  Вероятные корневые причины для hint-badges:
  - compute_hints_for_playbook() возвращает пустой словарь из-за того что
    step.opened_at не устанавливается при открытии таба (либо timestamp
    сравнение не срабатывает), либо hints dict не передаётся в template
    контекст, либо amber pill условие в _playbook_project_step.html не
    триггерится.

### 8. Demo seed запускается через docker compose и производит нужные строки
expected: |
  docker compose exec api python scripts/seed_demo_playbook.py →
  первый запуск 'Seeded: 1 ExpertSource, 6 PlaybookBlocks, 1 Playbook (6 steps).'
  → второй запуск 'Seed already applied (ExpertSource shestakov-demo exists). Skipping.'
result: pass
note: |
  Оба запуска вернули 'Seed already applied (ExpertSource shestakov-demo exists). Skipping.'
  БД уже была seeded во время исполнения Plan 05 (verifier 2026-04-11T19:18Z). Наблюдаемая
  ветка (idempotent skip) работает корректно — повторный запуск не дублирует данные.
  Fresh-seed ветка подтверждена ранее в verifier evidence.

## Summary

total: 8
passed: 3
issues: 4
pending: 0
skipped: 0
blocked: 1

## Gaps

- truth: "Категорийный фильтр на странице /ui/playbooks/blocks фильтрует блоки по BlockCategory"
  status: failed
  reason: "User reported: фильтрация по категориям не работает — при выборе категории список не сужается."
  severity: major
  test: 2
  root_cause: |
    Роут `ui_playbook_blocks` в `app/routers/playbooks.py:63-76` объявляет параметры
    `category_id: uuid.UUID | None = None` и `expert_source_id: uuid.UUID | None = None`,
    но HTMX-дропдауны в `app/templates/playbooks/blocks.html:15-42` через
    `hx-include="[name='expert_source_id']"` / `hx-include="[name='category_id']"`
    всегда отправляют ПУСТУЮ СТРОКУ для companion-селекта. FastAPI/Pydantic v2
    отклоняют `""` как invalid UUID → возвращают 422 до сервиса. HTMX swap'ит
    ошибку в `#blocks-grid` → пользователю кажется что фильтр сломан.
    Live repro: GET /ui/playbooks/blocks?category_id=<uuid>&expert_source_id= → 422
    `uuid_parsing: "Input should be a valid UUID, invalid length: expected length 32, found 0"`
  artifacts:
    - path: "app/routers/playbooks.py:63-76"
      issue: "Route signature uuid.UUID | None rejects empty string with 422"
    - path: "app/templates/playbooks/blocks.html:15-42"
      issue: "HTMX dropdowns cross-include sibling select's empty value"
  missing:
    - "Изменить route signature на `category_id: str | None = None` + `expert_source_id: str | None = None`"
    - "Внутри handler: `category_uuid = uuid.UUID(category_id) if category_id else None` с try/except ValueError → None"
    - "Не трогать template — cross-include нужен чтобы фильтр сохранял вторую размерность при смене первой"
  debug_session: .planning/debug/category-filter-not-filtering.md

- truth: "Клик по 'Применить' в модале выбора playbook закрывает модал и редиректит на #playbook таб проекта через HX-Redirect"
  status: failed
  reason: "User reported: после нажатия на 'Применить' модал не закрывается, HX-Redirect не срабатывает. Backend copy-on-apply работает, карточка в БД — но UI-handshake сломан."
  severity: major
  test: 4
  root_cause: |
    `app/routers/playbooks.py:675` возвращает `HX-Redirect: /ui/projects/{id}/kanban#playbook`,
    но к моменту клика на 'Применить' браузер УЖЕ находится на этом URL — пользователь
    открыл вкладку 'Плейбуки', что триггернуло `switchProjectTab('playbook')` в
    `app/templates/projects/kanban.html:174`, который через `history.replaceState(null, '', '#playbook')`
    записал хэш в текущий URL. HTMX 2.0 реагирует на HX-Redirect через `location.href = val` —
    присвоение same URL → browser no-op (hash-only changes не триггерят reload).
    У формы `_apply_modal.html:40-41` `hx-swap="none"` без fallback (`hx-on::after-request`,
    manual close, HX-Refresh) → пользователь видит полную тишину.
  artifacts:
    - path: "app/routers/playbooks.py:672-676"
      issue: "HX-Redirect to current URL is no-op"
    - path: "app/templates/projects/kanban.html:174"
      issue: "switchProjectTab writes #playbook to URL via history.replaceState, priming collision"
    - path: "app/templates/playbooks/_apply_modal.html:40-41"
      issue: "No client-side fallback for HX-Redirect no-op case"
  missing:
    - "Заменить HX-Redirect на HX-Refresh: true header — HTMX 2.0 всегда перезагружает страницу вне зависимости от URL"
    - "(Альтернатива: HX-Trigger с кастомным event + JS-handler закрывает модал и делает htmx.ajax для #tab-playbook)"
    - "Option 1 проще и ближе к оригинальному интенту плана"
  debug_session: .planning/debug/apply-modal-no-hx-redirect.md

- truth: "Клик 'Перейти к шагу' на карточке шага выполняет POST /open-action → /api/playbook-step-route → редирект на целевую страницу и показ sticky banner"
  status: failed
  reason: "User reported: 'Перейти к шагу - не работает'. Клик silent no-op."
  severity: major
  test: 5
  root_cause: |
    Две независимые проблемы, каждая по отдельности ломает клик:
    (1) `_playbook_project_step.html:108` пишет
    `onclick="openPlaybookStep('...', {{ step.block.title | tojson }}, ..., {{ pp_name | tojson }}, ...)"`.
    Jinja2 `|tojson` НЕ экранирует литеральные `"` в JSON-строке (экранирует только
    `<`, `>`, `&`, `'`) — HTML-парсер завершает атрибут `onclick="..."` на первой
    внутренней `"` → `onclick` превращается в оборванный `openPlaybookStep(` → SyntaxError.
    (2) `window.openPlaybookStep` определён в инлайн `<script>` ВНУТРИ partial
    `_playbook_project_step.html:115-162`, а таб `#tab-playbook` подгружается через
    `hx-swap="innerHTML"` в `kanban.html:149-155`. Известный баг HTMX 2.0
    (bigskysoftware/htmx#3418): inline `<script>` внутри innerHTML-swapped контента
    не выполняется браузером (inert `<script>` tags when created via innerHTML).
    → даже с исправленным квотингом функция не существует в window scope.
  artifacts:
    - path: "app/templates/projects/_playbook_project_step.html:108"
      issue: "onclick='...' с |tojson внутри — коллизия HTML attribute quoting"
    - path: "app/templates/projects/_playbook_project_step.html:115-162"
      issue: "window.openPlaybookStep определён в inline script внутри innerHTML-swapped partial"
    - path: "app/templates/projects/kanban.html:149-155"
      issue: "hx-swap='innerHTML' триггерит HTMX 2.0 inline-script regression"
  missing:
    - "Вынести openPlaybookStep JS в app/static/js/playbook_step.js и include в base.html (выполняется один раз при page boot, вне HTMX swap zone)"
    - "Удалить inline <script> блок из _playbook_project_step.html"
    - "Перевести атрибут onclick с двойных кавычек на одинарные: `onclick='openPlaybookStep(...)'` (Jinja |tojson безопасен внутри одинарных)"
    - "(Или: заменить onclick на data-* атрибуты + делегирование событий из глобального JS — cleaner, но больше работы)"
  debug_session: .planning/debug/project-step-interactions-broken.md

- truth: "Клик по кружку статуса шага (outerHTML swap через /api/project-playbook-steps/{id}/status) обновляет шаг без ошибки"
  status: failed
  reason: "User reported: 'Попытка нажать на кружок — окошко с ошибкой Server error 500'. Stack trace: sqlalchemy.exc.MissingGreenlet."
  severity: blocker
  test: 5
  root_cause: |
    Комбинация двух проблем в цепочке status toggle:
    (A) `get_project_step()` в `app/services/playbook_service.py:818-830` eager-загружает
    `ProjectPlaybookStep.project_playbook`, но НЕ цепляет дальше `.selectinload(ProjectPlaybook.steps)`.
    Шаблон `_playbook_project_step.html:106` делает
    `{% set pp_total = (step.project_playbook.steps | length) %}` — трогает незагруженную
    коллекцию `.steps` → lazy-load из sync Jinja2 контекста → `MissingGreenlet`.
    (B) `set_step_status()` в `playbook_service.py:849-851` вызывает `db.refresh(step)`
    который expires все relationship attributes и перезагружает только columns —
    даже если Session.expire_on_commit=False (а в `app/database.py:22-25` так и есть),
    явный refresh() всё равно делает attributes stale.
    Оба независимы: каждый сам по себе даёт `MissingGreenlet`.
  artifacts:
    - path: "app/services/playbook_service.py:818-830"
      issue: "get_project_step() missing selectinload(project_playbook).selectinload(steps)"
    - path: "app/services/playbook_service.py:849-851"
      issue: "db.refresh(step) expires all relationships before template render"
    - path: "app/templates/projects/_playbook_project_step.html:106"
      issue: "Reads step.project_playbook.steps | length — triggers lazy load"
    - path: "app/routers/playbooks.py:754-772"
      issue: "Status endpoint renders partial immediately after refresh-expired step"
  missing:
    - "В get_project_step(): добавить .selectinload(ProjectPlaybookStep.project_playbook).selectinload(ProjectPlaybook.steps)"
    - "В set_step_status(): удалить db.refresh(step) — ORM instance уже live, column mutations применены в памяти, нужен только db.flush() (или commit)"
    - "Альтернативно: не передавать live ORM object в template, построить plain dict в route и рендерить из него (более изоляция, но больше работы)"
    - "Проверить api_project_step_complete (Test 6 path) — использует ли тот же partial? Исправление должно закрыть и его."
  debug_session: .planning/debug/project-step-interactions-broken.md

- truth: "На вкладке плейбука для open-шагов с выполненным действием рендерится amber hint-pill 'Похоже, выполнено ✓' + кнопка 'Подтвердить'"
  status: failed
  reason: "User reported: 'Нет понимания что нажимать. Кружок не кликабельный, перейти к шагу — ничего не происходит'. Hint-badges не появляются."
  severity: major
  test: 7
  root_cause: |
    Каскад из предыдущего gap: hint-badges никогда не появятся, потому что
    `step.opened_at` остаётся NULL для всех шагов — `check_step_hint()` в
    `app/services/playbook_hints.py:47-50` short-circuit returns False при NULL.
    `step.opened_at` stamp'ится только внутри `open_step_action()` в
    `playbook_service.py:870-888`, который вызывается ТОЛЬКО через POST
    `/api/project-playbook-steps/{step_id}/open-action` — а этот endpoint триггерится
    из `openPlaybookStep()` JS, который сломан (см. предыдущий gap — tojson quoting +
    inline script bug). Таким образом, test 5 fix автоматически разморозит test 7.
    "Кружок не кликабельный" perception: on реально кликабельный (HTMX wires up circle
    button correctly), но клик → 500 MissingGreenlet → toast-error показывается
    3 секунды и исчезает (`base.html:126-128`) → пользователь пропускает ошибку,
    думает что просто не работает.
  artifacts:
    - path: "app/services/playbook_hints.py:47-50"
      issue: "check_step_hint short-circuits when step.opened_at is None — valid guard, but means hints require prior step visit"
    - path: "app/services/playbook_service.py:870-888"
      issue: "open_step_action is ONLY writer of step.opened_at"
    - path: "app/templates/base.html:126-128"
      issue: "Error toast auto-dismisses after 3s — user misses 500 error"
  missing:
    - "Фикс gap 'Перейти к шагу' (external JS + quoting) автоматически включает stamp opened_at и разморозит hints"
    - "Фикс gap '500 MissingGreenlet' автоматически уберёт невидимую ошибку на клике по кружку"
    - "Улучшить affordance: добавить cursor-pointer + border-indigo-500 на open status circle, чтобы он выглядел кликабельным"
    - "Опционально: увеличить auto-dismiss toast-error с 3s до 8s для диагностических ошибок"
  debug_session: .planning/debug/hint-badges-invisible.md
