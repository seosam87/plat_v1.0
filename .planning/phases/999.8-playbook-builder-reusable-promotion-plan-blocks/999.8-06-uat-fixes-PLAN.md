---
phase: 999.8-playbook-builder-reusable-promotion-plan-blocks
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/playbooks.py
  - app/services/playbook_service.py
  - app/static/js/playbook_step.js
  - app/templates/base.html
  - app/templates/projects/_playbook_project_step.html
autonomous: true
requirements:
  - PBK-UAT-A-category-filter
  - PBK-UAT-B-apply-modal-redirect
  - PBK-UAT-C-open-playbook-step
  - PBK-UAT-D-missinggreenlet-status
gap_closure: true

must_haves:
  truths:
    - "GET /ui/playbooks/blocks?category_id=<uuid>&expert_source_id= returns 200 and the block list is filtered by category"
    - "GET /ui/playbooks/blocks?category_id=&expert_source_id=<uuid> returns 200 and the block list is filtered by expert"
    - "POST /ui/projects/{id}/playbooks/apply closes the modal and the Kanban page reloads with the Плейбуки tab active"
    - "Click 'Перейти к шагу' stamps step.opened_at, calls /api/playbook-step-route, and navigates to the target screen"
    - "window.openPlaybookStep is a function after HTMX innerHTML swap of #tab-playbook"
    - "POST /api/project-playbook-steps/{id}/status returns 200 (not 500 MissingGreenlet) and re-renders the step partial"
    - "Hint-badges appear for open-шагов after the user visits them (cascade: closed by Gap C fix)"
  artifacts:
    - path: "app/routers/playbooks.py"
      provides: "Category filter accepts empty string; apply endpoint returns HX-Refresh"
      contains: "HX-Refresh"
    - path: "app/services/playbook_service.py"
      provides: "get_project_step eager-loads project_playbook.steps; set_step_status no longer refreshes"
      contains: "selectinload(ProjectPlaybook.steps)"
    - path: "app/static/js/playbook_step.js"
      provides: "Global window.openPlaybookStep, loaded once from base.html"
      contains: "window.openPlaybookStep"
    - path: "app/templates/base.html"
      provides: "Script tag including /static/js/playbook_step.js"
      contains: "/static/js/playbook_step.js"
    - path: "app/templates/projects/_playbook_project_step.html"
      provides: "Single-quoted onclick with |tojson payload; no inline <script>"
  key_links:
    - from: "app/templates/playbooks/blocks.html select[name=category_id]"
      to: "GET /ui/playbooks/blocks"
      via: "hx-get with hx-include of sibling select"
      pattern: "hx-include.*expert_source_id"
    - from: "app/routers/playbooks.py ui_playbook_blocks"
      to: "svc.list_blocks"
      via: "parsed uuid.UUID or None"
      pattern: "svc\\.list_blocks"
    - from: "app/templates/projects/_playbook_project_step.html button"
      to: "window.openPlaybookStep"
      via: "onclick='openPlaybookStep(...)' (single quotes)"
      pattern: "onclick='openPlaybookStep"
    - from: "window.openPlaybookStep"
      to: "POST /api/project-playbook-steps/{id}/open-action → GET /api/playbook-step-route"
      via: "fetch then window.location.href"
      pattern: "playbook-step-route"
    - from: "POST /api/project-playbook-steps/{id}/status"
      to: "projects/_playbook_project_step.html render"
      via: "Jinja2 reads step.project_playbook.steps (eager-loaded)"
      pattern: "step\\.project_playbook\\.steps"
---

<objective>
UAT для Phase 999.8 выявил 4 независимых блокера walking-skeleton flow:

A. Категорийный фильтр возвращает 422 (пустая строка в UUID).
B. Клик "Применить" в модале — тишина (HX-Redirect на тот же URL — no-op).
C. Клик "Перейти к шагу" — silent no-op (tojson ломает onclick + inline script после HTMX swap не выполняется).
D. Клик по кружку статуса — 500 MissingGreenlet (lazy-load .steps + refresh expires).

Gap E (hint-badges) — каскад от C+D, закроется автоматически.

Purpose: Довести walking-skeleton до реально работающего UX, чтобы UAT прошёл без issues и фазу можно было закрыть.

Output:
  - app/routers/playbooks.py с исправленной сигнатурой фильтра и HX-Refresh на apply
  - app/services/playbook_service.py с расширенным selectinload и без лишнего refresh
  - app/static/js/playbook_step.js (новый файл) с глобальной openPlaybookStep
  - app/templates/base.html подключает playbook_step.js
  - app/templates/projects/_playbook_project_step.html без inline <script>, с одинарными кавычками onclick
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.8-playbook-builder-reusable-promotion-plan-blocks/999.8-UAT.md
@.planning/phases/999.8-playbook-builder-reusable-promotion-plan-blocks/999.8-CONTEXT.md
@.planning/phases/999.8-playbook-builder-reusable-promotion-plan-blocks/999.8-VERIFICATION.md
@.planning/phases/999.8-playbook-builder-reusable-promotion-plan-blocks/999.8-04-apply-and-project-tab-SUMMARY.md
@.planning/phases/999.8-playbook-builder-reusable-promotion-plan-blocks/999.8-05-banner-and-seed-SUMMARY.md

<interfaces>
<!-- Key code that all 4 tasks touch. Executor should use this without re-reading. -->

From app/routers/playbooks.py:63-92 (current — BROKEN for Gap A):
```python
@router.get("/ui/playbooks/blocks", response_class=HTMLResponse)
async def ui_playbook_blocks(
    request: Request,
    category_id: uuid.UUID | None = None,
    expert_source_id: uuid.UUID | None = None,
    user: User = Depends(require_any_authenticated),
    db: AsyncSession = Depends(get_db),
) -> HTMLResponse:
    """Block library grid + HTMX filter. Any authenticated user can view."""
    blocks = await svc.list_blocks(
        db,
        category_id=category_id,
        expert_source_id=expert_source_id,
    )
    categories = await svc.list_categories(db)
    experts = await svc.list_experts(db)
    is_admin = user.role.value == "admin"
    ctx = {
        "blocks": blocks,
        "categories": categories,
        "experts": experts,
        "is_admin": is_admin,
        "selected_category_id": category_id,
        "selected_expert_id": expert_source_id,
    }
    if request.headers.get("HX-Request"):
        return templates.TemplateResponse(
            request, "playbooks/_block_card_grid.html", ctx
        )
    return templates.TemplateResponse(request, "playbooks/blocks.html", ctx)
```

From app/routers/playbooks.py:652-676 (current — BROKEN for Gap B):
```python
@router.post("/ui/projects/{project_id}/playbooks/apply")
async def ui_project_playbook_apply(
    request: Request,
    project_id: uuid.UUID,
    user: User = Depends(require_admin),
    db: AsyncSession = Depends(get_db),
):
    """Copy-on-apply a Playbook template to this project.

    Returns an HX-Redirect so the modal closes client-side and the
    Kanban page reloads with the Playbook tab active (#playbook hash).
    """
    form = await request.form()
    try:
        playbook_id = uuid.UUID(form["playbook_id"])
    except (KeyError, ValueError) as e:
        raise HTTPException(status_code=400, detail=f"Invalid playbook_id: {e}")
    pp = await svc.apply_playbook(db, project_id, playbook_id, applied_by=user.id)
    if pp is None:
        raise HTTPException(status_code=404, detail="Playbook not found")
    return HTMLResponse(
        "",
        status_code=200,
        headers={"HX-Redirect": f"/ui/projects/{project_id}/kanban#playbook"},
    )
```

From app/services/playbook_service.py:815-851 (current — BROKEN for Gap D):
```python
async def get_project_step(
    db: AsyncSession, step_id: uuid.UUID
) -> ProjectPlaybookStep | None:
    stmt = (
        select(ProjectPlaybookStep)
        .options(
            selectinload(ProjectPlaybookStep.block)
            .selectinload(PlaybookBlock.category),
            selectinload(ProjectPlaybookStep.block)
            .selectinload(PlaybookBlock.expert_source),
            selectinload(ProjectPlaybookStep.block)
            .selectinload(PlaybookBlock.media),
            selectinload(ProjectPlaybookStep.project_playbook),
        )
        .where(ProjectPlaybookStep.id == step_id)
    )
    result = await db.execute(stmt)
    return result.scalar_one_or_none()


async def set_step_status(
    db: AsyncSession,
    step_id: uuid.UUID,
    new_status: ProjectPlaybookStepStatus,
) -> ProjectPlaybookStep | None:
    step = await get_project_step(db, step_id)
    if step is None:
        return None
    step.status = new_status
    now = datetime.utcnow()
    if new_status == ProjectPlaybookStepStatus.done:
        step.completed_at = now
    elif new_status == ProjectPlaybookStepStatus.open:
        step.completed_at = None
    await db.flush()
    await db.refresh(step)   # ← BUG: expires relationship attributes
    return step
```

Also note: `open_step_action()` at line 870 has the same `await db.refresh(step)` and must be updated in the same way.

From app/templates/projects/_playbook_project_step.html (current — BROKEN for Gap C):
- Line 107-111: `onclick="openPlaybookStep('{{ step.id }}', ..., {{ step.block.title | tojson }}, ..., {{ pp_name | tojson }}, ...)"`
  The `|tojson` filter emits literal `"` inside the JSON string → collides with the outer `onclick="..."`.
- Lines 115-162: inline `<script>` defining `window.openPlaybookStep` — becomes inert after HTMX 2.0 innerHTML swap.

From app/templates/projects/kanban.html:177-183 (already has hash-on-load handler, do NOT re-add):
```javascript
// Auto-open a specific tab on page load if URL has #playbook or #kanban hash.
(function () {
  var hash = window.location.hash;
  if (hash === '#playbook' || hash === '#kanban') {
    switchProjectTab(hash.substring(1));
  }
})();
```

From app/templates/base.html line 17 (pattern for adding static JS):
```html
<script src="/static/js/tour.js" defer></script>
```
Use the same shape for `playbook_step.js`. Since it runs on every page, it should NOT be wrapped in `{% if is_admin %}`. Add it unconditionally, e.g. right after line 18 (after the Shepherd.js `{% endif %}`) or near the end of `<head>`.

From app/templates/base.html line 98 (where banner include sits — reference only, do not touch):
```jinja
{% include "components/playbook_banner.html" %}
```
</interfaces>

<!-- Architectural notes -->
- **CONTEXT.md D-15 / CD-02:** Russian UI strings must remain; never translate button labels or toasts.
- **CONTEXT.md D-12:** ProjectPlaybook copy-on-apply already works — do NOT touch apply_playbook() service or the FK rules.
- **D-16/D-17:** Banner is rendered globally via base.html include + sessionStorage — the openPlaybookStep JS is what writes to sessionStorage, so fixing Gap C is the one thing that makes the banner appear on the target screen.
- **app/database.py:** Session is configured with `expire_on_commit=False`. That means after `db.flush()` the ORM instance keeps its loaded relationships — only an *explicit* `db.refresh()` re-expires them. Removing the explicit refresh is safe and correct.
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Gap A): Fix category filter 422 on empty companion param</name>
  <files>app/routers/playbooks.py</files>
  <action>
Edit `app/routers/playbooks.py` route `ui_playbook_blocks` (currently at lines 63-92).

**Change the signature** from:
```python
async def ui_playbook_blocks(
    request: Request,
    category_id: uuid.UUID | None = None,
    expert_source_id: uuid.UUID | None = None,
    ...
```
to:
```python
async def ui_playbook_blocks(
    request: Request,
    category_id: str | None = None,
    expert_source_id: str | None = None,
    ...
```

**Add coercion at the top of the function body** (right after the docstring, before `await svc.list_blocks`):

```python
def _parse_uuid(raw: str | None) -> uuid.UUID | None:
    if not raw:
        return None
    try:
        return uuid.UUID(raw)
    except (ValueError, TypeError):
        return None

category_uuid = _parse_uuid(category_id)
expert_uuid = _parse_uuid(expert_source_id)
```

**Replace the call** `svc.list_blocks(db, category_id=category_id, expert_source_id=expert_source_id)` with
`svc.list_blocks(db, category_id=category_uuid, expert_source_id=expert_uuid)`.

**Update the context dict** to pass the parsed uuids for the selected_* keys so the template highlights the right options:
```python
"selected_category_id": category_uuid,
"selected_expert_id": expert_uuid,
```

**DO NOT touch** `app/templates/playbooks/blocks.html` — the `hx-include` cross-inclusion is needed to preserve the other filter dimension and is the correct HTMX pattern.

**DO NOT touch** `svc.list_blocks` — it already handles `None` correctly.

The helper `_parse_uuid` can be defined inline inside the function (local closure) to avoid polluting module scope. That is acceptable for this one-call site.
  </action>
  <verify>
    <automated>docker compose restart api && sleep 3 && docker compose exec -T api sh -c "python -c 'from app.main import app; print(len(app.routes))'" && docker compose exec -T api sh -c "curl -s -o /dev/null -w '%{http_code}\n' -b /tmp/cookies.txt -c /tmp/cookies.txt 'http://localhost:8000/ui/playbooks/blocks?category_id=&expert_source_id='"</automated>

Manual browser check after docker restart:
1. Login as admin → /ui/playbooks/blocks
2. In dropdown "Все категории" select any real category → список должен сузиться, не должно быть server error toast
3. Затем select expert в соседнем dropdown → фильтр комбинируется (категория + эксперт)
4. Выбрать "Все категории" обратно → список восстанавливается с фильтром по эксперту

Devtools Network: GET /ui/playbooks/blocks?category_id=&expert_source_id=... must be 200 (was 422).
  </verify>
  <done>
- Route signature accepts `str | None` for both filter params
- Empty string is coerced to `None` before reaching `svc.list_blocks`
- Invalid UUID string is also coerced to `None` (silent fallback, no 422)
- app.main imports cleanly, still 468 routes
- Manual: dropdown filtering works in browser, no red toast
  </done>
</task>

<task type="auto">
  <name>Task 2 (Gap B): Fix apply modal silent click — HX-Refresh instead of HX-Redirect</name>
  <files>app/routers/playbooks.py</files>
  <action>
Edit `app/routers/playbooks.py` route `ui_project_playbook_apply` (currently at lines 652-676).

**Replace the response** at the end of the function:

From:
```python
return HTMLResponse(
    "",
    status_code=200,
    headers={"HX-Redirect": f"/ui/projects/{project_id}/kanban#playbook"},
)
```

To:
```python
# HX-Refresh triggers a full page reload regardless of current URL
# (HX-Redirect is a no-op when target URL == current URL, which
# happens because switchProjectTab() already wrote #playbook into
# history via replaceState). kanban.html already auto-activates the
# #playbook tab on page load, so the reload lands in the right place.
return HTMLResponse(
    "",
    status_code=200,
    headers={"HX-Refresh": "true"},
)
```

**Update the docstring** of `ui_project_playbook_apply` to reflect the new behaviour:
```python
"""Copy-on-apply a Playbook template to this project.

Returns an HX-Refresh header so the modal closes and the Kanban
page fully reloads. kanban.html reads the URL hash on load and
auto-activates the #playbook tab, so the user lands on the new
ProjectPlaybook card immediately.
"""
```

**DO NOT add** a new hash handler to `kanban.html` — the existing IIFE at `kanban.html:177-183` already calls `switchProjectTab(hash.substring(1))` when `#playbook` is present, and the URL hash is preserved across `HX-Refresh: true` (browser reloads the same URL including hash).

**Note:** The user navigated to the Плейбуки tab BEFORE clicking Применить, so `#playbook` is already in the URL. After `HX-Refresh: true` the browser reloads the same URL (with hash), kanban.html runs the IIFE, switches to #tab-playbook, and the newly-created ProjectPlaybook appears via the normal lazy-load hx-get.
  </action>
  <verify>
    <automated>docker compose restart api && sleep 3 && docker compose exec -T api sh -c "grep -n 'HX-Refresh' app/routers/playbooks.py"</automated>

Manual browser check:
1. Login → open any project → switch to вкладка "Плейбуки" (confirm URL ends with `#playbook`)
2. Click "Применить плейбук" → select "SEO для коммерческих страниц (демо)" → click Применить
3. Expected: модал исчезает, страница перезагружается, вкладка "Плейбуки" активна, на ней карточка ProjectPlaybook с 0/6 прогрессом и 6 шагами
4. Devtools Network: POST /ui/projects/{id}/playbooks/apply → 200, response header `HX-Refresh: true`
  </verify>
  <done>
- Response header is `HX-Refresh: true`, no more `HX-Redirect`
- Docstring updated to match new behaviour
- Manual: модал закрывается, page reloads, Плейбуки tab active, new ProjectPlaybook visible
  </done>
</task>

<task type="auto">
  <name>Task 3 (Gap C): Extract openPlaybookStep to static JS + fix onclick quoting</name>
  <files>app/static/js/playbook_step.js, app/templates/base.html, app/templates/projects/_playbook_project_step.html</files>
  <action>
Three coordinated changes — do them in this order to keep the app working at every step.

**Step 3.1: Create `app/static/js/playbook_step.js` (new file).**

Copy the IIFE body from `_playbook_project_step.html:115-162` into a standalone file. Wrap it in a guard so it's idempotent. Content:

```javascript
/* Phase 999.8 Plan 06 — global openPlaybookStep handler.
 * Loaded once from base.html so HTMX innerHTML swaps of the Playbook tab
 * do not re-inline a <script> tag (HTMX 2.0 inline-script regression).
 */
(function () {
  if (typeof window.openPlaybookStep === 'function') {
    return;
  }
  window.openPlaybookStep = async function (stepId, ppId, projectId, title, pos, total, pbName, actionKind) {
    try {
      sessionStorage.setItem('active_playbook_step', JSON.stringify({
        step_id: stepId,
        project_playbook_id: ppId,
        project_id: projectId,
        title: title,
        position: pos,
        total: total,
        playbook_name: pbName,
        opened_at: Date.now()
      }));
    } catch (e) {
      console.warn('sessionStorage write failed', e);
    }
    try {
      await fetch('/api/project-playbook-steps/' + encodeURIComponent(stepId) + '/open-action', {
        method: 'POST',
        credentials: 'same-origin'
      });
    } catch (e) {
      console.warn('open-action failed', e);
    }
    try {
      const resp = await fetch(
        '/api/playbook-step-route?step_id=' + encodeURIComponent(stepId),
        { credentials: 'same-origin' }
      );
      if (!resp.ok) {
        alert('Не удалось открыть шаг (код ' + resp.status + ')');
        return;
      }
      const data = await resp.json();
      if (data && data.url) {
        window.location.href = data.url;
      } else {
        alert('Шаг завершается вручную');
      }
    } catch (e) {
      console.error('playbook-step-route failed', e);
      alert('Ошибка при открытии шага: ' + (e && e.message ? e.message : 'unknown'));
    }
  };
})();
```

**Step 3.2: Include the new script in `app/templates/base.html`.**

Add ONE line inside `<head>`, right after line 18 (after the `{% endif %}` that closes the admin Shepherd.js block, BEFORE `<style>`):

```html
  <!-- Phase 999.8: global openPlaybookStep handler (loaded on every page, survives HTMX swaps) -->
  <script src="/static/js/playbook_step.js" defer></script>
```

Do NOT wrap it in `{% if is_admin %}` — any authenticated user can click "Перейти к шагу".

**Step 3.3: Fix `app/templates/projects/_playbook_project_step.html`.**

Two edits:

(a) Change the `onclick` attribute on line 107-108 from double-quoted to single-quoted. Replace:
```jinja
  <button type="button"
    onclick="openPlaybookStep('{{ step.id }}', '{{ step.project_playbook_id }}', '{{ project_id }}', {{ step.block.title | tojson }}, {{ step.position + 1 }}, {{ pp_total }}, {{ pp_name | tojson }}, '{{ action_kind }}')"
    class="px-3 py-1.5 text-xs font-medium rounded bg-indigo-600 text-white hover:bg-indigo-700 flex-shrink-0 border-0 cursor-pointer">
    Перейти к шагу
  </button>
```
with:
```jinja
  <button type="button"
    onclick='openPlaybookStep("{{ step.id }}", "{{ step.project_playbook_id }}", "{{ project_id }}", {{ step.block.title | tojson }}, {{ step.position + 1 }}, {{ pp_total }}, {{ pp_name | tojson }}, "{{ action_kind }}")'
    class="px-3 py-1.5 text-xs font-medium rounded bg-indigo-600 text-white hover:bg-indigo-700 flex-shrink-0 border-0 cursor-pointer">
    Перейти к шагу
  </button>
```

Note: outer quotes flipped to single quotes, AND the previously single-quoted string arg literals flipped to double quotes so Jinja |tojson's literal `"` still fits inside double-quoted JSON string tokens. Jinja2 `|tojson` auto-escapes `'` to `\u0027` for exactly this single-quoted-HTML-attribute case — double-quoted JSON strings with literal `"` (like `"Первичный краулинг сайта"`) sit safely inside `onclick='...'` because the HTML parser only looks for the outer `'`.

(b) Delete lines 115-162 (the entire `<script>...</script>` block at the end of the file). The logic now lives in the external JS.

After the edit, the file should end right after `</div>` on (former) line 113 (the closing div of the step row) with a trailing newline.
  </action>
  <verify>
    <automated>docker compose restart api && sleep 3 && docker compose exec -T api sh -c "ls -l app/static/js/playbook_step.js && grep -n 'playbook_step.js' app/templates/base.html && ! grep -q '&lt;script' app/templates/projects/_playbook_project_step.html && grep -c \"onclick='openPlaybookStep\" app/templates/projects/_playbook_project_step.html"</automated>

Note: the `! grep -q '&lt;script'` is just shell-safe shorthand — in the actual verify run use:
`docker compose exec -T api sh -c 'if grep -q "<script" app/templates/projects/_playbook_project_step.html; then echo FAIL inline script remains; exit 1; fi; echo OK'`

Manual browser check:
1. Open any project with a ProjectPlaybook → switch to вкладка Плейбуки
2. Open DevTools console → type `typeof window.openPlaybookStep` → expect `"function"`
3. Click "Перейти к шагу" on step 1 (run_crawl)
4. DevTools Network: POST /api/project-playbook-steps/{id}/open-action → 200; GET /api/playbook-step-route?step_id=... → 200 with `{"url": "/ui/sites/.../crawls"}`
5. Browser navigates to /ui/sites/{site_id}/crawls
6. Full-width indigo баннер сверху показывает "Шаг 1/6 · Первичный краулинг сайта"
7. Back to кanban → step 1 has status "●" (in_progress) — opened_at was stamped
  </verify>
  <done>
- `app/static/js/playbook_step.js` exists with the IIFE defining `window.openPlaybookStep`
- `app/templates/base.html` loads it via `<script src="/static/js/playbook_step.js" defer>`
- `app/templates/projects/_playbook_project_step.html` no longer contains an inline `<script>` tag
- `onclick` attribute uses single outer quotes
- Manual: DevTools console `typeof window.openPlaybookStep === 'function'` both before AND after HTMX tab swap
- Manual: full walking-skeleton flow from tab → click → target page + banner works
  </done>
</task>

<task type="auto">
  <name>Task 4 (Gap D): Fix MissingGreenlet on status toggle — eager-load steps + drop refresh</name>
  <files>app/services/playbook_service.py</files>
  <action>
Edit `app/services/playbook_service.py` — two separate edits in the same file.

**Edit 4.1: Extend `get_project_step()` eager-load chain (currently lines 815-832).**

Replace the `selectinload(ProjectPlaybookStep.project_playbook)` line with a chained eager-load that also pulls `project_playbook.steps` and `project_playbook.steps.block` (the template reads `step.project_playbook.steps | length` at `_playbook_project_step.html:106`).

Change this options block:
```python
        .options(
            selectinload(ProjectPlaybookStep.block)
            .selectinload(PlaybookBlock.category),
            selectinload(ProjectPlaybookStep.block)
            .selectinload(PlaybookBlock.expert_source),
            selectinload(ProjectPlaybookStep.block)
            .selectinload(PlaybookBlock.media),
            selectinload(ProjectPlaybookStep.project_playbook),
        )
```

to:
```python
        .options(
            selectinload(ProjectPlaybookStep.block)
            .selectinload(PlaybookBlock.category),
            selectinload(ProjectPlaybookStep.block)
            .selectinload(PlaybookBlock.expert_source),
            selectinload(ProjectPlaybookStep.block)
            .selectinload(PlaybookBlock.media),
            selectinload(ProjectPlaybookStep.project_playbook)
            .selectinload(ProjectPlaybook.steps),
        )
```

You must also confirm `ProjectPlaybook` is already imported at the top of `playbook_service.py` (it is — `apply_playbook`, `list_project_playbooks`, `get_project_playbook` all use it). No new import needed.

**Edit 4.2: Drop `db.refresh()` from `set_step_status()` (currently lines 835-851).**

Remove the `await db.refresh(step)` line. The session has `expire_on_commit=False` (see `app/database.py:22-25`), so after `db.flush()` the ORM instance still holds all eager-loaded relationships. Explicit refresh re-expires them and forces a lazy-load inside sync Jinja2 context, which triggers `MissingGreenlet`.

Before:
```python
    await db.flush()
    await db.refresh(step)
    return step
```

After:
```python
    await db.flush()
    return step
```

**Edit 4.3: Drop `db.refresh()` from `open_step_action()` too (currently lines 870-888).**

Same rationale, same fix. This function is called by `POST /api/project-playbook-steps/{step_id}/open-action`, which currently returns JSON (not the partial), so this is not causing the visible 500 — but the bug is latent and we want consistency.

Before:
```python
    await db.flush()
    await db.refresh(step)
    return step
```

After:
```python
    await db.flush()
    return step
```

**DO NOT touch** `update_project_step_note()` at line 891 — it already uses only `db.flush()` with no refresh.

**DO NOT touch** `cycle_step_status()` — it delegates to `set_step_status()` and inherits the fix automatically.

**Verify Test 6 path by reading `app/routers/playbooks.py`:** look for `api_project_step_complete` (the `/complete` endpoint). It re-renders `_playbook_project_step.html` with an outerHTML swap. Because it presumably calls `set_step_status(step_id, ProjectPlaybookStepStatus.done)` internally, the fix above closes it automatically. If `api_project_step_complete` does something different (builds its own query), add the same eager-load there. Use Grep to find it:
```
Grep -n "api_project_step_complete\|project-playbook-steps.*complete" app/routers/playbooks.py
```
If that endpoint doesn't call `set_step_status` / `get_project_step`, add a note in the SUMMARY — but do NOT refactor speculatively; the goal is to unblock Test 5 and cascade to Test 6, not to audit everything.
  </action>
  <verify>
    <automated>docker compose restart api && sleep 3 && docker compose exec -T api sh -c "python -c 'from app.main import app; print(len(app.routes))'" && docker compose exec -T api sh -c "grep -n 'selectinload(ProjectPlaybook.steps)' app/services/playbook_service.py" && docker compose exec -T api sh -c "! grep -n 'db.refresh(step)' app/services/playbook_service.py && echo OK-no-refresh"</automated>

Manual browser + server logs check:
1. Open project → вкладка Плейбуки (after Task 2+3 fixes so we can actually get there)
2. `docker compose logs -f api` in another terminal
3. Click the circle button next to step 1 → expect: POST /api/project-playbook-steps/{id}/status → 200, step re-renders with indigo `●` (in_progress)
4. Click again → 200, step re-renders with emerald `✓` (done), title line-through
5. Server logs: NO `MissingGreenlet` tracebacks, NO 500s
6. Also verify /complete endpoint indirectly: Task 3 walking-skeleton flow ends with user clicking "Отметить выполненным" in the banner on the target page → returns to kanban → step 1 shows done state (no 500 in logs)
  </verify>
  <done>
- `get_project_step()` options chain includes `.selectinload(ProjectPlaybook.steps)`
- `set_step_status()` and `open_step_action()` no longer call `db.refresh(step)` — only `db.flush()`
- app.main imports cleanly, still 468 routes (no route count regression)
- Manual: status circle click → 200, no MissingGreenlet
- Manual: /complete path (triggered from banner) → 200, no MissingGreenlet
- Cascade: hint-badges begin to appear for steps that have been opened (Gap E closes automatically per UAT analysis)
  </done>
</task>

</tasks>

<verification>
After all 4 tasks completed:

**Automated sanity:**
```bash
docker compose restart api && sleep 3
docker compose exec -T api python -c "from app.main import app; print(len(app.routes))"   # expect 468
docker compose exec -T api grep -c "HX-Refresh" app/routers/playbooks.py                    # expect ≥1
docker compose exec -T api grep -c "selectinload(ProjectPlaybook.steps)" app/services/playbook_service.py  # expect ≥1
docker compose exec -T api sh -c '! grep -q "db.refresh(step)" app/services/playbook_service.py && echo OK'
docker compose exec -T api ls -l app/static/js/playbook_step.js                             # file exists
docker compose exec -T api grep -q "playbook_step.js" app/templates/base.html && echo OK
docker compose exec -T api sh -c '! grep -q "<script" app/templates/projects/_playbook_project_step.html && echo OK-no-inline'
docker compose logs --since=5m api 2>&1 | grep -iE "missinggreenlet|traceback" && exit 1 || echo "OK-no-greenlet-errors"
```

**Full walking-skeleton UAT replay (do this manually in the browser after the automated sanity passes):**

1. **Gap A smoke:** /ui/playbooks/blocks → выбрать категорию в dropdown → список сужается, не 422, не красный тост.
2. **Gap B smoke:** открыть любой проект → вкладка Плейбуки → Применить плейбук → выбрать демо-шаблон → Применить → модал исчезает, страница перезагружается, вкладка Плейбуки активна, карточка ProjectPlaybook видна.
3. **Gap C smoke:** на новой карточке ProjectPlaybook кликнуть "Перейти к шагу" на step 1 (run_crawl) → браузер переходит на /ui/sites/{site_id}/crawls → sticky indigo banner сверху: "Шаг 1/6 · Первичный краулинг сайта".
4. **Gap D smoke:** вернуться на вкладку Плейбуки → кликнуть круг статуса step 1 → 200 OK (не 500), step рендерится с новым статусом. Повторить 2 раза, пройти open → in_progress → done.
5. **Gap E cascade:** после возврата на вкладку видно, что step 1 сменил status circle, amber hint-pill "Похоже, выполнено" появляется если в базе есть соответствующий CrawlJob после opened_at.
6. **Regression:** вернуться в /ui/playbooks/blocks и /ui/playbooks → ничего не сломано, 468 routes всё ещё импортируются.
</verification>

<success_criteria>
Phase 999.8 UAT walking-skeleton passes end-to-end in a real browser without any 4xx/5xx errors:

- [ ] Test 2 (block library category filter) — pass: dropdown filters work, no 422
- [ ] Test 4 (apply playbook to project) — pass: modal closes, page reloads, tab active, ProjectPlaybook visible
- [ ] Test 5a ("Перейти к шагу") — pass: click navigates to target page with banner
- [ ] Test 5b (status circle click) — pass: 200 OK, no MissingGreenlet
- [ ] Test 6 ("Отметить выполненным" from banner) — pass: step toggles to done, progress advances
- [ ] Test 7 (hint-badges) — pass (cascade): amber pills appear for opened steps with matching system activity
- [ ] No new regressions: `app.main` still imports, 468 routes, existing /ui/playbooks/* pages still render
- [ ] `docker compose logs api` for the UAT session contains zero MissingGreenlet / 500 tracebacks

Target: Phase 999.8 can be closed via `/gsd:complete-milestone` after re-running `/gsd:review` in UAT mode.
</success_criteria>

<output>
After completion, create `.planning/phases/999.8-playbook-builder-reusable-promotion-plan-blocks/999.8-06-uat-fixes-SUMMARY.md` describing:
  - Which of the 5 UAT gaps are now closed (A, B, C, D, and cascaded E)
  - File-level diffs in prose (signature change, header swap, new static file, eager-load chain, dropped refresh)
  - Any unexpected findings during /complete endpoint audit
  - Recommendation: re-run `/gsd:review` in UAT mode to confirm, then `/gsd:complete-milestone 999.8`
</output>
