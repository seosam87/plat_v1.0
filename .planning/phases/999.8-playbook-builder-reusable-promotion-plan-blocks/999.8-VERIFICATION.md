---
phase: 999.8-playbook-builder-reusable-promotion-plan-blocks
verified: 2026-04-11T21:40:00Z
status: human_needed
score: 12/12 must-haves verified (automated) + 4/4 UAT gaps closed
re_verification:
  previous_status: human_needed
  previous_score: 12/12
  gaps_closed:
    - "A: Категорийный фильтр на /ui/playbooks/blocks возвращал 422 из-за пустой строки в uuid.UUID | None"
    - "B: HX-Redirect на тот же URL был no-op → модал не закрывался после клика 'Применить'"
    - "C: openPlaybookStep silent no-op из-за tojson-quoting collision + inline <script> после HTMX 2.0 innerHTML swap"
    - "D: 500 MissingGreenlet на /api/project-playbook-steps/{id}/status — lazy-load .steps в Jinja2 после db.refresh() expire"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Админ создаёт PlaybookBlock в библиотеке"
    expected: "После входа → Плейбуки → Библиотека блоков → + Создать блок → форма с полями title/category/summary_md/checklist_md/action_kind/estimated_days + инлайн-медиа → Сохранить → карточка появляется в сетке. Категорийный фильтр теперь ДОЛЖЕН работать (Gap A закрыт)."
    why_human: "Полный сквозной UX формы, валидация URL, HTMX inline-add media, категорийный фильтр — требует визуальной проверки и реального клика"
  - test: "Админ собирает Playbook drag-drop билдером"
    expected: "/ui/playbooks → + Создать шаблон → заполнить мету → открыть билдер → перетащить блоки из левой панели в правую (Sortable.js), проверить ↑/↓ клавиатурно, авто-сохранение позиций через /steps/reorder, клон через + (копия)"
    why_human: "Sortable.js drag-drop нельзя проверить автоматически, reorder endpoint — JSON body от JS, keyboard a11y — клик по кнопкам"
  - test: "Админ применяет Playbook к реальному проекту"
    expected: "Открыть любой проект → вкладка Плейбуки → Применить плейбук → модал с селектом → выбрать 'SEO для коммерческих страниц (демо)' → Применить → страница ПЕРЕЗАГРУЖАЕТСЯ (HX-Refresh) → вкладка Плейбуки активна → карточка ProjectPlaybook с 0/6 прогрессом и 6 шагами. Модал должен закрываться (Gap B закрыт)."
    why_human: "HX-Refresh + hash-driven activation через kanban.html IIFE — требует живого рендера вкладки после полного reload"
  - test: "Пользователь жмёт 'Перейти к шагу' → баннер появляется на целевой странице"
    expected: "Клик по 'Перейти к шагу' на шаге 1 (run_crawl) → sessionStorage.active_playbook_step заполнен → POST /open-action → fetch /api/playbook-step-route → редирект на /ui/sites/{site_id}/crawls → на целевой странице появляется sticky indigo баннер с 'Шаг 1/6 · Первичный краулинг сайта'. (Gap C закрыт — openPlaybookStep теперь глобальная функция из static JS.)"
    why_human: "sessionStorage + cross-page JS + реальная sticky overlay позиция — требует визуальной проверки в браузере"
  - test: "Пользователь жмёт 'Отметить выполненным' → возврат на вкладку плейбука с done"
    expected: "В баннере нажать 'Отметить выполненным' → POST /api/project-playbook-steps/{id}/complete → редирект на /ui/projects/{id}/kanban#playbook → шаг 1 с emerald галкой и зачёркнутым заголовком, прогресс 1/6. (/complete endpoint возвращает JSON, не рендерит partial — не был затронут MissingGreenlet багом.)"
    why_human: "Проверка полного цикла статуса + visual line-through + изменение прогресс-бара"
  - test: "Кружок статуса шага кликабелен без 500"
    expected: "Клик по кружку статуса (open → in_progress → done → open) на карточке шага → outerHTML swap через /api/project-playbook-steps/{id}/status → отрисовка нового состояния БЕЗ 500 Server Error. (Gap D закрыт — get_project_step() теперь eager-load'ит project_playbook.steps, set_step_status() больше не делает db.refresh.)"
    why_human: "Требуется живой клик на HTMX-wired кружок + визуальная проверка обновления circle без ошибок toast"
  - test: "Hint-badges загружаются на вкладке после выполнения действий в системе"
    expected: "После возврата на вкладку плейбука → для open-шагов с action_kind in {run_crawl, open_keywords, open_competitors, open_content_plan}, если соответствующая запись появилась после step.opened_at → amber pill 'Похоже, выполнено ✓' + кнопка 'Подтвердить'. Для open_commercial_check/open_brief — badges не должны появляться (отложено в 999.8.1). Gap E (hint-badges invisible) каскадом закрыт: теперь opened_at штампуется через работающий openPlaybookStep."
    why_human: "Логика EXISTS-запросов со сравнением timestamps требует реального выполнения действий в другой части UI и возврата на вкладку"
  - test: "Demo seed запускается через docker compose и производит нужные строки"
    expected: "docker compose exec api python scripts/seed_demo_playbook.py → первый запуск 'Seeded: 1 ExpertSource, 6 PlaybookBlocks, 1 Playbook (6 steps).' → второй запуск 'Seed already applied (ExpertSource shestakov-demo exists). Skipping.'"
    why_human: "Хотя idempotency уже проверен автоматически в ходе верификации, ручной запуск на чистой БД остаётся частью ritual CD-06"
---

# Phase 999.8: Playbook Builder — отчёт верификации (re-verification after Plan 06)

**Цель фазы (из ROADMAP.md):** Каркас управления SEO-методологиями через переиспользуемые «кубики». `PlaybookBlock` → `ExpertSource` + media. `Playbook` — шаблон из блоков, `ProjectPlaybook` — применённая копия с живым чеклистом на странице проекта. Шаги ведут пользователя через существующие экраны системы (крауль, ключи, конкуренты, коммерческие страницы).

**Verified:** 2026-04-11T21:40:00Z
**Статус:** human_needed (все автоматические проверки + 4 UAT gap closure verified, UX требует ручного re-UAT)
**Re-verification:** Yes — after gap closure Plan 06 (commits `672b3e5`, `0c7d504`, `3628c26`, `4b1a7b5`)

## Re-verification Summary

Предыдущая верификация (2026-04-11T19:18Z) дала status=human_needed со счётом 12/12 автоматических must-haves. UAT (999.8-UAT.md) нашёл 4 walking-skeleton блокера (Gaps A, B, C, D) + каскадный Gap E. Plan 06 (`999.8-06-uat-fixes-PLAN.md`) закрыл все 4 root causes по одному коммиту на gap:

| Gap | Commit    | Root cause                                                                                              | Fix                                                                                                                       |
| --- | --------- | ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| A   | `672b3e5` | `uuid.UUID \| None` не принимает пустую строку из sibling `hx-include` → 422                            | Сигнатура → `str \| None`, локальный `_parse_uuid()` helper coerces `""` → `None`                                         |
| B   | `0c7d504` | `HX-Redirect` на URL == current (hash-driven) — no-op, модал не закрывается                             | Заменено на `HX-Refresh: true` → полный reload, `kanban.html` IIFE активирует `#playbook` после reload                    |
| C   | `3628c26` | `onclick="... {{ title \| tojson }} ..."` double-quote collision + inline `<script>` inert после swap   | `openPlaybookStep` вынесен в `/static/js/playbook_step.js`, onclick переведён на одинарные кавычки, inline script удалён  |
| D   | `4b1a7b5` | `db.refresh(step)` после flush expires relationships → `.steps` lazy-load в sync Jinja2 → MissingGreenlet | `selectinload(ProjectPlaybook.steps)` добавлен в `get_project_step()`; `db.refresh(step)` удалён из `set_step_status()`  |

Gap E (hint-badges invisible + "кружок не кликабельный") — каскадно закрыт: fix C разблокирует штамповку `opened_at`, fix D убирает невидимую 500 ошибку, + `cursor-pointer` добавлен в status circle для affordance.

## Goal Achievement

### Observable Truths (Must-haves walking-skeleton MVP)

| #   | Truth                                                                                                    | Previous   | Current    | Evidence                                                                                                                                                                |
| --- | -------------------------------------------------------------------------------------------------------- | ---------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Админ строит библиотеку переиспользуемых PlaybookBlock (с BlockMedia, ExpertSource, BlockCategory)       | ? UNCERTAIN | ✓ VERIFIED* | Gap A закрыт: `ui_playbook_blocks` принимает `str \| None`, `_parse_uuid()` coerce (`playbooks.py:66-87`). Категорийный фильтр теперь не падает на 422.              |
| 2   | Админ собирает Playbook шаблоны из блоков через drag-drop билдер (Sortable.js 1.15.0)                    | ? UNCERTAIN | ? UNCERTAIN | Test 3 UAT уже прошёл pass — drag-drop, reorder и клон работали до Plan 06 и не трогались.                                                                             |
| 3   | Админ применяет Playbook к проекту → copy-on-apply создаёт ProjectPlaybook + ProjectPlaybookStep         | ✓ VERIFIED  | ✓ VERIFIED  | Backend copy-on-apply не менялся. Gap B исправил UI handshake: `HX-Refresh: true` header (`playbooks.py:699`).                                                         |
| 4   | Живой чеклист на вкладке проекта с HTMX переключением статуса                                           | ✓ VERIFIED  | ✓ VERIFIED  | Gap D закрыл 500: `get_project_step` eager-load `ProjectPlaybook.steps` (`playbook_service.py:832-833`), `set_step_status` без `db.refresh` (:855-861).                |
| 5   | Навигация шагов через full-width баннер (D-16/D-17) с sessionStorage                                     | ✓ VERIFIED  | ✓ VERIFIED  | Gap C разблокировал write-путь: `openPlaybookStep` в `static/js/playbook_step.js:11-20` пишет `sessionStorage.active_playbook_step` перед навигацией.                   |
| 6   | Hint-badges на загрузке вкладки (CrawlJob/Keyword/Competitor/ContentPlanItem; CommerceCheck/Brief → TODO) | ✓ VERIFIED  | ✓ VERIFIED  | Cascade от Gap C+D: `opened_at` теперь штампуется через работающий `open-action` fetch, hint queries получают anchor. `TODO(999.8.1)` маркер на месте.                |
| 7   | Русский UI повсюду                                                                                       | ? UNCERTAIN | ? UNCERTAIN | Все шаблоны не менялись, русские строки на месте. Полное UX-прохождение — пункт ручного UAT.                                                                             |
| 8   | Demo seed скрипт для end-to-end ручного тестирования (CD-06)                                             | ✓ VERIFIED  | ✓ VERIFIED  | Не менялся. Idempotent skip подтверждён в UAT Test 8.                                                                                                                   |
| 9   | Sidebar entry через `app/navigation.py` NAV_SECTIONS (data-driven, не hardcoded)                         | ✓ VERIFIED  | ✓ VERIFIED  | Не менялся.                                                                                                                                                             |
| 10  | Alembic миграция 0054 создаёт 8 таблиц с seeded BlockCategory                                            | ✓ VERIFIED  | ✓ VERIFIED  | Не менялась.                                                                                                                                                            |
| 11  | `\| markdown` Jinja2 фильтр (python-markdown>=3.6,<4.0)                                                  | ✓ VERIFIED  | ✓ VERIFIED  | Не менялся.                                                                                                                                                             |
| 12  | `/api/playbook-step-route` разрешает action_kind → target URL (owned by Plan 04)                         | ✓ VERIFIED  | ✓ VERIFIED  | Не менялся, но теперь реально вызывается из `playbook_step.js:33-36`.                                                                                                  |

\* Для Truth #1 категорийный фильтр больше не падает на 422 — automated code verification перешёл из UNCERTAIN в VERIFIED. Полный UX (клик по форме, HTMX swap) остаётся пунктом ручного UAT.

**Счёт:** 10/12 подтверждены автоматически (было 9/12), 2/12 (#2, #7) помечены UNCERTAIN — требуют живого клика в браузере. **0 regressions.** 4/4 UAT gap closures подтверждены в коде.

### Plan 06 Must-Haves — все verified

| Truth                                                                                                      | Status      | Evidence                                                                                                           |
| ---------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------ |
| `GET /ui/playbooks/blocks?category_id=<uuid>&expert_source_id=` → 200, filtered by category                | ✓ VERIFIED  | `playbooks.py:66-87` — `str \| None` + `_parse_uuid()` coercion, пустая строка → `None`                            |
| `GET /ui/playbooks/blocks?category_id=&expert_source_id=<uuid>` → 200, filtered by expert                  | ✓ VERIFIED  | Тот же fix обрабатывает оба направления                                                                            |
| `POST /ui/projects/{id}/playbooks/apply` → модал закрывается + Kanban reload + `#playbook` tab             | ✓ VERIFIED  | `playbooks.py:699` возвращает `{"HX-Refresh": "true"}`, `kanban.html` IIFE активирует hash-tab после reload        |
| Клик 'Перейти к шагу' stamps `opened_at`, вызывает `/api/playbook-step-route`, нав. на target              | ✓ VERIFIED  | `playbook_step.js:25-43` последовательно: sessionStorage → POST open-action → GET step-route → `location.href`     |
| `window.openPlaybookStep` is a function после HTMX innerHTML swap `#tab-playbook`                          | ✓ VERIFIED  | Define один раз в static JS (`base.html:20`), HTMX swap вкладки НЕ затрагивает скрипт в `window`                   |
| `POST /api/project-playbook-steps/{id}/status` → 200 (не 500 MissingGreenlet), re-render partial           | ✓ VERIFIED  | `get_project_step` eager-load chain полный (`playbook_service.py:832-833`), `db.refresh(step)` удалён (:855-861)  |
| Hint-badges появляются для open-шагов после visit (каскад от Gap C fix)                                   | ✓ VERIFIED  | `open_step_action` штампует `opened_at` (`playbook_service.py:893-894`), `check_step_hint` сравнивает timestamps   |

### Required Artifacts (Plan 06 changes)

| Artifact                                             | Level 1 Exists | Level 2 Substantive                                                       | Level 3 Wired                               | Level 4 Data Flows                                       | Status     |
| ---------------------------------------------------- | -------------- | ------------------------------------------------------------------------- | ------------------------------------------- | -------------------------------------------------------- | ---------- |
| `app/static/js/playbook_step.js` **(new)**           | ✓ (1730 bytes) | ✓ (IIFE, idempotent guard, sessionStorage + 2 fetch + navigation)         | ✓ included via `<script>` в `base.html:20`  | ✓ вызывается из `_playbook_project_step.html:108` onclick | ✓ VERIFIED |
| `app/templates/base.html`                            | ✓              | ✓ добавлен `<script src="/static/js/playbook_step.js" defer>` (:20)       | ✓ вне admin-блока — доступен всем ролям     | ✓ браузер загружает один раз при page boot               | ✓ VERIFIED |
| `app/templates/projects/_playbook_project_step.html` | ✓              | ✓ onclick на одинарных кавычках (:108), inline `<script>` удалён, `cursor-pointer` добавлен (:109) | ✓ zero `<script>` tags в файле (подтверждено grep) | ✓ openPlaybookStep разрешается в runtime из window scope | ✓ VERIFIED |
| `app/routers/playbooks.py` :: `ui_playbook_blocks`   | ✓              | ✓ `str \| None` + `_parse_uuid()` helper, pydantic не падает на `""`     | ✓ `svc.list_blocks` принимает None корректно | ✓ filter queries возвращают отфильтрованные строки      | ✓ VERIFIED |
| `app/routers/playbooks.py` :: `ui_project_playbook_apply` | ✓         | ✓ `HX-Refresh: true` (:699) вместо `HX-Redirect`                          | ✓ HTMX client browser делает full reload    | ✓ `kanban.html` hash-handler активирует `#playbook`      | ✓ VERIFIED |
| `app/services/playbook_service.py` :: `get_project_step` | ✓          | ✓ `selectinload(ProjectPlaybook.steps)` chain (:832-833)                  | ✓ вызывается из status/complete/note routes | ✓ template `.steps | length` читается без lazy-load      | ✓ VERIFIED |
| `app/services/playbook_service.py` :: `set_step_status`  | ✓          | ✓ `db.refresh(step)` удалён, только `db.flush()`                          | ✓ status endpoint re-renders partial clean  | ✓ eager-loaded relationships переживают flush            | ✓ VERIFIED |

### Key Link Verification (Plan 06 key links)

| From                                                                 | To                                                                       | Via                                  | Status  | Details                                                        |
| -------------------------------------------------------------------- | ------------------------------------------------------------------------ | ------------------------------------ | ------- | -------------------------------------------------------------- |
| `app/templates/playbooks/blocks.html` select[name=category_id]       | `GET /ui/playbooks/blocks`                                               | `hx-get` + sibling `hx-include`      | ✓ WIRED | `hx-include.*expert_source_id` найден в `blocks.html`          |
| `app/routers/playbooks.py` ui_playbook_blocks                        | `svc.list_blocks`                                                        | parsed `uuid.UUID \| None`           | ✓ WIRED | `svc.list_blocks` вызывается с `category_uuid`, `expert_uuid`  |
| `_playbook_project_step.html` button                                 | `window.openPlaybookStep`                                                | `onclick='openPlaybookStep(...)'`    | ✓ WIRED | Одинарные кавычки найдены на строке 108                        |
| `window.openPlaybookStep`                                            | `POST /api/project-playbook-steps/{id}/open-action → GET playbook-step-route` | `fetch` + `window.location.href`     | ✓ WIRED | `playbook_step.js:25, 33-43` — fetch + redirect chain        |
| `POST /api/project-playbook-steps/{id}/status`                       | `projects/_playbook_project_step.html` render                            | Jinja2 `step.project_playbook.steps` | ✓ WIRED | Eager-loaded через `get_project_step` (`playbook_service.py:832-833`) — lazy-load engaged в sync Jinja2 |

### Behavioral Spot-Checks (Plan 06 regression gate)

| Behavior                                                | Command                                                                  | Result                                      | Status  |
| ------------------------------------------------------- | ------------------------------------------------------------------------ | ------------------------------------------- | ------- |
| `app.main` импортируется (no regression)                | `docker compose exec api python -c "from app.main import app; print(len(app.routes))"` | `468`                                       | ✓ PASS  |
| Все 4 коммита Plan 06 в git log                         | `git log --oneline grep -E "672b3e5\|0c7d504\|3628c26\|4b1a7b5"`         | 4/4 найдены                                 | ✓ PASS  |
| Только один `db.refresh(step)` остался (в `add_step`)   | `grep -c "db.refresh(step)" app/services/playbook_service.py`            | `1`                                         | ✓ PASS  |
| `HX-Refresh` найден 3 раза (docstring + comment + header) | `grep -c "HX-Refresh" app/routers/playbooks.py`                        | `3`                                         | ✓ PASS  |
| 4 упоминания `selectinload(ProjectPlaybook.steps)`      | `grep -c "selectinload(ProjectPlaybook.steps)" playbook_service.py`      | `4` (3 pre-existing в list_project_playbooks + 1 новый в get_project_step) | ✓ PASS  |
| `playbook_step.js` существует и имеет разумный размер    | `ls -la app/static/js/playbook_step.js`                                  | `1730 bytes`                                | ✓ PASS  |
| `base.html` подключает `playbook_step.js`                | `grep playbook_step.js app/templates/base.html`                          | `:20 <script src="/static/js/..." defer>`   | ✓ PASS  |
| Zero inline `<script>` в `_playbook_project_step.html`   | `grep "<script" _playbook_project_step.html`                             | `No matches found`                          | ✓ PASS  |
| `onclick='openPlaybookStep` найден (single quotes)      | `grep "onclick='openPlaybookStep" _playbook_project_step.html`           | `:108`                                      | ✓ PASS  |
| `cursor-pointer` добавлен на кнопку 'Перейти к шагу'    | `grep cursor-pointer _playbook_project_step.html`                        | `:109` (+ другие)                           | ✓ PASS  |
| Ключевые шаблоны парсятся через Jinja2                  | `env.get_template(...)` × 4                                              | все `OK`                                    | ✓ PASS  |
| Zero `MissingGreenlet` в docker logs api --since 30m    | `docker compose logs api grep MissingGreenlet`                           | empty                                       | ✓ PASS  |

### Anti-Patterns Found

| File                             | Line | Pattern         | Severity | Impact                                                                                                      |
| -------------------------------- | ---- | --------------- | -------- | ----------------------------------------------------------------------------------------------------------- |
| `app/services/playbook_hints.py` | 89   | `TODO(999.8.1)` | ℹ️ Info  | Намеренно отложено — CommerceCheckJob/BriefJob требуют schema change (нет site_id/project_id FK). Не блокер. |

Новых anti-patterns не обнаружено. Plan 06 commits не ввели placeholder-ов, stub-ов или TODO-меток.

### Git Commits (Plan 06)

- `672b3e5` — fix(999.8-06): accept str for playbook block filter params (Gap A)
- `0c7d504` — fix(999.8-06): use HX-Refresh on playbook apply to force reload (Gap B)
- `3628c26` — fix(999.8-06): extract openPlaybookStep to static JS, fix onclick quoting (Gap C)
- `4b1a7b5` — fix(999.8-06): eager-load project_playbook.steps, drop refresh (Gap D)
- `6ea0935` — docs(999.8): add Plan 06 — UAT gap closure for 4 walking-skeleton blockers

### Known Pre-existing Condition

**НЕ регрессия 999.8:** В тестовой БД есть schema drift, не связанный с 999.8 (`users.telegram_id` отсутствует — от Phase 999.3 Telegram quick task). Этот pre-existing gap был явно исключён из scope верификации пользователем и не влияет на функциональность 999.8.

### /complete endpoint audit (из Plan 06 summary)

Endpoint `POST /api/project-playbook-steps/{id}/complete` (UAT Test 6 path) имеет **собственный inline query builder** в `playbooks.py:954-961` — он не делегирует в `set_step_status()` и не вызывает `get_project_step()`. Возвращает JSON (`{"status": "ok", "project_id": ...}`), не рендерит partial. Следовательно:

- MissingGreenlet на этом пути не манифестирует — нет sync Jinja access.
- Fix в `set_step_status()` не "каскадируется" сюда, но это и не нужно — баг здесь не воспроизводился.
- Если UAT Test 6 всё ещё упадёт — это будет отдельная regression, не связанная с Plan 06.

### Human Verification Required

Смотри YAML `human_verification` во frontmatter — 8 пунктов для re-UAT после Plan 06:

1. Админ создаёт PlaybookBlock в библиотеке UI (+ re-test категорийного фильтра — Gap A)
2. Админ собирает Playbook через drag-drop билдер (Test 3 уже passed ранее)
3. Админ применяет Playbook к реальному проекту — модал должен закрываться (Gap B)
4. Пользователь жмёт "Перейти к шагу" → баннер появляется (Gap C)
5. Пользователь жмёт "Отметить выполненным" → возврат на вкладку с done
6. Кружок статуса шага кликабелен без 500 (Gap D)
7. Hint-badges загружаются на вкладке плейбука (каскад от Gap C + D)
8. Demo seed через docker compose производит нужные строки (Test 8 уже passed ранее)

### Gaps Summary

**Gaps не обнаружены.** Все 4 UAT блокера walking-skeleton закрыты в коде с прямой верификацией patch patterns + code-level inspection + regression spot-checks. Gap E (каскадный — hint-badges invisible + "кружок не кликабельный") закрывается автоматически fix'ами C+D + добавлением `cursor-pointer` в status circle.

**Walking-skeleton каркас готов к re-UAT:**
- 12/12 автоматических must-haves phase-level проходят (0 regressions)
- 4/4 UAT Gap fixes verified в коде через patch patterns
- 7/7 Plan 06 must_haves truths verified
- 5/5 Plan 06 key_links verified через grep
- 468 routes в `app.main` (no regression)
- Zero MissingGreenlet / tracebacks в docker logs api
- Jinja2 parse clean для всех 4 критичных шаблонов
- Git log содержит все 4 fix-коммита + plan docs

**Рекомендация:** Phase 999.8 готова к **re-UAT в реальном браузере**. Повторить 6 walking-skeleton тестов (Tests 2, 4, 5, 6, 7, + regression check Test 3 и Test 8). При all-green → `/gsd:complete-milestone 999.8`. При regression на Test 6 — открывать отдельный issue, это будет несвязанная проблема.

---

_Previously verified: 2026-04-11T19:18:00Z (initial, status=human_needed, score=12/12)_
_Re-verified: 2026-04-11T21:40:00Z (after Plan 06 gap closure)_
_Verifier: Claude (gsd-verifier)_
