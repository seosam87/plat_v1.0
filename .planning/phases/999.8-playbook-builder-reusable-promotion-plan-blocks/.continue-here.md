---
phase: 999.8-playbook-builder-reusable-promotion-plan-blocks
stage: planned
total_plans: 5
total_tasks: 15
status: ready_to_execute
last_updated: 2026-04-11T15:21:37.767Z
---

<current_state>
Фаза 999.8 (Playbook Builder — walking skeleton MVP) полностью спланирована и провалидирована. 5 PLAN.md файлов закоммичены (2ba5428), 15 задач в 3 волнах. Чекер прошёл после 2 итераций: все 3 блокера + 4 major + 1 minor устранены. Готово к запуску `/gsd:execute-phase 999.8`.
</current_state>

<completed_work>

- **CONTEXT.md** (commit abbe979) — заморожены решения D-01..D-20 и CD-01..CD-10
- **UI-SPEC.md** (commits 9be6bac, 87f0d34) — дизайн-контракт (Tailwind токены, focal points, color contract, copywriting)
- **RESEARCH.md** (commit 0735ea3) — инвентарь существующего кода, резолюции CD, рекомендованная декомпозиция на 5 планов
- **5 PLAN.md + ROADMAP update** (commit 2ba5428) — все планы прошли плановика + чекера (2 итерации ревизии)
</completed_work>

<remaining_work>

Выполнение (Execute phase):

- **Wave 1 (foundation)** — Plan 01: data-foundation — 3 задачи
  - 8 SQLAlchemy моделей (`app/models/playbook.py`)
  - Alembic 0054 (8 таблиц + seed BlockCategory + 5 enum типов)
  - `| markdown` filter в `app/template_engine.py` + `Markdown>=3.6,<4.0` в requirements.txt
  - Sidebar через `app/navigation.py` NAV_SECTIONS + `book-open` SVG в sidebar.html
  - Router skeleton `app/routers/playbooks.py` + регистрация в main.py

- **Wave 2 (parallelizable)** — 6 задач
  - **Plan 02: admin-crud** — CRUD блоков/экспертов/категорий, HTMX фильтры, media fieldset, Pydantic AnyHttpUrl
  - **Plan 03: playbook-builder** — список шаблонов, форма создания, drag-drop билдер (Sortable.js@1.15.0), two-phase reorder, clone

- **Wave 3 (sequential: 04 → 05)** — 6 задач
  - **Plan 04: apply-and-project-tab** — copy-on-apply, `playbook_hints.py` (4/6 триггеров), рефакторинг `kanban.html` во вкладки, чеклист, manual_note inline, apply modal, **`/api/playbook-step-route` endpoint**
  - **Plan 05: banner-and-seed** — 2 banner endpoints (active + complete), `playbook_banner.html/js`, include в `base.html` (перед breadcrumbs), `scripts/seed_demo_playbook.py`
</remaining_work>

<decisions_made>

- **Research-first flow** выбран вместо skip-research — фаза крупная, нужен код-инвентарь
- **5 планов в 3 волнах** (не 10+, не <4) — баланс верифицируемости и параллелизации
- **`/api/playbook-step-route` в Plan 04, не Plan 05** — оба плана Wave 3 с недетерминированным порядком, Plan 04 Task 3 зависит от эндпоинта
- **`hx-trigger="intersect once"` вместо `revealed`** — `style.display` toggling не срабатывает на `revealed`, паттерн из `crm/detail.html`
- **CommerceCheckJob/BriefJob триггеры возвращают False** с TODO(999.8.1) — модели без `project_id`/`site_id` FK
- **Markdown filter — новый wave-1 task** — `python-markdown` не установлен (RESEARCH подтвердил)
- **Sidebar через `app/navigation.py` NAV_SECTIONS** — data-driven паттерн проекта
- **kanban.html рефакторится in-place** во вкладки — highest-risk задача в Plan 04
- **Sortable.js 1.15.0 pinned CDN**, загружается ТОЛЬКО в `builder.html`, не в base.html
</decisions_made>

<blockers>
Нет блокеров. Всё готово к исполнению.
</blockers>

<human_actions_pending>
Нет действий, требующих ручного вмешательства перед запуском. Во время исполнения могут понадобиться:
- Запуск Alembic миграции в dev-БД (выполнит executor через task verify)
- Visual QA плашки-banner после Plan 05 (optional, `/gsd:verify-work` покроет)
</human_actions_pending>

<uncommitted_files>

Файлы изменены до паузы плановой работы, не относятся к 999.8:
- `app/routers/crawl.py` — модификации из предыдущей сессии
- `app/templates/crawl/feed.html` — модификации из предыдущей сессии

Эти изменения НЕ затронуты работой по 999.8 и не мешают запуску execute.
</uncommitted_files>

<context>

Архитектурные нюансы, которые должен помнить executor:

1. **Kanban page не табличная сейчас** — Plan 04 Task 3 превращает её во вкладки. Нельзя удалять существующий функционал — обёрнуть содержимое в `<div id="tab-kanban">` и добавить `<div id="tab-playbook" hx-get="..." hx-trigger="intersect once">` рядом.

2. **Plan 04 — highest-risk**. Два сервис-слоя (`playbook_service` + `playbook_hints`), рефакторинг kanban.html, 4 новых шаблона. Если что-то сломается — останавливаемся и дебажим.

3. **Русский UI везде**. Все user-facing строки (кнопки, флеш-сообщения, alert'ы) — на русском. Executor проверяет через grep.

4. **Walking skeleton тест** — после всех 5 планов должна работать сквозная цепочка: admin создаёт ExpertSource → создаёт PlaybookBlock → собирает Playbook → применяет к Project → открывает вкладку Playbook → кликает "Перейти к шагу" → видит плашку → "Отметить выполненным" → шаг done.

5. **Phase 999.8 — BACKLOG**, `requirements_addressed: []` во всех frontmatter. Проверка покрытия — через phase goal (CONTEXT.md), не через REQ-IDs.

6. **Sortable.js CDN pinned**: `https://cdn.jsdelivr.net/npm/sortablejs@1.15.0/Sortable.min.js` — ТОЛЬКО в `builder.html`, НЕ в base.html.

7. **Banner include порядок**: В `base.html` ДО `breadcrumb.html` (внутри `.main-content`). Проверка — Python one-liner в acceptance criteria Plan 05.
</context>

<next_action>
Запустить выполнение фазы:

```
/clear
/gsd:execute-phase 999.8
```

Начнётся с Wave 1 (Plan 01: data-foundation). После успешного применения миграции и создания моделей — Wave 2 (Plans 02 + 03 параллельно или последовательно, разделяют `playbook_service.py` и `playbooks.py` роутер). Затем Wave 3: Plan 04 → Plan 05 строго последовательно.

Если нужна пауза между волнами для визуальной проверки — `/gsd:verify-work` после каждой.
</next_action>
