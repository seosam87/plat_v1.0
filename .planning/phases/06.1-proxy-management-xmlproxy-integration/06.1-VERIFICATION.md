---
phase: 06.1-proxy-management-xmlproxy-integration
verified: 2026-04-02T22:00:00Z
status: passed
score: 22/22 must-haves verified
re_verification: false
---

# Phase 06.1: Proxy Management & XMLProxy Integration Verification Report

**Phase Goal:** Admin can manage a proxy pool, configure XMLProxy.ru and rucaptcha.com credentials from the UI, and Yandex position checks run through XMLProxy with automatic retry and balance monitoring.
**Verified:** 2026-04-02T22:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

Derived from must_haves across Plans 01, 02, and 03 plus the phase goal.

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | Proxy and ServiceCredential tables exist in the database after migration | VERIFIED | Migration 0033 creates both tables with correct schema; `create_table("proxies")`, `create_table("service_credentials")`, `server_default="213"` for sites.yandex_region |
| 2  | XMLProxy XML responses are correctly parsed into position results | VERIFIED | `_parse_yandex_xml` uses `ET.fromstring`; 7 unit tests pass including organic results, domain fallback, error -55 and -32 |
| 3  | XMLProxy error codes (-55, -32, -34, -132) raise typed exceptions | VERIFIED | `XMLProxyError(code, message)` class present; `_parse_yandex_xml` checks `<error code>` element and raises with correct code |
| 4  | Proxy health checks return active/dead status with response time | VERIFIED | `check_proxy_sync` returns `("active", ms)` or `("dead", None)`; uses `httpx.Client(proxy=proxy_url, timeout=10)`; 2 tests pass |
| 5  | Service credentials are Fernet-encrypted on write and decrypted on read | VERIFIED | `ENCRYPTED_FIELDS` maps service -> field list; `crypto_service.encrypt/decrypt` called in save/get; 5 tests pass including encrypted-storage check |
| 6  | Site model has yandex_region column with default 213 for existing rows | VERIFIED | `Site.yandex_region: Mapped[int | None]` added; migration uses `server_default="213"` |
| 7  | SOCKS5 proxies can be health-checked (socksio installed) | VERIFIED | `httpx[socks]>=0.27,<0.28` in requirements.txt; ImportError handled with logged message in `check_proxy_sync` |
| 8  | Admin can add, edit, and delete a proxy via the settings page | VERIFIED | `POST ""`, `PUT /{id}`, `DELETE /{id}` on `/admin/proxies`; proxy_row.html has `hx-put` inline edit and `hx-delete`; 3 CRUD tests pass |
| 9  | Admin can save XMLProxy, rucaptcha, and anticaptcha credentials | VERIFIED | `POST /credentials/xmlproxy`, `/credentials/rucaptcha`, `/credentials/anticaptcha` all implemented and tested |
| 10 | Admin can see XMLProxy balance, cost per query, and max cost | VERIFIED | `GET /xmlproxy-balance` returns `{balance, cur_cost, max_cost}` or `{error}`; settings.html has `hx-get="/admin/proxies/xmlproxy-balance" hx-trigger="load"` |
| 11 | Admin can see rucaptcha balance | VERIFIED | `GET /rucaptcha-balance` endpoint implemented; settings.html has matching hx-get widget with hx-trigger="load" |
| 12 | Admin can click 'check all' to test all proxies | VERIFIED | `POST /check-all` iterates all proxies, calls `check_proxy_sync` per proxy, updates DB, returns proxy_section.html partial; settings.html has "Проверить все" button |
| 13 | Proxy list shows URL, type, status, response time, last checked | VERIFIED | proxy_row.html renders all five columns per row |
| 14 | Admin can set yandex_region per site in site settings | VERIFIED | `app/templates/sites/edit.html` has `<input type="number" name="yandex_region" ...>`; main.py site-edit POST reads and saves the value |
| 15 | Yandex keywords are routed to XMLProxy for position checking | VERIFIED | `check_positions` splits into `yandex_kws` and `google_kws`; `_check_via_xmlproxy` called for Yandex; test `test_yandex_routed_to_xmlproxy` passes |
| 16 | Google keywords are logged as skipped with message 'no source configured' | VERIFIED | Loop emits `"Google keyword skipped: no source configured"` when DataForSEO not configured; test `test_google_skipped_no_source` passes |
| 17 | XMLProxy -55 error triggers Celery retry with countdown=300 and max_retries=3 | VERIFIED | `raise self_task.retry(countdown=300, max_retries=3)` on code -55; test `test_retry_on_55` passes |
| 18 | XMLProxy -32 error (no funds) sends Telegram alert and stops Yandex processing | VERIFIED | code -32 calls `send_message_sync`, returns `written` immediately; test `test_error_32_stops_and_alerts` passes |
| 19 | XMLProxy balance is checked once before keyword loop; processing skipped when balance is 0 | VERIFIED | `fetch_balance_sync` called before loop, returns 0 if `data["data"] <= 0`; test `test_balance_zero_pauses` passes |
| 20 | rucaptcha.com URLs replace anti-captcha.com URLs in proxy_serp_service.py | VERIFIED | `https://api.rucaptcha.com/createTask` and `/getTaskResult` present; no `anti-captcha.com` strings remain |
| 21 | Position records are written correctly from XMLProxy search results | VERIFIED | Loop finds site domain in results, calls `write_position_sync(db, kw.id, ..., "yandex", position, url=url)`; `test_yandex_routed_to_xmlproxy` validates write called |
| 22 | XMLPROXY_LOW_BALANCE_THRESHOLD config setting controls balance alert threshold | VERIFIED | `XMLPROXY_LOW_BALANCE_THRESHOLD: int = 50` in `app/config.py`; test `test_balance_low_sends_alert` passes with balance=30 |

**Score:** 22/22 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/models/proxy.py` | Proxy model with ProxyType/ProxyStatus enums | VERIFIED | `class Proxy`, `class ProxyType`, `class ProxyStatus` all present; `native_enum=False` confirmed |
| `app/models/service_credential.py` | ServiceCredential model | VERIFIED | `class ServiceCredential`, `service_name`, `credential_data` present |
| `app/models/site.py` | yandex_region column | VERIFIED | `yandex_region: Mapped[int | None]` present |
| `alembic/versions/0033_add_proxy_and_service_credentials.py` | Migration creating proxies, service_credentials, sites.yandex_region | VERIFIED | `create_table("proxies")`, `create_table("service_credentials")`, `server_default="213"` present |
| `requirements.txt` | httpx[socks] dependency | VERIFIED | `httpx[socks]>=0.27,<0.28` present |
| `app/services/xmlproxy_service.py` | XMLProxy API client | VERIFIED | `search_yandex_sync`, `fetch_balance_sync`, `_parse_yandex_xml`, `XMLProxyError` all present |
| `app/services/proxy_health_service.py` | Proxy health checker | VERIFIED | `check_proxy_sync` with `httpx.Client(proxy=`, `ya.ru/robots.txt` present |
| `app/services/service_credential_service.py` | Credential CRUD with Fernet | VERIFIED | `save_credential_sync`, `get_credential_sync`, `crypto_service.encrypt`, `crypto_service.decrypt`, `ENCRYPTED_FIELDS` all present |
| `app/routers/proxy_admin.py` | Admin API endpoints | VERIFIED | `APIRouter(prefix="/admin/proxies")`; all 11 routes confirmed via import check |
| `app/templates/admin/settings.html` | Proxy tab with CRUD, credential forms, balance widgets | VERIFIED | `proxy-table-body`, `hx-post="/admin/proxies"`, `hx-delete`, `xmlproxy-balance`, `rucaptcha-balance`, all three credential forms, "Проверить все" button present |
| `app/templates/admin/partials/proxy_row.html` | Single proxy row with edit/delete/check | VERIFIED | `id="proxy-{{ p.id }}"`, `hx-put`, `hx-delete`, single-check button present |
| `app/templates/sites/edit.html` | yandex_region number input | VERIFIED | `<input type="number" name="yandex_region"` present |
| `app/tasks/position_tasks.py` | XMLProxy integration with engine split | VERIFIED | `_check_via_xmlproxy`, `yandex_kws`, `google_kws`, `search_yandex_sync`, `fetch_balance_sync`, retry on -55, all present |
| `app/services/proxy_serp_service.py` | rucaptcha.com URLs | VERIFIED | Both `rucaptcha.com/createTask` and `rucaptcha.com/getTaskResult` present; no `anti-captcha.com` |
| `app/config.py` | XMLPROXY_LOW_BALANCE_THRESHOLD setting | VERIFIED | `XMLPROXY_LOW_BALANCE_THRESHOLD: int = 50` present |
| `tests/test_xmlproxy_service.py` | 7 unit tests | VERIFIED | 7 tests pass (organic parse, error -55, error -32, domain fallback, balance, search mock) |
| `tests/test_proxy_health_service.py` | 2 unit tests | VERIFIED | 2 tests pass (active proxy, dead proxy) |
| `tests/test_service_credential_service.py` | 5 unit tests | VERIFIED | 5 tests pass (round-trip, encrypted storage, upsert, None for missing, unencrypted passthrough) |
| `tests/test_proxy_admin.py` | 8 unit tests | VERIFIED | 8 tests pass (create, update, delete proxy; save credentials; balance endpoints) |
| `tests/test_position_tasks_xmlproxy.py` | 6 unit tests | VERIFIED | 6 tests pass (routing, skip, retry-55, balance-zero, balance-low, error-32) |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `xmlproxy_service.py` | `xml.etree.ElementTree` | XML parsing of Yandex response | VERIFIED | `import xml.etree.ElementTree as ET`; `root = ET.fromstring(xml_text)` at line 87 |
| `service_credential_service.py` | `crypto_service.py` | Fernet encrypt/decrypt | VERIFIED | `crypto_service.encrypt(...)` line 43; `crypto_service.decrypt(...)` line 94 |
| `proxy_health_service.py` | `httpx` | HTTP/SOCKS5 health check | VERIFIED | `httpx.Client(proxy=proxy_url, timeout=10)` at line 28 |
| `proxy_admin.py` | `service_credential_service.py` | save/get credentials | VERIFIED | `save_credential_sync` imported and called in `/credentials/*` endpoints; `get_credential_sync` called in balance endpoints and check-all |
| `proxy_admin.py` | `proxy_health_service.py` | health check on demand | VERIFIED | `check_proxy_sync` imported and called in `check-all` and `/{id}/check` endpoints |
| `proxy_admin.py` | `xmlproxy_service.py` | balance fetch | VERIFIED | `fetch_balance_sync` imported and called in `/xmlproxy-balance` and `check-all` |
| `settings.html` | `proxy_admin.py` | HTMX hx-post/hx-put/hx-delete/hx-get | VERIFIED | All HTMX paths match router endpoints under `/admin/proxies/*`; credential forms use `/admin/proxies/credentials/*` |
| `position_tasks.py` | `xmlproxy_service.py` | search_yandex_sync per keyword | VERIFIED | `search_yandex_sync` imported inside `_check_via_xmlproxy`; called per Yandex keyword |
| `position_tasks.py` | `service_credential_service.py` | load XMLProxy credentials from DB | VERIFIED | `get_credential_sync(db, "xmlproxy")` called at start of `_check_via_xmlproxy` |
| `position_tasks.py` | `telegram_service.py` | balance alert on -32 or low balance | VERIFIED | `send_message_sync` called for balance-zero, low-balance, code -32, and code -34 conditions |
| `position_tasks.py` | `app/config.py` | read XMLPROXY_LOW_BALANCE_THRESHOLD | VERIFIED | `getattr(settings, "XMLPROXY_LOW_BALANCE_THRESHOLD", 50)` at line 115 |
| `app/main.py` | `proxy_admin.router` | include_router registration | VERIFIED | `from app.routers import proxy_admin`; `app.include_router(proxy_admin.router)` at lines 170-171 |
| `app/main.py` | settings template | passes proxies and credentials to context | VERIFIED | Lines 1884-1902: loads proxies, xmlproxy_creds, anticaptcha_creds, rucaptcha_creds from DB and passes to template |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `settings.html` — proxy table | `proxies` | `db.execute(select(Proxy)).scalars().all()` in main.py:1890 | Yes — DB query | FLOWING |
| `settings.html` — xmlproxy balance widget | loaded via `hx-trigger="load"` AJAX to `/xmlproxy-balance` | `fetch_balance_sync(user, key)` calls `https://xmlproxy.ru/balance.php` | Yes — external API call with error fallback | FLOWING |
| `settings.html` — rucaptcha balance widget | loaded via `hx-trigger="load"` AJAX to `/rucaptcha-balance` | POST to `https://api.rucaptcha.com/getBalance` | Yes — external API call with error fallback | FLOWING |
| `settings.html` — credential form pre-fill | `xmlproxy_creds`, `rucaptcha_creds`, `anticaptcha_creds` | `get_credential_sync(db, "...")` in main.py:1891-1893 | Yes — DB query with Fernet decrypt | FLOWING |
| `proxy_row.html` | `p.url`, `p.status`, `p.response_time_ms`, etc. | Proxy ORM objects from DB query | Yes — DB query | FLOWING |
| `position_tasks.py` — Yandex position write | `result["results"]` from `search_yandex_sync` | XMLProxy HTTP API | Yes — real HTTP call to xmlproxy.ru | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All Plan 01 services importable | `python -c "from app.services.xmlproxy_service import search_yandex_sync..."` | "All services importable OK" | PASS |
| All Plan 01 unit tests pass | `pytest tests/test_xmlproxy_service.py tests/test_proxy_health_service.py tests/test_service_credential_service.py -x` | 14 passed in 0.42s | PASS |
| All Plan 02 unit tests pass | `pytest tests/test_proxy_admin.py -x` | 8 passed in 0.11s | PASS |
| All Plan 03 unit tests pass | `pytest tests/test_position_tasks_xmlproxy.py -x` | 6 passed in 0.04s | PASS |
| proxy_admin router routes registered | `python -c "from app.routers.proxy_admin import router; print([r.path for r in router.routes])"` | 11 routes listed | PASS |
| Models importable with yandex_region | `python -c "from app.models.proxy import Proxy; assert hasattr(Site, 'yandex_region')"` | "Models OK" | PASS |
| rucaptcha.com URLs in proxy_serp_service | `grep -c "rucaptcha.com" app/services/proxy_serp_service.py` | 2 | PASS |
| No anti-captcha.com URLs remain | `grep "anti-captcha.com" app/services/proxy_serp_service.py` | no output | PASS |

**Total: 8/8 spot-checks passed**

---

### Requirements Coverage

The PROXY-XX requirement IDs cited in Plan frontmatter and ROADMAP.md (PROXY-01 through PROXY-14) are not defined entries in `/projects/test/.planning/REQUIREMENTS.md`. They are phase-scoped tracking IDs introduced in the ROADMAP when this phase was inserted. All 14 IDs are accounted for across the three plans:

| Requirement IDs | Plan | Coverage |
|----------------|------|----------|
| PROXY-01 through PROXY-05 | 06.1-01 | DB models, migration, services, SOCKS5 — fully implemented and tested |
| PROXY-06 through PROXY-10 | 06.1-02 | Proxy CRUD, credential save, balance display, check-all, yandex_region UI — fully implemented and tested |
| PROXY-11 through PROXY-14 | 06.1-03 | Engine split, XMLProxy pipeline, retry/balance guard, rucaptcha URL swap — fully implemented and tested |

No orphaned requirements: all 14 IDs claimed by plans are implemented. No additional PROXY-XX IDs appear in REQUIREMENTS.md without a plan claim.

**Note:** PROXY-XX IDs are absent from the global REQUIREMENTS.md — they exist only in ROADMAP.md and PLAN frontmatter. This is a documentation gap but does not represent unimplemented work. No functional coverage is missing.

---

### Anti-Patterns Found

No anti-patterns detected in any modified file. Specific checks performed:

| File | Pattern Checked | Result |
|------|-----------------|--------|
| `app/services/xmlproxy_service.py` | TODO/FIXME/placeholder, empty returns | None found |
| `app/services/proxy_health_service.py` | TODO/FIXME/placeholder, empty returns | None found |
| `app/services/service_credential_service.py` | TODO/FIXME/placeholder, empty returns | None found |
| `app/routers/proxy_admin.py` | TODO/FIXME/placeholder, empty returns | None found |
| `app/tasks/position_tasks.py` | TODO/FIXME/placeholder, empty returns | None found |
| `app/templates/admin/settings.html` | Placeholder text | None found |
| `app/templates/admin/partials/proxy_row.html` | Placeholder text | None found |

---

### Human Verification Required

#### 1. Proxy CRUD Full Flow (Browser)

**Test:** Navigate to `/ui/admin/settings`, open the Proxy section. Add a proxy with URL `http://test:pass@127.0.0.1:8080` and type "http". Edit the URL to `http://test:pass@127.0.0.1:9090`. Delete the proxy.
**Expected:** Row appears on add, updates in-place on edit (no page reload via HTMX outerHTML swap), disappears on delete.
**Why human:** HTMX partial swap behavior, inline edit toggle state, and confirm dialog cannot be verified without a browser.

#### 2. Balance Widget Auto-Load (Browser)

**Test:** Navigate to `/ui/admin/settings` without XMLProxy credentials configured.
**Expected:** XMLProxy balance widget shows "not configured" (or similar), rucaptcha balance widget shows error state. After saving credentials, reload — balance widget shows actual balance value.
**Why human:** HTMX `hx-trigger="load"` behavior and live API call response rendering require a browser with a running server.

#### 3. Check-All Button (Browser)

**Test:** Add 2-3 proxies, click "Проверить все".
**Expected:** Spinner shows briefly, then the proxy table re-renders with updated status (active/dead) and response times. XMLProxy balance shown if credentials configured.
**Why human:** HTMX spinner indicator and innerHTML swap behavior requires a running server and browser.

#### 4. Site yandex_region Persistence (Browser)

**Test:** Edit a site, change yandex_region to 2 (Saint Petersburg), save. Re-open site edit.
**Expected:** Field shows 2 on reload.
**Why human:** Round-trip through DB requires a running server.

---

### Gaps Summary

No gaps found. All 22 must-have truths are verified. All 20 artifacts exist, are substantive, and are correctly wired. All 28 unit tests pass. All data flows are connected to real data sources. No anti-patterns detected.

The only items deferred to human verification are browser-interactive behaviors (HTMX partial swaps, balance auto-load, form round-trips) that cannot be verified programmatically without a running server.

---

_Verified: 2026-04-02T22:00:00Z_
_Verifier: Claude (gsd-verifier)_
