---
plan: 03-03
phase: 03-crawler-core
title: Page Snapshot + Diff Computation
status: in_progress
---

## Objective

Add PageSnapshot model, Alembic migration 0005, diff_service with compute_diff and build_snapshot, and wire snapshot saving into the crawl_site task.

## Tasks

1. Add `PageSnapshot` model to `app/models/crawl.py`
2. Create `alembic/versions/0005_add_page_snapshots.py`
3. Create `app/services/diff_service.py` with compute_diff and build_snapshot
4. Update `app/tasks/crawl_tasks.py` to save PageSnapshot with diff after each page
5. Create `tests/test_diff_service.py`
6. Run tests and fix failures

## Verification

`docker compose run --rm api pytest tests/test_diff_service.py -x -q`
