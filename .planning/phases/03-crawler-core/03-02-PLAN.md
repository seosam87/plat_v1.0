---
plan: 03-02
phase: 03-crawler-core
title: Page Data Model + Sitemap Crawler
status: in_progress
---

## Objective

Create CrawlJob and Page SQLAlchemy models, Alembic migration 0004, crawler_service with sitemap parsing and SEO extraction, implement full crawl_site task, and add POST /sites/{site_id}/crawl endpoint.

## Tasks

1. Create `app/models/crawl.py` with CrawlJob and Page models
2. Create `alembic/versions/0004_add_crawl_tables.py`
3. Create `app/services/crawler_service.py` with parse_sitemap, extract_seo_data, classify_page_type
4. Update `app/tasks/crawl_tasks.py` with full BFS crawl implementation
5. Add `POST /sites/{site_id}/crawl` endpoint to `app/routers/sites.py`
6. Create `tests/test_crawl_service.py`
7. Run tests and fix failures

## Verification

`docker compose run --rm api pytest tests/test_crawl_service.py -x -q`
