---
phase: 33.1-fix-mobile-integration-wiring
verified: 2026-04-12T18:30:00Z
status: passed
score: 6/6 must-haves verified
re_verification: false
gaps: []
human_verification: []
---

# Phase 33.1: Fix Mobile Integration Wiring — Verification Report

**Phase Goal:** Закрыть 6 интеграционных gaps из v4.0-MILESTONE-AUDIT.md — мёртвые маршруты, сломанные шаблоны, toast и digest ссылки
**Verified:** 2026-04-12T18:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                                           | Status     | Evidence                                                                                 |
|----|----------------------------------------------------------------------------------------------------------------|------------|------------------------------------------------------------------------------------------|
| 1  | GET /m/sites returns 200 with a list of sites (bottom nav 'Сайты' tab no longer 404s)                          | VERIFIED | `mobile.py:150` — `@router.get("/sites")` handler calls `get_sites(db)`, renders `mobile/sites.html` |
| 2  | GET /m/reports returns 302 redirect to /m/reports/new (bot /report command no longer 404s)                      | VERIFIED | `mobile.py:649` — `@router.get("/reports", response_class=RedirectResponse)` returns status 302 to `/m/reports/new` |
| 3  | Bot /start 'Здоровье' button points to /m/sites, not /m/health (no-site_id 404 eliminated)                     | VERIFIED | `miniapp.py:59` — `make_webapp_button("Сайты", "/m/sites", base)` — label changed to "Сайты", URL is `/m/sites`; no `/m/health` without site_id in bot |
| 4  | mobile/reports/new.html, mobile/tools/list.html, mobile/tools/result.html, mobile/tools/run.html render without TemplateNotFound error | VERIFIED | All 4 files have `{% extends "base_mobile.html" %}` on line 1; `extends "mobile/base_mobile.html"` not found anywhere in mobile templates |
| 5  | HX-Trigger showToast responses from HTMX calls are caught and displayed as toasts in mobile UI                  | VERIFIED | `base_mobile.html:122–132` — full `htmx:afterOnLoad` listener wired; parses `HX-Trigger` header, calls `showToast(trigger.showToast.msg, trigger.showToast.type)` |
| 6  | Digest section links point to /m/* mobile routes, not /ui/* desktop routes                                      | VERIFIED | All 4 digest sections use `/m/` paths: positions→`/m/positions`, crawl errors→`/m/errors`, alerts→`/m/health/{{ item.site_id }}`, tasks→`/m/tasks/new`; zero `/ui/` links in digest.html |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact                                        | Expected                              | Status   | Details                                                                               |
|-------------------------------------------------|---------------------------------------|----------|---------------------------------------------------------------------------------------|
| `app/routers/mobile.py`                         | /m/sites route and /m/reports redirect | VERIFIED | Line 150: `@router.get("/sites")` with `get_sites(db)` call. Line 649: `@router.get("/reports")` returning `RedirectResponse(url="/m/reports/new", status_code=302)` |
| `app/templates/base_mobile.html`                | htmx:afterOnLoad showToast listener    | VERIFIED | Lines 122–132: listener present; reads HX-Trigger header; calls showToast; positioned between showToast function and htmx:responseError listener |
| `app/templates/mobile/digest.html`              | mobile-correct section links           | VERIFIED | Contains `/m/health/{{ item.site_id }}` (alerts block, line 71); no `/ui/` links found |
| `bot/handlers/miniapp.py`                       | corrected 'Здоровье' button URL        | VERIFIED | Line 59: `make_webapp_button("Сайты", "/m/sites", base)` — URL is `/m/sites`; `/m/health` not found anywhere in file |
| `app/templates/mobile/sites.html`               | Site picker template                   | VERIFIED | File exists; line 1: `{% extends "base_mobile.html" %}`; contains `/m/health/{{ site.id }}` links per-site |
| `app/templates/mobile/reports/new.html`         | Correct extends path                   | VERIFIED | Line 1: `{% extends "base_mobile.html" %}` |
| `app/templates/mobile/tools/list.html`          | Correct extends path                   | VERIFIED | Line 1: `{% extends "base_mobile.html" %}` |
| `app/templates/mobile/tools/result.html`        | Correct extends path                   | VERIFIED | Line 1: `{% extends "base_mobile.html" %}` |
| `app/templates/mobile/tools/run.html`           | Correct extends path                   | VERIFIED | Line 1: `{% extends "base_mobile.html" %}` |

### Key Link Verification

| From                                        | To                             | Via                       | Status   | Details                                                                                       |
|---------------------------------------------|--------------------------------|---------------------------|----------|-----------------------------------------------------------------------------------------------|
| base_mobile.html (bottom nav)               | /m/sites route                 | href=/m/sites             | WIRED    | `base_mobile.html:73` links to `/m/sites`; `mobile.py:150` handles that path                 |
| bot/handlers/miniapp.py                     | /m/sites                       | make_webapp_button         | WIRED    | `miniapp.py:59`: `make_webapp_button("Сайты", "/m/sites", base)` — URL matches route         |
| mobile/reports/new.html                     | base_mobile.html               | {% extends %}             | WIRED    | `{% extends "base_mobile.html" %}` — Jinja2 root is `app/templates/`, path resolves correctly |
| base_mobile.html htmx:afterOnLoad listener  | showToast function             | trigger.showToast          | WIRED    | Listener at line 122 calls `showToast(trigger.showToast.msg, trigger.showToast.type)`; `showToast` defined at line 109 (above listener in same script block) |

### Data-Flow Trace (Level 4)

| Artifact                      | Data Variable | Source                                  | Produces Real Data | Status   |
|-------------------------------|---------------|-----------------------------------------|--------------------|----------|
| `app/templates/mobile/sites.html` | `sites`    | `get_sites(db)` via `AsyncSession`      | Yes — DB query via service layer | FLOWING |

Note: `get_sites` is an existing service function already used elsewhere in `mobile.py` (e.g., `/positions` and `/traffic` endpoints). It queries the database for site records — not a stub or hardcoded empty return.

### Behavioral Spot-Checks

Step 7b: SKIPPED — server must be running for HTTP route checks; can verify statically via grep/file checks (all done above). No runnable CLI entry points for these specific fixes without launching the FastAPI app.

### Requirements Coverage

| Requirement | Source Plan  | Description                                                               | Status    | Evidence                                                                    |
|-------------|--------------|---------------------------------------------------------------------------|-----------|-----------------------------------------------------------------------------|
| MOB-01      | 33.1-01-PLAN | Touch-friendly layout with bottom navigation                              | SATISFIED | `/m/sites` added to bottom nav; route now exists; INT-01 closed             |
| HLT-01      | 33.1-01-PLAN | Health card: availability, errors, crawl status, position changes         | SATISFIED | INT-01 fixed: `/m/sites` route bridges to `/m/health/{site_id}` picker      |
| HLT-02      | 33.1-01-PLAN | Launch crawling or create task from health card                           | SATISFIED | INT-01 fixed: navigation to health card no longer blocked by 404            |
| REP-01      | 33.1-01-PLAN | Generate PDF report from phone                                            | SATISFIED | INT-02 fixed: `/m/reports` → 302 to `/m/reports/new`; INT-04: template extends fixed |
| BOT-03      | 33.1-01-PLAN | Bot opens Mini Apps via inline buttons                                    | SATISFIED | INT-02 + INT-03 fixed: bot /reports and /sites buttons both route to valid endpoints |
| TLS-01      | 33.1-01-PLAN | Run any of 6 SEO tools from phone                                         | SATISFIED | INT-04 fixed: `tools/list.html`, `tools/run.html`, `tools/result.html` extends corrected |
| PAG-03      | 33.1-01-PLAN | Quick fix: update title/meta/schema/TOC → push to WP                     | SATISFIED | INT-05 fixed: `htmx:afterOnLoad` listener added; HX-Trigger toasts now visible |
| PAG-04      | 33.1-01-PLAN | Bulk operations with confirmation                                         | SATISFIED | INT-05 fixed: same toast wiring enables bulk operation feedback in mobile UI |
| DIG-02      | 33.1-01-PLAN | Navigate to issue from digest with one tap                                | SATISFIED | INT-06 fixed: all 4 digest links now point to valid `/m/` routes            |

All 9 requirement IDs from the PLAN frontmatter are accounted for. No orphaned requirements from REQUIREMENTS.md for this phase — REQUIREMENTS.md maps all these IDs to phases 26–31 (their originating phases), not to phase 33.1 which is a gap-closure patch phase.

### Commit Verification

All 6 task commits confirmed present in git log:

| Commit   | Task                                            |
|----------|-------------------------------------------------|
| bb007cc  | Add /m/sites route and /m/reports redirect      |
| 4fa5cd6  | Create mobile/sites.html template               |
| bd65912  | Fix broken template extends paths (4 files)     |
| 496760d  | Wire HX-Trigger showToast in base_mobile.html   |
| 9f908b9  | Fix bot button URL                              |
| 905f44c  | Fix digest.html /ui/ links to /m/ routes        |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | None found | — | — |

No TODO/FIXME/placeholder comments found in modified files. No empty return stubs. No hardcoded empty data passed to rendering.

One noted deviation from PLAN: the bot button label was changed from "Здоровье" to "Сайты" to match the destination page. This is a correct decision documented in the SUMMARY and does not constitute an anti-pattern. The URL fix (`/m/health` → `/m/sites`) is what matters for the integration gap.

### Human Verification Required

None. All 6 fixes are statically verifiable:
- Routes: grep-confirmed in `mobile.py`
- Templates: extends path confirmed by file read
- Toast listener: full JS block confirmed in `base_mobile.html`
- Bot button: URL confirmed in `miniapp.py`
- Digest links: no `/ui/` pattern found in `digest.html`
- Commits: all 6 hashes confirmed in git log

### Gaps Summary

No gaps. All 6 integration issues from the v4.0 milestone audit (INT-01 through INT-06) are resolved in the actual codebase, not just documented as complete. The SUMMARY claims match what the code actually contains.

---

_Verified: 2026-04-12T18:30:00Z_
_Verifier: Claude (gsd-verifier)_
