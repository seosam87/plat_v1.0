---
phase: 22-proposal-templates
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified: []
autonomous: false
requirements: [TPL-01, TPL-02, TPL-03, TPL-04]

must_haves:
  truths:
    - "Admin can create a template with name, type, description, and HTML body"
    - "Admin can edit and save an existing template"
    - "Admin can clone a template and land on the clone's edit page"
    - "Admin can delete a template with confirmation"
    - "User can select client and site, click preview, and see rendered template in iframe"
    - "Unresolved variables display with amber background in preview"
    - "Variable panel click inserts variable at CodeMirror cursor"
    - "Sidebar shows 'Шаблоны КП' under CRM section"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete proposal templates feature end-to-end: template CRUD, clone, preview with variable resolution, CodeMirror editor, and navigation.

Purpose: Human verification that all visual and interactive elements work correctly before marking Phase 22 complete.
Output: Verified feature or list of issues to fix.
</objective>

<execution_context>
@/opt/seo-platform/.claude/get-shit-done/workflows/execute-plan.md
@/opt/seo-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-proposal-templates/22-CONTEXT.md
@.planning/phases/22-proposal-templates/22-UI-SPEC.md
@.planning/phases/22-proposal-templates/22-01-SUMMARY.md
@.planning/phases/22-proposal-templates/22-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Full feature verification</name>
  <files>none</files>
  <action>
Human verifies the complete proposal templates feature. No code changes — verification only.

What was built:
- ProposalTemplate model with TemplateType enum (proposal, audit_report, brief)
- CRUD service layer with clone support
- Variable resolver extracting ~15 platform variables into safe dict
- Jinja2 SandboxedEnvironment rendering with unresolved variable highlighting
- Templates router with 9 endpoints (admin-guarded writes, all-user reads)
- List page with card grid, type badges, action buttons, empty state
- Edit page with three-column layout: CodeMirror 6 editor, iframe preview, variable panel
- Dependent client/site select for preview
- Navigation integration under CRM sidebar section

How to verify — start the app if not running: `cd /opt/seo-platform and docker compose up -d`

1. Navigation (D-11): Log in as admin. Verify "Шаблоны КП" under CRM sidebar. Click to navigate to /ui/templates.
2. Empty state (UI-SPEC): First visit shows "Шаблонов пока нет" + CTA.
3. Create (TPL-01, D-01-D-04): Click "Создать шаблон" -> /ui/templates/new. Verify 3-column layout. Enter name/type/description/body. Save.
4. Variable panel (D-06, D-08): 3 groups (КЛИЕНТ, САЙТ, АНАЛИТИКА), 15 vars. Click inserts at cursor.
5. Preview (TPL-02, TPL-03, D-07, D-13-D-15): Select client/site, click preview, check real data. Add unknown var, check amber highlight.
6. Clone (TPL-04, D-12): Clone template, verify "(копия)" name, same body.
7. Edit/save (TPL-01): Change name, save, verify toast, refresh to confirm persistence.
8. Delete (TPL-01): Delete with confirm dialog, verify removal and toast.
9. Type badges (D-09): Create all 3 types, verify violet/blue/green badges.
10. Performance (D-15): Preview under 5 seconds.
11. Access control (D-11): Non-admin sees list but no write buttons; preview still works.

Resume signal: Type "approved" or describe issues.
  </action>
  <verify>Human visual and functional verification — no automated check applicable</verify>
  <done>All 11 verification checks pass, or issues identified and documented for gap closure</done>
</task>

</tasks>

<verification>
All 11 verification steps above pass without errors.
</verification>

<success_criteria>
- All CRUD operations work (create, read, update, delete)
- Clone creates copy with "(копия)" suffix and redirects to edit page
- Preview renders with real data, unresolved vars highlighted in amber
- CodeMirror editor loads and accepts input
- Variable panel inserts at cursor
- Navigation appears in sidebar
- Access control enforced (admin-only writes)
- Performance under 5 seconds for preview
</success_criteria>

<output>
After completion, create `.planning/phases/22-proposal-templates/22-03-SUMMARY.md`
</output>
