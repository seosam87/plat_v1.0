---
phase: 16-ai-geo-readiness-llm-briefs
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - app/routers/audit.py
  - app/services/audit_service.py
  - app/templates/audit/site_audit.html
  - tests/routers/test_audit_geo_filter.py
autonomous: true
requirements: [GEO-03]
must_haves:
  truths:
    - "User can see geo_score column for every page in the audit table"
    - "User can filter the audit table by GEO score range (min/max)"
    - "User can filter the audit table by an individual geo_* check code"
  artifacts:
    - path: app/templates/audit/site_audit.html
      provides: "geo_score column + filter controls (range + check-type select)"
    - path: app/services/audit_service.py
      provides: "Query supports geo_score_min, geo_score_max, geo_check filters"
  key_links:
    - from: app/routers/audit.py
      to: app/services/audit_service.py
      via: "query string params geo_score_min, geo_score_max, geo_check"
      pattern: "geo_score_min|geo_score_max|geo_check"
---

<objective>
Surface GEO score in the existing site audit table UI with filtering by score range and individual check code.

Purpose: Close GEO-03 — GEO readiness becomes visible and filterable in the audit table that already exists at `/audit/{site_id}`. No new audit page is created.
Output: Extended audit router/service/template + smoke test for filter combinations.
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-ai-geo-readiness-llm-briefs/16-CONTEXT.md
@.planning/phases/16-ai-geo-readiness-llm-briefs/16-RESEARCH.md
@app/routers/audit.py
@app/services/audit_service.py
@app/templates/audit/site_audit.html
@app/services/llm/geo_checks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit service + router accept geo filters</name>
  <files>app/services/audit_service.py, app/routers/audit.py, tests/routers/test_audit_geo_filter.py</files>
  <read_first>
    - app/services/audit_service.py (find the function that lists pages with audit results — likely `get_site_audit_pages` or similar; understand its current filter args)
    - app/routers/audit.py (the GET route that renders site_audit.html — its current Query params)
    - app/services/llm/geo_checks.py (GEO_WEIGHTS keys → valid geo_check values)
    - tests/routers/ (existing router test style + smoke_client fixture from Phase 15.1)
    - tests/_smoke_helpers.py (if reusing seed)
  </read_first>
  <action>
    Extend the audit-listing service function (the one called by the audit router GET endpoint) with three new keyword args:
    - `geo_score_min: int | None = None` — filter `Page.geo_score >= geo_score_min`
    - `geo_score_max: int | None = None` — filter `Page.geo_score <= geo_score_max`
    - `geo_check: str | None = None` — when set, JOIN audit_results + audit_check_definitions and require an entry for this page where check_code = geo_check AND status = 'pass'. Validate against `GEO_WEIGHTS.keys()` (the 9 known codes); if invalid, ignore (do not raise — silent skip is the audit-router convention).

    SELECT must include `Page.geo_score` so it's available to the template.

    Extend the router GET (path that currently renders `site_audit.html`) to accept matching `Query()` params:
    ```python
    geo_score_min: int | None = Query(None, ge=0, le=100),
    geo_score_max: int | None = Query(None, ge=0, le=100),
    geo_check: str | None = Query(None),
    ```
    Pass them through to the service. Add `geo_check_options = sorted(GEO_WEIGHTS.keys())` to the template context so the dropdown can render them. Import GEO_WEIGHTS from `app.services.llm.geo_checks`.

    Create `tests/routers/test_audit_geo_filter.py` using the Phase 15.1 `smoke_client` + seed fixtures. Tests:
    - GET /audit/{seeded_site_id} returns 200, body contains "geo_score" or "GEO" header
    - GET /audit/{seeded_site_id}?geo_score_min=50 returns 200
    - GET /audit/{seeded_site_id}?geo_score_min=0&geo_score_max=100 returns 200
    - GET /audit/{seeded_site_id}?geo_check=geo_faq_schema returns 200
    - GET /audit/{seeded_site_id}?geo_check=invalid_code returns 200 (silent skip)
  </action>
  <verify>
    <automated>cd /projects/test && docker compose exec -T api pytest tests/routers/test_audit_geo_filter.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "geo_score_min\|geo_score_max\|geo_check" app/routers/audit.py app/services/audit_service.py` shows ≥6 hits
    - Router test file exists with ≥5 test cases, all pass
    - Existing smoke crawler still passes for /audit routes (`pytest tests/test_ui_smoke.py -k audit -x -q`)
  </acceptance_criteria>
  <done>Audit service and router accept the three new filter params and the template context exposes geo_check_options.</done>
</task>

<task type="auto">
  <name>Task 2: site_audit.html template — geo_score column + filter controls</name>
  <files>app/templates/audit/site_audit.html</files>
  <read_first>
    - app/templates/audit/site_audit.html (existing table layout, filter form pattern, Tailwind classes already used)
    - app/templates/keywords/suggest_results.html (if needed for HTMX-style filter form reference)
    - .planning/phases/16-ai-geo-readiness-llm-briefs/16-CONTEXT.md (D-05 — display labels for each geo code)
  </read_first>
  <action>
    Edit `site_audit.html`:
    1. Add a `<th>GEO</th>` column header in the audit results table (place it next to existing audit-status columns).
    2. In the table row loop, render `{{ page.geo_score if page.geo_score is not none else '—' }}` with a small color badge: green ≥70, yellow 40-69, red <40, gray for None. Use existing Tailwind palette (memory: project uses Tailwind hex consistently).
    3. Add filter controls (place inside or above the existing filter form):
       - Two number inputs: `name="geo_score_min"` (placeholder "Мин GEO", min=0, max=100) and `name="geo_score_max"` (placeholder "Макс GEO")
       - One select: `name="geo_check"` with first `<option value="">— все GEO-проверки —</option>` and one option per `geo_check_options` from context. Use the code as both value and label (Russian titles can stay as a backlog improvement).
    4. Preserve current values via `value="{{ request.query_params.get('geo_score_min', '') }}"` etc., matching how existing filters in this template do it.
    5. Submit button stays the same — form is GET to current URL.

    All labels in Russian (memory: russian_language preference). Do NOT introduce new CSS — use Tailwind utility classes already in the file.
  </action>
  <verify>
    <automated>cd /projects/test && docker compose exec -T api pytest tests/test_ui_smoke.py -k audit -x -q && grep -c "geo_score\|geo_check" app/templates/audit/site_audit.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "geo_score" app/templates/audit/site_audit.html` ≥3
    - `grep -c "geo_check" app/templates/audit/site_audit.html` ≥2
    - UI smoke crawler passes for /audit routes (no Jinja errors)
    - Manual visual check (next plan via human-verify checkpoint at end of phase): GEO column + filter controls visible
  </acceptance_criteria>
  <done>site_audit.html shows geo_score with color badges and exposes filter controls for score range + check code; smoke crawler passes.</done>
</task>

</tasks>

<verification>
- pytest tests/routers/test_audit_geo_filter.py passes
- pytest tests/test_ui_smoke.py -k audit passes
- Audit page renders geo_score column with no Jinja errors
</verification>

<success_criteria>
GEO-03 satisfied: every page row in audit table shows GEO score; user can filter by min/max range and by individual geo_* check code.
</success_criteria>

<output>
After completion, create `.planning/phases/16-ai-geo-readiness-llm-briefs/16-02-SUMMARY.md`
</output>
