---
phase: 16-ai-geo-readiness-llm-briefs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/llm/__init__.py
  - app/services/llm/geo_checks.py
  - app/services/content_audit_service.py
  - app/models/page.py
  - alembic/versions/0041_phase16_geo_and_llm.py
  - tests/services/test_geo_checks.py
autonomous: true
requirements: [GEO-01, GEO-02]
must_haves:
  truths:
    - "Every crawled page can be assigned a GEO score 0-100 from 9 rule-based DOM checks"
    - "GEO checks live in audit_check_definitions as geo_* codes — no parallel infrastructure"
    - "pages.geo_score column is updated after each audit run"
  artifacts:
    - path: app/services/llm/geo_checks.py
      provides: "9 pure check functions + GEO_WEIGHTS dict + compute_geo_score()"
    - path: alembic/versions/0041_phase16_geo_and_llm.py
      provides: "geo_score column on pages, geo_* rows in audit_check_definitions"
    - path: tests/services/test_geo_checks.py
      provides: "Unit tests for all 9 check functions + score computation"
  key_links:
    - from: app/services/content_audit_service.py
      to: app/services/llm/geo_checks.py
      via: "_CHECK_RUNNERS dict registers geo_* runners"
      pattern: "geo_faq_schema|geo_answer_first|compute_geo_score"
---

<objective>
Add 9 rule-based GEO readiness checks to the existing audit_check_definitions system, store the resulting score as `pages.geo_score`, and unit-test every check function.

Purpose: Establish GEO-01 and GEO-02 — every page gets a 0-100 GEO score computed at audit time using BeautifulSoup-only DOM checks. No new audit infra; reuses content_audit_service runner pattern.
Output: geo_checks module, migration adding column + rows, runner wiring, and tests.
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ai-geo-readiness-llm-briefs/16-CONTEXT.md
@.planning/phases/16-ai-geo-readiness-llm-briefs/16-RESEARCH.md
@app/services/content_audit_service.py
@app/models/page.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GEO check functions + weights module</name>
  <files>app/services/llm/__init__.py, app/services/llm/geo_checks.py, tests/services/test_geo_checks.py</files>
  <read_first>
    - app/services/content_audit_service.py (existing _CHECK_RUNNERS pattern, runner signature)
    - .planning/phases/16-ai-geo-readiness-llm-briefs/16-RESEARCH.md (Pattern 1 — concrete code samples for geo_faq_schema and geo_answer_first; Pattern 2 — GEO_WEIGHTS dict)
    - .planning/phases/16-ai-geo-readiness-llm-briefs/16-CONTEXT.md (D-05 table — exact 9 codes, weights, what each checks)
    - tests/services/ (existing pytest async style, fixture conventions)
  </read_first>
  <behavior>
    - test_geo_faq_schema: returns True for HTML with FAQPage JSON-LD (incl. inside @graph), False otherwise
    - test_geo_article_author: True when Article + Author/Person schema both present
    - test_geo_breadcrumbs: True for BreadcrumbList JSON-LD
    - test_geo_answer_first: True when first <p> after <h1> has ≤60 words AND contains a verb (RU/EN heuristic from RESEARCH)
    - test_geo_update_date: True when time[datetime] OR JSON-LD dateModified present
    - test_geo_h2_questions: True when ≥30% of H2s start with Who/What/How/Why/When/Что/Как/Почему/Когда/Где
    - test_geo_external_citations: True when ≥2 outbound links to whitelist domains (gov, edu, wikipedia.org, reuters.com, bbc.com, nytimes.com)
    - test_geo_ai_robots: True when robots_txt string does NOT block GPTBot, ClaudeBot, PerplexityBot, OAI-SearchBot, Google-Extended
    - test_geo_summary_block: True when element with class/id matching summary|tldr|key-takeaways exists before first H2
    - test_compute_geo_score: passing all 9 → 100; passing none → 0; partial subset sums correctly
    - test_geo_weights_sum_to_100: GEO_WEIGHTS values sum to exactly 100
  </behavior>
  <action>
    Create `app/services/llm/__init__.py` (empty package marker).

    Create `app/services/llm/geo_checks.py` containing:
    - `GEO_WEIGHTS: dict[str, int]` with EXACTLY: geo_faq_schema=15, geo_article_author=15, geo_breadcrumbs=10, geo_answer_first=15, geo_update_date=10, geo_h2_questions=10, geo_external_citations=10, geo_ai_robots=10, geo_summary_block=5 (sum=100, per D-05)
    - `AI_BOT_USER_AGENTS = ["GPTBot", "ClaudeBot", "PerplexityBot", "OAI-SearchBot", "Google-Extended"]` (per D-05 #8)
    - `AUTHORITATIVE_DOMAINS = {".gov", ".edu", "wikipedia.org", "reuters.com", "bbc.com", "nytimes.com", "nature.com", "who.int"}`
    - `QUESTION_PREFIXES = ("who", "what", "how", "why", "when", "where", "что", "как", "почему", "когда", "где", "кто", "зачем")`
    - 9 functions with signature `def check_<code>(html: str, page_data: dict) -> bool` implementing the rules from D-05. Use BeautifulSoup with `lxml` parser. For geo_ai_robots accept `page_data["robots_txt"]` (string, may be empty → return True i.e. not blocked).
    - `compute_geo_score(results: list[dict]) -> int`: sums GEO_WEIGHTS for entries where `r["check_code"].startswith("geo_") and r["status"] == "pass"`. Returns int 0-100.
    - `GEO_CHECK_RUNNERS: dict[str, Callable]` mapping each code to its check function (used by Task 2 wiring).

    Reference exact code shapes from RESEARCH.md Pattern 1 (geo_faq_schema, geo_answer_first). Use the verb regex from RESEARCH verbatim. Per D-05, no ML/NER — BeautifulSoup + regex only.

    Create `tests/services/test_geo_checks.py` with the behaviors listed above. Use small inline HTML strings as fixtures. Use `pytest` (not async — these are pure functions).
  </action>
  <verify>
    <automated>cd /projects/test && docker compose exec -T api pytest tests/services/test_geo_checks.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from app.services.llm.geo_checks import GEO_WEIGHTS; assert sum(GEO_WEIGHTS.values()) == 100"` exits 0
    - All 9 `check_geo_*` functions exist and importable
    - `compute_geo_score([{"check_code":"geo_faq_schema","status":"pass"}])` returns 15
    - pytest test_geo_checks.py: all tests pass, ≥11 test cases
  </acceptance_criteria>
  <done>geo_checks.py implements 9 checks + score function, weights sum to 100, all unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Alembic migration + content_audit_service wiring</name>
  <files>alembic/versions/0041_phase16_geo_and_llm.py, app/models/page.py, app/services/content_audit_service.py</files>
  <read_first>
    - alembic/versions/ (most recent migration — match style, naming, revision chain)
    - app/models/page.py (Page model — see existing nullable int columns like has_toc/has_schema for reference)
    - app/services/content_audit_service.py (find _CHECK_RUNNERS dict and the function that persists check results + how it updates page-level flags after a run)
    - app/services/llm/geo_checks.py (the GEO_CHECK_RUNNERS dict and compute_geo_score from Task 1)
    - .planning/phases/16-ai-geo-readiness-llm-briefs/16-CONTEXT.md (D-05 table for seed rows; D-02 for users column; D-06 for llm_usage table — defined here too because single migration is cleaner)
  </read_first>
  <action>
    Create migration `alembic/versions/0041_phase16_geo_and_llm.py` (revision id e.g. `0041_phase16`, down_revision = previous head — discover via `ls alembic/versions/ | sort | tail -3`).

    The migration MUST do (upgrade):
    1. `op.add_column("pages", sa.Column("geo_score", sa.Integer(), nullable=True))`
    2. `op.add_column("users", sa.Column("anthropic_api_key_encrypted", sa.Text(), nullable=True))` (per D-02 — column on users table; researcher discretion taken: simpler than new table for MVP)
    3. Create table `llm_brief_jobs` (per D-04): id bigserial pk, user_id fk users, brief_id fk content_briefs (nullable), status varchar(20) not null default 'pending' (pending|running|done|failed), output_json jsonb nullable, error_message text nullable, created_at timestamptz not null default now(), updated_at timestamptz not null default now()
    4. Create table `llm_usage` (per D-06): id bigserial pk, user_id fk users, brief_id fk content_briefs nullable, job_id fk llm_brief_jobs nullable, model varchar(64) not null, input_tokens int not null default 0, output_tokens int not null default 0, cost_usd numeric(10,6) not null default 0, status varchar(20) not null, error_message text nullable, created_at timestamptz not null default now()
    5. Index `ix_llm_usage_user_created` on (user_id, created_at desc)
    6. Insert 9 rows into `audit_check_definitions` with codes from D-05 (geo_faq_schema..geo_summary_block), category='geo', weights from GEO_WEIGHTS, severity='info', enabled=true. Use `op.bulk_insert` against a lightweight table reflection. For each row also include a Russian description (1 sentence) — copy from D-05 column 4.

    Downgrade: drop the 9 audit_check_definitions rows by code, drop tables llm_usage and llm_brief_jobs, drop columns users.anthropic_api_key_encrypted and pages.geo_score.

    Update `app/models/page.py`: add `geo_score: Mapped[int | None] = mapped_column(Integer, nullable=True)` matching existing column declaration style.

    Update `app/services/content_audit_service.py`:
    - Import `from app.services.llm.geo_checks import GEO_CHECK_RUNNERS, compute_geo_score`
    - Merge GEO_CHECK_RUNNERS into `_CHECK_RUNNERS` (or extend whatever the existing dispatcher uses) so that when the audit pipeline runs for a page, geo_* checks run alongside existing checks. The runner signature must match — adapt minimally if existing runners take additional args (e.g., async wrapper). DO NOT rewrite existing runners.
    - After per-page checks complete and results are persisted, compute `geo_score = compute_geo_score(results_for_page)` and persist via `page.geo_score = geo_score` then commit. Look for the existing function that finishes a page's audit and add the line there.
    - For `geo_ai_robots` check: pass `page_data={"robots_txt": site.robots_txt or ""}` — if Site model has no `robots_txt` field, pass empty string (the check returns True when not blocked, which is the safe default). DO NOT add a new robots fetch in this plan; surface as a TODO comment referencing Phase 16 backlog.

    Run migration: `docker compose exec -T api alembic upgrade head`
  </action>
  <verify>
    <automated>cd /projects/test && docker compose exec -T api alembic upgrade head && docker compose exec -T api psql $DATABASE_URL -c "select code, weight from audit_check_definitions where code like 'geo_%' order by code" && docker compose exec -T api psql $DATABASE_URL -c "\d pages" | grep geo_score && docker compose exec -T api psql $DATABASE_URL -c "\dt llm_*"</automated>
  </verify>
  <acceptance_criteria>
    - alembic upgrade head succeeds, no errors
    - `select count(*) from audit_check_definitions where code like 'geo_%'` returns 9
    - `select sum(weight) from audit_check_definitions where code like 'geo_%'` returns 100
    - `pages.geo_score` column exists (integer, nullable)
    - `users.anthropic_api_key_encrypted` column exists (text, nullable)
    - `llm_brief_jobs` and `llm_usage` tables exist
    - `grep -n "GEO_CHECK_RUNNERS\|compute_geo_score" app/services/content_audit_service.py` returns ≥2 lines
    - Existing audit tests still pass: `pytest tests/services/test_content_audit_service.py -x -q` (if file exists)
  </acceptance_criteria>
  <done>Migration 0041 applied; geo_* check definitions seeded; pages.geo_score wired; llm tables ready for plan 03; existing audit pipeline still passes its tests.</done>
</task>

</tasks>

<verification>
- All 9 GEO check functions unit-tested
- Migration applied cleanly, downgrade reversible
- audit_check_definitions has exactly 9 geo_* rows summing to weight 100
- Existing content_audit_service tests still pass (no regression)
</verification>

<success_criteria>
GEO-01 backend: 9 rule-based checks implemented and wired into audit pipeline; pages.geo_score gets populated.
GEO-02: geo_* codes live inside audit_check_definitions — no parallel infrastructure created.
LLM tables (llm_brief_jobs, llm_usage) and users.anthropic_api_key_encrypted created in same migration so plans 03/04 don't need their own migration.
</success_criteria>

<output>
After completion, create `.planning/phases/16-ai-geo-readiness-llm-briefs/16-01-SUMMARY.md`
</output>
