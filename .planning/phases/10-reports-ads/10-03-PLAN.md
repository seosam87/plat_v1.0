---
phase: 10-reports-ads
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - requirements.txt
  - app/config.py
  - app/models/report_schedule.py
  - app/services/morning_digest_service.py
  - app/services/smtp_service.py
  - app/services/report_service.py
  - app/tasks/report_tasks.py
  - app/main.py
  - app/templates/admin/report_schedule.html
  - alembic/versions/xxx_add_report_schedules.py
  - tests/test_morning_digest_service.py
autonomous: true
requirements: [DASH-03, DASH-04]

must_haves:
  truths:
    - "Morning digest Telegram message summarises cross-project status daily"
    - "Report delivery is scheduled via Celery Beat + redbeat"
    - "Weekly summary report can be delivered as PDF via email or Telegram"
    - "Admin can configure digest schedule from UI at /ui/admin/report-schedule"
  artifacts:
    - path: "app/models/report_schedule.py"
      provides: "ReportSchedule singleton model for delivery configuration"
      contains: "report_schedules"
    - path: "app/services/morning_digest_service.py"
      provides: "build_morning_digest function"
      exports: ["build_morning_digest"]
    - path: "app/services/smtp_service.py"
      provides: "SMTP email wrapper"
      exports: ["send_email_sync"]
    - path: "app/tasks/report_tasks.py"
      provides: "Celery Beat tasks for morning digest and weekly report"
      contains: "send_morning_digest"
  key_links:
    - from: "app/tasks/report_tasks.py"
      to: "app/services/morning_digest_service.py"
      via: "import build_morning_digest"
      pattern: "build_morning_digest"
    - from: "app/tasks/report_tasks.py"
      to: "app/services/telegram_service.py"
      via: "send_message_sync for Telegram delivery"
      pattern: "send_message_sync"
    - from: "app/tasks/report_tasks.py"
      to: "app/services/smtp_service.py"
      via: "send_email_sync for SMTP delivery"
      pattern: "send_email_sync"
---

<objective>
Add scheduled report delivery via Telegram and SMTP, plus a configurable morning digest (D-07, D-08, D-09). Admin configures schedule from a settings page.

Purpose: DASH-03 requires scheduled report delivery via Celery Beat. DASH-04 requires a configurable morning Telegram digest summarising all projects.

Output: ReportSchedule model, morning_digest_service, smtp_service, Celery Beat tasks, admin config UI.
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-reports-ads/10-CONTEXT.md
@.planning/phases/10-reports-ads/10-RESEARCH.md
@.planning/phases/10-reports-ads/10-01-SUMMARY.md
@.planning/phases/10-reports-ads/10-02-SUMMARY.md

<interfaces>
From app/services/telegram_service.py:
```python
def send_message_sync(text: str) -> bool:
    # Sync Telegram send for Celery tasks, uses httpx
async def send_message(text: str) -> bool:
    # Async version
def is_configured() -> bool:
```

From app/services/digest_service.py (existing pattern):
```python
def compute_digest_cron(day_of_week: int, hour: int, minute: int) -> str:
def register_digest_beat(site_id: str, cron_expression: str) -> None:
    # Uses RedBeatSchedulerEntry
```

From app/tasks/digest_tasks.py (existing pattern):
```python
@celery_app.task(name="app.tasks.digest_tasks.send_weekly_digest", bind=True, max_retries=2, queue="default")
def send_weekly_digest(self, site_id: str) -> dict:
    # Uses get_sync_db() context manager
```

From app/database.py:
```python
def get_sync_db() -> Session:  # context manager for Celery sync tasks
```

From app/config.py (after Plan 01):
```python
APP_URL: str = "http://localhost:8000"
# No SMTP settings yet — must add
```

From app/services/report_service.py (after Plan 02):
```python
async def generate_pdf_report(db: AsyncSession, project_id: UUID, report_type: str) -> bytes:
```

From app/services/dashboard_service.py (from Plan 01):
```python
async def projects_table(db: AsyncSession) -> list[dict]:
    # Returns per-project dicts with top3, top10, top30, open_tasks, in_progress_tasks
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ReportSchedule model, morning digest service, SMTP service, and config</name>
  <files>app/config.py, app/models/report_schedule.py, app/services/morning_digest_service.py, app/services/smtp_service.py, requirements.txt, tests/test_morning_digest_service.py</files>
  <read_first>app/config.py, app/models/change_monitoring.py, app/services/digest_service.py, app/services/telegram_service.py, app/database.py, requirements.txt</read_first>
  <action>
    1. Add `aiosmtplib>=3,<4` to requirements.txt.

    2. Add SMTP settings to app/config.py Settings class:
       ```python
       SMTP_HOST: str = ""
       SMTP_PORT: int = 587
       SMTP_USER: str = ""
       SMTP_PASSWORD: str = ""
       SMTP_FROM: str = ""
       ```
       All default empty — SMTP silently skips if not configured (matching Telegram pattern per D-08).

    3. Create app/models/report_schedule.py:
       - `ReportSchedule(Base)` with `__tablename__ = "report_schedules"`:
         - `id: int` PK (always 1, singleton row).
         - `morning_digest_enabled: bool` default False.
         - `morning_hour: int` default 9.
         - `morning_minute: int` default 0.
         - `weekly_report_enabled: bool` default False.
         - `weekly_day_of_week: int` default 1 (Monday).
         - `weekly_hour: int` default 10.
         - `weekly_minute: int` default 0.
         - `smtp_to: str | None` nullable (admin email recipient, per D-08).
         - `updated_at: datetime` with onupdate=func.now().
       - Import this model in app/models/__init__.py (or wherever models are collected for Alembic).

    4. Create Alembic migration: `cd /projects/test && alembic revision --autogenerate -m "add report_schedules table"`.
       Run `alembic upgrade head` to apply.

    5. Create app/services/morning_digest_service.py:
       - `build_morning_digest(db: Session) -> str`:
         - Query all non-archived projects joined with sites.
         - For each project (max 10), compute: name, site_name, top10 count (from latest keyword_positions), open_tasks, in_progress_tasks.
         - Use synchronous SQLAlchemy session (this runs in Celery sync task).
         - Format as Telegram HTML message (per D-07):
           ```
           <b>SEO Morning Digest - {date}</b>

           {status_icon} <b>{project_name}</b> ({site_name})
           TOP-10: {n}, Tasks: {open} open / {in_progress} in progress
           ...

           <a href='{APP_URL}/ui/dashboard'>Open Dashboard</a>
           ```
         - Truncate to 4000 chars (Telegram 4096 limit).
         - Import `settings.APP_URL` for the dashboard link.

    6. Create app/services/smtp_service.py:
       - `send_email_sync(to: str, subject: str, body_html: str) -> bool`:
         - If `settings.SMTP_HOST` is empty, log debug and return False (skip silently).
         - Use `asyncio.run(_send_email_async(...))` wrapper pattern.
         - `_send_email_async`: create MIMEMultipart, attach HTML body, call `aiosmtplib.send()` with settings.SMTP_HOST/PORT/USER/PASSWORD, use_tls=True, timeout=30.
         - Return True on success, False on exception (log warning with loguru).

    7. Create tests/test_morning_digest_service.py:
       - Test build_morning_digest returns a string containing "<b>SEO Morning Digest".
       - Test message is truncated to <= 4000 chars.
       - Test with 0 projects returns a message (not crash).
       - Mock db session with mock execute results.
  </action>
  <verify>
    <automated>cd /projects/test && python -m pytest tests/test_morning_digest_service.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "aiosmtplib" requirements.txt
    - grep -q "SMTP_HOST" app/config.py
    - grep -q "report_schedules" app/models/report_schedule.py
    - grep -q "build_morning_digest" app/services/morning_digest_service.py
    - grep -q "send_email_sync" app/services/smtp_service.py
    - grep -q "test_" tests/test_morning_digest_service.py
  </acceptance_criteria>
  <done>ReportSchedule model migrated, morning_digest_service builds Telegram message, smtp_service wraps aiosmtplib, SMTP config added, tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create Celery Beat tasks and admin schedule UI</name>
  <files>app/tasks/report_tasks.py, app/main.py, app/templates/admin/report_schedule.html, app/navigation.py</files>
  <read_first>app/tasks/digest_tasks.py, app/services/digest_service.py, app/celery_app.py, app/main.py, app/navigation.py, app/models/report_schedule.py, app/services/morning_digest_service.py, app/services/smtp_service.py, app/services/telegram_service.py, app/templates/base.html</read_first>
  <action>
    1. Create app/tasks/report_tasks.py with two Celery tasks:

       a. `send_morning_digest` task:
          - `@celery_app.task(name="app.tasks.report_tasks.send_morning_digest", bind=True, max_retries=2, queue="default", soft_time_limit=60, time_limit=90)`
          - Uses `get_sync_db()` context manager.
          - Calls `build_morning_digest(db)` to get message text.
          - Sends via `telegram_service.send_message_sync(msg)` (per D-07 — Telegram text, not PDF).
          - Returns `{"sent_telegram": bool}`.

       b. `send_weekly_summary_report` task:
          - Same task decorator pattern with max_retries=2, queue="default".
          - Uses `get_sync_db()` for DB access.
          - Fetches all non-archived projects.
          - For each project, generates brief PDF via `report_service.generate_pdf_report` (need sync wrapper — use `asyncio.run()` around the async function).
          - Sends PDF via SMTP (`smtp_service.send_email_sync`) if SMTP configured (per D-08).
          - Sends summary text via Telegram if configured.
          - Returns `{"projects_reported": int, "smtp_sent": bool, "telegram_sent": bool}`.

    2. Add schedule registration logic in app/tasks/report_tasks.py:
       - `register_report_beats(schedule: ReportSchedule) -> None`:
         - If `schedule.morning_digest_enabled`: create/update RedBeatSchedulerEntry with name "report:morning-digest" and crontab from schedule.morning_hour/minute.
         - If not enabled: delete the entry if it exists.
         - Same pattern for weekly summary with "report:weekly-summary" entry using schedule.weekly_day_of_week/hour/minute.
         - Follow the existing pattern from `digest_service.register_digest_beat()`.
       - `restore_report_schedules_from_db() -> None`:
         - Load ReportSchedule from DB (singleton row, id=1).
         - If exists and has any enabled schedule, call register_report_beats.
         - Called during app startup to restore schedules after Redis flush (per D-09).

    3. Add UI route in app/main.py:
       - `GET /ui/admin/report-schedule` handler:
         - Requires admin auth.
         - Load ReportSchedule from DB (create default if not exists: id=1, all disabled).
         - Render `admin/report_schedule.html` with schedule data.
       - `POST /ui/admin/report-schedule` handler:
         - Requires admin auth.
         - Parse form fields: morning_digest_enabled (checkbox), morning_hour, morning_minute, weekly_report_enabled, weekly_day_of_week, weekly_hour, weekly_minute, smtp_to.
         - Upsert ReportSchedule (id=1) with new values.
         - Call `register_report_beats(schedule)` to update Celery Beat.
         - Redirect back to GET with success flash.

    4. Create app/templates/admin/report_schedule.html (extends base.html):
       - Breadcrumb: Dashboard > Settings > Report Schedule.
       - Title: "Report Schedule".
       - Form with sections:
         - Morning Digest: checkbox to enable, hour/minute selectors (0-23 / 0-59), note "Compact Telegram message (D-07)".
         - Weekly Summary: checkbox to enable, day-of-week selector (Mon-Sun), hour/minute selectors, note "PDF report via email".
         - Email recipient: text input for smtp_to (per D-08 — admin email).
       - Submit button "Save Schedule".
       - Use HTMX for form submit (hx-post, hx-swap) or standard form POST.
       - Tailwind classes, no inline styles.

    5. Add "Report Schedule" child to the Settings section in app/navigation.py:
       - Add to the "settings" section children list: `{"id": "settings-report-schedule", "label": "Report Schedule", "url": "/ui/admin/report-schedule", "admin_only": True}`.
  </action>
  <verify>
    <automated>cd /projects/test && python -c "from app.tasks.report_tasks import send_morning_digest, register_report_beats; print('imports OK')" && grep -q "report-schedule" app/navigation.py && echo "nav OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "send_morning_digest" app/tasks/report_tasks.py
    - grep -q "send_weekly_summary_report" app/tasks/report_tasks.py
    - grep -q "register_report_beats" app/tasks/report_tasks.py
    - grep -q "report-schedule" app/navigation.py
    - grep -q "/ui/admin/report-schedule" app/main.py
    - test -f app/templates/admin/report_schedule.html
    - grep -q "morning_digest_enabled" app/templates/admin/report_schedule.html
  </acceptance_criteria>
  <done>Morning digest and weekly summary tasks registered via Celery Beat + redbeat. Admin can configure schedule from /ui/admin/report-schedule. Telegram digest is text-only (D-07). Reports go to single admin recipient (D-08).</done>
</task>

</tasks>

<verification>
1. `python -c "from app.tasks.report_tasks import send_morning_digest"` — import succeeds
2. `python -m pytest tests/test_morning_digest_service.py -x -v` — digest tests pass
3. `grep "report-schedule" app/navigation.py` — nav entry added
4. Report schedule template has morning/weekly toggle controls
</verification>

<success_criteria>
- Morning digest task sends compact Telegram text (not PDF) per D-07
- Weekly summary task sends PDF via SMTP per D-08
- Schedule configurable from admin UI with daily/weekly options per D-09
- Celery Beat entries registered via redbeat, survive Redis flush
- SMTP silently skips if not configured
</success_criteria>

<output>
After completion, create `.planning/phases/10-reports-ads/10-03-SUMMARY.md`
</output>
