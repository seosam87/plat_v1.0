---
phase: v4-04-section-positions-keywords
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/keywords/index.html
  - app/templates/positions/index.html
autonomous: true
requirements:
  - KW-V4-01
  - KW-V4-02
  - KW-V4-03

must_haves:
  truths:
    - "Keywords page renders with Tailwind utility classes and zero style= attributes"
    - "Positions page renders with Tailwind utility classes and zero style= attributes"
    - "All existing functionality preserved: HTMX inline edits, Chart.js modals, async position check polling, filters, pagination"
    - "Import link on keywords page still points to /ui/uploads (temporary until v4-07)"
  artifacts:
    - path: "app/templates/keywords/index.html"
      provides: "Tailwind-migrated keywords list page"
      contains: "class=\"bg-white"
    - path: "app/templates/positions/index.html"
      provides: "Tailwind-migrated positions page with distribution bar and modals"
      contains: "class=\"bg-white"
  key_links:
    - from: "app/templates/keywords/index.html"
      to: "/ui/keywords/{kw.id}"
      via: "hx-patch and hx-delete HTMX attributes"
      pattern: "hx-patch=.*keywords"
    - from: "app/templates/positions/index.html"
      to: "Chart.js position-chart canvas"
      via: "showChart() JS function"
      pattern: "showChart\\("
---

<objective>
Migrate keywords/index.html and positions/index.html to pure Tailwind CSS — replacing all inline style= attributes with Tailwind utility classes while preserving every functional element (HTMX inline edits, group filters, pagination, distribution bar, Chart.js modal, compare modal, lost/gained modal, async position check polling).

Purpose: Core keyword and position pages are the most-used pages in the section; migrating them first establishes the pattern for the remaining 4 templates.
Output: Two fully Tailwind-migrated templates with zero style= attributes.
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP-v4.md
@.planning/phases/v4-04-section-positions-keywords/v4-04-section-CONTEXT.md

<interfaces>
<!-- Reference pattern: v4-03 sites/index.html (already Tailwind-migrated) -->
<!-- Key Tailwind patterns to follow: -->

Table pattern:
- Wrapper: class="bg-white rounded-lg shadow-sm border border-gray-200 p-4"
- Table: class="min-w-full divide-y divide-gray-200"
- Thead: class="bg-gray-50"
- Th: class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
- Tbody: class="bg-white divide-y divide-gray-100"
- Tr hover: class="hover:bg-gray-50"

Button patterns:
- Primary: class="inline-flex items-center px-3 py-1.5 bg-indigo-600 text-white text-sm font-medium rounded-md hover:bg-indigo-700"
- Small: class="px-2 py-1 text-xs font-medium rounded"
- Colored: bg-{color}-50 text-{color}-700 hover:bg-{color}-100
- Danger: bg-red-600 text-white hover:bg-red-700

Badge pattern:
- class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-{color}-50 text-{color}-700"

Form input pattern:
- class="px-2 py-1 border border-gray-300 rounded text-xs bg-white focus:ring-indigo-500 focus:border-indigo-500"

Colors: indigo=primary, emerald=success, red=danger, sky=info, amber=warning, gray=neutral
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate keywords/index.html to Tailwind</name>
  <files>app/templates/keywords/index.html</files>
  <read_first>
    - app/templates/keywords/index.html (current state — 116 lines, 23 inline styles)
    - app/templates/sites/index.html (Tailwind reference pattern from v4-03)
  </read_first>
  <action>
Rewrite app/templates/keywords/index.html replacing ALL style= attributes with Tailwind utility classes per D-05. Keep extends "base.html", block title, block breadcrumbs, block content structure intact.

Specific migrations:
1. Outer wrapper: replace `class="card" style="margin-bottom:1.5rem"` with `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4 mb-6"`
2. Header row (h2 + buttons): replace `style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1rem"` with `class="flex justify-between items-center mb-4"`
3. Count span: replace `style="color:#6b7280; margin-left:1rem"` with `class="text-gray-500 ml-4"`
4. Nav buttons (Positions, Clusters, Import): replace inline bg/color styles with Tailwind:
   - Positions: `class="inline-flex items-center px-2 py-1 text-xs font-medium rounded bg-violet-600 text-white hover:bg-violet-700"`
   - Clusters: `class="inline-flex items-center px-2 py-1 text-xs font-medium rounded bg-sky-500 text-white hover:bg-sky-600"`
   - Import: `class="inline-flex items-center px-2 py-1 text-xs font-medium rounded bg-emerald-600 text-white hover:bg-emerald-700"` (keep href="/ui/uploads" per D-02)
5. Group filter bar: replace flex styles with `class="flex gap-2 mb-4 flex-wrap items-center"`, label with `class="text-sm text-gray-500"`
6. Add keyword form: replace flex/gap styles with `class="flex gap-2 mb-6 flex-wrap"`, inputs with `class="flex-1 min-w-[200px] px-3 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"` etc.
7. Table: apply standard table pattern from interfaces block above
8. Keyword phrase td: replace `style="font-weight:500"` with `class="font-medium"`
9. Group select dropdown in table rows: replace inline styles with `class="px-1 py-0.5 border border-gray-300 rounded text-xs bg-white focus:ring-indigo-500 focus:border-indigo-500"`
10. Target URL inline edit form: replace `style="display:flex;gap:4px"` with `class="flex gap-1"`, input with `class="w-[150px] px-1 py-0.5 border border-gray-300 rounded text-xs"`, OK button with `class="px-2 py-0.5 text-xs font-medium rounded bg-gray-100 text-gray-700 hover:bg-gray-200"`
11. Delete button: replace `style="background:#991b1b;color:white"` with `class="px-2 py-1 text-xs font-medium rounded bg-red-800 text-white hover:bg-red-700"`
12. Pagination: replace flex/margin styles with `class="mt-4 flex gap-2 justify-center"`, span with `class="px-2 py-1 text-gray-500 text-sm"`
13. Empty state p: replace `style="color:#6b7280"` with `class="text-gray-500"`

Preserve ALL hx-patch, hx-delete, hx-target, hx-swap, hx-confirm attributes exactly as they are.
Preserve ALL Jinja2 template variables and control flow ({% if %}, {% for %}, etc.) exactly.
Preserve the btn and btn-primary CSS classes where used on buttons (they come from base.html stylesheet).
  </action>
  <verify>
    <automated>cd /projects/test && grep -c 'style=' app/templates/keywords/index.html | grep -q '^0$' && echo "PASS: zero inline styles" || echo "FAIL: inline styles remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'style=' app/templates/keywords/index.html` returns 0
    - `grep -c 'class="' app/templates/keywords/index.html` returns >= 20
    - `grep 'hx-patch' app/templates/keywords/index.html` returns at least 2 matches (group select + target URL form)
    - `grep 'hx-delete' app/templates/keywords/index.html` returns at least 1 match (delete button)
    - `grep '/ui/uploads' app/templates/keywords/index.html` returns at least 1 match (Import link preserved per D-02)
    - `grep 'bg-white rounded-lg' app/templates/keywords/index.html` returns at least 1 match (card wrapper)
    - `grep 'extends "base.html"' app/templates/keywords/index.html` returns 1 match
  </acceptance_criteria>
  <done>keywords/index.html has zero style= attributes, all Tailwind utility classes, all HTMX interactions preserved, Import link still points to /ui/uploads</done>
</task>

<task type="auto">
  <name>Task 2: Migrate positions/index.html to Tailwind</name>
  <files>app/templates/positions/index.html</files>
  <read_first>
    - app/templates/positions/index.html (current state — 330 lines, 68 inline styles)
    - app/templates/sites/index.html (Tailwind reference pattern from v4-03)
  </read_first>
  <action>
Rewrite app/templates/positions/index.html replacing ALL style= attributes with Tailwind utility classes per D-05. This is the most complex template with distribution bar, filters, table, 3 modals, Chart.js, and async position check polling.

Specific migrations:

**Main card and header:**
1. Outer wrapper: keep `class="card"` — add no style attributes
2. Header flex: `class="flex justify-between items-center mb-4"`
3. h2: `class="text-lg font-semibold text-gray-800 m-0"`
4. Check status span: `class="text-sm text-gray-500 mr-2"` (remove style=, keep id="check-status")
5. Check button: keep `class="btn btn-primary"`, remove any style= (keep id="check-btn" and onclick)

**Distribution bar:**
6. Distribution wrapper: `class="mb-6"`
7. Legend flex: `class="flex gap-4 text-sm mb-2"`
8. Legend items — replace inline color styles with Tailwind text colors:
   - TOP-3: `class="text-emerald-600"`
   - TOP-10: `class="text-blue-600"`
   - TOP-30: `class="text-gray-500"`
   - TOP-100: `class="text-amber-500"`
   - Not ranked: `class="text-red-600"`
   - Total: `class="text-gray-700 ml-auto"`
9. Bar container: `class="flex h-2 rounded overflow-hidden bg-gray-200"`
10. Bar segments: replace inline width+background styles with Tailwind bg colors. Width MUST stay as inline style because it is dynamic (Jinja2 calculation). So use: `class="bg-emerald-600" style="width:{{ ... }}%"` — this is the ONE exception where style= is acceptable (dynamic width percentage). Same for other segments using bg-blue-600, bg-gray-500, bg-amber-500, bg-red-600.

**Filters:**
11. Top/Engine filter bar: `class="flex gap-3 mb-4 flex-wrap items-center"`
12. Filter labels: `class="text-sm text-gray-500"` (replace inline styles)
13. Engine label: `class="ml-4 text-sm text-gray-500"`

**URL filter:**
14. URL filter row: `class="flex gap-2 mb-4 items-center"`
15. Form inside: `class="flex gap-2 items-center"`
16. URL input: `class="w-[300px] px-2 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"`
17. Compare dates button: `class="px-2 py-1 text-xs font-medium rounded bg-violet-600 text-white hover:bg-violet-700"` (replace style + onclick kept as-is)
18. Lost/Gained button: `class="px-2 py-1 text-xs font-medium rounded bg-emerald-600 text-white hover:bg-emerald-700"` (replace style + onclick kept)
19. Button group: `class="ml-auto flex gap-2"`

**Positions table:**
20. Apply standard table pattern (same as keywords task 1)
21. Keyword td: `class="font-medium"`
22. Position value: replace inline color+weight styles with Tailwind classes using Jinja2 conditions:
    - `{% if p.position <= 3 %}class="font-bold text-emerald-600"{% elif p.position <= 10 %}class="font-bold text-blue-600"{% elif p.position <= 30 %}class="font-bold text-gray-500"{% else %}class="font-bold text-red-600"{% endif %}`
23. Delta up: `class="text-emerald-600"`, delta down: `class="text-red-600"`, delta zero: `class="text-gray-500"`
24. URL td: `class="max-w-[250px] overflow-hidden text-ellipsis whitespace-nowrap"`
25. Chart button: keep `class="btn btn-sm"` and onclick

**Chart modal:**
26. Overlay: `class="hidden fixed inset-0 bg-black/50 z-[100] justify-center items-center"` (keep id="chart-modal")
27. Modal body: `class="bg-white rounded-lg p-8 max-w-[800px] w-[90%] max-h-[80vh] relative"`
28. Close button: `class="absolute top-2 right-4 bg-transparent border-none text-2xl cursor-pointer"`
29. Chart title h3: `class="m-0 mb-4"`

**Compare dates modal:**
30. Overlay: `class="hidden fixed inset-0 bg-black/50 z-[100] justify-center items-center"` (keep id="compare-modal")
31. Modal body: `class="bg-white rounded-lg p-8 max-w-[900px] w-[95%] max-h-[85vh] overflow-y-auto relative"`
32. Close button: same as chart modal
33. Form inside: `class="flex gap-2 mb-4 items-center"`
34. Date labels: `class="text-sm"`, second label: `class="text-sm ml-2"`
35. Date inputs: `class="px-2 py-1 border border-gray-300 rounded"`

**Lost/Gained modal:**
36. Overlay: `class="hidden fixed inset-0 bg-black/50 z-[100] justify-center items-center"` (keep id="lg-modal")
37. Modal body: `class="bg-white rounded-lg p-8 max-w-[700px] w-[90%] max-h-[80vh] overflow-y-auto relative"`
38. Close button: same pattern
39. Loading text: `class="text-gray-500"`

**JavaScript section:**
40. In the JS functions that dynamically set styles (compare result table, lost/gained HTML), replace inline style= strings with Tailwind classes:
    - Compare "no data" p: change `style="color:#6b7280"` to `class="text-gray-500"`
    - Gained h4: change `style="color:#059669..."` to `class="text-emerald-600 mt-0 mb-2"`
    - Lost h4: change `style="color:#dc2626..."` to `class="text-red-600 mt-4 mb-2"`
    - Delta spans in compare JS: `class="text-emerald-600"` for positive, `class="text-red-600"` for negative, `class="text-gray-500"` for zero
    - Loading text in lg-result: `class="text-gray-500"`
    - No data paragraphs: `class="text-gray-500"`
41. In pollTaskStatus and startPositionCheck, replace inline color styles in status.innerHTML with Tailwind classes:
    - Blue status: `class="text-blue-600"`
    - Green success: `class="text-emerald-600"`
    - Red error: `class="text-red-600"`
    - Gray: `class="text-gray-500"`

**Critical:** Modals use `style.display='flex'` and `style.display='none'` in JS onclick handlers. Replace this pattern with Tailwind hidden class toggling:
- Initial state: add `hidden` class (replacing `display:none`)
- JS show: use `classList.remove('hidden'); classList.add('flex')` instead of `style.display='flex'`
- JS hide: use `classList.add('hidden'); classList.remove('flex')` instead of `style.display='none'`
- Update all onclick handlers and JS functions accordingly

Preserve ALL Jinja2 template variables, control flow, Chart.js script, fetch URLs, polling logic exactly.
  </action>
  <verify>
    <automated>cd /projects/test && STYLE_COUNT=$(grep -c 'style=' app/templates/positions/index.html); DYNAMIC_WIDTHS=$(grep -c 'style="width:' app/templates/positions/index.html); REAL_STYLES=$((STYLE_COUNT - DYNAMIC_WIDTHS)); echo "Total style=: $STYLE_COUNT, Dynamic widths: $DYNAMIC_WIDTHS, Non-width styles: $REAL_STYLES"; [ "$REAL_STYLES" -le 0 ] && echo "PASS" || echo "FAIL: $REAL_STYLES non-width inline styles remain"</automated>
  </verify>
  <acceptance_criteria>
    - All `style=` attributes removed EXCEPT dynamic width percentages on distribution bar segments (up to 5 allowed for `style="width:{{ ... }}%"`)
    - `grep -c 'class="' app/templates/positions/index.html` returns >= 40
    - `grep 'Chart(' app/templates/positions/index.html` returns 1 match (Chart.js instantiation preserved)
    - `grep 'pollTaskStatus' app/templates/positions/index.html` returns at least 1 match (async polling preserved)
    - `grep 'showChart' app/templates/positions/index.html` returns at least 2 matches (function def + button onclick)
    - `grep 'compare-modal' app/templates/positions/index.html` returns at least 2 matches (modal element + JS reference)
    - `grep 'lg-modal' app/templates/positions/index.html` returns at least 2 matches (modal element + JS reference)
    - `grep 'hidden fixed inset-0' app/templates/positions/index.html` returns 3 matches (three modal overlays)
    - `grep 'extends "base.html"' app/templates/positions/index.html` returns 1 match
  </acceptance_criteria>
  <done>positions/index.html has zero non-dynamic-width style= attributes, all Tailwind utility classes, Chart.js modal works, compare modal works, lost/gained modal works, async position check polling preserved, distribution bar renders with colored segments</done>
</task>

</tasks>

<verification>
- `grep -rc 'style=' app/templates/keywords/index.html` returns 0
- `grep 'style=' app/templates/positions/index.html` returns only dynamic width lines (5 max for distribution bar segments)
- Both templates still extend base.html
- All HTMX attributes (hx-patch, hx-delete, hx-target, hx-swap, hx-confirm, hx-post, hx-get) preserved in both files
- Chart.js script and all JS functions preserved in positions template
</verification>

<success_criteria>
Keywords and positions pages render with pure Tailwind CSS. Zero inline style= (except distribution bar dynamic widths). All interactive features (HTMX edits, Chart.js, modals, async polling, filters, pagination) preserved exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/v4-04-section-positions-keywords/v4-04-01-SUMMARY.md`
</output>
