---
phase: v4-04-section-positions-keywords
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/clusters/index.html
  - app/templates/clusters/cannibalization.html
autonomous: true
requirements:
  - KW-V4-01

must_haves:
  truths:
    - "Clusters page renders with Tailwind utility classes and zero style= attributes"
    - "Cannibalization page renders with Tailwind utility classes and zero style= attributes"
    - "All existing functionality preserved: auto-cluster form, intent dropdown, HTMX delete, cannibalization resolution forms, status cards"
  artifacts:
    - path: "app/templates/clusters/index.html"
      provides: "Tailwind-migrated clusters page with cards and intent dropdowns"
      contains: "bg-white rounded-lg"
    - path: "app/templates/clusters/cannibalization.html"
      provides: "Tailwind-migrated cannibalization page with resolution forms"
      contains: "bg-white rounded-lg"
  key_links:
    - from: "app/templates/clusters/index.html"
      to: "/clusters/{c.id}"
      via: "fetch PUT for intent change and hx-delete for cluster removal"
      pattern: "hx-delete=.*clusters"
    - from: "app/templates/clusters/cannibalization.html"
      to: "/clusters/sites/{site_id}/cannibalization/resolve"
      via: "hx-post resolution forms"
      pattern: "hx-post=.*cannibalization"
---

<objective>
Migrate clusters/index.html and clusters/cannibalization.html to pure Tailwind CSS — replacing all inline style= attributes with Tailwind utility classes while preserving auto-cluster form, intent dropdowns, HTMX interactions, and cannibalization resolution workflow.

Purpose: Cluster and cannibalization pages are tightly coupled (same router, cross-linked) and share UI patterns (cards, badges, forms).
Output: Two fully Tailwind-migrated templates with zero style= attributes.
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP-v4.md
@.planning/phases/v4-04-section-positions-keywords/v4-04-section-CONTEXT.md

<interfaces>
<!-- Same Tailwind patterns as Plan 01 — reference v4-03 sites/index.html -->

Card wrapper: class="bg-white rounded-lg shadow-sm border border-gray-200 p-4"
Table: class="min-w-full divide-y divide-gray-200"
Thead: class="bg-gray-50"
Th: class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
Badge: class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-{color}-50 text-{color}-700"
Small button: class="px-2 py-1 text-xs font-medium rounded bg-{color}-{shade} text-{color|white} hover:bg-{color}-{shade+1}"
Form input: class="px-3 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"

Resolution button colors (cannibalization):
- Merge (blue): bg-blue-500 text-white hover:bg-blue-600
- Canonical (violet): bg-violet-500 text-white hover:bg-violet-600
- 301 Redirect (amber): bg-amber-500 text-white hover:bg-amber-600
- Split keywords (emerald): bg-emerald-500 text-white hover:bg-emerald-600

Status badge colors (cannibalization resolutions):
- resolved: bg-emerald-50 text-emerald-700
- in_progress: bg-blue-50 text-blue-700
- rejected: bg-red-50 text-red-700
- default: bg-gray-100 text-gray-600

Resolution type badge: bg-violet-50 text-violet-700

Status border-left colors (cards):
- resolved: border-l-4 border-emerald-500
- in_progress: border-l-4 border-blue-500
- rejected: border-l-4 border-red-500
- default: border-l-4 border-gray-400
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate clusters/index.html to Tailwind</name>
  <files>app/templates/clusters/index.html</files>
  <read_first>
    - app/templates/clusters/index.html (current state — 74 lines, 26 inline styles)
    - app/templates/sites/index.html (Tailwind reference pattern)
  </read_first>
  <action>
Rewrite app/templates/clusters/index.html replacing ALL style= attributes with Tailwind utility classes per D-05.

Specific migrations:
1. Outer wrapper: replace `class="card" style="margin-bottom:1.5rem"` with `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4 mb-6"`
2. Header flex row: `class="flex justify-between items-center mb-4"`
3. h2 with inline display: `class="text-lg font-semibold text-gray-800 inline"`
4. Cluster count span: `class="text-gray-500 ml-4"`
5. Action buttons row: `class="flex gap-2"`
6. Auto-cluster form: `class="inline-flex gap-1 items-center"` (replace style)
7. Auto-cluster number input: `class="w-[50px] px-1 py-0.5 border border-gray-300 rounded text-xs"` (replace style)
8. Auto-cluster button: `class="px-2 py-1 text-xs font-medium rounded bg-violet-600 text-white hover:bg-violet-700"`
9. Cannibalization link: `class="px-2 py-1 text-xs font-medium rounded bg-red-600 text-white hover:bg-red-700"`
10. Intent mismatches link: `class="px-2 py-1 text-xs font-medium rounded bg-amber-500 text-white hover:bg-amber-600"`
11. Intent link: `class="px-2 py-1 text-xs font-medium rounded bg-violet-600 text-white hover:bg-violet-700"`
12. Export CSV link: `class="px-2 py-1 text-xs font-medium rounded bg-emerald-600 text-white hover:bg-emerald-700"`
13. Create cluster form: `class="flex gap-2 mb-6"`, input name: `class="flex-1 min-w-[200px] px-3 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"`, target URL input: `class="w-[300px] px-3 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"`
14. Cluster card div: replace `style="background:#f9fafb; border-radius:8px; padding:1rem; margin-bottom:1rem"` with `class="bg-gray-50 rounded-lg p-4 mb-4"`
15. Cluster card header flex: `class="flex justify-between items-center mb-2"`
16. Cluster name strong: `class="text-base font-semibold"`
17. Intent dropdown select: `class="ml-2 px-1 py-0.5 border border-gray-300 rounded text-xs bg-white"` (replace style, keep onchange JS)
18. Target URL link: `class="ml-3 text-sm text-blue-600 hover:underline"` (replace style)
19. Keyword count span: `class="text-gray-500 ml-3 text-sm"`
20. Delete button: `class="px-2 py-1 text-xs font-medium rounded bg-red-800 text-white hover:bg-red-700"` (keep hx-delete, hx-target, hx-swap, hx-confirm)
21. Keywords tag container: `class="flex flex-wrap gap-1.5"`
22. Keyword badge span: replace `class="badge" style="background:#dbeafe; color:#1e40af"` with `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-blue-100 text-blue-800"`
23. Empty cluster text: `class="text-gray-500 text-sm"`
24. No clusters text: `class="text-gray-500"`

Preserve ALL hx-delete, hx-post, hx-target, hx-swap, hx-confirm attributes.
Preserve ALL onchange/onclick/onsubmit JS handlers.
Preserve ALL Jinja2 template variables and control flow.
  </action>
  <verify>
    <automated>cd /projects/test && grep -c 'style=' app/templates/clusters/index.html | grep -q '^0$' && echo "PASS: zero inline styles" || echo "FAIL: inline styles remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'style=' app/templates/clusters/index.html` returns 0
    - `grep -c 'class="' app/templates/clusters/index.html` returns >= 15
    - `grep 'hx-delete' app/templates/clusters/index.html` returns at least 1 match
    - `grep 'hx-post.*auto-cluster' app/templates/clusters/index.html` returns 1 match
    - `grep 'onchange="fetch' app/templates/clusters/index.html` returns matches (intent dropdown JS preserved)
    - `grep 'bg-gray-50 rounded-lg' app/templates/clusters/index.html` returns at least 1 match (cluster card)
    - `grep 'extends "base.html"' app/templates/clusters/index.html` returns 1 match
  </acceptance_criteria>
  <done>clusters/index.html has zero style= attributes, all Tailwind utility classes, auto-cluster form works, intent dropdown JS preserved, HTMX delete preserved, keyword badges styled</done>
</task>

<task type="auto">
  <name>Task 2: Migrate clusters/cannibalization.html to Tailwind</name>
  <files>app/templates/clusters/cannibalization.html</files>
  <read_first>
    - app/templates/clusters/cannibalization.html (current state — 163 lines, 33 inline styles)
    - app/templates/sites/index.html (Tailwind reference pattern)
  </read_first>
  <action>
Rewrite app/templates/clusters/cannibalization.html replacing ALL style= attributes with Tailwind utility classes per D-05.

Specific migrations:

**Main card and header:**
1. Outer `class="card"`: replace with `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4"`
2. Header flex: `class="flex justify-between items-center mb-6"`
3. h2: `class="text-lg font-semibold text-gray-800 inline"`
4. Affected count span: `class="text-gray-500 ml-4"`
5. Back to clusters link: keep `class="btn btn-sm"`

**Info paragraph:**
6. Description p: `class="text-gray-500 text-sm mb-4"`

**Cannibalization table:**
7. Apply standard table pattern (min-w-full, divide-y, bg-gray-50 thead, etc.)
8. Keyword td: `class="font-medium"`
9. Competing pages div: `class="mb-1"` (replace margin style)
10. Position badges: replace `class="badge badge-connected/unknown/failed" style="min-width:40px; text-align:center"` with Tailwind:
    - Position <= 10: `class="inline-flex items-center justify-center px-2 py-0.5 rounded text-xs font-medium min-w-[40px] bg-emerald-50 text-emerald-700"`
    - Position <= 30: `class="inline-flex items-center justify-center px-2 py-0.5 rounded text-xs font-medium min-w-[40px] bg-gray-100 text-gray-600"`
    - Position > 30: `class="inline-flex items-center justify-center px-2 py-0.5 rounded text-xs font-medium min-w-[40px] bg-red-50 text-red-700"`
11. Page URL link: `class="ml-2 text-sm text-indigo-500 hover:underline"`
12. Resolve td: `class="min-w-[300px]"`
13. Resolution form: `class="flex flex-col gap-2"` (replace style)
14. Primary URL label: `class="text-xs text-gray-500"`
15. Primary URL select: `class="px-2 py-1 border border-gray-300 rounded text-sm"`
16. Resolution button row: `class="flex gap-1.5 flex-wrap"`
17. Resolution buttons — replace inline bg/color/border/cursor styles:
    - Merge: `class="px-2 py-1 text-xs font-medium rounded bg-blue-500 text-white hover:bg-blue-600 border-none cursor-pointer"`
    - Canonical: `class="px-2 py-1 text-xs font-medium rounded bg-violet-500 text-white hover:bg-violet-600 border-none cursor-pointer"`
    - 301 Redirect: `class="px-2 py-1 text-xs font-medium rounded bg-amber-500 text-white hover:bg-amber-600 border-none cursor-pointer"`
    - Split keywords: `class="px-2 py-1 text-xs font-medium rounded bg-emerald-500 text-white hover:bg-emerald-600 border-none cursor-pointer"`
18. No cannibalization message: `class="text-emerald-600 font-medium"`

**Resolution history section:**
19. History card wrapper: `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4 mt-6"`
20. History header flex: `class="flex justify-between items-center mb-4"`
21. h3: `class="text-base font-semibold text-gray-800 m-0"`

**Resolution cards (in for loop):**
22. Each resolution card: replace `class="card" style="margin-bottom:1rem; border-left:4px solid ..."` with Tailwind using Jinja2 conditions:
    - `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4 mb-4 {% if res.status == 'resolved' %}border-l-4 border-l-emerald-500{% elif res.status == 'in_progress' %}border-l-4 border-l-blue-500{% elif res.status == 'rejected' %}border-l-4 border-l-red-500{% else %}border-l-4 border-l-gray-400{% endif %}"`
23. Resolution card header flex: `class="flex justify-between items-start"`
24. Status badge: replace inline bg/color style with Jinja2-conditional Tailwind classes:
    - `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium ml-2 {% if res.status == 'resolved' %}bg-emerald-50 text-emerald-700{% elif res.status == 'in_progress' %}bg-blue-50 text-blue-700{% elif res.status == 'rejected' %}bg-red-50 text-red-700{% else %}bg-gray-100 text-gray-600{% endif %}"`
25. Resolution type badge: replace inline style with `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium ml-1.5 bg-violet-50 text-violet-700"`
26. Action buttons div: `class="flex gap-1.5"`
27. "Resolved" button: replace `style="background:#10b981; color:#fff; border:none; cursor:pointer"` with `class="px-2 py-1 text-xs font-medium rounded bg-emerald-500 text-white hover:bg-emerald-600"`
28. Primary URL info: `class="mt-2 text-xs text-gray-500"`
29. Details element: `class="mt-2"`
30. Summary: `class="cursor-pointer text-sm text-blue-500"`
31. Pre (action plan): `class="mt-2 text-xs whitespace-pre-wrap bg-gray-50 p-3 rounded"`
32. Check result div: `class="mt-1 text-xs"`
33. Empty history message: `class="text-gray-500 text-sm"`

Preserve ALL hx-post, hx-get, hx-target, hx-swap, hx-vals, hx-confirm attributes exactly.
Preserve ALL Jinja2 template variables and control flow.
  </action>
  <verify>
    <automated>cd /projects/test && grep -c 'style=' app/templates/clusters/cannibalization.html | grep -q '^0$' && echo "PASS: zero inline styles" || echo "FAIL: inline styles remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'style=' app/templates/clusters/cannibalization.html` returns 0
    - `grep -c 'class="' app/templates/clusters/cannibalization.html` returns >= 25
    - `grep 'hx-post.*cannibalization/resolve' app/templates/clusters/cannibalization.html` returns 1 match
    - `grep 'hx-post.*resolutions' app/templates/clusters/cannibalization.html` returns at least 2 matches (status update buttons)
    - `grep 'hx-get.*resolutions' app/templates/clusters/cannibalization.html` returns 1 match (refresh button)
    - `grep 'border-l-4' app/templates/clusters/cannibalization.html` returns at least 1 match (status border)
    - `grep 'extends "base.html"' app/templates/clusters/cannibalization.html` returns 1 match
  </acceptance_criteria>
  <done>cannibalization.html has zero style= attributes, all Tailwind utility classes, all HTMX resolution forms preserved, status cards with colored left borders, resolution type badges styled</done>
</task>

</tasks>

<verification>
- `grep -rc 'style=' app/templates/clusters/index.html app/templates/clusters/cannibalization.html` returns 0 for both
- Both templates still extend base.html
- All HTMX attributes preserved in both files
- Cluster intent dropdown onchange JS preserved
- Cannibalization resolution form buttons all have hx-post
</verification>

<success_criteria>
Clusters and cannibalization pages render with pure Tailwind CSS. Zero inline style= attributes. All interactive features (auto-cluster, intent dropdown, HTMX delete, resolution forms, status history cards) preserved exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/v4-04-section-positions-keywords/v4-04-02-SUMMARY.md`
</output>
