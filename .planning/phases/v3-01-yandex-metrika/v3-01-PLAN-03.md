---
phase: v3-01
plan: "03"
title: "Metrika router and main.py registration"
wave: 2
depends_on: ["v3-01-PLAN-01", "v3-01-PLAN-02"]
files_modified:
  - app/routers/metrika.py
  - app/main.py
autonomous: true
requirements_addressed: []
---

# Plan v3-01-03: Metrika router and main.py registration

<objective>
Create the Metrika router with all endpoints (traffic page, data fetch trigger, daily chart data, page table, period comparison, event CRUD, settings save) and register it in main.py. This establishes the full API surface that templates and widgets will consume in subsequent plans.
</objective>

<tasks>

<task id="01">
<title>Create metrika router with all endpoints</title>
<read_first>
- app/routers/competitors.py
- app/routers/sites.py
- app/routers/positions.py
- app/services/metrika_service.py
- app/services/crypto_service.py
- app/tasks/metrika_tasks.py
- app/models/site.py
- app/dependencies.py
- .planning/phases/v3-01-yandex-metrika/v3-01-RESEARCH.md (section 5 — Router Pattern)
- .planning/phases/v3-01-yandex-metrika/v3-01-UI-SPEC.md (Interaction Patterns)
</read_first>
<action>
Create `app/routers/metrika.py`:

```python
router = APIRouter(prefix="/metrika", tags=["metrika"])
```

**Endpoints:**

1. `GET /metrika/{site_id}` → HTMLResponse
   - Renders `metrika/index.html` template
   - Context: site object, daily_data (from DB), page_data (from DB for default period = last 30 days), events list
   - If site has no metrika_counter_id → show "not configured" empty state
   - If no data in DB → show "no data fetched yet" empty state

2. `POST /metrika/{site_id}/fetch` → JSON
   - Reads date1/date2 from request body (or defaults: date1=90 days ago, date2=yesterday)
   - Dispatches `fetch_metrika_data.delay(str(site_id), date1, date2)` Celery task
   - Returns `{"status": "queued", "task_id": task.id}`

3. `GET /metrika/{site_id}/daily` → JSON
   - Query params: `date_from` (optional), `date_to` (optional)
   - Calls `metrika_service.get_daily_traffic(db, site_id, date_from, date_to)`
   - Returns `{"labels": [...], "visits": [...], "bounce_rate": [...], "page_depth": [...], "avg_duration": [...]}`

4. `GET /metrika/{site_id}/pages` → JSON
   - Query params: `period_start`, `period_end`
   - Calls `metrika_service.get_page_traffic(db, site_id, period_start, period_end)`
   - Returns list of page dicts

5. `GET /metrika/{site_id}/compare` → JSON
   - Query params: `a_start`, `a_end`, `b_start`, `b_end`
   - Calls `get_page_traffic` for both periods, then `compute_period_delta`
   - Returns list of delta dicts with is_new, is_lost flags

6. `POST /metrika/{site_id}/events` → HTMLResponse (HTMX partial)
   - Form fields: `event_date`, `label`, `color` (default `#7c3aed`)
   - Calls `metrika_service.create_event(db, site_id, ...)`
   - Returns rendered `metrika/_events_list.html` partial

7. `DELETE /metrika/{site_id}/events/{event_id}` → empty 200
   - Calls `metrika_service.delete_event(db, event_id)`
   - Returns empty string (HTMX removes the element via hx-swap="outerHTML")

8. `PUT /metrika/{site_id}/settings` → JSON
   - Body: `{"counter_id": str, "token": str}`
   - Encrypts token via `crypto_service.encrypt(token)`, saves both fields to Site
   - Returns `{"status": "saved"}`

Pydantic schemas defined in the same file (following competitors.py pattern):
```python
class MetrikaSettingsUpdate(BaseModel):
    counter_id: str
    token: str

class MetrikaFetchRequest(BaseModel):
    date1: str | None = None
    date2: str | None = None
```

All endpoints require `current_user: User = Depends(require_admin)`.
</action>
<acceptance_criteria>
- [ ] `python -c "from app.routers.metrika import router"` succeeds
- [ ] `grep -q 'prefix="/metrika"' app/routers/metrika.py` exits 0
- [ ] `grep -q "fetch_metrika_data.delay" app/routers/metrika.py` exits 0
- [ ] `grep -q "compute_period_delta" app/routers/metrika.py` exits 0
- [ ] `grep -q "crypto_service.encrypt" app/routers/metrika.py` exits 0
- [ ] `grep -q "MetrikaSettingsUpdate" app/routers/metrika.py` exits 0
- [ ] `grep -c "@router\." app/routers/metrika.py` returns at least 8 (8 endpoints)
- [ ] `grep -q "require_admin" app/routers/metrika.py` exits 0
</acceptance_criteria>
</task>

<task id="02">
<title>Register metrika router in main.py and add help module mapping</title>
<read_first>
- app/main.py (first 40 lines — import and include_router section)
</read_first>
<action>
Edit `app/main.py`:

1. Add import after the yandex router import line:
   ```python
   from app.routers.metrika import router as metrika_router
   ```

2. Add `app.include_router(metrika_router)` call alongside existing router includes.

3. Add to `_HELP_MODULE_MAP`:
   ```python
   "/ui/metrika": "metrika",
   ```

Search for where `include_router` calls are made and add metrika_router in the same block.
</action>
<acceptance_criteria>
- [ ] `grep -q "from app.routers.metrika import router as metrika_router" app/main.py` exits 0
- [ ] `grep -q "metrika_router" app/main.py` finds at least 2 occurrences (import + include)
- [ ] `grep -q '"/ui/metrika"' app/main.py` exits 0
- [ ] `python -c "from app.main import app"` succeeds (app loads without import errors)
</acceptance_criteria>
</task>

</tasks>

<verification>
1. App starts without errors: `python -c "from app.main import app"`
2. Router module imports cleanly: `python -c "from app.routers.metrika import router"`
3. All 8 endpoints registered on the router
</verification>

<must_haves>
- Router serves all 8 endpoints (page, fetch, daily, pages, compare, events CRUD, settings)
- Pydantic request schemas defined for settings update and fetch request
- All endpoints gated behind require_admin dependency
- Router registered in main.py with help module mapping
- Fetch endpoint dispatches Celery task via fetch_metrika_data.delay
- Settings endpoint encrypts token via crypto_service before saving
</must_haves>
