---
phase: v3-01
plan: "04"
title: "Site Overview traffic widget and site detail integration"
wave: 3
depends_on: ["v3-01-PLAN-01", "v3-01-PLAN-02", "v3-01-PLAN-03"]
files_modified:
  - app/templates/metrika/_widget.html
  - app/templates/sites/detail.html
  - app/routers/metrika.py
autonomous: true
requirements_addressed: []
---

# Plan v3-01-04: Site Overview traffic widget and site detail integration

<objective>
Add a traffic summary widget to the Site Overview (detail) page that shows 30-day organic traffic KPIs and a sparkline chart. Add a quick-action button for Metrika on the site detail page. The widget loads lazily via HTMX and is hidden when Metrika is not configured.
</objective>

<tasks>

<task id="01">
<title>Add widget endpoint to metrika router</title>
<read_first>
- app/routers/metrika.py
- app/services/metrika_service.py
- .planning/phases/v3-01-yandex-metrika/v3-01-RESEARCH.md (section 5 — Template/UI Pattern, widget)
- .planning/phases/v3-01-yandex-metrika/v3-01-UI-SPEC.md (Widget Layout)
</read_first>
<action>
Add endpoint to `app/routers/metrika.py` (if not already present from Plan 03):

```python
@router.get("/{site_id}/widget", response_class=HTMLResponse)
async def metrika_widget(
    site_id: uuid.UUID,
    request: Request,
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(require_admin),
):
    """HTMX partial: traffic summary widget for site overview."""
    site = await site_service.get_site(db, site_id)
    if not site or not site.metrika_counter_id:
        return HTMLResponse("")  # hidden entirely if not configured

    from datetime import date, timedelta
    date_to = date.today()
    date_from = date_to - timedelta(days=30)

    daily = await metrika_service.get_daily_traffic(db, site_id, date_from, date_to)

    if not daily:
        # Configured but no data fetched yet
        return templates.TemplateResponse(
            request,
            "metrika/_widget.html",
            {"site": site, "has_data": False, "totals": None, "sparkline_data": []},
        )

    # Compute 30-day totals
    total_visits = sum(d["visits"] for d in daily)
    avg_bounce = round(sum(d.get("bounce_rate") or 0 for d in daily) / max(len(daily), 1), 1)
    avg_depth = round(sum(d.get("page_depth") or 0 for d in daily) / max(len(daily), 1), 1)
    avg_dur = int(sum(d.get("avg_duration_seconds") or 0 for d in daily) / max(len(daily), 1))

    totals = {
        "visits": total_visits,
        "bounce_rate": avg_bounce,
        "page_depth": avg_depth,
        "avg_duration_seconds": avg_dur,
    }
    sparkline_data = [d["visits"] for d in daily]

    return templates.TemplateResponse(
        request,
        "metrika/_widget.html",
        {"site": site, "has_data": True, "totals": totals, "sparkline_data": sparkline_data},
    )
```

Note: The computation above is done in the endpoint for simplicity. The totals are 30-day aggregates (sum for visits, averages for rates).
</action>
<acceptance_criteria>
- [ ] `grep -q "widget" app/routers/metrika.py` exits 0
- [ ] `grep -q "_widget.html" app/routers/metrika.py` exits 0
- [ ] `grep -q "sparkline_data" app/routers/metrika.py` exits 0
- [ ] `grep -q "has_data" app/routers/metrika.py` exits 0
</acceptance_criteria>
</task>

<task id="02">
<title>Create widget partial template</title>
<read_first>
- app/templates/sites/detail.html
- .planning/phases/v3-01-yandex-metrika/v3-01-UI-SPEC.md (Widget Layout: Site Overview)
- .planning/phases/v3-01-yandex-metrika/v3-01-UI-SPEC.md (Chart Specification — sparkline)
- .planning/phases/v3-01-yandex-metrika/v3-01-UI-SPEC.md (Copywriting Contract)
</read_first>
<action>
Create `app/templates/metrika/_widget.html`:

This is an HTMX partial (no `{% extends %}`) that renders inside the site detail page.

**If `has_data` is False (configured but no data):**
```html
<div class="card" style="text-align:center; padding:1rem">
  <div style="font-size:.875rem; color:#6b7280">Данные не загружены.</div>
  <a href="/ui/metrika/{{ site.id }}" class="btn btn-sm" style="margin-top:.5rem">Настройте счётчик →</a>
</div>
```

**If `has_data` is True:**
```html
<div class="card" style="padding:1rem">
  <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:.75rem">
    <div>
      <div style="font-weight:600; font-size:1rem">Трафик (органика)</div>
      <div style="font-size:.8rem; color:#6b7280">За последние 30 дней</div>
    </div>
  </div>

  <!-- Mini metrics row -->
  <div style="display:flex; gap:1rem; margin-bottom:.75rem; flex-wrap:wrap">
    <div style="font-size:.875rem">
      <span style="color:#6b7280">Визиты:</span>
      <span style="font-weight:600">{{ totals.visits }}</span>
    </div>
    <div style="font-size:.875rem">
      <span style="color:#6b7280">Отказы:</span>
      <span style="font-weight:600">{{ totals.bounce_rate }}%</span>
    </div>
    <div style="font-size:.875rem">
      <span style="color:#6b7280">Глубина:</span>
      <span style="font-weight:600">{{ totals.page_depth }}</span>
    </div>
    <div style="font-size:.875rem">
      <span style="color:#6b7280">Длительность:</span>
      <span style="font-weight:600">{{ (totals.avg_duration_seconds // 60) }}м {{ (totals.avg_duration_seconds % 60) }}с</span>
    </div>
  </div>

  <!-- Sparkline -->
  <canvas id="metrika-sparkline-{{ site.id }}" style="height:80px; width:100%"></canvas>

  <div style="margin-top:.5rem; text-align:right">
    <a href="/ui/metrika/{{ site.id }}" style="font-size:.85rem; color:#4f46e5; text-decoration:none">Подробный трафик →</a>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
<script>
(function() {
  const ctx = document.getElementById('metrika-sparkline-{{ site.id }}');
  if (!ctx) return;
  new Chart(ctx, {
    type: 'line',
    data: {
      labels: {{ sparkline_data | tojson }}.map((_, i) => i),
      datasets: [{
        data: {{ sparkline_data | tojson }},
        borderColor: '#4f46e5',
        backgroundColor: 'rgba(79,70,229,0.08)',
        fill: true,
        tension: 0.3,
        pointRadius: 0,
        borderWidth: 2,
      }]
    },
    options: {
      responsive: true,
      maintainAspectRatio: false,
      scales: {
        x: { display: false },
        y: { display: false, beginAtZero: true }
      },
      plugins: { legend: { display: false } }
    }
  });
})();
</script>
```

Sparkline specs per UI-SPEC: 80px height, no axes, no legend, no points, indigo line, 8% opacity fill.
</action>
<acceptance_criteria>
- [ ] File `app/templates/metrika/_widget.html` exists
- [ ] `grep -q "Трафик (органика)" app/templates/metrika/_widget.html` exits 0
- [ ] `grep -q "За последние 30 дней" app/templates/metrika/_widget.html` exits 0
- [ ] `grep -q "Подробный трафик" app/templates/metrika/_widget.html` exits 0
- [ ] `grep -q "metrika-sparkline" app/templates/metrika/_widget.html` exits 0
- [ ] `grep -q "pointRadius: 0" app/templates/metrika/_widget.html` exits 0
- [ ] `grep -q "rgba(79,70,229,0.08)" app/templates/metrika/_widget.html` exits 0
- [ ] `grep -q "Данные не загружены" app/templates/metrika/_widget.html` exits 0
- [ ] `grep -q "sparkline_data" app/templates/metrika/_widget.html` exits 0
</acceptance_criteria>
</task>

<task id="03">
<title>Integrate widget and quick action into site detail page</title>
<read_first>
- app/templates/sites/detail.html
- app/routers/sites.py
- .planning/phases/v3-01-yandex-metrika/v3-01-UI-SPEC.md (Widget Layout)
- .planning/phases/v3-01-yandex-metrika/v3-01-RESEARCH.md (section 5 — site overview widget HTMX)
</read_first>
<action>
Edit `app/templates/sites/detail.html`:

1. **Add Metrika widget via HTMX lazy load** — insert after the stats row (after the grid of 4 stat cards, before the Quick Actions card). Add a div that lazy-loads the widget:

```html
<!-- Metrika traffic widget -->
<div hx-get="/metrika/{{ site.id }}/widget" hx-trigger="load" hx-swap="innerHTML" style="margin-bottom:1.5rem"></div>
```

2. **Add "Трафик" quick action button** in the Quick Actions card, after existing buttons:

```html
<a href="/ui/metrika/{{ site.id }}" class="btn" style="background:#0ea5e9;color:white">Трафик</a>
```

Place it after the "Content Pipeline" button and before the "Verify Connection" button.
</action>
<acceptance_criteria>
- [ ] `grep -q "hx-get=\"/metrika/{{ site.id }}/widget\"" app/templates/sites/detail.html` exits 0
- [ ] `grep -q "hx-trigger=\"load\"" app/templates/sites/detail.html` exits 0
- [ ] `grep -q "/ui/metrika/{{ site.id }}" app/templates/sites/detail.html` exits 0
- [ ] `grep -q "Трафик" app/templates/sites/detail.html` exits 0
</acceptance_criteria>
</task>

<task id="04">
<title>Add metrika UI route to sites router or main.py</title>
<read_first>
- app/main.py (search for "/ui/" route definitions)
- app/routers/sites.py
</read_first>
<action>
The traffic page is served by `GET /metrika/{site_id}` in the metrika router. However, the navigation links use `/ui/metrika` and `/ui/metrika/{site_id}` paths. Add a UI route in `app/main.py` or in the metrika router that handles the `/ui/metrika` prefix.

Check the existing codebase pattern: look at how `/ui/sites`, `/ui/positions`, etc. are routed. If they are defined in `main.py` as standalone `@app.get("/ui/...")` routes, follow that pattern. If they are in routers, follow that.

Two routes needed:

1. `GET /ui/metrika` → redirect to site selection or show a site list (follow the pattern of `/ui/positions` which likely requires a site_id)

2. `GET /ui/metrika/{site_id}` → renders `metrika/index.html` (this is the main traffic page endpoint)

If the existing pattern is that the router handles both API and UI routes (e.g., positions router serves `/ui/positions/{site_id}`), add the UI routes to `app/routers/metrika.py`. If `main.py` has a separate UI block, add there.

Ensure the route calls the metrika router's page handler which loads site data, daily traffic, page traffic, and events from DB and passes to the template.
</action>
<acceptance_criteria>
- [ ] Navigating to `/ui/metrika/{site_id}` renders the traffic page template
- [ ] `grep -q "/ui/metrika" app/routers/metrika.py` exits 0 OR `grep -q "/ui/metrika" app/main.py` exits 0
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Site detail page loads without errors: navigate to `/ui/sites/{site_id}`
2. Widget div is present in HTML source with `hx-get="/metrika/{site_id}/widget"`
3. For a site without Metrika configured: widget div renders empty (invisible)
4. For a site with Metrika configured but no data: widget shows "Данные не загружены" message
5. For a site with data: widget shows 4 KPI metrics and a 30-day sparkline
6. "Трафик" quick action button links to `/ui/metrika/{site_id}`
7. Sparkline chart uses correct styling (no axes, no points, indigo line)
</verification>

<must_haves>
- D-11: Widget in Site Overview shows traffic summary when data is available
- D-11: Widget hidden entirely when Metrika not configured (no empty card)
- D-11: Widget shows "Данные не загружены" with settings link when configured but no data
- D-12: Traffic quality metrics (visits, bounce_rate, page_depth, avg_duration) visible alongside existing site stats (keywords, crawls, tasks)
- Sparkline matches UI-SPEC: 80px, no axes, no legend, no points, #4f46e5 line, rgba(79,70,229,0.08) fill
- All copy in Russian per UI-SPEC Copywriting Contract
- "Трафик" quick action button in site detail page
</must_haves>
