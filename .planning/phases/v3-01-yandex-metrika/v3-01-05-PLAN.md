---
phase: v3-01
plan: "05"
title: "Traffic page template, events partial, and navigation link"
wave: 3
depends_on: ["v3-01-PLAN-01", "v3-01-PLAN-02", "v3-01-PLAN-03"]
files_modified:
  - app/templates/metrika/index.html
  - app/templates/metrika/_events_list.html
  - app/templates/base.html
autonomous: true
requirements_addressed: []
---

# Plan v3-01-05: Traffic page template, events partial, and navigation link

<objective>
Build the main traffic page template with Chart.js line chart, annotation plugin event overlay, period comparison table, event management UI, and the events list HTMX partial. Add the "Трафик" navigation link to base.html. These templates consume the router endpoints created in Plan 03.
</objective>

<tasks>

<task id="01">
<title>Create traffic page template with chart, KPI cards, comparison table, and event manager</title>
<read_first>
- app/templates/positions/index.html
- app/templates/sites/detail.html
- app/templates/base.html
- .planning/phases/v3-01-yandex-metrika/v3-01-UI-SPEC.md (Page Layout: Traffic Page)
- .planning/phases/v3-01-yandex-metrika/v3-01-UI-SPEC.md (Chart Specification)
- .planning/phases/v3-01-yandex-metrika/v3-01-UI-SPEC.md (Copywriting Contract)
- .planning/phases/v3-01-yandex-metrika/v3-01-UI-SPEC.md (Delta Display Pattern)
- .planning/phases/v3-01-yandex-metrika/v3-01-RESEARCH.md (section 4 — Chart.js Event Overlay)
</read_first>
<action>
Create directory `app/templates/metrika/` and file `app/templates/metrika/index.html`:

**Template structure** (extends `base.html`):

1. **Page header row:**
   - `<h2>Трафик: {{ site.name }}</h2>`
   - Fetch button: `<button class="btn btn-verify" hx-post="/metrika/{{ site.id }}/fetch" hx-swap="none" onclick="this.textContent='Загрузка...'; this.disabled=true">Загрузить данные Метрики</button>`

2. **Empty states** (conditional):
   - If `not site.metrika_counter_id`: show card with text "Счётчик Метрики не настроен. Добавьте ID счётчика и токен в настройках сайта."
   - If `site.metrika_counter_id and not daily_data`: show "Данные ещё не загружены. Нажмите «Загрузить данные Метрики»."

3. **KPI stats row** (4 cards, grid `repeat(auto-fit, minmax(160px, 1fr))`):
   - Визиты: `{{ totals.visits }}` (color `#2563eb`)
   - Отказы: `{{ totals.bounce_rate }}%` (color `#dc2626` if >50, `#059669` if <30, `#6b7280` otherwise)
   - Глубина просмотра: `{{ totals.page_depth }}` (color `#7c3aed`)
   - Время на сайте: formatted as `Xм Yс` (color `#0ea5e9`)

4. **Traffic chart card:**
   - `<canvas id="traffic-chart" style="height:300px"></canvas>`
   - Load Chart.js 4.4.0 (already in positions templates) and chartjs-plugin-annotation@3.0.1 via CDN
   - Chart config: type `line`, line color `#4f46e5`, fill `rgba(79,70,229,0.1)`, tension 0.3, pointRadius 2
   - Event annotations: built from `events` list using `buildAnnotations()` function
   - X axis: date strings, Y axis: visits, beginAtZero true

5. **Date range pickers + comparison:**
   - Period A: date_a_start, date_a_end inputs
   - Period B: date_b_start, date_b_end inputs
   - "Сравнить периоды" button: onclick calls `fetchComparison()` JS function
   - `fetchComparison()` calls `GET /metrika/{{ site.id }}/compare?a_start=...&a_end=...&b_start=...&b_end=...`
   - Results rendered into `#compare-result` div as a table

6. **Event markers card:**
   - List existing events: `{% for ev in events %}` → `<li>` with date, label, color swatch, delete button
   - Delete: `hx-delete="/metrika/{{ site.id }}/events/{{ ev.id }}" hx-confirm="Удалить это событие?" hx-target="closest li" hx-swap="outerHTML swap:200ms"`
   - Add event form: date input, label input, color input (default `#7c3aed`), submit button "Добавить событие"
   - Form: `hx-post="/metrika/{{ site.id }}/events" hx-target="#events-list" hx-swap="innerHTML"`

7. **Period comparison table** (in `#compare-result` div):
   - Columns: Страница | Визиты (А) | Визиты (Б) | Дельта | Отказы | Глубина | Длительность
   - Delta display: `▲ +N` green (#059669) if delta > 0, `▼ N` red (#dc2626) if delta < 0, `=` gray (#6b7280) if 0
   - New page badge: `.badge-connected` with "Новая"
   - Lost page badge: `.badge-failed` with "Потеря"
   - Filter buttons: "Все страницы" | "Новые" | "Потери" — JS toggle to show/hide rows

8. **Metrika Settings section** (at bottom of page):
   - Card with counter_id input, token (password) input, "Сохранить настройки" button
   - Submit via `fetch('/metrika/{{ site.id }}/settings', {method: 'PUT', body: JSON.stringify({...})})`

9. **JS at bottom:**
   - `buildAnnotations(events)` — from RESEARCH.md section 4
   - `fetchComparison()` — fetch comparison data and render table
   - Chart initialization with event annotations
   - Auto-reload after fetch task: `setTimeout(() => location.reload(), 5000)` on fetch button click

All copy in Russian per UI-SPEC Copywriting Contract.
</action>
<acceptance_criteria>
- [ ] File `app/templates/metrika/index.html` exists
- [ ] `grep -q "extends.*base.html" app/templates/metrika/index.html` exits 0
- [ ] `grep -q "Трафик:" app/templates/metrika/index.html` exits 0
- [ ] `grep -q "traffic-chart" app/templates/metrika/index.html` exits 0
- [ ] `grep -q "chartjs-plugin-annotation" app/templates/metrika/index.html` exits 0
- [ ] `grep -q "buildAnnotations" app/templates/metrika/index.html` exits 0
- [ ] `grep -q "Загрузить данные Метрики" app/templates/metrika/index.html` exits 0
- [ ] `grep -q "Сравнить периоды" app/templates/metrika/index.html` exits 0
- [ ] `grep -q "Добавить событие" app/templates/metrika/index.html` exits 0
- [ ] `grep -q "hx-delete" app/templates/metrika/index.html` exits 0
- [ ] `grep -q "Счётчик Метрики не настроен" app/templates/metrika/index.html` exits 0
- [ ] `grep -q "#059669" app/templates/metrika/index.html` exits 0 (green for positive delta)
- [ ] `grep -q "#dc2626" app/templates/metrika/index.html` exits 0 (red for negative delta)
- [ ] `grep -q "badge-connected" app/templates/metrika/index.html` exits 0 (new page badge)
- [ ] `grep -q "badge-failed" app/templates/metrika/index.html` exits 0 (lost page badge)
- [ ] `grep -q "Все страницы" app/templates/metrika/index.html` exits 0
- [ ] `grep -q "Новые" app/templates/metrika/index.html` exits 0
- [ ] `grep -q "Потери" app/templates/metrika/index.html` exits 0
</acceptance_criteria>
</task>

<task id="02">
<title>Create events list HTMX partial template</title>
<read_first>
- app/templates/metrika/index.html
- .planning/phases/v3-01-yandex-metrika/v3-01-UI-SPEC.md (Interaction Patterns — Add/Delete event)
</read_first>
<action>
Create `app/templates/metrika/_events_list.html`:

This is a partial template returned by `POST /metrika/{site_id}/events` to update the events list via HTMX.

Content:
```html
{% for ev in events %}
<li style="display:flex; align-items:center; gap:.5rem; padding:.4rem 0; border-bottom:1px solid #e5e7eb">
  <span style="width:12px; height:12px; border-radius:50%; background:{{ ev.color }}; display:inline-block"></span>
  <span style="font-size:.875rem; color:#6b7280">{{ ev.event_date.strftime('%Y-%m-%d') }}</span>
  <span style="font-size:.875rem">{{ ev.label }}</span>
  <button class="btn btn-sm" style="margin-left:auto; background:#991b1b; color:white; font-size:.75rem"
    hx-delete="/metrika/{{ site_id }}/events/{{ ev.id }}"
    hx-confirm="Удалить это событие?"
    hx-target="closest li"
    hx-swap="outerHTML swap:200ms">Удалить</button>
</li>
{% endfor %}
{% if not events %}
<li style="padding:.5rem 0; color:#6b7280; font-size:.875rem">
  Нет событий. Добавьте метку на графике, чтобы связать действие с изменением трафика.
</li>
{% endif %}
```
</action>
<acceptance_criteria>
- [ ] File `app/templates/metrika/_events_list.html` exists
- [ ] `grep -q "hx-delete" app/templates/metrika/_events_list.html` exits 0
- [ ] `grep -q "Удалить" app/templates/metrika/_events_list.html` exits 0
- [ ] `grep -q "Нет событий" app/templates/metrika/_events_list.html` exits 0
- [ ] `grep -q "ev.color" app/templates/metrika/_events_list.html` exits 0
</acceptance_criteria>
</task>

<task id="03">
<title>Add "Трафик" link to site navigation in base.html</title>
<read_first>
- app/templates/base.html
- .planning/phases/v3-01-yandex-metrika/v3-01-UI-SPEC.md (Copywriting Contract — Nav link)
</read_first>
<action>
Edit `app/templates/base.html`:

In the `<nav>` element, add a "Трафик" link. Place it after the "Positions" link and before the "Uploads" link:

```html
<a href="/ui/metrika">Трафик</a>
```

The nav currently has: Dashboard, Sites, Projects, Tasks, Positions, Uploads, Users, Groups, Logout.
After change: Dashboard, Sites, Projects, Tasks, Positions, Трафик, Uploads, Users, Groups, Logout.
</action>
<acceptance_criteria>
- [ ] `grep -q "Трафик" app/templates/base.html` exits 0
- [ ] `grep -q "/ui/metrika" app/templates/base.html` exits 0
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Traffic page template renders: navigate to `/ui/metrika/{site_id}` (after site has metrika configured)
2. "Трафик" link visible in navigation bar
3. Chart.js and annotation plugin loaded on traffic page
4. Event add/delete works via HTMX without page reload
5. Period comparison returns delta data with is_new/is_lost flags
6. Settings form saves encrypted token to Site model
</verification>

<must_haves>
- User can navigate to /ui/metrika/{site_id} and see traffic data
- Traffic chart shows organic visit timeline with event overlays
- Period comparison table displays visit delta with new/lost badges
- Event markers can be added and deleted without page reload via HTMX
- "Трафик" navigation link appears in the global nav bar
- All copy in Russian per UI-SPEC Copywriting Contract
- Chart colors and styles match UI-SPEC Chart Specification exactly
</must_haves>
