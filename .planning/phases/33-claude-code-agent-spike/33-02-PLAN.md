---
phase: 33-claude-code-agent-spike
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - app/routers/mobile.py
  - app/templates/mobile/agent/diff.html
autonomous: false
requirements:
  - AGT-02

must_haves:
  truths:
    - "User can view full diff on /m/agent/diff/{task_id} page"
    - "Diff page renders with syntax-colored pre block inside mobile base template"
    - "SPIKE-REPORT.md documents the experiment results and go/no-go recommendation"
  artifacts:
    - path: "app/templates/mobile/agent/diff.html"
      provides: "Diff viewer template"
      contains: "diff"
    - path: "app/routers/mobile.py"
      provides: "Diff viewer endpoint"
      contains: "agent/diff"
    - path: ".planning/phases/33-claude-code-agent-spike/SPIKE-REPORT.md"
      provides: "Spike experiment documentation"
      contains: "go/no-go"
  key_links:
    - from: "app/routers/mobile.py"
      to: "/tmp/agent_diffs/{task_id}.txt"
      via: "file read from shared volume"
      pattern: "agent_diffs"
    - from: "bot/handlers/agent.py"
      to: "app/routers/mobile.py"
      via: "WebApp button links to /m/agent/diff/{task_id}"
      pattern: "/m/agent/diff/"
---

<objective>
Add the diff viewer WebApp page and document the spike results.

Purpose: Complete the AGT-02 flow by providing a full diff view in the mobile WebApp (linked from the bot's inline button). Then verify the end-to-end flow and document findings in SPIKE-REPORT.md.

Output: Working /m/agent/diff/{task_id} page + SPIKE-REPORT.md with go/no-go recommendation.
</objective>

<execution_context>
@/opt/seo-platform/.claude/get-shit-done/workflows/execute-plan.md
@/opt/seo-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-claude-code-agent-spike/33-CONTEXT.md
@.planning/phases/33-claude-code-agent-spike/33-RESEARCH.md
@.planning/phases/33-claude-code-agent-spike/33-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From bot/handlers/agent.py (created in Plan 01):
```python
DIFF_DIR = pathlib.Path("/tmp/agent_diffs")
# Writes diff as: (DIFF_DIR / f"{task.task_id}.txt").write_text(diff_output)
# task_id is 8-char UUID prefix, e.g. "a1b2c3d4"
```

From app/routers/mobile.py (existing pattern):
```python
from fastapi import APIRouter, Depends, Request
from fastapi.responses import HTMLResponse
from fastapi.templating import Jinja2Templates
from app.auth.dependencies import get_current_user
from app.models.user import User

router = APIRouter(prefix="/m", tags=["mobile"])
mobile_templates = Jinja2Templates(directory="app/templates")
```

From app/templates/base_mobile.html:
```html
<!-- Base mobile template with bottom nav, viewport meta, etc. -->
{% block title %}{% endblock %}
{% block content %}{% endblock %}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diff viewer WebApp endpoint and template</name>
  <files>app/routers/mobile.py, app/templates/mobile/agent/diff.html</files>
  <read_first>app/routers/mobile.py, app/templates/base_mobile.html, bot/handlers/agent.py, .planning/phases/33-claude-code-agent-spike/33-RESEARCH.md</read_first>
  <action>
**Create app/templates/mobile/agent/diff.html** (per D-11):

This is a simple diff viewer page. Use base_mobile.html. Show task_id in header, render diff in a `<pre>` block with dark background and monospace font. Add basic diff coloring via inline CSS — lines starting with `+` in green (#22c55e), lines starting with `-` in red (#ef4444), lines starting with `@@` in cyan (#06b6d4). Use JavaScript to apply line coloring by splitting the diff text and wrapping each line in a `<span>`.

Template structure:
```html
{% extends "base_mobile.html" %}
{% block title %}Diff: {{ task_id }}{% endblock %}
{% block content %}
<div style="padding: 12px;">
  <h2 style="font-size: 1rem; font-weight: 600; margin-bottom: 8px;">
    Diff задачи: {{ task_id }}
  </h2>
  <div id="diff-container"
       style="font-family: 'Courier New', monospace; font-size: 0.7rem;
              background: #1e1b4b; color: #e5e7eb; padding: 12px;
              border-radius: 8px; overflow-x: auto; white-space: pre-wrap;
              word-break: break-all; line-height: 1.4;">
    {{ diff | e }}
  </div>
</div>
<script>
  // Basic diff syntax coloring
  (function() {
    var el = document.getElementById('diff-container');
    var lines = el.textContent.split('\n');
    var html = lines.map(function(line) {
      var escaped = line.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
      if (line.startsWith('+++') || line.startsWith('---')) {
        return '<span style="color:#94a3b8;font-weight:bold">' + escaped + '</span>';
      } else if (line.startsWith('+')) {
        return '<span style="color:#22c55e">' + escaped + '</span>';
      } else if (line.startsWith('-')) {
        return '<span style="color:#ef4444">' + escaped + '</span>';
      } else if (line.startsWith('@@')) {
        return '<span style="color:#06b6d4">' + escaped + '</span>';
      }
      return escaped;
    }).join('\n');
    el.innerHTML = html;
  })();
</script>
{% endblock %}
```

**Add endpoint to app/routers/mobile.py** (per D-11):

Add at the end of the file, before any final lines:

```python
@router.get("/agent/diff/{task_id}", response_class=HTMLResponse)
async def agent_diff_page(
    request: Request,
    task_id: str,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    """Show full diff from a Claude Code agent task (spike)."""
    import pathlib
    diff_file = pathlib.Path(f"/tmp/agent_diffs/{task_id}.txt")
    diff_content = ""
    if diff_file.exists():
        diff_content = diff_file.read_text(errors="replace")
    else:
        diff_content = "Diff не найден. Возможно, задача ещё выполняется или уже удалена."
    return mobile_templates.TemplateResponse(
        "mobile/agent/diff.html",
        {"request": request, "task_id": task_id, "diff": diff_content},
    )
```

Note: Use the existing `mobile_templates` Jinja2Templates instance already defined in the file. The endpoint follows the same pattern as other `/m/` endpoints in the file (Depends(get_current_user), Depends(get_db), HTMLResponse).

Create the `app/templates/mobile/agent/` directory if it doesn't exist (mkdir -p).
  </action>
  <verify>
    <automated>cd /opt/seo-platform && test -f app/templates/mobile/agent/diff.html && grep -q "agent/diff" app/routers/mobile.py && grep -q "agent_diffs" app/routers/mobile.py && grep -q "diff-container" app/templates/mobile/agent/diff.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `test -f app/templates/mobile/agent/diff.html` succeeds
- `grep "agent/diff" app/routers/mobile.py` returns a match for the endpoint
- `grep "agent_diffs" app/routers/mobile.py` returns a match for the file path
- `grep "base_mobile.html" app/templates/mobile/agent/diff.html` returns a match
- `grep "diff-container" app/templates/mobile/agent/diff.html` returns a match for the JS coloring target
- `grep "22c55e" app/templates/mobile/agent/diff.html` returns a match for green diff coloring
  </acceptance_criteria>
  <done>/m/agent/diff/{task_id} endpoint reads diff from shared volume and renders it in a mobile-friendly template with basic syntax coloring (green for additions, red for deletions, cyan for hunks).</done>
</task>

<task type="checkpoint:human-verify" gate="informational">
  <name>Task 2: End-to-end flow verification</name>
  <what-built>
Complete Claude Code agent spike:
1. /task command in Telegram bot creates git branch, runs Claude CLI, collects diff
2. Bot sends diff summary with approve/reject buttons + WebApp link
3. /m/agent/diff/{task_id} shows full diff with syntax coloring
4. Approve merges branch, reject deletes it
  </what-built>
  <how-to-verify>
1. Rebuild bot container: `docker compose build bot`
2. Verify Claude CLI is installed: `docker compose run --rm bot claude --version`
3. Start services: `docker compose up -d bot api`
4. In Telegram, send: `/task Add a comment to the top of bot/config.py explaining what each setting does`
5. Verify bot responds with "Принято, создаю ветку..." then "Claude работает..."
6. Wait for completion (up to 6 minutes)
7. Verify bot sends diff summary with file stats and three buttons
8. Click "Полный diff" WebApp button — verify diff page loads with colored diff
9. Click "Применить" — verify merge confirmation message
10. Check git log: `git log --oneline -5` should show merge commit

If testing is not possible now (no ANTHROPIC_API_KEY, etc.), mark as informational and proceed to SPIKE-REPORT.
  </how-to-verify>
  <resume-signal>Type "tested" with results, or "skip" to proceed without testing</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write SPIKE-REPORT.md</name>
  <files>.planning/phases/33-claude-code-agent-spike/SPIKE-REPORT.md</files>
  <read_first>bot/handlers/agent.py, .planning/phases/33-claude-code-agent-spike/33-CONTEXT.md, .planning/phases/33-claude-code-agent-spike/33-RESEARCH.md</read_first>
  <action>
Create `.planning/phases/33-claude-code-agent-spike/SPIKE-REPORT.md` (per D-13) with the following structure:

```markdown
# Spike Report: Claude Code Agent via Telegram Bot

**Date:** {current date}
**Phase:** 33-claude-code-agent-spike
**Status:** {tested | untested — fill based on checkpoint result}

## What We Built

A Telegram bot `/task` command that:
1. Accepts a text task description from an authorized user
2. Creates an isolated git branch (`agent/task-{uuid}`)
3. Runs Claude Code CLI (`claude --print -p '{task}'`) as a subprocess in the bot container
4. Collects `git diff master` after Claude finishes
5. Sends diff summary (file stats) to chat with inline approve/reject buttons
6. Provides a WebApp link to full diff view with syntax coloring
7. On approve: merges branch into master with `--no-ff`
8. On reject: force-deletes the branch
9. On error/timeout: auto-cleans up the branch

## Architecture

### Components
- **bot/handlers/agent.py** — handler + subprocess + git lifecycle
- **Dockerfile.bot** — Node.js 20 + `@anthropic-ai/claude-code` CLI
- **docker-compose.yml** — ANTHROPIC_API_KEY env, repo volume mount, shared agent_diffs volume
- **app/routers/mobile.py** + **app/templates/mobile/agent/diff.html** — WebApp diff viewer

### Key Decisions
- In-memory state (no DB) — sufficient for one-task-at-a-time constraint
- asyncio.Lock mutex — prevents concurrent Claude runs
- Shared filesystem volume (`/tmp/agent_diffs`) — simplest cross-container diff delivery
- `--output-format text` — simpler than JSON for spike; actual changes captured by `git diff`
- `--dangerously-skip-permissions` — required for non-interactive container context

## What Works

- [ ] Bot accepts /task command and validates single-task mutex
- [ ] Git branch creation and isolation
- [ ] Claude CLI subprocess invocation with timeout
- [ ] Diff collection and stat formatting
- [ ] Inline keyboard approve/reject flow
- [ ] WebApp diff viewer with syntax coloring
- [ ] Branch merge on approve, deletion on reject
- [ ] Error cleanup (branch deletion + force checkout)

## What Doesn't Work / Limitations

1. **Single task at a time** — by design for spike; production needs a queue
2. **No conversation** — Claude gets one prompt, no follow-up; may need clarification for complex tasks
3. **In-memory state** — bot restart loses current task state; production needs DB/Redis persistence
4. **No task history** — no record of past tasks; production needs audit log
5. **Large diffs** — Telegram message limit forces truncation; WebApp shows full diff but mobile UX may suffer for 1000+ line diffs
6. **No cost tracking** — Claude API costs are not logged or limited
7. **Security** — `--dangerously-skip-permissions` gives Claude full system access within the container; production needs sandboxing

## Performance Observations

- Claude CLI startup: ~{X}s (measure during testing)
- Typical task execution: ~{X}s for small changes
- Git operations: < 1s each
- Diff viewer page load: < 1s

## Cost Observations

- Typical small task: ~${X} (measure during testing)
- ANTHROPIC_API_KEY is shared; no per-user billing

## Go/No-Go Recommendation

### Go (with conditions)
The mechanic works for simple, well-defined tasks. Recommended path to production:

1. **Phase 1 (hardening):** Add Redis-backed task state + task history table + cost logging
2. **Phase 2 (safety):** Add file path restrictions, cost caps per task, sandboxed Docker execution
3. **Phase 3 (UX):** Add conversation mode (follow-up questions), task templates, progress streaming
4. **Phase 4 (scale):** Task queue (Celery), concurrent execution, per-user quotas

### No-Go conditions
- If Claude CLI latency > 5 minutes for typical tasks — reconsider approach
- If API costs > $X/task average — add cost caps before production
- If merge conflicts are frequent — need conflict resolution UX

## Files Created/Modified

| File | Action |
|------|--------|
| bot/handlers/agent.py | Created — agent handler |
| bot/main.py | Modified — handler registration |
| Dockerfile.bot | Modified — Node.js + Claude CLI |
| docker-compose.yml | Modified — env vars + volumes |
| app/routers/mobile.py | Modified — diff viewer endpoint |
| app/templates/mobile/agent/diff.html | Created — diff viewer template |
```

Fill in performance/cost data if checkpoint testing was performed. If not tested, mark fields with "TBD — requires live testing with ANTHROPIC_API_KEY" and set Status to "untested".
  </action>
  <verify>
    <automated>test -f /opt/seo-platform/.planning/phases/33-claude-code-agent-spike/SPIKE-REPORT.md && grep -q "Go/No-Go" /opt/seo-platform/.planning/phases/33-claude-code-agent-spike/SPIKE-REPORT.md && grep -q "bot/handlers/agent.py" /opt/seo-platform/.planning/phases/33-claude-code-agent-spike/SPIKE-REPORT.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `test -f .planning/phases/33-claude-code-agent-spike/SPIKE-REPORT.md` succeeds
- `grep "Go/No-Go" .planning/phases/33-claude-code-agent-spike/SPIKE-REPORT.md` returns a match
- `grep "bot/handlers/agent.py" .planning/phases/33-claude-code-agent-spike/SPIKE-REPORT.md` returns a match
- `grep "What Works" .planning/phases/33-claude-code-agent-spike/SPIKE-REPORT.md` returns a match
- `grep "Limitations" .planning/phases/33-claude-code-agent-spike/SPIKE-REPORT.md` returns a match
  </acceptance_criteria>
  <done>SPIKE-REPORT.md documents the experiment: what was built, architecture, what works, limitations, performance/cost observations, and a go/no-go recommendation with conditions for production.</done>
</task>

</tasks>

<verification>
1. `test -f app/templates/mobile/agent/diff.html` — diff template exists
2. `grep "agent/diff" app/routers/mobile.py` — endpoint registered
3. `test -f .planning/phases/33-claude-code-agent-spike/SPIKE-REPORT.md` — spike documented
4. `grep "Go/No-Go" .planning/phases/33-claude-code-agent-spike/SPIKE-REPORT.md` — recommendation present
</verification>

<success_criteria>
- /m/agent/diff/{task_id} endpoint reads diff from shared volume and renders colored diff in mobile template
- End-to-end flow verified (or documented as untested with reason)
- SPIKE-REPORT.md contains what was built, limitations, and go/no-go recommendation
</success_criteria>

<output>
After completion, create `.planning/phases/33-claude-code-agent-spike/33-02-SUMMARY.md`
</output>
