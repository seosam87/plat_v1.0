---
phase: 13-impact-scoring-growth-opportunities
plan: "03"
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - app/main.py
  - app/templates/projects/kanban.html
  - app/routers/opportunities.py
  - app/templates/analytics/opportunities.html
  - app/templates/analytics/partials/slide_over.html
  - app/templates/analytics/partials/detail_gap.html
  - app/templates/analytics/partials/detail_loss.html
  - app/templates/analytics/partials/detail_cannibal.html
autonomous: true
requirements: [IMP-02, GRO-02]

must_haves:
  truths:
    - "User can sort the Kanban board by impact_score so highest-traffic errors appear first (IMP-02)"
    - "Each Kanban task card shows an impact score badge when score > 0 (IMP-02)"
    - "User can click any row in Opportunities tabs to open a slide-over panel with details (GRO-02, D-08)"
    - "Slide-over panel has a 'Подробнее' link navigating to the full detail page (GRO-02, D-09)"
  artifacts:
    - path: "app/templates/projects/kanban.html"
      provides: "Kanban with impact sort toggle and score badges"
      contains: "sort=impact"
    - path: "app/templates/analytics/partials/slide_over.html"
      provides: "Reusable slide-over drawer partial"
      contains: "slide-over"
    - path: "app/templates/analytics/partials/detail_gap.html"
      provides: "Gap keyword detail for slide-over"
      contains: "Подробнее"
  key_links:
    - from: "app/main.py"
      to: "app/services/impact_score_service.py"
      via: "get_max_impact_score_by_url() in ui_kanban handler"
      pattern: "get_max_impact_score_by_url"
    - from: "app/templates/analytics/opportunities.html"
      to: "app/templates/analytics/partials/slide_over.html"
      via: "include or inline slide-over container"
      pattern: "slide-over"
    - from: "app/routers/opportunities.py"
      to: "detail partials"
      via: "detail route handlers"
      pattern: "/detail/"
---

<objective>
Add Kanban sort-by-impact toggle with impact score badges on task cards (IMP-02), and add slide-over drill-down panels to the Growth Opportunities dashboard (GRO-02). These integrate the impact scores from Plan 01 into the Kanban UI and add drill-down navigation to the dashboard from Plan 02.

Purpose: IMP-02 requires Kanban sorting by impact_score. GRO-02 requires drill-down from Opportunities dashboard cards to detail views via slide-over panels.
Output: Modified Kanban template, slide-over partial, detail partials, detail route handlers
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-impact-scoring-growth-opportunities/13-CONTEXT.md
@.planning/phases/13-impact-scoring-growth-opportunities/13-RESEARCH.md
@.planning/phases/13-impact-scoring-growth-opportunities/13-01-SUMMARY.md
@.planning/phases/13-impact-scoring-growth-opportunities/13-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From app/services/impact_score_service.py:
```python
async def get_max_impact_score_by_url(db: AsyncSession, site_id: uuid.UUID) -> dict[str, int]:
    """Returns dict mapping normalized page_url -> MAX(impact_score) for Kanban use."""
```

<!-- From Plan 02 -->
From app/routers/opportunities.py:
```python
router = APIRouter(prefix="/analytics", tags=["analytics"])
# GET /{site_id}/opportunities — full page
# GET /{site_id}/opportunities/tabs/gaps — partial
# GET /{site_id}/opportunities/tabs/losses — partial
# GET /{site_id}/opportunities/tabs/cannibal — partial
# GET /{site_id}/opportunities/tabs/trend — partial
```

From app/services/opportunities_service.py:
```python
async def get_gap_summary(db, site_id, limit=100) -> dict
async def get_lost_positions(db, site_id, limit=200) -> dict
async def get_cannibalization(db, site_id) -> dict
```

<!-- Existing Kanban handler -->
From app/main.py lines 871-910:
```python
@app.get("/ui/projects/{project_id}/kanban", response_class=HTMLResponse)
async def ui_kanban(project_id: str, request: Request, db: AsyncSession = Depends(get_db)):
    # Loads tasks, groups by status, renders kanban.html
    # Tasks are dicts: {"id", "title", "task_type", "due_date"}
```

From app/templates/projects/kanban.html line 1-10:
```html
{% extends "base.html" %}
{% block content %}
<div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6">
  <div class="flex justify-between items-center mb-6">
    <h2 class="text-lg font-semibold text-gray-800 m-0">{{ project_name }} — Tasks</h2>
    <button class="..." onclick="...">+ Create Task</button>
  </div>
```

From app/models/task.py:
```python
class SeoTask(Base):
    site_id: Mapped[uuid.UUID]
    url: Mapped[str] = mapped_column(String(2000), nullable=False)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kanban impact sort toggle and score badges</name>
  <files>app/main.py, app/templates/projects/kanban.html</files>
  <read_first>app/main.py, app/templates/projects/kanban.html, app/services/impact_score_service.py, app/models/task.py, app/utils/url_normalize.py</read_first>
  <action>
    **Modify `ui_kanban()` in `app/main.py`** (lines 871-910):
    
    1. Add `sort: str = "created"` query parameter to the function signature
    2. After loading tasks, import and call `get_max_impact_score_by_url` from `app.services.impact_score_service`:
       ```python
       from app.services.impact_score_service import get_max_impact_score_by_url
       from app.utils.url_normalize import normalize_url
       ```
    3. Get the project's site_id (query Project model — it has a site_id FK). If site_id exists, call `impact_map = await get_max_impact_score_by_url(db, site_id)`. Otherwise `impact_map = {}`.
    4. When building the task dict in the `for t in tasks` loop, add `"impact_score": impact_map.get(normalize_url(t.url), 0)` to each task dict.
    5. If `sort == "impact"`, sort each status group list by `impact_score` DESC (highest first) instead of the default created_at order.
    6. Pass `sort=sort` to the template context.

    **Modify `kanban.html`**:
    
    1. In the header div (after the h2, before the "+ Create Task" button), add a sort toggle (per D-04, Claude's discretion — using a dropdown):
       ```html
       <div class="flex items-center gap-2">
         <select 
           hx-get="/ui/projects/{{ project_id }}/kanban"
           hx-target="body"
           hx-push-url="true"
           name="sort"
           class="px-2 py-1.5 text-sm border border-gray-300 rounded bg-white text-gray-700 focus:ring-indigo-500">
           <option value="created" {% if sort != 'impact' %}selected{% endif %}>По дате</option>
           <option value="impact" {% if sort == 'impact' %}selected{% endif %}>По Impact Score</option>
         </select>
       </div>
       ```
    2. On each task card, if `task.impact_score` > 0, add an impact badge:
       ```html
       {% if task.impact_score %}
       <span class="inline-flex items-center text-[0.65rem] font-mono text-orange-600 bg-orange-50 px-1.5 py-0.5 rounded ml-1" title="Impact Score">
         {{ task.impact_score }}
       </span>
       {% endif %}
       ```
       Place this badge next to the task title or task_type badge in the card markup.
  </action>
  <verify>
    <automated>cd /projects/test && grep -c "sort=impact" app/templates/projects/kanban.html && grep -c "get_max_impact_score_by_url" app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - app/main.py ui_kanban function has `sort: str = "created"` parameter
    - app/main.py contains `from app.services.impact_score_service import get_max_impact_score_by_url`
    - app/main.py contains `normalize_url(t.url)` for impact score lookup
    - app/templates/projects/kanban.html contains `sort=impact` in the sort toggle
    - app/templates/projects/kanban.html contains `impact_score` in task card badge rendering
    - app/templates/projects/kanban.html contains `hx-get` on the sort select element
  </acceptance_criteria>
  <done>Kanban board has a sort dropdown toggle between "По дате" and "По Impact Score", task cards show orange impact score badges, sort=impact orders tasks by highest impact first</done>
</task>

<task type="auto">
  <name>Task 2: Slide-over panel + detail routes for Opportunities drill-down</name>
  <files>app/routers/opportunities.py, app/templates/analytics/opportunities.html, app/templates/analytics/partials/slide_over.html, app/templates/analytics/partials/detail_gap.html, app/templates/analytics/partials/detail_loss.html, app/templates/analytics/partials/detail_cannibal.html, app/templates/analytics/partials/opportunities_gaps.html, app/templates/analytics/partials/opportunities_losses.html, app/templates/analytics/partials/opportunities_cannibal.html</files>
  <read_first>app/routers/opportunities.py, app/templates/analytics/opportunities.html, app/templates/analytics/partials/opportunities_gaps.html, app/templates/analytics/partials/opportunities_losses.html, app/templates/analytics/partials/opportunities_cannibal.html, app/models/gap.py, app/models/keyword_latest_position.py</read_first>
  <action>
    **Slide-over partial** (`app/templates/analytics/partials/slide_over.html`):
    Create a reusable slide-over drawer:
    ```html
    <!-- Slide-over overlay + drawer -->
    <div id="slide-over" class="hidden fixed inset-0 z-50">
      <div class="absolute inset-0 bg-black bg-opacity-30" onclick="closeSlideOver()"></div>
      <div class="absolute right-0 top-0 h-full w-96 bg-white shadow-xl overflow-y-auto transition-transform duration-200"
           id="slide-over-content">
        <!-- Filled by HTMX -->
      </div>
    </div>
    <script>
    function closeSlideOver() {
      document.getElementById('slide-over').classList.add('hidden');
      document.getElementById('slide-over-content').innerHTML = '';
    }
    function openSlideOver() {
      document.getElementById('slide-over').classList.remove('hidden');
    }
    </script>
    ```

    **Include slide-over in opportunities.html**: Add `{% include "analytics/partials/slide_over.html" %}` at the bottom of the content block, before `{% endblock %}`.

    **Update tab partials to trigger slide-over** (per D-08):
    
    In `opportunities_gaps.html`: Each table row gets:
    ```html
    hx-get="/analytics/{{ site.id }}/opportunities/detail/gap/{{ item.id }}"
    hx-target="#slide-over-content"
    hx-on::after-request="openSlideOver()"
    ```

    In `opportunities_losses.html`: Each table row gets:
    ```html
    hx-get="/analytics/{{ site.id }}/opportunities/detail/loss/{{ item.keyword_id }}"
    hx-target="#slide-over-content"
    hx-on::after-request="openSlideOver()"
    ```

    In `opportunities_cannibal.html`: Each group header row gets:
    ```html
    hx-get="/analytics/{{ site.id }}/opportunities/detail/cannibal/{{ item.keyword_id }}"
    hx-target="#slide-over-content"
    hx-on::after-request="openSlideOver()"
    ```

    **Detail partials** (per D-09 — each has a "Подробнее" link to the full page):

    `detail_gap.html`:
    - Header: "Gap-ключ: {{ item.phrase }}"
    - Fields: Конкурент, Позиция конкурента, Наша позиция, Потенциал, Частотность (if available)
    - Close button (X) calling `closeSlideOver()`
    - "Подробнее" link: `<a href="/gap/{{ site.id }}" class="...">Перейти к Gap-анализу →</a>`

    `detail_loss.html`:
    - Header: "Потеря: {{ item.phrase }}"
    - Fields: URL, Текущая позиция, Предыдущая позиция, Изменение (delta, red)
    - Close button
    - "Подробнее" link: `<a href="/ui/positions/{{ site.id }}" class="...">Перейти к позициям →</a>`

    `detail_cannibal.html`:
    - Header: "Каннибализация: {{ item.phrase }}"
    - Table of competing URLs with positions
    - Close button
    - "Подробнее" link: `<a href="/ui/cannibalization/{{ site.id }}" class="...">Перейти к каннибализации →</a>`

    **Add detail routes to `app/routers/opportunities.py`**:
    
    `GET /{site_id}/opportunities/detail/gap/{gap_keyword_id}` — Query GapKeyword by id, render detail_gap.html partial.
    
    `GET /{site_id}/opportunities/detail/loss/{keyword_id}` — Query KeywordLatestPosition by keyword_id + site_id, JOIN keywords for phrase, render detail_loss.html partial.
    
    `GET /{site_id}/opportunities/detail/cannibal/{keyword_id}` — Query all KeywordLatestPosition entries for keyword_id + site_id where position <= 50, JOIN keywords for phrase, render detail_cannibal.html partial.

    All detail routes return HTML partials (not full pages). Each passes `site` object for building the "Подробнее" link.
  </action>
  <verify>
    <automated>cd /projects/test && python -c "from app.routers.opportunities import router; print('Routes:', len(router.routes))" && ls app/templates/analytics/partials/slide_over.html app/templates/analytics/partials/detail_gap.html app/templates/analytics/partials/detail_loss.html app/templates/analytics/partials/detail_cannibal.html</automated>
  </verify>
  <acceptance_criteria>
    - app/routers/opportunities.py contains routes for `/detail/gap/`, `/detail/loss/`, `/detail/cannibal/` (total 8 routes: 5 original + 3 detail)
    - app/templates/analytics/partials/slide_over.html contains `id="slide-over"` and `closeSlideOver` function
    - app/templates/analytics/opportunities.html contains `slide_over.html` include
    - app/templates/analytics/partials/detail_gap.html contains "Подробнее" and `/gap/`
    - app/templates/analytics/partials/detail_loss.html contains "Подробнее" and `/ui/positions/`
    - app/templates/analytics/partials/detail_cannibal.html contains "Подробнее" and `/ui/cannibalization/`
    - app/templates/analytics/partials/opportunities_gaps.html contains `hx-get` with `/detail/gap/`
    - app/templates/analytics/partials/opportunities_losses.html contains `hx-get` with `/detail/loss/`
    - app/templates/analytics/partials/opportunities_cannibal.html contains `hx-get` with `/detail/cannibal/`
  </acceptance_criteria>
  <done>Clicking any row in Opportunities tabs opens a slide-over panel with item details and a "Подробнее" link to the full section page; slide-over can be closed by clicking overlay or X button</done>
</task>

</tasks>

<verification>
- `python -c "from app.routers.opportunities import router; print(len(router.routes))"` prints 8
- `grep -c "sort=impact" app/templates/projects/kanban.html` returns >= 1
- `grep -c "get_max_impact_score_by_url" app/main.py` returns >= 1
- `ls app/templates/analytics/partials/slide_over.html app/templates/analytics/partials/detail_gap.html app/templates/analytics/partials/detail_loss.html app/templates/analytics/partials/detail_cannibal.html` succeeds
- `grep "Подробнее" app/templates/analytics/partials/detail_*.html` shows links in all 3 detail partials
</verification>

<success_criteria>
- Kanban has sort dropdown toggling between created_at and impact_score ordering (IMP-02)
- Task cards show orange impact score badge when score > 0 (IMP-02)
- Slide-over panel opens on row click in any Opportunities tab (GRO-02, D-08)
- Each detail panel shows relevant fields and "Подробнее" link to full page (GRO-02, D-09)
- Slide-over closes cleanly on overlay click or X button (D-08)
</success_criteria>

<output>
After completion, create `.planning/phases/13-impact-scoring-growth-opportunities/13-03-SUMMARY.md`
</output>
