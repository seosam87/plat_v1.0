---
phase: 18-project-health-widget
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/site_service.py
  - app/main.py
  - app/templates/sites/detail.html
  - app/templates/macros/health.html
  - tests/test_site_health.py
autonomous: true
requirements: [PHW-01, PHW-02, PHW-03, PHW-04, PHW-05, PHW-06]

must_haves:
  truths:
    - "User opening /ui/sites/{site_id} sees a 7-step Project Health checklist"
    - "Each step shows ✅/⏳/⚠️ icon, title, one-line RU description and (if not done) a 'Сделать сейчас' link"
    - "Widget shows progress N/7 and highlights the first incomplete step as 'Следующий шаг: ... →'"
    - "When all 7 steps are complete the widget renders collapsed with success message + 'Показать снова' toggle"
    - "Widget is rendered from data computed by site_service.compute_site_health(db, site_id) with no extra Celery tasks and no extra HTTP round-trips"
    - "/ui/sites/{site_id} is reached and rendered (200) by the Phase 15.1 smoke crawler"
    - "compute_site_health unit-tests cover each of the 7 signals independently (positive + negative)"
  artifacts:
    - path: "app/services/site_service.py"
      provides: "compute_site_health() returning SiteHealth dataclass"
      contains: "def compute_site_health"
    - path: "app/templates/macros/health.html"
      provides: "project_health_widget(health) Jinja macro"
      contains: "macro project_health_widget"
    - path: "app/templates/sites/detail.html"
      provides: "Site Overview page hosting the widget"
      contains: "project_health_widget"
    - path: "app/main.py"
      provides: "GET /ui/sites/{site_id} route rendering sites/detail.html with health"
      contains: "ui_site_overview"
    - path: "tests/test_site_health.py"
      provides: "Unit tests for each of the 7 signals"
      contains: "compute_site_health"
  key_links:
    - from: "app/main.py:/ui/sites/{site_id}"
      to: "site_service.compute_site_health"
      via: "direct call inside the route handler, same db session"
      pattern: "compute_site_health\\(db,"
    - from: "app/templates/sites/detail.html"
      to: "app/templates/macros/health.html"
      via: "{% from 'macros/health.html' import project_health_widget %}"
      pattern: "macros/health.html"
---

<objective>
Ship the Project Health Widget for v2.1 Phase 18: a 7-step setup checklist on the per-site Overview page, derived synchronously from existing DB state with zero new tables, zero migrations, zero Celery tasks, zero new HTTP calls.

Purpose: When the user (or a teammate) returns to a site after a break, the Overview page must answer "what's set up?" and "what to do next?" in one glance.

Output:
- `site_service.compute_site_health()` — single source of truth, returns a structured `SiteHealth` dataclass
- A Jinja macro `project_health_widget(health)` for reuse on future dashboards
- A restored per-site Overview route at `GET /ui/sites/{site_id}` rendering `sites/detail.html` with the widget
- Unit tests covering each of the 7 signals individually
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-project-health-widget/18-CONTEXT.md
@.planning/REQUIREMENTS.md

Real source files (read before editing):
@/opt/seo-platform/app/services/site_service.py
@/opt/seo-platform/app/models/site.py
@/opt/seo-platform/app/models/keyword.py
@/opt/seo-platform/app/models/competitor.py
@/opt/seo-platform/app/models/crawl.py
@/opt/seo-platform/app/models/position.py
@/opt/seo-platform/app/models/schedule.py
@/opt/seo-platform/app/templates/sites/detail.html

<interfaces>
<!-- Verified facts from the codebase, embedded so the executor doesn't need to re-explore. -->

# Models and the columns we will read (verified, do NOT assume others)

app/models/site.py — Site:
  - id: UUID
  - url: str (NOT NULL, may be empty-string-rstripped — treat empty as not configured)
  - wp_username: str | None
  - encrypted_app_password: str | None  ← THIS is the "wp_password set" signal (NOT `wp_password`)
  - metrika_token: str | None
  - metrika_counter_id: str | None

app/models/keyword.py — Keyword:    .site_id (UUID)
app/models/competitor.py — Competitor: .site_id (UUID)
app/models/crawl.py — CrawlJob:        .site_id (UUID)
app/models/position.py — KeywordPosition: .site_id (UUID)
   ← USE THIS for "first position check ran" — there is NO `position_check_runs` model

app/models/schedule.py — CrawlSchedule and PositionSchedule:
  - .site_id (UUID)
  - .schedule_type: ScheduleType enum (manual, daily, weekly, every_12h)
  - .is_active: bool
  ← "Schedule configured" = at least one of these rows exists with is_active=True AND schedule_type != ScheduleType.manual

# Existing service helpers
app/services/keyword_service.py:
  async def count_keywords(db: AsyncSession, site_id: UUID) -> int
  ← NOT used by the new route handler — use SiteHealth.keyword_count instead to avoid duplicate queries.

# OAuth token (for analytics secondary signal)
app/models/oauth_token.py — OAuthToken: store of GSC tokens; check by site_id (verify exact column name during execution; if absent, fall back to `False` for GSC and only check metrika_token).

# Routes that already exist and serve as next_url targets (VERIFIED in main.py)
- /ui/sites/{site_id}/edit            (WP creds)
- /ui/keywords/{site_id}              (keywords list — no /import sub-route)
- /ui/competitors/{site_id}           (competitors — main.py line 1843, VERIFIED)
- /ui/sites/{site_id}/crawls          (crawl history)
- /ui/positions/{site_id}             (positions)
- /ui/sites/{site_id}/schedule        (crawl/position schedule combined page)

# Current state of /ui/sites/{site_id}/detail
main.py line ~2181: redirects 301 → /ui/sites (per legacy decisions D-06, D-08).
We are NOT touching that legacy route. We are introducing a NEW route at the parent path /ui/sites/{site_id} (no trailing /detail) that renders the same sites/detail.html template, augmented with the health widget.

# sites/detail.html template variables (audited 2026-04-08)
The existing template references the following variables in {% block content %}:
  - site (with .id, .name, .connection_status)
  - keyword_count          (int, displayed in stats card)
  - crawl_count            (int, displayed in stats card)
  - task_count             (int, displayed in stats card)
  - recent_tasks           (list, optional — rendered only inside {% if recent_tasks %})
  - recent_crawls          (list, optional — rendered only inside {% if recent_crawls %})
  - crawl_schedule         (str, used by <select> — must be one of "manual"|"daily"|"weekly")
  - position_schedule      (str, used by <select> — must be one of "manual"|"daily"|"weekly"|"every_12h")
The route MUST supply all of the above (or the {% if %}-guarded ones may be omitted/empty).

# Smoke crawler
tests/test_smoke_routes.py (Phase 15.1) iterates every GET route in app.routes, substitutes {site_id} from a seed fixture, asserts 200. Adding /ui/sites/{site_id} is sufficient — no fixture changes needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for compute_site_health (RED)</name>
  <files>tests/test_site_health.py</files>
  <behavior>
    Tests for `app.services.site_service.compute_site_health(db, site_id)` returning a `SiteHealth` object with these attributes:
      - steps: list[HealthStep] of length 7 in fixed order
      - current_step_index: int (0-based index of first incomplete step, or None if all done)
      - completed_count: int (0..7)
      - is_fully_set_up: bool (True iff completed_count == 7)
      - analytics_connected: bool (secondary signal — does NOT affect is_fully_set_up)
      - keyword_count: int (raw COUNT of Keyword rows for site_id — exposed for template reuse)
      - crawl_count: int (raw COUNT of CrawlJob rows for site_id — exposed for template reuse)
      - competitor_count: int (raw COUNT of Competitor rows for site_id — exposed for template reuse)

    Each HealthStep has: key (str), title (str RU), description (str RU), done (bool), status ("done"|"current"|"pending"), next_url (str|None), is_current (bool).

    Required test cases (one per signal, plus aggregate):
      - test_step1_site_created_always_done: brand-new Site row → step[0].done is True
      - test_step2_wp_creds_done: site.wp_username + site.encrypted_app_password set + non-empty url → step[1].done True; missing encrypted_app_password → False
      - test_step3_keywords_done: insert one Keyword for site → step[2].done True AND health.keyword_count == 1; zero → False AND keyword_count == 0
      - test_step4_competitors_done: insert one Competitor → step[3].done True AND health.competitor_count == 1; zero → False
      - test_step5_crawl_done: insert one CrawlJob → step[4].done True AND health.crawl_count == 1; zero → False
      - test_step6_position_done: insert one KeywordPosition → step[5].done True; zero → False
      - test_step7_schedule_done: insert one CrawlSchedule with is_active=True and schedule_type=daily → step[6].done True;
        with schedule_type=manual → False; with is_active=False → False
      - test_progress_counts_partial: site with steps 1,2,3 satisfied → completed_count == 3, current_step_index == 3, is_fully_set_up False
      - test_fully_set_up: all 7 satisfied → is_fully_set_up True, current_step_index is None
      - test_analytics_secondary: site.metrika_token set → analytics_connected True; does NOT change is_fully_set_up when other steps incomplete
      - test_next_step_url_present_for_pending: every step where done is False has a non-empty next_url string
      - test_competitors_next_url_format: the competitors step's next_url == "/ui/competitors/{site_id}" (NOT /ui/sites/{id}/competitors)

    Use the existing pytest-asyncio + AsyncSession fixture pattern from tests/test_overview_service.py and tests/conftest.py. Use real DB inserts via the test session — do NOT mock SQLAlchemy.
  </behavior>
  <action>
    1. Read tests/conftest.py and tests/test_overview_service.py to copy the async DB fixture pattern.
    2. Create tests/test_site_health.py implementing all test cases listed in <behavior>. Tests MUST currently fail with ImportError or AttributeError because compute_site_health does not yet exist.
    3. Run tests and capture the failure to confirm RED.
    4. Commit: `test(18-01): add failing tests for compute_site_health (RED)`
  </action>
  <verify>
    <automated>cd /opt/seo-platform &amp;&amp; pytest tests/test_site_health.py -x 2>&amp;1 | tail -20  # MUST fail (RED expected)</automated>
  </verify>
  <done>tests/test_site_health.py exists with 12+ tests covering each signal AND keyword_count/crawl_count/competitor_count assertions; pytest run confirms RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement compute_site_health() in site_service (GREEN)</name>
  <files>app/services/site_service.py</files>
  <behavior>
    `compute_site_health(db: AsyncSession, site_id: uuid.UUID) -> SiteHealth` returns the SiteHealth dataclass described in Task 1, computed from the DB with one COUNT per signal (6 COUNTs + 1 SELECT for the Site row + 1 EXISTS for schedule = ≤ 8 fast indexed queries, well under 200ms).

    The dataclass exposes raw counts (keyword_count, crawl_count, competitor_count) so the route handler can reuse them WITHOUT issuing duplicate COUNT queries.

    All test cases from Task 1 MUST pass.
  </behavior>
  <action>
    1. Add at the top of app/services/site_service.py:
       - `from dataclasses import dataclass, field`
       - imports for Keyword, Competitor, CrawlJob, KeywordPosition, CrawlSchedule, PositionSchedule, ScheduleType, OAuthToken (if exists)
       - `from sqlalchemy import func, or_, select`

    2. Define module-level dataclasses:
       ```python
       @dataclass
       class HealthStep:
           key: str
           title: str          # RU
           description: str    # RU one-liner
           done: bool
           status: str         # "done" | "current" | "pending"
           next_url: str | None
           is_current: bool = False

       @dataclass
       class SiteHealth:
           steps: list[HealthStep]
           completed_count: int
           current_step_index: int | None
           is_fully_set_up: bool
           analytics_connected: bool
           # Raw counts exposed so route handlers / templates don't re-query:
           keyword_count: int = 0
           crawl_count: int = 0
           competitor_count: int = 0
       ```

    3. Define a private constant `_STEP_DEFS` listing the 7 steps in order with key/title/description/next_url template (use `{site_id}` placeholder, format per call). Step copy in Russian:
       - site_created — "Сайт создан" — "Запись о сайте существует в системе" — None
       - wp_creds — "Доступ к WordPress настроен" — "Указаны URL, логин и Application Password" — /ui/sites/{site_id}/edit
       - keywords — "Ключевые слова добавлены" — "Импортированы или заведены вручную ключи" — /ui/keywords/{site_id}
       - competitors — "Конкуренты добавлены" — "Указан хотя бы один конкурент для отслеживания" — /ui/competitors/{site_id}   ← VERIFIED route in main.py line 1843, hard-code this exact path, NO fallback needed
       - crawl — "Первый краул выполнен" — "Сайт хотя бы раз был просканирован" — /ui/sites/{site_id}/crawls
       - positions — "Позиции проверены" — "Хотя бы одна проверка позиций выполнена" — /ui/positions/{site_id}
       - schedule — "Расписание настроено" — "Включён автоматический краул или проверка позиций" — /ui/sites/{site_id}/schedule

    4. Implement `compute_site_health` using these queries (capture COUNT scalars into local vars and reuse):
       - site = await db.get(Site, site_id); if None return SiteHealth(steps=[], completed_count=0, current_step_index=None, is_fully_set_up=False, analytics_connected=False, keyword_count=0, crawl_count=0, competitor_count=0)
       - kw_count = (await db.execute(select(func.count()).select_from(Keyword).where(Keyword.site_id==site_id))).scalar() or 0
       - comp_count = same for Competitor
       - crawl_count_val = same for CrawlJob
       - pos_exists = same for KeywordPosition (>0 boolean is enough)
       - sched_count = count of (CrawlSchedule where site_id=? AND is_active AND schedule_type != manual) UNION ALL same on PositionSchedule (or two queries summed; still ≤8 total)
       - step1 done: site is not None
       - step2 done: bool(site.wp_username and site.encrypted_app_password and site.url)
       - step3 done: kw_count > 0
       - step4 done: comp_count > 0
       - step5 done: crawl_count_val > 0
       - step6 done: pos_exists > 0
       - step7 done: sched_count > 0
       - analytics_connected: bool(site.metrika_token) OR (existence query on OAuthToken if model has site_id; otherwise just metrika_token)

    5. Build `steps` list from `_STEP_DEFS` with computed `done` values; format `next_url` with site_id; mark first not-done step as `is_current=True` and status="current"; done steps status="done"; remaining pending status="pending"; done steps next_url=None.

    6. Compute completed_count, current_step_index (None if all done), is_fully_set_up.

    7. Populate SiteHealth with keyword_count=kw_count, crawl_count=crawl_count_val, competitor_count=comp_count. These are the SAME scalars already used for steps 3/4/5 — no duplicate queries.

    8. Run pytest until all Task 1 tests pass (GREEN). Iterate on any signal mismatch.

    9. Commit: `feat(18-01): implement compute_site_health (GREEN)`
  </action>
  <verify>
    <automated>cd /opt/seo-platform &amp;&amp; pytest tests/test_site_health.py -x 2>&amp;1 | tail -20</automated>
  </verify>
  <done>All tests in tests/test_site_health.py pass. compute_site_health is exported from app.services.site_service. Function uses ≤ 8 queries total. SiteHealth.keyword_count / crawl_count / competitor_count are populated from the same scalars used for step.done evaluation (no duplicate COUNTs).</done>
</task>

<task type="auto">
  <name>Task 3: Create project_health_widget Jinja macro</name>
  <files>app/templates/macros/health.html</files>
  <action>
    1. Create directory app/templates/macros/ if it doesn't exist (verify with `ls app/templates/macros/`).
    2. Create app/templates/macros/health.html containing one macro:
       `{% macro project_health_widget(health) %}`

    Layout requirements (Tailwind hex colors per project memory; reuse `.card` classes from sites/detail.html):
    - Root `<div class="card">` with id="project-health-widget"
    - If health.is_fully_set_up: render collapsed success state:
        * Header: ✅ "Проект полностью настроен (7/7)"
        * Small button `<button onclick="document.getElementById('phw-full').classList.toggle('hidden')">Показать снова</button>`
        * Hidden `<div id="phw-full" class="hidden">` containing the full checklist
    - Else (not fully set up):
        * Header row: "Настройка проекта" + progress badge `{{ health.completed_count }}/7`
        * Vertical `<ul>` of 7 `<li>` items, one per step:
          - Icon column: ✅ if step.done, → if step.is_current, ○ otherwise (color: #059669 done, #f59e0b current, #9ca3af pending)
          - Title (bold) + one-line description
          - If not done: `<a class="btn btn-sm" href="{{ step.next_url }}">Сделать сейчас</a>`
        * Below the list, if current_step_index is not None:
          `<div>Следующий шаг: <a href="{{ health.steps[health.current_step_index].next_url }}">{{ health.steps[health.current_step_index].title }} →</a></div>`
        * Secondary line: "Аналитика подключена: ✅" if health.analytics_connected else "Аналитика не подключена (необязательно)"

    Use only RU text. No JavaScript beyond the inline toggle. No HTMX needed.
  </action>
  <verify>
    <automated>cd /opt/seo-platform &amp;&amp; python -c "from jinja2 import Environment, FileSystemLoader; e=Environment(loader=FileSystemLoader('app/templates')); t=e.get_template('macros/health.html'); print('OK')"</automated>
  </verify>
  <done>Macro file exists, parses without Jinja2 syntax errors, exports project_health_widget(health) symbol.</done>
</task>

<task type="auto">
  <name>Task 4: Add /ui/sites/{site_id} route and wire widget into sites/detail.html</name>
  <files>app/main.py, app/templates/sites/detail.html</files>
  <action>
    **Step 0 — Template variable audit (MUST run before writing the route):**

    Read app/templates/sites/detail.html top-to-bottom and enumerate every `{{ ... }}` and `{% if/for ... %}` reference. The audit performed during planning (2026-04-08) found these required variables:

    | Variable          | Used by                              | Required? | How the route supplies it                                                  |
    |-------------------|--------------------------------------|-----------|----------------------------------------------------------------------------|
    | site              | header, edit link, all hx- urls      | YES       | `await get_site(db, sid)`                                                  |
    | site.connection_status | connection badge                | YES       | column on Site model, already loaded via get_site                          |
    | keyword_count     | stats card                           | YES       | `health.keyword_count` (no extra query)                                    |
    | crawl_count       | stats card                           | YES       | `health.crawl_count` (no extra query)                                      |
    | task_count        | stats card                           | YES       | `0` (Task model not in scope for Phase 18 — display 0; PHW does not block) |
    | recent_tasks      | optional table inside `{% if %}`     | optional  | `[]` (rendered block is skipped)                                            |
    | recent_crawls     | optional table inside `{% if %}`     | optional  | `[]` (rendered block is skipped)                                            |
    | crawl_schedule    | <select> default value               | YES       | query CrawlSchedule for this site → `.schedule_type.value` or "manual"     |
    | position_schedule | <select> default value               | YES       | query PositionSchedule for this site → `.schedule_type.value` or "manual"  |
    | health            | NEW — for widget                     | YES       | `compute_site_health(db, sid)`                                              |

    Re-run this audit during execution: `grep -nE '\{\{|\{%' app/templates/sites/detail.html`. If any variable not in the table above appears, add it to the route context (do NOT silently let Jinja UndefinedError fire).

    **Step 1 — Add the new route to app/main.py.** Place it BEFORE the `/ui/sites/{site_id}/detail` redirect at line ~2181 (FastAPI matches in declaration order):

    ```python
    @app.get("/ui/sites/{site_id}", response_class=HTMLResponse)
    async def ui_site_overview(site_id: str, request: Request, db: AsyncSession = Depends(get_db)) -> HTMLResponse:
        import uuid as _uuid
        from app.services.site_service import get_site, compute_site_health
        from app.models.schedule import CrawlSchedule, PositionSchedule
        from sqlalchemy import select as sa_select

        sid = _uuid.UUID(site_id)
        site = await get_site(db, sid)
        if not site:
            return HTMLResponse("Site not found", status_code=404)

        health = await compute_site_health(db, sid)

        # Schedule values for the existing <select> dropdowns in detail.html.
        # These are NOT covered by SiteHealth (which only checks "any active non-manual exists").
        cs_row = (await db.execute(
            sa_select(CrawlSchedule).where(CrawlSchedule.site_id == sid).limit(1)
        )).scalar_one_or_none()
        ps_row = (await db.execute(
            sa_select(PositionSchedule).where(PositionSchedule.site_id == sid).limit(1)
        )).scalar_one_or_none()
        crawl_schedule = cs_row.schedule_type.value if cs_row else "manual"
        position_schedule = ps_row.schedule_type.value if ps_row else "manual"

        return templates.TemplateResponse(request, "sites/detail.html", {
            "site": site,
            "health": health,
            # Reuse counts from SiteHealth — NO duplicate queries:
            "keyword_count": health.keyword_count,
            "crawl_count": health.crawl_count,
            "task_count": 0,         # Task model out of scope for Phase 18; safe stub
            "recent_tasks": [],      # Rendered only inside {% if recent_tasks %}, safe to stub empty
            "recent_crawls": [],     # Same — guarded by {% if recent_crawls %}
            "crawl_schedule": crawl_schedule,
            "position_schedule": position_schedule,
        })
    ```

    Total query count for the new route: 1 (get_site) + ≤8 (compute_site_health) + 2 (schedule rows) = ≤ 11. Still well under 200ms; the constraint "≤8 in compute_site_health" is preserved. The handler issues NO duplicate COUNTs — keyword_count and crawl_count are read directly from `health`.

    **Step 2 — Wire the widget into sites/detail.html.** At the top of `{% block content %}` (right after the existing header div, BEFORE the stats grid):
       ```jinja
       {% from "macros/health.html" import project_health_widget %}
       {% if health %}
         <div style="margin-bottom:1.5rem">{{ project_health_widget(health) }}</div>
       {% endif %}
       ```

    The `{% if health %}` guard keeps the existing template safe if rendered from another context that doesn't pass `health`.

    **Step 3 — Manually exercise:** start the dev server (or use TestClient inline) and hit `/ui/sites/{any-real-site-id}` — confirm 200, no UndefinedError, widget HTML appears, stats cards still show real keyword/crawl counts.
  </action>
  <verify>
    <automated>cd /opt/seo-platform &amp;&amp; python -c "
from fastapi.testclient import TestClient
from app.main import app
routes = [r.path for r in app.routes if hasattr(r,'path')]
assert '/ui/sites/{site_id}' in routes, f'route missing; have: {[r for r in routes if \"/ui/sites/\" in r]}'
print('route registered: OK')
"</automated>
  </verify>
  <done>GET /ui/sites/{site_id} is registered in FastAPI routes, declared before the /detail redirect, renders sites/detail.html with `health` + all template-required variables, reuses health.keyword_count / health.crawl_count (no duplicate COUNT queries), and the template embeds project_health_widget. All variables in the audit table above are supplied.</done>
</task>

<task type="auto">
  <name>Task 5: Run smoke crawler + full health test suite, confirm green</name>
  <files>(no file changes — verification gate)</files>
  <action>
    1. Run the Phase 15.1 smoke crawler on the routes test module to confirm the new /ui/sites/{site_id} route returns 200 for the seeded site fixture and no Jinja errors are raised:
       `pytest tests/test_smoke_routes.py -k sites -x` (or whatever the smoke module is named — find it: `ls tests/test_smoke*`).
    2. Run the targeted health unit tests: `pytest tests/test_site_health.py -x`.
    3. If either fails, fix and re-run.
    4. Commit: `feat(18-01): wire project health widget into Site Overview page`
  </action>
  <verify>
    <automated>cd /opt/seo-platform &amp;&amp; pytest tests/test_site_health.py tests/test_smoke_routes.py -x 2>&amp;1 | tail -30</automated>
  </verify>
  <done>Both pytest runs are green. The /ui/sites/{site_id} route is exercised by the smoke crawler with status 200. No Jinja TemplateSyntaxError or UndefinedError on the page.</done>
</task>

</tasks>

<verification>
End-to-end checks:
1. `pytest tests/test_site_health.py -v` — all signal tests pass (PHW-02, PHW-05)
2. `pytest tests/test_smoke_routes.py -k sites -v` — `/ui/sites/{site_id}` returns 200 for seeded fixture (success criterion 7)
3. Manual: open `/ui/sites/{real-site-id}` in browser — see widget with 7 steps, progress N/7, "Следующий шаг" line (PHW-01, PHW-03, PHW-04)
4. Manual: a fully-configured site shows the collapsed success state with "Показать снова" toggle (PHW-06)
5. `grep -n "compute_site_health" app/services/site_service.py` — function exists
6. `grep -n "project_health_widget" app/templates/macros/health.html app/templates/sites/detail.html` — macro defined and used
</verification>

<success_criteria>
- compute_site_health() returns the documented SiteHealth dataclass (with keyword_count/crawl_count/competitor_count fields) and is unit-tested per signal (PHW-02, PHW-05)
- /ui/sites/{site_id} renders the widget on top of the Site Overview page (PHW-01)
- Each pending step shows description + "Сделать сейчас" link to a route that actually exists — competitors links to /ui/competitors/{site_id} (PHW-03)
- N/7 progress bar + "Следующий шаг" line highlight first incomplete step (PHW-04)
- Fully-complete state collapses with "Показать снова" toggle (PHW-06)
- compute_site_health uses ≤ 8 indexed COUNT queries; the route handler issues NO duplicate COUNTs (reuses health.keyword_count / health.crawl_count)
- Smoke crawler hits /ui/sites/{site_id} → 200, no Jinja UndefinedError; existing stats cards (Keywords, Crawls, Connection) still display real values
- Zero new tables, zero migrations, zero new Celery tasks, zero new external API calls
</success_criteria>

<output>
After completion, create `.planning/phases/18-project-health-widget/18-01-SUMMARY.md` documenting:
- The exact SiteHealth dataclass shape (including keyword_count/crawl_count/competitor_count) and the 7 step keys
- Final next_url paths (competitors confirmed at /ui/competitors/{site_id})
- Confirmation that smoke crawler covers the new route
- Template variable audit results and any edge cases discovered (e.g. site with NULL url, OAuthToken model schema)
</output>
