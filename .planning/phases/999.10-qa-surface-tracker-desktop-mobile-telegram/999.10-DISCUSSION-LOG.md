# Phase 999.10: QA Surface Tracker — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-12
**Phase:** 999.10-qa-surface-tracker-desktop-mobile-telegram
**Areas discussed:** Granularity, Surface Matrix, Retest Policy, Registry Population

---

## Granularity

| Option | Description | Selected |
|--------|-------------|----------|
| Route | Each GET endpoint = entry. ~73 desktop + ~38 mobile. Auto from discover_routes() | |
| Screen group | Related routes grouped. ~25-30 entries. Manual grouping | |
| User flow | End-to-end scenario. ~15-20 entries. Fully manual | ✓ |

**User's choice:** User flow (пользовательский сценарий)
**Notes:** Highest-level granularity, focuses on what matters to end users

---

## Surface Matrix

| Option | Description | Selected |
|--------|-------------|----------|
| One record, three status fields | Compact, immediate matrix view. Inflexible if surfaces diverge | |
| Separate records per surface | 3x entries, flexible. Matrix needs JOIN | |
| Parent + child SurfaceCheck | FeatureSurface parent + SurfaceCheck children per surface. Balance of both | ✓ |

**User's choice:** Parent + child SurfaceCheck
**Notes:** Best of both — matrix visible at parent level, each surface independent

---

## Retest Policy & Triggers

| Option | Description | Selected |
|--------|-------------|----------|
| Manual | UI button to mark "needs retest". Depends on discipline | |
| Periodicity | retest_policy interval, Celery Beat marks overdue | ✓ |
| Git trigger | Analyze changed files, map to features | |
| Periodicity + manual | Interval + manual override | |

**User's choice:** Periodicity
**Notes:** Pure time-based via Celery Beat, no git analysis complexity

---

## Registry Population

| Option | Description | Selected |
|--------|-------------|----------|
| Fully manual | Admin creates all entries via UI form | |
| Seed + manual | Hardcoded seed migration + UI edits | |
| Auto-discovery + curation | discover_routes() proposes, admin groups into scenarios | ✓ |

**User's choice:** Auto-discovery + manual curation
**Notes:** discover_routes() provides route candidates, admin groups them into user-flow scenarios

---

## Claude's Discretion

- DB schema design (column types, indexes, constraints)
- UI layout for the registry and matrix view
- Celery Beat task implementation details
- How discover_routes() candidates are presented for grouping

## Deferred Ideas

None — discussion stayed within phase scope
