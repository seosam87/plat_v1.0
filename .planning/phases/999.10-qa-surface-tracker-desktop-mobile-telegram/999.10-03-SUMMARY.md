---
phase: 999.10-qa-surface-tracker-desktop-mobile-telegram
plan: "03"
subsystem: qa-surface-tracker
tags: [celery, redbeat, periodic-task, beat-init]
dependency_graph:
  requires:
    - app/services/qa_surface_service.py (scan_overdue function — from Plan 01)
    - app/celery_app.py (include list + beat_init handler)
  provides:
    - app/tasks/qa_surface_tasks.py (scan_overdue_surfaces task + register_qa_surface_scan_schedule)
  affects:
    - app/celery_app.py (include list + beat_init signal handler)
tech_stack:
  added: []
  patterns:
    - asyncio.new_event_loop() bridge pattern for async service calls from sync Celery tasks
    - RedBeatSchedulerEntry idempotent registration (delete-if-exists, then save)
    - beat_init signal handler for schedule restoration on container restart
key_files:
  created:
    - app/tasks/qa_surface_tasks.py
  modified:
    - app/celery_app.py
decisions:
  - scan_overdue_surfaces uses asyncio.new_event_loop() bridge (consistent with all other async-calling tasks in the project)
  - RedBeat key is "qa-surface-scan-daily" — unique, descriptive, avoids collision
  - Schedule set to crontab(hour=4, minute=0) — 04:00 Moscow time per D-03/D-04 requirements
metrics:
  duration: 2
  completed_date: "2026-04-12"
  tasks: 1
  files: 2
---

# Phase 999.10 Plan 03: QA Surface Celery Beat Task Summary

**One-liner:** Celery Beat task `scan_overdue_surfaces` with RedBeat registration at 04:00 Moscow time, wired into celery_app.py include list and beat_init signal handler.

## What Was Built

**Task 1: Create Celery task + RedBeat registration**

Created `app/tasks/qa_surface_tasks.py` with:
- `scan_overdue_surfaces` Celery task: `bind=True, max_retries=3, queue="default", soft_time_limit=120, time_limit=150`
- Uses `asyncio.new_event_loop()` bridge pattern (consistent with `notification_tasks.py`) to call `scan_overdue()` from the service layer
- `retry(exc=exc, countdown=60)` on failure — 3 attempts with 60-second delays
- `register_qa_surface_scan_schedule()`: idempotent RedBeat registration (deletes existing entry if present, then saves new)
- Schedule: `crontab(hour=4, minute=0)` — 04:00 Europe/Moscow per D-03/D-04

Updated `app/celery_app.py`:
- Added `"app.tasks.qa_surface_tasks"` to the `include=[...]` list (after `agent_tasks`)
- Added `register_qa_surface_scan_schedule()` call in `restore_crawl_schedules()` beat_init handler (after `register_notifications_cleanup_schedule()`)

## Deviations from Plan

None — plan executed exactly as written. All acceptance criteria met.

## Known Stubs

None — task module is fully functional. No placeholder logic.

## Self-Check: PASSED

Files exist:
- app/tasks/qa_surface_tasks.py: FOUND
- app/celery_app.py: FOUND (modified)

Commits exist:
- ac88dfe: feat(999.10-03): create scan_overdue_surfaces Celery task + RedBeat schedule registration
