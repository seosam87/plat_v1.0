---
phase: 15.1-ui-smoke-crawler
plan: 04
type: execute
wave: 3
depends_on: ["15.1-03"]
files_modified:
  - tests/smoke_test.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Existing tests/smoke_test.py becomes a thin __main__ wrapper that delegates to shared helpers"
    - "Standalone invocation `python -m tests.smoke_test` still works for ad-hoc runs"
    - "No duplication of route discovery / param resolution logic between smoke_test.py and _smoke_helpers.py"
  artifacts:
    - path: "tests/smoke_test.py"
      provides: "Standalone CLI runner for ad-hoc smoke checks against in-process or live server"
      contains: "if __name__"
  key_links:
    - from: "tests/smoke_test.py"
      to: "tests/_smoke_helpers.py"
      via: "imports discover_routes, resolve_path, etc."
      pattern: "from tests\\._smoke_helpers"
---

<objective>
Per CONTEXT D-08: evolve, don't rewrite. Convert the existing 396-line `tests/smoke_test.py` into a thin CLI wrapper that reuses helpers from Plan 02. Preserve standalone CLI capability for live-server runs.

Output: Slimmed `tests/smoke_test.py` (~80 lines) that imports helpers and provides argparse CLI.
</objective>

<context>
@.planning/phases/15.1-ui-smoke-crawler/15.1-CONTEXT.md
@tests/smoke_test.py
@tests/_smoke_helpers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Slim smoke_test.py to thin CLI wrapper</name>
  <files>tests/smoke_test.py</files>
  <read_first>
    - tests/smoke_test.py (existing 396 lines — preserve argparse interface, colored output, exit codes)
    - tests/_smoke_helpers.py
  </read_first>
  <action>
    Rewrite `tests/smoke_test.py` (overwrite full file) to:

    1. Keep module docstring with usage examples (`python -m tests.smoke_test`, `--url`, `--email`, `--password`).
    2. Remove `discover_routes_from_nav`, `discover_routes_from_main`, `merge_routes`, `_normalize_template`, `resolve_url`, `SKIP_ROUTES`, `SKIP_PATTERNS`, `FAKE_SITE_ID`, `FAKE_PROJECT_ID` — all replaced by helpers.
    3. Keep `authenticate()` (real login flow for live-server runs only) and `print_report()` (colored output).
    4. New `run_smoke_test(base_url=None)` flow:
       - `from tests._smoke_helpers import discover_routes, build_param_map, resolve_path, assert_no_error_markers, JINJA_ERROR_MARKERS`
       - `from app.main import app`
       - `routes = discover_routes(app)`
       - For in-process runs: query DB for first real Site row to build a runtime PARAM_MAP (mirroring existing `get_first_site_id` function — keep that helper)
       - For live-server runs: call `authenticate()` then loop routes, calling `resolve_path` and capturing status + body marker results
       - Build same `results: list[dict]` shape so `print_report()` keeps working
    5. Keep `if __name__ == "__main__"` block + `asyncio.run(main())`.
    6. Result file should be roughly 100 lines or fewer.
  </action>
  <verify>
    <automated>python -m tests.smoke_test --help</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l tests/smoke_test.py` → < 150 lines
    - `grep -q "from tests\._smoke_helpers import" tests/smoke_test.py`
    - NOT `grep -q "discover_routes_from_nav\|discover_routes_from_main\|merge_routes" tests/smoke_test.py`
    - `python -m tests.smoke_test --help` exits 0
    - `print_report` and `authenticate` functions still present
  </acceptance_criteria>
  <done>Standalone CLI works; helper code is single-sourced; no duplication.</done>
</task>

</tasks>

<verification>
- python -m tests.smoke_test --help works
- File significantly shrunk
- pytest tests/test_ui_smoke.py still passes (untouched)
</verification>

<success_criteria>
One source of truth for route discovery. Standalone CLI preserved per D-08.
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-ui-smoke-crawler/15.1-04-SUMMARY.md`
</output>
