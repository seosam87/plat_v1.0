---
phase: 15.1-ui-smoke-crawler
plan: "05"
subsystem: ci
tags: [ci, github-actions, smoke-tests, docs]
dependency_graph:
  requires: ["15.1-03"]
  provides: ["ci-smoke-gate", "smoke-extension-docs"]
  affects: [".github/workflows/test.yml", "tests/README.md"]
tech_stack:
  added: ["GitHub Actions", "postgres:16 service container"]
  patterns: ["uv pip install --system -r requirements.txt", "httpx ASGI smoke (browser-free)"]
key_files:
  created:
    - .github/workflows/test.yml
    - tests/README.md
  modified: []
decisions:
  - "Use requirements.txt (not pyproject extras, no uv.lock) as CI install source of truth"
  - "Smoke job omits playwright browser install — tests use httpx ASGITransport in-process"
metrics:
  duration_min: 3
  tasks: 2
  files: 2
  completed: 2026-04-07
---

# Phase 15.1 Plan 05: CI Gate & Documentation Summary

Wired the mandatory CI smoke gate (GitHub Actions with Postgres 16 service) and documented how to extend smoke coverage for future UI routes.

## What Was Built

### Task 1 — `.github/workflows/test.yml` (commit 9f8befc)
- New GitHub Actions workflow `tests` triggering on push/PR to `master`.
- Provisions `postgres:16` service container matching production credentials.
- Installs dependencies deterministically via `uv pip install --system -r requirements.txt` (confirmed requirements.txt already contains `pytest`, `pytest-asyncio`, `httpx`, `respx`; no uv.lock exists).
- Runs `pytest -x --tb=short` which includes the UI smoke crawler in the default collection (per D-06).
- Intentionally omits `playwright install` — smoke tests are in-process HTTP via httpx ASGITransport.
- YAML validated with `yaml.safe_load`.

### Task 2 — `tests/README.md` (commit 3a6ad93)
- New "UI Smoke Crawler (Phase 15.1)" section.
- Four-step guide for adding new routes to smoke coverage (known params, new params, opt-out, partials).
- Common failures table includes today's `data.items` Jinja dict-collision as the canonical example.
- Local run commands (pytest + standalone `python -m tests.smoke_test`).

## Deviations from Plan

None — plan executed exactly as written. `requirements.txt` already contains all test dependencies, so the fallback to `pyproject.toml` extras was not needed.

## Verification

- `.github/workflows/test.yml` exists and parses as YAML.
- Contains `postgres:16`, `pytest`, `python-version: "3.12"`, `requirements.txt`.
- Does NOT contain `playwright install` or `uv.lock`.
- `tests/README.md` contains `Adding a new route to smoke coverage`, `PARAM_MAP`, `SMOKE_SKIP`, `data.items`.

## Self-Check: PASSED

- FOUND: .github/workflows/test.yml
- FOUND: tests/README.md
- FOUND: 9f8befc (CI workflow commit)
- FOUND: 3a6ad93 (README commit)
