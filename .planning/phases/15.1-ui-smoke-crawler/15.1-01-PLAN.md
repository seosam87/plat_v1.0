---
phase: 15.1-ui-smoke-crawler
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/__init__.py
  - tests/fixtures/smoke_seed.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Session-scoped smoke seed fixture exists and produces deterministic UUIDs"
    - "Seed creates Site, admin User, 10 Keywords, 5 KeywordPositions, 2 GapKeywords, 1 SuggestJob, 1 CrawlJob, 1 AuditCheckDefinition, 1 AuditResult, 1 ClientReport, ServiceCredential rows"
    - "Seed is rolled back at session end (no test pollution)"
  artifacts:
    - path: "tests/fixtures/smoke_seed.py"
      provides: "smoke_seed pytest fixture and SMOKE_IDS dict of deterministic UUIDs"
      contains: "SMOKE_IDS"
  key_links:
    - from: "tests/fixtures/smoke_seed.py"
      to: "app.models"
      via: "ORM imports — uses real models, not raw SQL"
      pattern: "from app\\.models"
---

<objective>
Build the deterministic "smoke site" seed fixture that downstream smoke crawler tests will use. Per CONTEXT D-01: session-scoped, ORM-based, full related data.

Purpose: Stable UUIDs for URL parameter substitution; migration-safe; one source of truth for what data exists during smoke testing.
Output: tests/fixtures/smoke_seed.py exporting `smoke_seed` fixture + `SMOKE_IDS` constant dict.
</objective>

<context>
@.planning/phases/15.1-ui-smoke-crawler/15.1-CONTEXT.md
@.planning/phases/15.1-ui-smoke-crawler/15.1-RESEARCH.md
@tests/conftest.py
@CLAUDE.md

<interfaces>
Existing reusable fixtures from tests/conftest.py:
- `db_session` — per-test async session with SAVEPOINT isolation; we will request session-scope DB binding via a separate engine bind.

Models to import (verify exact paths during execution via `grep -rn "class Site\|class User\|class Keyword\|class KeywordPosition\|class GapKeyword\|class SuggestJob\|class CrawlJob\|class AuditCheckDefinition\|class AuditResult\|class ClientReport\|class ServiceCredential" app/models/`).

CORRECTION FROM RESEARCH: AuditSnapshot does NOT exist. Use AuditCheckDefinition + AuditResult instead. CONTEXT D-01 mentions AuditSnapshot — that is wrong; honor research correction.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SMOKE_IDS constants and seed fixture skeleton</name>
  <files>tests/fixtures/__init__.py, tests/fixtures/smoke_seed.py</files>
  <read_first>
    - tests/conftest.py (db_session fixture)
    - app/models/ — grep all model classes listed above to confirm import paths and required fields
    - app/auth/* or app/dependencies.py — User model password_hash field name
  </read_first>
  <behavior>
    - SMOKE_IDS["site_id"] == "11111111-1111-1111-1111-111111111111"
    - SMOKE_IDS contains keys: site_id, user_id, keyword_id, gap_keyword_id, suggest_job_id, crawl_job_id, job_id, report_id, audit_id, audit_check_id
    - All values are valid UUID strings, deterministic across runs
    - Importing the module has no side effects (no DB calls)
  </behavior>
  <action>
    Create `tests/fixtures/__init__.py` (empty).

    Create `tests/fixtures/smoke_seed.py` with:
    1. Module docstring referencing CONTEXT D-01.
    2. `SMOKE_IDS` dict (module-level constant) with deterministic UUID strings:
       ```python
       SMOKE_IDS = {
           "site_id":          "11111111-1111-1111-1111-111111111111",
           "user_id":          "22222222-2222-2222-2222-222222222222",
           "keyword_id":       "33333333-3333-3333-3333-333333333333",
           "gap_keyword_id":   "44444444-4444-4444-4444-444444444444",
           "suggest_job_id":   "55555555-5555-5555-5555-555555555555",
           "crawl_job_id":     "66666666-6666-6666-6666-666666666666",
           "job_id":           "55555555-5555-5555-5555-555555555555",  # alias for suggest_job_id (see SMOKE_SKIP for wp_pipeline collision)
           "report_id":        "77777777-7777-7777-7777-777777777777",
           "audit_id":         "88888888-8888-8888-8888-888888888888",
           "audit_check_id":   "99999999-9999-9999-9999-999999999999",
       }
       ```
    3. Empty placeholder `smoke_seed` async fixture (will be populated in Task 2).
    4. Unit test `tests/fixtures/test_smoke_seed_constants.py` (NEW) asserting:
       - `SMOKE_IDS["site_id"] == "11111111-1111-1111-1111-111111111111"`
       - All values parse as `uuid.UUID(v)` without raising
       - All required keys present
  </action>
  <verify>
    <automated>pytest tests/fixtures/test_smoke_seed_constants.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/fixtures/smoke_seed.py` exists; `grep -q "SMOKE_IDS" tests/fixtures/smoke_seed.py`
    - `grep -q "11111111-1111-1111-1111-111111111111" tests/fixtures/smoke_seed.py`
    - `python -c "from tests.fixtures.smoke_seed import SMOKE_IDS; assert len(SMOKE_IDS) >= 10"` exits 0
    - test_smoke_seed_constants.py passes
  </acceptance_criteria>
  <done>SMOKE_IDS constants importable; constants test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement session-scoped smoke_seed fixture creating all rows via ORM</name>
  <files>tests/fixtures/smoke_seed.py</files>
  <read_first>
    - app/models/site.py, app/models/user.py, app/models/keyword.py and ALL other model files for the entities listed in must_haves (use grep first to find paths)
    - tests/conftest.py for engine creation pattern
    - app/auth.py or wherever password hashing lives — admin user must have a valid bcrypt hash
  </read_first>
  <behavior>
    - smoke_seed fixture (scope="session") yields a SeedHandle dataclass with `.ids` (= SMOKE_IDS) and `.session`
    - After fixture setup, DB contains: 1 Site (id=site_id, deterministic name "smoke-site"), 1 admin User (is_admin=True, email "smoke-admin@example.com"), 10 Keywords (mix of engine='yandex'/'google'/None) linked to the Site, 5 KeywordPosition rows, 2 GapKeyword rows with non-null potential_score and frequency, 1 SuggestJob (status='completed', result_count > 0), 1 CrawlJob (status='completed'), 1 AuditCheckDefinition (code='smoke_check'), 1 AuditResult linking to it, 1 ClientReport, 1 ServiceCredential per known service (yandex_direct, gsc) so opt-in gates resolve
    - On fixture teardown, all rows rolled back — no smoke_site row remains
    - Re-running the test session does not collide on UUIDs (because rollback removes them)
  </behavior>
  <action>
    Implement `smoke_seed` as `@pytest_asyncio.fixture(scope="session")` in tests/fixtures/smoke_seed.py:

    1. Build a session-scoped async engine (mirror conftest.py pattern, but session scope). Use `TEST_DATABASE_URL` from conftest (re-import or duplicate the resolution).
    2. `Base.metadata.create_all` with checkfirst=True.
    3. Open one connection + outer transaction; create AsyncSession bound to it with `join_transaction_mode="create_savepoint"`.
    4. Insert all rows listed in <behavior> using ORM model classes. For each entity use `SMOKE_IDS[...]` as primary key. Use `bcrypt`/`passlib` (whichever the codebase already uses — grep first) to hash a known smoke password.
    5. `await session.flush()` then `await session.commit()` is FORBIDDEN — we want rollback. Instead just flush so rows are visible to the same session/connection used by the smoke client.
    6. Yield `SeedHandle(ids=SMOKE_IDS, session=session, connection=conn)`.
    7. On teardown: close session, rollback outer transaction, close connection, dispose engine.

    Add a unit test `tests/fixtures/test_smoke_seed_fixture.py`:
    - Uses `smoke_seed` fixture
    - Asserts `await session.execute(select(Site).where(Site.id == UUID(SMOKE_IDS["site_id"]))).scalar_one()` returns a row
    - Asserts keyword count == 10 for the site
    - Asserts gap keyword count == 2

    IMPORTANT field-name verification: before writing, grep each model for actual column names. Do NOT assume; CLAUDE.md mandates real values.
  </action>
  <verify>
    <automated>pytest tests/fixtures/test_smoke_seed_fixture.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "scope=\"session\"" tests/fixtures/smoke_seed.py`
    - `grep -qE "Site|User|Keyword|GapKeyword|SuggestJob|CrawlJob|AuditCheckDefinition|AuditResult|ClientReport|ServiceCredential" tests/fixtures/smoke_seed.py` matches all 10 model names
    - NOT `grep -q "AuditSnapshot" tests/fixtures/smoke_seed.py` (research correction)
    - test_smoke_seed_fixture.py passes
  </acceptance_criteria>
  <done>smoke_seed fixture creates all required rows; fixture test confirms row counts; session rollback verified via second test invocation seeing no leftover data.</done>
</task>

</tasks>

<verification>
- pytest tests/fixtures/ -x passes
- No AuditSnapshot references anywhere
- SMOKE_IDS importable as plain constant
</verification>

<success_criteria>
Smoke seed fixture is ready for consumption by Plan 03 smoke crawler tests.
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-ui-smoke-crawler/15.1-01-SUMMARY.md`
</output>
