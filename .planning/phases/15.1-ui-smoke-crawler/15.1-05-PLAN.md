---
phase: 15.1-ui-smoke-crawler
plan: 05
type: execute
wave: 3
depends_on: ["15.1-03"]
files_modified:
  - .github/workflows/test.yml
  - tests/README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "GitHub Actions workflow runs pytest on push/PR and blocks merge on smoke failure"
    - "Workflow provisions a Postgres 16 service container matching production"
    - "Install step is deterministic — uses the actual dependency files present in the repo (pyproject.toml + requirements.txt, no uv.lock)"
    - "Smoke job does NOT install Playwright browsers (smoke tests do not use browsers)"
    - "tests/README.md documents how to add a new route to smoke coverage (PARAM_MAP / SMOKE_SKIP)"
  artifacts:
    - path: ".github/workflows/test.yml"
      provides: "CI gate running pytest with Postgres service"
      contains: "pytest"
    - path: "tests/README.md"
      provides: "How to add a new route to smoke coverage section"
      contains: "Adding a new route to smoke coverage"
  key_links:
    - from: ".github/workflows/test.yml"
      to: "tests/test_ui_smoke.py"
      via: "pytest invocation in default test job"
      pattern: "pytest"
---

<objective>
Per CONTEXT D-06: smoke gate must be mandatory and unskippable. Create the GitHub Actions workflow (none exists today per RESEARCH) and document how to extend smoke coverage.

Output: .github/workflows/test.yml + tests/README.md.
</objective>

<context>
@.planning/phases/15.1-ui-smoke-crawler/15.1-CONTEXT.md
@.planning/phases/15.1-ui-smoke-crawler/15.1-RESEARCH.md
@CLAUDE.md
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions test workflow with Postgres service</name>
  <files>.github/workflows/test.yml</files>
  <read_first>
    - docker-compose.yml — Postgres version, env vars, DB name
    - CLAUDE.md — Python version (3.12), test stack
    - pyproject.toml — confirmed present at repo root
    - requirements.txt — confirmed present at repo root
    - (uv.lock is NOT present — do not reference it)
  </read_first>
  <action>
    Create `.github/workflows/test.yml` with a deterministic install step.

    Decision (documented here, not deferred to executor):
    - Repo has BOTH `pyproject.toml` and `requirements.txt`, NO `uv.lock`. Use `requirements.txt` as the source of truth for CI installs since it's the explicit pinned list.
    - Smoke tests do NOT use browsers (HTTP-level only via httpx ASGI). Therefore `playwright install chromium` is NOT included in this workflow. If a future test module needs browsers, it adds the install step in a separate job.

    ```yaml
    name: tests
    on:
      push:
        branches: [master]
      pull_request:
        branches: [master]

    jobs:
      pytest:
        runs-on: ubuntu-latest
        services:
          postgres:
            image: postgres:16
            env:
              POSTGRES_USER: seo_user
              POSTGRES_PASSWORD: changeme
              POSTGRES_DB: seo_platform_test
            ports:
              - 5432:5432
            options: >-
              --health-cmd pg_isready
              --health-interval 10s
              --health-timeout 5s
              --health-retries 5
        env:
          DATABASE_URL: postgresql+asyncpg://seo_user:changeme@localhost:5432/seo_platform_test
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-python@v5
            with:
              python-version: "3.12"
          - name: Install uv
            run: pip install uv
          - name: Install deps from requirements.txt
            run: uv pip install --system -r requirements.txt
          # NOTE: playwright browser install intentionally omitted — smoke tests
          # use httpx ASGITransport in-process and do not launch a browser.
          - name: Run pytest (includes UI smoke crawler)
            run: pytest -x --tb=short
    ```

    Before committing, verify `requirements.txt` actually contains the test dependencies (pytest, pytest-asyncio, httpx). If it does not, fall back to `uv pip install --system -e .[test]` based on whether `pyproject.toml` declares an optional `test` extra — read `pyproject.toml` first to confirm.
  </action>
  <verify>
    <automated>python -c "import yaml; yaml.safe_load(open('.github/workflows/test.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/test.yml` exists
    - `grep -q "postgres:16" .github/workflows/test.yml`
    - `grep -q "pytest" .github/workflows/test.yml`
    - `grep -q "python-version.*3.12" .github/workflows/test.yml`
    - `grep -q "requirements.txt" .github/workflows/test.yml`
    - NOT `grep -q "playwright install" .github/workflows/test.yml` (smoke job stays browser-free)
    - NOT `grep -q "uv.lock" .github/workflows/test.yml` (file does not exist in repo)
    - YAML parses without error
  </acceptance_criteria>
  <done>CI workflow file present and valid; install step deterministic; smoke job runs in default pytest invocation.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/README.md "Adding a new route to smoke coverage" section</name>
  <files>tests/README.md</files>
  <read_first>
    - tests/_smoke_helpers.py
    - tests/fixtures/smoke_seed.py
    - tests/test_ui_smoke.py
  </read_first>
  <action>
    Create or append `tests/README.md` with:

    ```markdown
    # Tests

    ## UI Smoke Crawler (Phase 15.1)

    `tests/test_ui_smoke.py` parametrizes over every UI GET route discovered from `app.routes` and asserts each renders without Jinja errors. The smoke gate runs in default `pytest` invocation and blocks CI merge.

    ### Adding a new route to smoke coverage

    A new UI GET route is automatically picked up — no test code changes needed — IF its path parameters are already in `PARAM_MAP`.

    1. **Path uses only known params** (`site_id`, `keyword_id`, `module`, etc.): nothing to do. Run `pytest tests/test_ui_smoke.py -k <your_route>` to confirm.

    2. **Path uses a NEW param name** (e.g. `{cluster_id}`):
       - Add the entity to `tests/fixtures/smoke_seed.py` (create one row in the seed)
       - Add the UUID to `SMOKE_IDS` dict
       - The helper's `build_param_map` will pick it up automatically

    3. **Route cannot be smoke-tested** (file download, external redirect, websocket, destructive side-effect):
       - Add path to `SMOKE_SKIP` dict in `tests/_smoke_helpers.py` with a reason comment

    4. **Route is a partial** (path contains `/tabs/`, `/partials/`, `/detail/`):
       - Picked up automatically; structural HTML check is relaxed (no `<title>` requirement)

    ### Common failures and fixes

    | Failure | Cause | Fix |
    |---|---|---|
    | `UnknownParamError: Unknown path param 'foo'` | New param name | Add to PARAM_MAP via SMOKE_IDS |
    | `response body contains error marker 'builtin_function_or_method'` | Jinja attr/key collision (e.g. `data.items` resolved to dict.items method) | Rename context var or use `data['items']` |
    | `response body contains error marker 'UndefinedError'` | Template references undefined variable | Add var to route context |
    | `full page missing <title>` | Template not extending base layout | Use `{% extends "base.html" %}` |

    ### Running locally

    ```bash
    pytest tests/test_ui_smoke.py -x          # full smoke suite
    pytest tests/test_ui_smoke.py -k opp     # only opportunities routes
    python -m tests.smoke_test                # standalone CLI (in-process)
    python -m tests.smoke_test --url http://localhost:8000  # against live server
    ```
    ```
  </action>
  <verify>
    <automated>test -f tests/README.md && grep -q "Adding a new route to smoke coverage" tests/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Adding a new route to smoke coverage" tests/README.md`
    - `grep -q "PARAM_MAP" tests/README.md`
    - `grep -q "SMOKE_SKIP" tests/README.md`
    - `grep -q "data.items" tests/README.md`
  </acceptance_criteria>
  <done>Documentation explains exactly how to extend smoke coverage; today's bug pattern documented as canonical example.</done>
</task>

</tasks>

<verification>
- .github/workflows/test.yml exists and parses
- tests/README.md exists with required section
- All 8 CONTEXT success criteria checkboxes covered across plans 01-05
</verification>

<success_criteria>
CI gate live; documentation in place; phase 15.1 complete and unskippable.
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-ui-smoke-crawler/15.1-05-SUMMARY.md`
</output>
