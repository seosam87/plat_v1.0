---
phase: 999.3-smart-route-discovery-htmlresponse-filter
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_smoke_helpers.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "stub_app fixture covers all 13 UI_PREFIXES including /profile/ and /notifications"
    - "pytest tests/test_smoke_helpers.py passes 100% (zero failures)"
    - "All parametrized prefix coverage tests green"
  artifacts:
    - path: "tests/test_smoke_helpers.py"
      provides: "Complete stub_app fixture with routes for every UI_PREFIX"
      contains: "/profile/"
  key_links:
    - from: "tests/test_smoke_helpers.py::stub_app"
      to: "tests/_smoke_helpers.py::UI_PREFIXES"
      via: "parametrized prefix coverage test"
      pattern: "UI_PREFIXES"
---

<objective>
Fix 2 failing tests in `test_smoke_helpers.py` — the `stub_app` fixture is missing routes for `/profile/` and `/notifications` prefixes that were added to `UI_PREFIXES` after the stub was created. The parametrized `test_discover_routes_covers_every_ui_prefix` test fails for these 2 prefixes.

Purpose: Complete Phase 999.3 by making the full test suite green. The core filter logic (Plan 01) is already implemented and working; this is a stub fixture gap.
Output: Updated `test_smoke_helpers.py` with 2 additional stub routes, all 42+ tests passing.
</objective>

<execution_context>
@/opt/seo-platform/.claude/get-shit-done/workflows/execute-plan.md
@/opt/seo-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.3-smart-route-discovery-htmlresponse-filter/999.3-CONTEXT.md
@tests/_smoke_helpers.py
@tests/test_smoke_helpers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing /profile/ and /notifications stub routes to test fixture</name>
  <files>tests/test_smoke_helpers.py</files>
  <read_first>tests/test_smoke_helpers.py, tests/_smoke_helpers.py</read_first>
  <action>
In `tests/test_smoke_helpers.py`, inside the `stub_app` fixture function (after the existing `/metrika/{site_id}` route at approx line 72), add two new stub routes:

1. A `/profile/` route with `response_class=HTMLResponse`:
```python
@app.get("/profile/", response_class=HTMLResponse)
async def ui_profile() -> str:  # pragma: no cover
    return "ok"
```

2. A `/notifications` route (note: NO trailing slash — matches the UI_PREFIXES entry exactly) with `response_class=HTMLResponse`:
```python
@app.get("/notifications", response_class=HTMLResponse)
async def ui_notifications() -> str:  # pragma: no cover
    return "ok"
```

These follow the exact same pattern as all other stub routes in the fixture. Both use `response_class=HTMLResponse` to be classified as HTML by the tier-1 filter.

After adding, update the assertion in `test_discover_routes_returns_routespecs` if the count check `assert len(routes) >= 5` needs adjustment (it should still pass since 5 is a minimum, but verify).
  </action>
  <verify>
    <automated>cd /opt/seo-platform && python -m pytest tests/test_smoke_helpers.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- grep -c "/profile/" tests/test_smoke_helpers.py returns at least 1
- grep -c "/notifications" tests/test_smoke_helpers.py returns at least 1 (inside stub_app)
- pytest tests/test_smoke_helpers.py exits with 0 (all tests pass)
- pytest tests/test_smoke_helpers.py -k "prefix" exits with 0 (all 13 prefix tests pass)
  </acceptance_criteria>
  <done>All tests in test_smoke_helpers.py pass including the 2 previously-failing prefix coverage tests for /profile/ and /notifications</done>
</task>

</tasks>

<verification>
- `pytest tests/test_smoke_helpers.py -x` — all tests green (0 failures)
- `pytest tests/test_smoke_helpers.py -k "prefix"` — all 13 parametrized prefix tests green
- No changes to `tests/_smoke_helpers.py` (the production helper is already correct)
- No changes to `tests/test_ui_smoke.py` (downstream consumer unchanged per D-03)
</verification>

<success_criteria>
- pytest tests/test_smoke_helpers.py: 42+ passed, 0 failed
- All 13 UI_PREFIXES have matching stub routes in the test fixture
- Phase 999.3 is fully green with no remaining test failures
</success_criteria>

<output>
After completion, create `.planning/phases/999.3-smart-route-discovery-htmlresponse-filter/999.3-02-SUMMARY.md`
</output>
