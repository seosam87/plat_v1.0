# Phase 999.9: Prompt Library — каркас AI-агентов — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-12
**Phase:** 999.9-prompt-library-ai-agents-skeleton
**Areas discussed:** Структура промптов, Связь с ExpertSource, Интерфейс использования, Каталог и навигация

---

## Структура промптов

| Option | Description | Selected |
|--------|-------------|----------|
| Свободный текст с `{{переменными}}` | Простое текстовое поле, переменные через двойные фигурные скобки, платформа парсит и строит форму | ✓ |
| Структурированная схема (system/user/assistant) | JSON с отдельными полями, ближе к Anthropic API | |
| Jinja2 шаблоны | Полноценный Jinja2 с условиями и циклами | |

**User's choice:** Свободный текст с `{{переменными}}`
**Notes:** Без автоверсионирования. Пользователь вручную создаёт копию (форк) хорошей версии промпта.

---

## Связь с ExpertSource

| Option | Description | Selected |
|--------|-------------|----------|
| Переиспользовать ExpertSource | Тот же ExpertSource что и PlaybookBlock, один каталог экспертов | |
| Отдельная сущность "AI-агент" | Новая модель, заточенная под промпты, свой каталог | ✓ |
| ExpertSource + расширение | Тот же ExpertSource с дополнительными AI-полями | |

**User's choice:** Отдельная сущность "AI-агент"
**Notes:** Пользователь попросил объяснить концепцию AI-агентов. После объяснения на SEO-примерах выбрал максимально расширенную структуру полей: основные (название, описание, иконка, system prompt, шаблон, категория), LLM-настройки (модель, температура, max_tokens, output format), мета (автор, дата, теги, статистика, видимость, форк-ссылка).

---

## Интерфейс использования

| Option | Description | Selected |
|--------|-------------|----------|
| Realtime (streaming) | Текст появляется посимвольно, как в ChatGPT | |
| Celery-задача с поллингом | Отправка задачи, HTMX поллит статус, результат когда готово | ✓ |
| Копирование промпта | Платформа подставляет переменные, пользователь копирует текст | |

**User's choice:** Celery-задача с поллингом
**Notes:** Консистентно с существующим паттерном LLM briefs.

---

## Каталог и навигация

| Option | Description | Selected |
|--------|-------------|----------|
| По категориям | Древовидная навигация в сайдбаре | |
| По категориям + поиск + избранное | Категории + текстовый поиск + звёздочка избранного | ✓ |
| Плоский список с фильтрами и тегами | Все агенты в одном списке, фильтры по тегам/категориям/автору | |

**User's choice:** По категориям + поиск + избранное

---

## Claude's Discretion

- Конкретные имена таблиц и структура миграций
- Детали HTMX-поллинга и UI-компонентов
- Механизм парсинга `{{переменных}}` из шаблона

## Deferred Ideas

None — discussion stayed within phase scope
