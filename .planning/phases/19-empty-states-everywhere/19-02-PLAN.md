---
phase: 19-empty-states-everywhere
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - app/templates/metrika/index.html
  - app/templates/traffic_analysis/index.html
  - app/templates/analytics/partials/opportunities_gaps.html
  - app/templates/analytics/partials/dead_content_table.html
  - app/templates/analytics/partials/quick_wins_table.html
  - app/templates/pipeline/jobs.html
  - app/templates/client_reports/partials/history_table.html
  - app/templates/keyword_suggest/index.html
autonomous: true
requirements: [EMP-03, EMP-04, EMP-05]

must_haves:
  truths:
    - "Metrika page shows structured empty state for both no-counter and no-data conditions"
    - "Traffic Analysis page shows structured empty state when no analyses exist"
    - "Growth Opportunities (gaps tab) shows structured empty state when no gap data"
    - "Dead Content page shows structured empty state when no dead pages found"
    - "Quick Wins page shows structured empty state when no quick win data (SVG icon removed per D-02)"
    - "Content Pipeline page shows structured empty state when no jobs exist (SVG icon removed per D-02)"
    - "Client Reports history shows structured empty state when no reports generated"
    - "Keyword Suggest page shows structured empty state when no suggest jobs"
  artifacts:
    - path: "app/templates/metrika/index.html"
      provides: "Empty state for Metrika (two conditions)"
      contains: "from \"macros/empty_state.html\" import empty_state"
    - path: "app/templates/traffic_analysis/index.html"
      provides: "Empty state for traffic analysis"
      contains: "from \"macros/empty_state.html\" import empty_state"
    - path: "app/templates/analytics/partials/opportunities_gaps.html"
      provides: "Empty state for growth opportunities gaps"
      contains: "from \"macros/empty_state.html\" import empty_state"
    - path: "app/templates/analytics/partials/dead_content_table.html"
      provides: "Empty state for dead content"
      contains: "from \"macros/empty_state.html\" import empty_state"
    - path: "app/templates/analytics/partials/quick_wins_table.html"
      provides: "Empty state for quick wins"
      contains: "from \"macros/empty_state.html\" import empty_state"
    - path: "app/templates/pipeline/jobs.html"
      provides: "Empty state for content pipeline"
      contains: "from \"macros/empty_state.html\" import empty_state"
    - path: "app/templates/client_reports/partials/history_table.html"
      provides: "Empty state for client reports history"
      contains: "from \"macros/empty_state.html\" import empty_state"
    - path: "app/templates/keyword_suggest/index.html"
      provides: "Empty state for keyword suggest"
      contains: "from \"macros/empty_state.html\" import empty_state"
  key_links:
    - from: "app/templates/analytics/partials/dead_content_table.html"
      to: "app/templates/macros/empty_state.html"
      via: "{% from %} import in partial (not parent)"
      pattern: "from \"macros/empty_state.html\" import empty_state"
    - from: "app/templates/analytics/partials/quick_wins_table.html"
      to: "app/templates/macros/empty_state.html"
      via: "{% from %} import in partial (not parent)"
      pattern: "from \"macros/empty_state.html\" import empty_state"
---

<objective>
Apply the empty_state macro to all analytics and content pages: Metrika (two conditions), Traffic Analysis, Growth Opportunities, Dead Content, Quick Wins, Content Pipeline, Client Reports history, and Keyword Suggest.

Purpose: Cover EMP-03 (analytics pages) and EMP-04 (content pages) with contextual empty states, migrating existing ad-hoc markup to the reusable macro. Each empty state explains why there is no data and gives a direct CTA (EMP-05).
Output: 8 migrated templates.
</objective>

<execution_context>
@/opt/seo-platform/.claude/get-shit-done/workflows/execute-plan.md
@/opt/seo-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-empty-states-everywhere/19-CONTEXT.md
@.planning/phases/19-empty-states-everywhere/19-RESEARCH.md
@.planning/phases/19-empty-states-everywhere/19-01-SUMMARY.md

<interfaces>
<!-- Macro created in Plan 01 — this is the contract -->
From app/templates/macros/empty_state.html:
```jinja2
{% macro empty_state(reason, cta_label, cta_url,
                     secondary_label=none, secondary_url=none,
                     docs_url=none) %}
  {# Card with reason, collapsible how-to via caller(), CTA buttons #}
{% endmacro %}
```

Usage pattern:
```jinja2
{% from "macros/empty_state.html" import empty_state %}
{% call empty_state(reason="...", cta_label="...", cta_url="...") %}
  <p>How-to content here</p>
{% endcall %}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply empty_state to analytics pages (Metrika, Traffic, Opportunities, Dead Content, Quick Wins)</name>
  <files>
    app/templates/metrika/index.html
    app/templates/traffic_analysis/index.html
    app/templates/analytics/partials/opportunities_gaps.html
    app/templates/analytics/partials/dead_content_table.html
    app/templates/analytics/partials/quick_wins_table.html
  </files>
  <read_first>
    - app/templates/macros/empty_state.html
    - app/templates/metrika/index.html
    - app/templates/traffic_analysis/index.html
    - app/templates/analytics/partials/opportunities_gaps.html
    - app/templates/analytics/partials/dead_content_table.html
    - app/templates/analytics/partials/quick_wins_table.html
  </read_first>
  <action>
For each template, add `{% from "macros/empty_state.html" import empty_state %}` near the top, then replace the existing ad-hoc empty state markup. IMPORTANT: For partials, the import goes at the top of the PARTIAL file (Pitfall 2 from research — partials render independently).

**1. metrika/index.html** — Has TWO separate empty conditions. Replace both:

Condition 1 — no counter (around line 32, `{% if not site.metrika_counter_id %}`):
```jinja2
{% call empty_state(
    reason="Счётчик Яндекс.Метрики не настроен.",
    cta_label="Настроить счётчик",
    cta_url="/ui/sites/" ~ site.id ~ "/edit"
) %}
  <p>1. Перейдите в настройки сайта и укажите ID счётчика Метрики.</p>
  <p>2. Также добавьте OAuth-токен Метрики в разделе «Источники данных».</p>
  <p>3. После настройки данные начнут загружаться автоматически.</p>
{% endcall %}
```

Condition 2 — counter set but no data (around line 40, `{% elif not daily_data %}`):
```jinja2
{% call empty_state(
    reason="Данные из Метрики ещё не загружены.",
    cta_label="Обновить данные",
    cta_url="/metrika/" ~ site.id
) %}
  <p>Счётчик настроен. Данные загружаются автоматически по расписанию.</p>
  <p>Первая загрузка может занять несколько минут.</p>
{% endcall %}
```

**2. traffic_analysis/index.html** (replace `<p class="text-gray-500 text-center py-4">Нет анализов...` around line 88):
```jinja2
{% call empty_state(
    reason="Анализы трафика ещё не проводились.",
    cta_label="Загрузить данные",
    cta_url="/traffic-analysis/" ~ site.id
) %}
  <p><strong>Предусловия:</strong> настроена Яндекс.Метрика или Google Search Console.</p>
  <p>1. Используйте кнопки выше для загрузки и анализа данных о трафике.</p>
  <p>2. Система покажет динамику посещений, источники и проблемные страницы.</p>
{% endcall %}
```

**3. analytics/partials/opportunities_gaps.html** (replace `<div>...Нет данных по gap-ключам...` around line 62):
```jinja2
{% call empty_state(
    reason="Нет данных по gap-ключам для анализа возможностей.",
    cta_label="Перейти к Gap Analysis",
    cta_url="/gap/" ~ site.id
) %}
  <p>1. Загрузите gap-ключи в разделе Gap Analysis.</p>
  <p>2. После загрузки здесь появятся возможности роста по ключам конкурентов.</p>
{% endcall %}
```

**4. analytics/partials/dead_content_table.html** (replace `<div class="text-center py-16"><p>Мёртвых страниц не найдено...` around line 82):
```jinja2
{% call empty_state(
    reason="Мёртвые страницы не обнаружены.",
    cta_label="Запустить краул",
    cta_url="/ui/sites/" ~ site.id ~ "/crawls",
    secondary_label="Проверить позиции",
    secondary_url="/ui/positions/" ~ site.id
) %}
  <p><strong>Предусловия:</strong> нужны данные краула и проверки позиций.</p>
  <p>Мёртвый контент — страницы без трафика и с плохими позициями.</p>
  <p>Если краул и позиции собраны, а этот раздел пуст — значит мёртвых страниц нет (это хорошо!).</p>
{% endcall %}
```

**5. analytics/partials/quick_wins_table.html** (replace the SVG icon + h3 + p block around line 92-102 — REMOVE the SVG icon per D-02):
```jinja2
{% call empty_state(
    reason="Quick Wins пока не найдены.",
    cta_label="Проверить позиции",
    cta_url="/ui/positions/" ~ site.id
) %}
  <p><strong>Предусловия:</strong> нужны данные проверки позиций.</p>
  <p>Quick Wins — ключевые слова на позициях 4–20, которые можно поднять в ТОП-3 с минимальными усилиями.</p>
  <p>Запустите проверку позиций, и система автоматически найдёт такие возможности.</p>
{% endcall %}
```

IMPORTANT: Preserve ALL existing template structure outside the replaced empty-state blocks.
  </action>
  <verify>
    <automated>grep -l "from \"macros/empty_state.html\" import empty_state" app/templates/metrika/index.html app/templates/traffic_analysis/index.html app/templates/analytics/partials/opportunities_gaps.html app/templates/analytics/partials/dead_content_table.html app/templates/analytics/partials/quick_wins_table.html | wc -l | grep -q "5" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 template files contain `{% from "macros/empty_state.html" import empty_state %}`
    - metrika/index.html has TWO `{% call empty_state %}` blocks (no-counter and no-data)
    - metrika no-counter reason contains "Счётчик Яндекс.Метрики не настроен"
    - metrika no-data reason contains "Данные из Метрики ещё не загружены"
    - traffic_analysis/index.html: reason contains "Анализы трафика"
    - opportunities_gaps.html: reason contains "gap-ключам"
    - dead_content_table.html: reason contains "Мёртвые страницы не обнаружены"
    - quick_wins_table.html: reason contains "Quick Wins пока не найдены"
    - NO SVG elements remain in the quick_wins_table.html empty state block (D-02)
    - NO SVG elements in any new empty state block across all 5 files
    - Partials (opportunities_gaps, dead_content_table, quick_wins_table) have the import at their own top, not relying on parent template
  </acceptance_criteria>
  <done>All 5 analytics templates migrated to empty_state macro with Russian reason text, how-to content, and appropriate CTAs. SVG icons removed per D-02.</done>
</task>

<task type="auto">
  <name>Task 2: Apply empty_state to content pages (Pipeline, Client Reports, Keyword Suggest)</name>
  <files>
    app/templates/pipeline/jobs.html
    app/templates/client_reports/partials/history_table.html
    app/templates/keyword_suggest/index.html
  </files>
  <read_first>
    - app/templates/macros/empty_state.html
    - app/templates/pipeline/jobs.html
    - app/templates/client_reports/partials/history_table.html
    - app/templates/keyword_suggest/index.html
  </read_first>
  <action>
For each template, add the import and replace existing ad-hoc empty states.

**1. pipeline/jobs.html** (replace the SVG + text block at lines 80-95 inside `{% else %}` branch — REMOVE the SVG icon per D-02):
```jinja2
{% call empty_state(
    reason="Задач контент-пайплайна ещё нет.",
    cta_label="Запустить Batch",
    cta_url="/ui/pipeline/" ~ site_id
) %}
  <p><strong>Предусловия:</strong> сайт должен быть прокраулен (страницы обнаружены).</p>
  <p>1. Убедитесь, что краул выполнен (раздел «Краулинг»).</p>
  <p>2. Нажмите «Run Batch» — система найдёт страницы без TOC, schema.org или внутренних ссылок.</p>
  <p>3. Проверьте и одобрите предложенные изменения перед публикацией в WordPress.</p>
{% endcall %}
```

**2. client_reports/partials/history_table.html** (replace the `<div class="text-center py-10">` block at lines 89-92 in the `{% else %}` branch — this is a PARTIAL, import goes at its top):
```jinja2
{% call empty_state(
    reason="Отчётов пока нет.",
    cta_label="Сгенерировать PDF",
    cta_url="/ui/client-reports/"
) %}
  <p>1. Выберите сайт и блоки отчёта в форме выше.</p>
  <p>2. Нажмите «Сгенерировать PDF» — отчёт появится в этом списке.</p>
  <p>3. Готовый PDF можно скачать, отправить на email или в Telegram.</p>
{% endcall %}
```

**3. keyword_suggest/index.html** — This page has a form but may lack an empty state for when there are no suggest jobs/results. Read the full template to find the empty condition. If there is an existing empty state block in the results area, replace it. If there is no explicit empty state, add one in the results section (likely after the form, where results would appear) wrapped in an appropriate condition. Use:
```jinja2
{% call empty_state(
    reason="Подсказки ещё не собирались.",
    cta_label="Запустить сбор подсказок",
    cta_url="/ui/keyword-suggest"
) %}
  <p>1. Введите базовые ключевые слова в форму выше.</p>
  <p>2. Система соберёт поисковые подсказки из Яндекс (и опционально Google).</p>
  <p>3. Результаты можно экспортировать или добавить в проект.</p>
{% endcall %}
```

IMPORTANT: For keyword_suggest, read the FULL template first to understand the layout before making changes. The suggest page may use HTMX polling for results — do not break the polling mechanism.
  </action>
  <verify>
    <automated>grep -l "from \"macros/empty_state.html\" import empty_state" app/templates/pipeline/jobs.html app/templates/client_reports/partials/history_table.html app/templates/keyword_suggest/index.html | wc -l | grep -q "3" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 template files contain `{% from "macros/empty_state.html" import empty_state %}`
    - pipeline/jobs.html: reason contains "контент-пайплайна", NO SVG in the empty state block
    - client_reports/partials/history_table.html: reason contains "Отчётов пока нет", import is at top of PARTIAL file
    - keyword_suggest/index.html: has empty_state macro call with reason about подсказки
    - No old ad-hoc empty state text remains in replaced sections
    - HTMX attributes (hx-get, hx-trigger, hx-swap) in keyword_suggest are preserved
  </acceptance_criteria>
  <done>All 3 content templates migrated to empty_state macro with Russian text and CTAs. Pipeline SVG icon removed. Client reports partial has its own import. Keyword suggest HTMX polling preserved.</done>
</task>

</tasks>

<verification>
1. `grep -r "from \"macros/empty_state.html\" import empty_state" app/templates/ | wc -l` returns 15 total (7 from Plan 01 + 8 from Plan 02)
2. No SVG icons remain in any empty state `{% else %}` blocks that were modified
3. All partial files (opportunities_gaps, dead_content_table, quick_wins_table, history_table) have imports at their own top
</verification>

<success_criteria>
- All 8 analytics/content templates migrated to the empty_state macro
- Metrika has two separate empty states (no counter vs no data)
- SVG icons removed from quick_wins and pipeline empty states per D-02
- Partials have their own imports (not relying on parent)
- Russian text throughout, matching existing UI language
- Each empty state has reason + how-to + CTA (EMP-05)
</success_criteria>

<output>
After completion, create `.planning/phases/19-empty-states-everywhere/19-02-SUMMARY.md`
</output>
