---
phase: 19.2-interactive-tour-player
verified: 2026-04-09T07:05:00Z
status: passed
score: 5/5 must-haves verified
re_verification: false
---

# Phase 19.2: Interactive Tour Player — Verification Report

**Phase Goal:** Frontend overlay (`app/static/js/tour.js` — Shepherd.js via CDN) that highlights elements with tooltip/next/prev controls. Consumes the same `scenarios/*.yaml` files from Phase 19.1 to auto-generate user onboarding tours. Admin-only "Show tour" button on each page. Step types: `highlight` + `say` + `wait_for_click`. One source of truth for tests and tours.
**Verified:** 2026-04-09T07:05:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Shepherd.js CSS + JS + tour.js load from base.html only when user is admin | VERIFIED | `{% if is_admin %}` block at base.html lines 13–18 wraps shepherd.css, shepherd.min.js, and `/static/js/tour.js defer` — the block closes at line 18 with `{% endif %}` |
| 2 | On admin page load, tour.js fetches `/api/tours/?page=<pathname>` and renders a floating "Show tour" button if list is non-empty | VERIFIED | `bootstrap()` (lines 203–229) fetches `/api/tours/?page=` + `encodeURIComponent(location.pathname)` and calls `renderShowTourButton(tours)` when array is non-empty |
| 3 | Clicking the button fetches `/api/tours/{name}` and starts a Shepherd.Tour | VERIFIED | `startTour(name)` (lines 153–166) calls `fetch('/api/tours/' + encodeURIComponent(name))`, parses JSON, calls `buildTour().start()` |
| 4 | `say` / `highlight` / `wait_for_click` / `open` ops execute; click / fill / wait_for / expect_* ops log a warning and skip | VERIFIED | `buildTour()` (lines 65–148) handles all four supported ops with explicit return; default branch calls `console.warn('[tour] skipping test-only op:', op)` with no step added |
| 5 | One source of truth: `scenarios/*.yaml` used by both the 19.1 test runner and the 19.2 tour API | VERIFIED | `app/services/scenario_schema.py` defines the shared `Scenario` / `TourMeta` Pydantic schema; `app/routers/tours.py` imports it; `tests/fixtures/scenario_runner/collector.py` line 34 skips files where `scenario.tour is not None`, making tour YAML invisible to the pytest runner |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/static/js/tour.js` | Shepherd-based tour player + bootstrap, min 180 lines | VERIFIED | 238 lines, valid JS syntax (`node --check` passes), contains `Shepherd.Tour`, `RESUME_KEY`, `resolveSelector`, `buildTour`, `startTour`, `renderShowTourButton`, `bootstrap` |
| `app/templates/base.html` | Admin-gated Shepherd CDN include + tour.js script tag | VERIFIED | Contains `shepherd.js@14.5.1` (CSS + JS), `/static/js/tour.js defer`, all inside `{% if is_admin %}` block |
| `app/routers/tours.py` | FastAPI router with `/api/tours/` + `/api/tours/{name}` | VERIFIED | `APIRouter(prefix="/api/tours")`, two admin-gated endpoints, `lru_cache` loader, SCENARIOS_DIR computed via `parents[2]` |
| `app/main.py` | tours router included | VERIFIED | `from app.routers.tours import router as tours_router` at line 167; `app.include_router(tours_router)` at line 168 |
| `tests/test_tours_api.py` | httpx AsyncClient test coverage for list + detail + auth | VERIFIED | 3 tests all pass: `test_list_tours_empty_or_ok`, `test_list_tours_forbidden_for_non_admin`, `test_get_tour_not_found` |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `app/templates/base.html` | `/static/js/tour.js` | `script src` inside `{% if is_admin %}` block | WIRED | Lines 13–18 of base.html; `is_admin` passed by `app/template_engine.py` line 107 from JWT role check |
| `app/static/js/tour.js` | `/api/tours/` | `fetch` call in `bootstrap()` | WIRED | Line 221: `fetch('/api/tours/?page=' + encodeURIComponent(location.pathname))` |
| `app/static/js/tour.js` | `/api/tours/{name}` | `fetch` call in `startTour()` | WIRED | Line 155: `fetch('/api/tours/' + encodeURIComponent(name))` |
| `app/static/js/tour.js` | `sessionStorage` | `RESUME_KEY` persist/restore | WIRED | 6 references: `getItem` in bootstrap (line 205), `setItem` in `open` handler (lines 124–127), `removeItem` in complete/cancel handlers (lines 141–145), `removeItem` on parse error (line 215) |
| `app/routers/tours.py` | `app/services/scenario_schema.py` | `Scenario` import | WIRED | Line 18: `from app.services.scenario_schema import Scenario`; used in `_load_all_tours` for `Scenario.model_validate(raw)` |
| `app/routers/tours.py` | `scenarios/` directory | `SCENARIOS_DIR.glob("*.yaml")` | WIRED | Line 23 computes path; line 36 globs `*.yaml`; `_load_all_tours()` confirmed to find the directory correctly |
| `app/routers/tours.py` | `app/auth/dependencies.py` | `Depends(require_admin)` | WIRED | Both endpoints use `_user: User = Depends(require_admin)` — count = 2 |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `app/routers/tours.py` list endpoint | `_load_all_tours()` | `scenarios/*.yaml` files via `yaml.safe_load` + `Scenario.model_validate` | Yes — reads filesystem; returns `[]` when no tour-block YAMLs exist (correct, no tour YAML has been added yet in Plan 05) | FLOWING |
| `app/static/js/tour.js` button renderer | `tours` array from fetch | `/api/tours/?page=` response | Yes — real API response; button only rendered when array non-empty | FLOWING |

Note: no tour-block YAML files exist yet (`scenarios/01-suggest-to-results.yaml` and `02-site-form-submit.yaml` both have `tour: None`). The loader correctly returns `[]` — the button simply will not appear until Plan 05 ships a tour YAML file. This is expected behavior, not a defect.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `tour.js` valid JS syntax | `node --check app/static/js/tour.js` | exit 0 | PASS |
| `tour.js` line count >= 150 | `wc -l app/static/js/tour.js` | 238 | PASS |
| `RESUME_KEY` usage >= 4 | `grep -c "RESUME_KEY" tour.js` | 6 | PASS |
| `Shepherd.Tour` present | `grep -c "Shepherd.Tour" tour.js` | 1 | PASS |
| `/api/tours` fetch calls >= 2 | `grep -c "api/tours" tour.js` | 3 | PASS |
| `sessionStorage` uses >= 3 | `grep -c "sessionStorage" tour.js` | 6 | PASS |
| Test-only op skip warning | `grep -c "skipping test-only op" tour.js` | 1 | PASS |
| `window.location.assign` in code | `grep -n "window.location.assign" tour.js` | line 12 (comment), line 128 (code) | PASS (1 code-level call; line 12 is header comment) |
| Tours router registered in app | `python -c "from app.main import app; ..."` | `/api/tours/`, `/api/tours/{name}` found | PASS |
| All 3 API tests pass | `pytest tests/test_tours_api.py -v` | 3 passed in 0.05s | PASS |
| `app.main` imports cleanly | `python -c "from app.main import app"` | exit 0 | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| D-01 | 19.2-04 | Shepherd via CDN, tour.js thin wrapper | SATISFIED | shepherd.js@14.5.1 CDN URLs in base.html; tour.js is a 238-line IIFE wrapper |
| D-02 | 19.2-04 | Hybrid op handling with sessionStorage resume | SATISFIED | `open` op stores `{name, index}` in `sessionStorage`; `bootstrap()` resumes on next page load |
| D-05 | 19.2-03 | Tours API endpoints | SATISFIED | `/api/tours/` and `/api/tours/{name}` live, admin-gated, tested |
| D-06 | 19.2-04 | Floating admin-only button bottom-right | SATISFIED | `renderShowTourButton` creates fixed-position button with z-index 100, bottom/right 1.5rem |
| D-07 (schema one-source) | 19.2-01 | Single schema validates for both runner and tour player | SATISFIED | `app/services/scenario_schema.py` shared; collector skips tour-block files; `say/highlight/wait_for_click` step types are schema-valid but skipped by runner with WARNING |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `app/static/js/tour.js` | 12 | `window.location.assign` in header comment | Info | Causes `grep -c` count to return 2 vs expected 1; documented in SUMMARY. No functional impact — exactly one code-level call at line 128 |

No stubs, placeholders, hardcoded empty returns, or TODO/FIXME markers found in any phase-modified file.

---

### Human Verification Required

#### 1. Admin sees floating "Show tour" button

**Test:** Start dev stack, ship a tour YAML file (Plan 05 prerequisite), log in as admin, navigate to the matching page
**Expected:** Floating "Показать тур" button appears bottom-right; non-admin user sees no button and devtools Network shows no shepherd.css / shepherd.min.js / tour.js requests
**Why human:** Requires a running server, a real tour YAML, and browser devtools inspection

#### 2. Tour playback: step types render correctly

**Test:** With a tour YAML containing `say`, `highlight`, `wait_for_click`, and `open` steps, click the tour button and walk through all steps
**Expected:** `say` shows a centered modal with "Далее"; `highlight` attaches tooltip to the target element; `wait_for_click` advances on user click without a button; `open` navigates to new URL and resumes tour on next page
**Why human:** Requires live browser with DOM interaction; Shepherd.js rendering cannot be verified statically

#### 3. Non-admin asset isolation

**Test:** Log in as a non-admin user; open browser devtools Network tab; load any page
**Expected:** No requests to `shepherd.js@14.5.1` CSS/JS or `/static/js/tour.js` appear in Network log
**Why human:** Requires browser devtools inspection on a live authenticated session

---

### Gaps Summary

No gaps found. All five observable truths are verified: the admin gate in base.html correctly restricts Shepherd.js and tour.js to admin sessions; tour.js implements all four required step types; the `/api/tours/` API is live, admin-gated, and tested; the shared Pydantic schema in `app/services/scenario_schema.py` serves as the single source of truth for both the 19.1 test runner and the 19.2 tour player.

The absence of tour-block YAML files in `scenarios/` is intentional — Plan 05 will ship the first tour YAML. The system correctly returns an empty list and renders no button until that file exists.

---

_Verified: 2026-04-09T07:05:00Z_
_Verifier: Claude (gsd-verifier)_
