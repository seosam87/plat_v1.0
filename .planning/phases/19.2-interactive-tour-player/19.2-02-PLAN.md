---
phase: 19.2-interactive-tour-player
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/static/js/.gitkeep
  - app/main.py
  - app/template_engine.py
autonomous: true
requirements: [D-01, D-06]
must_haves:
  truths:
    - "GET /static/js/<anything> is served by the FastAPI app"
    - "Jinja templates rendered via app.template_engine can reference {% if is_admin %}"
  artifacts:
    - path: app/static/js/.gitkeep
      provides: "Static directory exists so StaticFiles mount succeeds at boot"
    - path: app/main.py
      provides: "StaticFiles mount at /static"
      contains: "app.mount(\"/static\""
    - path: app/template_engine.py
      provides: "is_admin injected into template context"
      contains: "ctx[\"is_admin\"]"
  key_links:
    - from: app/main.py
      to: app/static/
      via: "StaticFiles(directory=\"app/static\")"
      pattern: "StaticFiles\\(directory=\"app/static\"\\)"
    - from: app/template_engine.py
      to: base.html
      via: "ctx[\"is_admin\"] assignment"
      pattern: "ctx\\[\"is_admin\"\\]\\s*="
---

<objective>
Create the `app/static/` directory, mount `StaticFiles` at `/static`, and expose the already-computed `is_admin` flag into every template render context.

Purpose: Two independent infrastructure enablers that Plan 04 depends on — tour.js can only be served after the static mount exists, and the admin-gated Shepherd CDN block in base.html can only work after is_admin is in the Jinja context.
Output: Working `/static/*` route, `is_admin` available server-side in Jinja.
</objective>

<context>
@.planning/phases/19.2-interactive-tour-player/19.2-RESEARCH.md
@app/main.py
@app/template_engine.py
@app/templates/base.html

<interfaces>
Current `app/template_engine.py` (lines 96-106) already computes `is_admin` from the JWT cookie — it's used to call `build_sidebar_sections(..., is_admin)` but is NOT placed into `ctx`. Templates currently have no access to it.

Current `app/main.py` imports FastAPI but has NO `StaticFiles` import and NO `app.mount` call. Router registrations begin at line 106. The mount must be added BEFORE the first `app.include_router` call per research Pitfall 4 (mount-order shadowing risk, even if theoretical here).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create static directory + StaticFiles mount</name>
  <read_first>
    - app/main.py lines 1-110 (imports + router registration)
    - .planning/phases/19.2-interactive-tour-player/19.2-RESEARCH.md (Pattern 3 and Pitfall 4)
  </read_first>
  <action>
    1. Create the directory `app/static/js/` and put an empty `.gitkeep` file in it so git tracks the directory even before `tour.js` lands (Plan 04 creates tour.js).
    2. In `app/main.py`, add the import near the other fastapi imports (around line 7-9):
       ```python
       from fastapi.staticfiles import StaticFiles
       ```
    3. In `app/main.py`, add the mount line IMMEDIATELY after `app = FastAPI(...)` construction (~line 64) and BEFORE the first `app.include_router(...)` call (currently line 106). Exact placement: after the rate-limiter setup block (after line 73) and before the middleware class definition is fine, OR directly after `app.add_middleware(UIAuthMiddleware)` on line 104 and before line 106 — pick the latter (keeps middleware+mount contiguous, last thing before routers). Line to add:
       ```python
       app.mount("/static", StaticFiles(directory="app/static"), name="static")
       ```
    4. Do NOT touch any router registration order, middleware, or the UIAuthMiddleware PUBLIC_PATHS set. `/static/*` is outside `/ui/*`, so it's not subject to the UI auth middleware — this is intentional (Shepherd CSS + tour.js must load on the login page too if we ever want it there; for now, base.html gates the inclusion behind `is_admin`).
  </action>
  <verify>
    <automated>cd /opt/seo-platform && python -c "from app.main import app; routes = [r.path for r in app.routes if hasattr(r, 'path')]; assert '/static' in routes, routes; print('mount ok')" && ls app/static/js/.gitkeep</automated>
  </verify>
  <acceptance_criteria>
    - `test -d app/static/js` → true
    - `test -f app/static/js/.gitkeep` → true
    - `grep -c "StaticFiles" app/main.py` → ≥2 (import + mount)
    - `grep -n 'app.mount("/static"' app/main.py` returns a line number < the first `app.include_router` line
    - python smoke import shows `/static` in app.routes
  </acceptance_criteria>
  <done>Static file mount is live; directory exists; future tour.js will be served at /static/js/tour.js.</done>
</task>

<task type="auto">
  <name>Task 2: Expose is_admin in Jinja template context</name>
  <read_first>
    - app/template_engine.py lines 78-110 (is_admin is computed at lines 97-105 but never placed in ctx)
    - .planning/phases/19.2-interactive-tour-player/19.2-RESEARCH.md (Pitfall 3)
  </read_first>
  <action>
    In `app/template_engine.py`, locate the `is_admin` computation block (lines 96-106, inside `TemplateResponse`, which builds `is_admin` from the JWT cookie and passes it to `build_sidebar_sections`).

    Add ONE line immediately after line 106 (`ctx["nav_sections"] = build_sidebar_sections(ctx["selected_site_id"], is_admin)`):

    ```python
            ctx["is_admin"] = is_admin
    ```

    (Match the existing indentation — 12 spaces, inside the `if "nav_sections" not in ctx:` block at line 79.)

    Do NOT move the computation, do NOT refactor the try/except, do NOT add a new helper. Single-line injection per Pitfall 3.
  </action>
  <verify>
    <automated>cd /opt/seo-platform && grep -n 'ctx\["is_admin"\]' app/template_engine.py && python -c "from app.template_engine import templates; print('import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ctx\["is_admin"\] = is_admin' app/template_engine.py` → 1
    - Line is inside the same `if "nav_sections" not in ctx:` block (indentation check: 12 spaces prefix)
    - `python -c "from app.template_engine import templates"` exits 0 (no syntax error)
  </acceptance_criteria>
  <done>Every nav-aware template render now has `is_admin` in its context; base.html `{% if is_admin %}` will evaluate correctly for admin JWT holders.</done>
</task>

</tasks>

<verification>
- `python -c "from app.main import app"` succeeds
- Manual (deferred to Plan 05 phase gate): `curl http://localhost:8000/static/js/.gitkeep` returns 200 when dev stack is up
- Template response integration test deferred — Plan 04 exercises `{% if is_admin %}` end-to-end
</verification>

<success_criteria>
`app/static/` exists and is mounted; `is_admin` is present in the template context for all nav-aware renders; no existing route or middleware behavior changed.
</success_criteria>

<output>
After completion, create `.planning/phases/19.2-interactive-tour-player/19.2-02-SUMMARY.md` covering: new mount, directory, one-line template edit.
</output>
