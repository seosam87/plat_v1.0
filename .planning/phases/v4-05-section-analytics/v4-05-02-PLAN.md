---
phase: v4-05-section-analytics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/analytics/index.html
  - app/templates/architecture/index.html
autonomous: true
requirements:
  - AN-V4-01

must_haves:
  truths:
    - "Analytics Workspace page renders with Tailwind utility classes and zero style= attributes"
    - "Architecture page renders with Tailwind utility classes and zero style= attributes"
    - "All existing functionality preserved: multi-step wizard, D3.js tree, SERP analysis, role detection, inlinks diff, SF import"
    - "Step indicator active states use classList toggling not inline style changes"
  artifacts:
    - path: "app/templates/analytics/index.html"
      provides: "Tailwind-migrated analytics workspace with multi-step wizard"
      contains: "class=\"bg-white"
    - path: "app/templates/architecture/index.html"
      provides: "Tailwind-migrated architecture page with D3.js tree"
      contains: "class=\"bg-white"
  key_links:
    - from: "app/templates/analytics/index.html"
      to: "/analytics/sites/{SITE_ID}/keywords"
      via: "fetch in searchKeywords()"
      pattern: "searchKeywords"
    - from: "app/templates/analytics/index.html"
      to: "showStep()"
      via: "step indicator onclick and JS navigation"
      pattern: "showStep"
    - from: "app/templates/architecture/index.html"
      to: "d3.hierarchy"
      via: "D3.js tree rendering"
      pattern: "d3\\.hierarchy"
---

<objective>
Migrate analytics/index.html (367 lines, multi-step wizard) and architecture/index.html (211 lines, D3.js tree) to pure Tailwind CSS — replacing all inline style= attributes with Tailwind utility classes while preserving the wizard step navigation, D3.js visualization, and all interactive elements.

Purpose: Analytics Workspace is the most complex wizard-based template; Architecture has D3.js. Migrating them together isolates the two unique UI patterns.
Output: Two fully Tailwind-migrated templates with zero style= attributes.
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP-v4.md
@.planning/phases/v4-05-section-analytics/v4-05-section-CONTEXT.md

<interfaces>
<!-- Tailwind patterns established in v4-02/v4-03/v4-04 -->

Card: class="bg-white rounded-lg shadow-sm border border-gray-200 p-4"
Table: class="min-w-full divide-y divide-gray-200 text-sm"
Thead: class="bg-gray-50"
Th: class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
Tbody: class="bg-white divide-y divide-gray-100"

Button:
- Primary: class="btn px-3 py-1.5 text-sm font-medium rounded bg-indigo-600 text-white hover:bg-indigo-700"
- Colored: bg-{color}-600 text-white hover:bg-{color}-700
- Small: class="btn btn-sm px-2 py-1 text-xs font-medium rounded"

Form input: class="w-full px-2 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"
Label: class="text-xs text-gray-500 block"
Badge: class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-{color}-100 text-{color}-700"

Step panel show/hide (established v4-04 modal pattern adapted for steps):
- Hidden: add class="hidden"
- Show JS: classList.remove('hidden')
- Hide JS: classList.add('hidden')

Colors: indigo=primary, emerald=success, red=danger, violet=accent, amber=warning, blue=info, gray=neutral
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate analytics/index.html to Tailwind</name>
  <files>app/templates/analytics/index.html</files>
  <read_first>
    - app/templates/analytics/index.html (current state — 367 lines, 92 inline styles)
    - app/templates/positions/index.html (Tailwind reference — modal/classList pattern from v4-04)
  </read_first>
  <action>
Rewrite app/templates/analytics/index.html replacing ALL style= attributes with Tailwind utility classes per D-04. This template has a multi-step wizard with 6 panels, keyword search, sessions, SERP analysis, comparison, and brief generation.

**Page header (lines 7-18):**
1. Header flex: replace `style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1.5rem"` with `class="flex justify-between items-center mb-6"`
2. h2: replace `style="margin:0"` with `class="m-0 text-lg font-semibold text-gray-800"`
3. Controls div: replace `style="display:flex; gap:.5rem; align-items:center"` with `class="flex gap-2 items-center"`
4. Session selector: replace inline styles with `class="px-3 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"`

**Step indicator (lines 21-28):**
5. Step container: replace `style="display:flex; gap:.25rem; margin-bottom:1.5rem"` with `class="flex gap-1 mb-6"`
6. Each step div: replace `style="flex:1; text-align:center; padding:.5rem; border-radius:4px; font-size:.8rem; background:#f3f4f6; color:#6b7280; cursor:pointer"` with `class="flex-1 text-center py-2 px-1 rounded text-xs cursor-pointer bg-gray-100 text-gray-500"` and add `data-step="{{ num }}"` attribute
7. Remove the color= parameter from the Jinja2 steps tuple — it is no longer used since steps use data attributes
8. IMPORTANT: The step indicator colors are set dynamically by JS. The initial state uses bg-gray-100 text-gray-500. The active state will be set by showStep() using classList (see JS section below).

**Step 1 — Keyword filter (lines 31-110):**
9. Card: replace `class="card" style="margin-bottom:1rem"` with `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4 mb-4"`
10. h3: replace `style="margin:0 0 1rem"` with `class="m-0 mb-4 text-base font-semibold"`
11. Filter grid: replace `style="display:grid; grid-template-columns:repeat(auto-fit, minmax(180px, 1fr)); gap:.75rem; margin-bottom:1rem"` with `class="grid grid-cols-[repeat(auto-fit,minmax(180px,1fr))] gap-3 mb-4"`
12. Each filter label: replace `style="font-size:.75rem; color:#6b7280; display:block"` with `class="text-xs text-gray-500 block"`
13. Each filter input/select: replace inline styles with `class="w-full px-2 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"`
14. Search button row: replace `style="display:flex; gap:.5rem; align-items:center"` with `class="flex gap-2 items-center"`
15. Search button: replace `style="background:#4f46e5; color:white"` with `class="btn px-3 py-1.5 text-sm font-medium rounded bg-indigo-600 text-white hover:bg-indigo-700"`
16. Filter count span: replace `style="font-size:.85rem; color:#6b7280"` with `class="text-sm text-gray-500"`
17. Results card: replace `class="card"` with `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4"`
18. Results header: replace `style="display:flex; justify-content:space-between; align-items:center; margin-bottom:.75rem"` with `class="flex justify-between items-center mb-3"`
19. h3: `class="m-0 text-base font-semibold"`
20. Select-all label: replace `style="font-size:.85rem"` with `class="text-sm"`
21. Table wrapper: replace `style="overflow-x:auto; max-height:400px; overflow-y:auto"` with `class="overflow-x-auto max-h-[400px] overflow-y-auto"`
22. Table: replace `style="font-size:.85rem"` with `class="min-w-full divide-y divide-gray-200 text-sm"`
23. Session save row: replace `style="margin-top:1rem; display:flex; gap:.75rem; align-items:center"` with `class="mt-4 flex gap-3 items-center"`
24. Session name input: replace inline styles with `class="px-3 py-1.5 border border-gray-300 rounded text-sm min-w-[250px] focus:ring-indigo-500 focus:border-indigo-500"`
25. Save button: replace `style="background:#059669; color:white"` with `class="btn px-3 py-1.5 text-sm font-medium rounded bg-emerald-600 text-white hover:bg-emerald-700"`
26. Session status span: replace `style="font-size:.85rem"` with `class="text-sm"`

**Step 2 — Session detail (lines 113-128):**
27. Panel: replace `style="display:none"` with `class="hidden"` (keep class="step-panel")
28. Card: `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4"`
29. h3: `class="m-0 mb-4 text-base font-semibold"`
30. Stats row: replace `style="display:flex; gap:1rem; margin-bottom:1rem"` with `class="flex gap-4 mb-4"`
31. Session status badge: replace `class="badge" style="background:#f3f4f6"` with `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-gray-100 text-gray-500"`
32. Button row: replace `style="display:flex; gap:.5rem; margin-bottom:1rem"` with `class="flex gap-2 mb-4"`
33. Check positions button: replace `style="background:#7c3aed; color:white"` with `class="btn px-3 py-1.5 text-sm font-medium rounded bg-violet-600 text-white hover:bg-violet-700"`
34. Parse SERP button: replace `style="background:#059669; color:white"` with `class="btn px-3 py-1.5 text-sm font-medium rounded bg-emerald-600 text-white hover:bg-emerald-700"`
35. Keywords table div: replace `style="overflow-x:auto; max-height:300px; overflow-y:auto"` with `class="overflow-x-auto max-h-[300px] overflow-y-auto"`

**Step 3 — SERP results (lines 131-149):**
36. Panel: replace `style="display:none"` with `class="hidden"`
37. Cards: same bg-white rounded-lg pattern
38. h3 elements: `class="m-0 mb-4 text-base font-semibold"`
39. Competitor input row: replace `style="margin-top:1rem; display:flex; gap:.5rem; align-items:center"` with `class="mt-4 flex gap-2 items-center"`
40. Manual competitor input: standard input class
41. Crawl mode select: standard select class
42. Crawl button: replace `style="background:#d97706; color:white"` with `class="btn px-3 py-1.5 text-sm font-medium rounded bg-amber-600 text-white hover:bg-amber-700"`

**Step 4 — Comparison (lines 152-157):**
43. Panel: replace `style="display:none"` with `class="hidden"`
44. Card + h3 + comparison div: standard Tailwind patterns
45. Comparison table div: replace `style="overflow-x:auto"` with `class="overflow-x-auto"`

**Step 5 — Brief (lines 160-176):**
46. Panel: replace `style="display:none"` with `class="hidden"`
47. Card: standard pattern
48. Brief fields grid: replace `style="display:flex; gap:1rem; margin-bottom:1rem; flex-wrap:wrap"` with `class="flex gap-4 mb-4 flex-wrap"`
49. Target URL div: replace `style="flex:1; min-width:200px"` with `class="flex-1 min-w-[200px]"`
50. Structure div: replace `style="flex:2; min-width:200px"` with `class="flex-[2] min-w-[200px]"`
51. Labels: `class="text-xs text-gray-500 block"`
52. Inputs: standard input class with `class="w-full ..."`
53. Generate button: replace `style="background:#dc2626; color:white"` with `class="btn px-3 py-1.5 text-sm font-medium rounded bg-red-600 text-white hover:bg-red-700"`
54. Brief preview div: replace `style="margin-top:1rem"` with `class="mt-4"`

**Step 6 — Brief result (lines 179-188):**
55. Panel: replace `style="display:none"` with `class="hidden"`
56. Brief content div: replace `style="white-space:pre-wrap; font-family:monospace; font-size:.85rem; background:#f9fafb; padding:1rem; border-radius:4px; max-height:500px; overflow-y:auto"` with `class="whitespace-pre-wrap font-mono text-sm bg-gray-50 p-4 rounded max-h-[500px] overflow-y-auto"`
57. Export buttons row: replace `style="margin-top:1rem; display:flex; gap:.5rem"` with `class="mt-4 flex gap-2"`
58. Export TS button: replace `style="background:#4f46e5; color:white"` with `class="btn px-3 py-1.5 text-sm font-medium rounded bg-indigo-600 text-white hover:bg-indigo-700"`
59. Export CSV button: replace `style="background:#059669; color:white"` with `class="btn px-3 py-1.5 text-sm font-medium rounded bg-emerald-600 text-white hover:bg-emerald-700"`

**Briefs list section (lines 191-210):**
60. Card: replace `class="card" style="margin-top:1.5rem"` with `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4 mt-6"`
61. h3: `class="m-0 mb-4 text-base font-semibold"`
62. Table: replace `style="font-size:.85rem"` with `class="min-w-full divide-y divide-gray-200 text-sm"` + thead/tbody classes
63. URL td: replace `style="max-width:200px; overflow:hidden; text-overflow:ellipsis"` with `class="max-w-[200px] overflow-hidden text-ellipsis"`
64. Download link: replace `style="font-size:.75rem"` with `class="btn btn-sm text-xs"`

**Style tag (line 212):**
65. Remove the `<style>.step-panel { transition: opacity 0.2s; }</style>` — replace with Tailwind: add `transition-opacity duration-200` to each step-panel class

**JavaScript section — CRITICAL showStep() rewrite (lines 220-229):**
66. Replace showStep() to use classList instead of style.display:
```javascript
function showStep(n) {
  document.querySelectorAll('.step-panel').forEach(p => p.classList.add('hidden'));
  const panel = document.getElementById('step-' + n);
  if (panel) panel.classList.remove('hidden');
  document.querySelectorAll('[id^="step-ind-"]').forEach(el => {
    el.classList.remove('bg-indigo-600', 'text-white');
    el.classList.add('bg-gray-100', 'text-gray-500');
  });
  const ind = document.getElementById('step-ind-' + n);
  if (ind) {
    ind.classList.remove('bg-gray-100', 'text-gray-500');
    ind.classList.add('bg-indigo-600', 'text-white');
  }
}
```

67. In searchKeywords() JS (line 246-247): replace inline `style="color:#059669"` with `class="text-emerald-600"`, `style="color:#dc2626"` with `class="text-red-600"`, `style="max-width:150px;overflow:hidden;text-overflow:ellipsis"` with `class="max-w-[150px] overflow-hidden text-ellipsis"`

68. In saveSession() (line 264): replace `style="color:#059669"` with `class="text-emerald-600"`

69. In loadSerpResults() (lines 298-312): replace all inline style= in dynamically generated HTML:
    - Summary cards: replace `style="padding:.75rem;text-align:center"` with `class="bg-white rounded-lg shadow-sm border border-gray-200 p-3 text-center"`
    - Strong color: replace `style="color:#d97706"` etc. with `class="text-amber-600"`, `class="text-blue-600"`, `class="text-gray-500"`
    - Label divs: replace `style="font-size:.75rem;color:#6b7280"` with `class="text-xs text-gray-500"`
    - Flex container: replace `style="display:flex;gap:1rem;margin-bottom:1rem"` with `class="flex gap-4 mb-4"`
    - Table in JS: replace `style="font-size:.85rem"` with `class="min-w-full divide-y divide-gray-200 text-sm"`
    - Badge in JS: replace `style="background:${typeColor}22;color:${typeColor}"` with Tailwind classes. Use a lookup:
      ```javascript
      const typeClasses = {commercial:'bg-amber-100 text-amber-700', informational:'bg-blue-100 text-blue-700', aggregator:'bg-gray-100 text-gray-500'};
      ```
      Then use `class="${typeClasses[c.site_type] || 'bg-gray-100 text-gray-500'}"` instead of inline style.

70. In loadComparison() (lines 335-341): replace `style="background:#f0fdf4"` with `class="bg-emerald-50"` for our pages, `style="background:#fef2f2"` with `class="bg-red-50"` for competitor pages. Replace `style="max-width:200px;overflow:hidden;text-overflow:ellipsis"` with `class="max-w-[200px] overflow-hidden text-ellipsis"`. Replace table `style="font-size:.8rem"` with `class="min-w-full divide-y divide-gray-200 text-xs"`.

Preserve ALL Jinja2 template variables and control flow.
Preserve ALL fetch URLs, JS function logic, wizard navigation.
  </action>
  <verify>
    <automated>cd /projects/test && grep -c 'style=' app/templates/analytics/index.html | grep -q '^0$' && echo "PASS: zero inline styles" || echo "FAIL: inline styles remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'style=' app/templates/analytics/index.html` returns 0
    - `grep -c 'class="' app/templates/analytics/index.html` returns >= 50
    - `grep 'showStep' app/templates/analytics/index.html` returns at least 5 matches (function + callers)
    - `grep 'classList' app/templates/analytics/index.html` returns at least 6 matches (step show/hide + indicator toggle)
    - `grep 'searchKeywords' app/templates/analytics/index.html` returns at least 2 matches
    - `grep 'saveSession' app/templates/analytics/index.html` returns at least 2 matches
    - `grep 'loadSerpResults' app/templates/analytics/index.html` returns at least 1 match
    - `grep 'generateBrief' app/templates/analytics/index.html` returns at least 2 matches
    - `grep 'bg-white rounded-lg' app/templates/analytics/index.html` returns at least 5 matches
    - `grep 'extends "base.html"' app/templates/analytics/index.html` returns 1 match
  </acceptance_criteria>
  <done>analytics/index.html has zero style= attributes, all Tailwind utility classes, multi-step wizard uses classList for panel visibility and step indicator active states, all 6 steps functional, all JS functions preserved</done>
</task>

<task type="auto">
  <name>Task 2: Migrate architecture/index.html to Tailwind</name>
  <files>app/templates/architecture/index.html</files>
  <read_first>
    - app/templates/architecture/index.html (current state — 211 lines, 58 inline styles)
    - app/templates/analytics/index.html (just-migrated reference for consistent Tailwind patterns)
  </read_first>
  <action>
Rewrite app/templates/architecture/index.html replacing ALL style= attributes with Tailwind utility classes per D-04. Keep D3.js tree rendering and all interactive elements intact.

**Page header (lines 4-7):**
1. Header flex: replace inline styles with `class="flex justify-between items-center mb-6"`
2. h2: replace `style="margin:0"` with `class="m-0 text-lg font-semibold text-gray-800"`

**Import section (lines 10-32):**
3. Card: replace `class="card" style="margin-bottom:1.5rem"` with `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4 mb-6"`
4. h3: replace `style="margin:0 0 1rem"` with `class="m-0 mb-4 text-base font-semibold"`
5. Import flex: replace `style="display:flex; gap:2rem; flex-wrap:wrap"` with `class="flex gap-8 flex-wrap"`
6. SF column: replace `style="flex:1; min-width:220px"` with `class="flex-1 min-w-[220px]"`
7. h4: replace `style="margin:0 0 .5rem; font-size:.9rem"` with `class="m-0 mb-2 text-sm font-medium"`
8. File input: replace `style="margin-bottom:.5rem"` with `class="mb-2 text-sm"`
9. Import SF button: replace `style="background:#4f46e5; color:white"` with `class="btn px-3 py-1.5 text-sm font-medium rounded bg-indigo-600 text-white hover:bg-indigo-700"`
10. Sitemap column: same flex-1 min-w pattern
11. Sitemap buttons div: replace `style="display:flex; gap:.5rem; margin-bottom:.5rem"` with `class="flex gap-2 mb-2"`
12. Fetch sitemap button: replace `style="background:#059669; color:white"` with `class="btn px-3 py-1.5 text-sm font-medium rounded bg-emerald-600 text-white hover:bg-emerald-700"`
13. Inner form: replace `style="display:inline"` with `class="inline"`
14. Hidden file input: replace `style="display:none"` with `class="hidden"`
15. Import status span: replace `style="font-size:.85rem; display:block; margin-top:.5rem"` with `class="text-sm block mt-2"`

**Sitemap comparison (lines 35-57):**
16. Card: `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4 mb-6"`
17. h3: `class="m-0 mb-4 text-base font-semibold"`
18. Stats row: replace `style="display:flex; gap:1rem; margin-bottom:1rem"` with `class="flex gap-4 mb-4"`
19. Orphan stat: replace `style="text-align:center; padding:.5rem 1rem; border-radius:4px; background:#fee2e2"` with `class="text-center px-4 py-2 rounded bg-red-100"`
20. Orphan value: replace `style="font-size:1.25rem; font-weight:700; color:#dc2626"` with `class="text-xl font-bold text-red-600"`
21. Orphan label: replace `style="font-size:.75rem; color:#6b7280"` with `class="text-xs text-gray-500"`
22. Missing stat: replace background `#fef3c7` with `class="text-center px-4 py-2 rounded bg-amber-100"`
23. Missing value: replace color `#d97706` with `class="text-xl font-bold text-amber-600"`
24. OK stat: replace background `#d1fae5` with `class="text-center px-4 py-2 rounded bg-emerald-100"`
25. OK value: replace color `#059669` with `class="text-xl font-bold text-emerald-600"`
26. Sitemap table div: replace `style="max-height:300px; overflow-y:auto"` with `class="max-h-[300px] overflow-y-auto"`
27. Filter buttons: replace `style="color:#dc2626"` with `class="text-red-600"`, `style="color:#d97706"` with `class="text-amber-600"` on the button elements. Use `class="btn btn-sm text-red-600"` pattern.
28. Margin div: replace `style="margin-top:.5rem"` with `class="mt-2 flex gap-2"`

**URL Tree D3.js (lines 60-66):**
29. Card: `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4 mb-6"`
30. Header flex: replace `style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1rem"` with `class="flex justify-between items-center mb-4"`
31. h3: `class="m-0 text-base font-semibold"`
32. Tree container: replace `style="width:100%; overflow:auto; min-height:400px; background:#fafafa; border-radius:4px"` with `class="w-full overflow-auto min-h-[400px] bg-gray-50 rounded"`

**Architecture roles (lines 69-75):**
33. Card: `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4 mb-6"`
34. Header flex: same pattern as tree header
35. h3: `class="m-0 text-base font-semibold"`
36. Detect roles button: replace `style="background:#7c3aed; color:white"` with `class="btn px-3 py-1.5 text-sm font-medium rounded bg-violet-600 text-white hover:bg-violet-700"`

**Inlinks diff (lines 78-93):**
37. Card: `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4"`
38. h3: `class="m-0 mb-4 text-base font-semibold"`
39. Controls row: replace `style="display:flex; gap:1rem; align-items:center; margin-bottom:1rem"` with `class="flex gap-4 items-center mb-4"`
40. Crawl selects: replace inline styles with `class="px-2 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"`

**JavaScript section (lines 96-210):**
41. In importSF() (line 105): replace `style="color:#059669"` with `class="text-emerald-600"`
42. In fetchSitemap() (line 111): replace `style="color:#059669"` with `class="text-emerald-600"`
43. In uploadSitemap() (line 119): replace `style="color:#059669"` with `class="text-emerald-600"`
44. In loadSitemapResults() (lines 127-133): replace table `style="font-size:.8rem"` with `class="min-w-full divide-y divide-gray-200 text-xs"`. Replace status span inline color with Tailwind classes:
    ```javascript
    const statusClasses = {orphan:'text-red-600 font-semibold', missing:'text-amber-600 font-semibold', ok:'text-emerald-600 font-semibold'};
    ```
    Use `class="${statusClasses[e.status] || 'text-gray-500'}"` instead of `style="color:${color};font-weight:600"`.
    Replace `style="max-width:400px;overflow:hidden;text-overflow:ellipsis"` with `class="max-w-[400px] overflow-hidden text-ellipsis"`.

45. In loadRoles() (lines 172-187): replace inline styles in dynamically generated HTML:
    - Role header h4: replace `style="color:${color};margin:0 0 .5rem"` with Tailwind classes. Use a lookup object:
      ```javascript
      const ROLE_TW = {pillar:'text-violet-600',service:'text-blue-600',subservice:'text-sky-500',article:'text-emerald-600',trigger:'text-amber-600',authority:'text-teal-600',link_accelerator:'text-pink-500',unknown:'text-gray-400'};
      ```
      Then: `class="${ROLE_TW[role] || 'text-gray-500'} m-0 mb-2"`
    - Wrapper div: replace `style="margin-bottom:1rem"` with `class="mb-4"`
    - Table: replace `style="font-size:.8rem"` with `class="min-w-full divide-y divide-gray-200 text-xs"`
    - URL td: replace `style="max-width:250px;overflow:hidden;text-overflow:ellipsis"` with `class="max-w-[250px] overflow-hidden text-ellipsis"`
    - Role select: replace `style="font-size:.7rem"` with `class="text-xs px-1 py-0.5 border border-gray-300 rounded"`
    - Empty state p: replace `style="color:#6b7280"` with `class="text-gray-500"`

46. In loadInlinksDiff() (lines 199-205): replace inline styles:
    - Summary div: replace `style="margin-bottom:.5rem"` with `class="mb-2"`
    - Added count span: replace `style="color:#059669;font-weight:600"` with `class="text-emerald-600 font-semibold"`
    - Removed count span: replace `style="color:#dc2626;font-weight:600;margin-left:1rem"` with `class="text-red-600 font-semibold ml-4"`
    - Table: `class="min-w-full divide-y divide-gray-200 text-xs"`
    - Added rows: replace `style="background:#f0fdf4"` with `class="bg-emerald-50"`
    - Removed rows: replace `style="background:#fef2f2"` with `class="bg-red-50"`
    - Added status td: replace `style="color:#059669"` with `class="text-emerald-600"`
    - Removed status td: replace `style="color:#dc2626"` with `class="text-red-600"`

Preserve D3.js script tag and all D3 rendering code exactly.
Preserve ALL fetch URLs, JS function logic, file upload handling.
  </action>
  <verify>
    <automated>cd /projects/test && grep -c 'style=' app/templates/architecture/index.html | grep -q '^0$' && echo "PASS: zero inline styles" || echo "FAIL: inline styles remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'style=' app/templates/architecture/index.html` returns 0
    - `grep -c 'class="' app/templates/architecture/index.html` returns >= 25
    - `grep 'd3.hierarchy' app/templates/architecture/index.html` returns 1 match (D3 tree preserved)
    - `grep 'renderTree' app/templates/architecture/index.html` returns at least 2 matches
    - `grep 'loadRoles' app/templates/architecture/index.html` returns at least 2 matches
    - `grep 'loadInlinksDiff' app/templates/architecture/index.html` returns at least 2 matches
    - `grep 'importSF' app/templates/architecture/index.html` returns at least 2 matches
    - `grep 'bg-white rounded-lg' app/templates/architecture/index.html` returns at least 5 matches
    - `grep 'extends "base.html"' app/templates/architecture/index.html` returns 1 match
  </acceptance_criteria>
  <done>architecture/index.html has zero style= attributes, all Tailwind utility classes, D3.js tree rendering preserved, SF import + sitemap comparison + role detection + inlinks diff all functional</done>
</task>

</tasks>

<verification>
- `grep -rc 'style=' app/templates/analytics/index.html` returns 0
- `grep -rc 'style=' app/templates/architecture/index.html` returns 0
- Both templates still extend base.html
- Analytics wizard showStep() uses classList not style.display
- Architecture D3.js tree script and CDN import preserved
- All JS functions preserved in both templates
</verification>

<success_criteria>
Analytics Workspace and Architecture pages render with pure Tailwind CSS. Zero inline style= attributes. Multi-step wizard uses classList for step visibility and indicator active states. D3.js tree visualization intact. All interactive features preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/v4-05-section-analytics/v4-05-02-SUMMARY.md`
</output>
