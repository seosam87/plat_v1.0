---
phase: v4-05-section-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/competitors/index.html
  - app/templates/gap/index.html
autonomous: true
requirements:
  - AN-V4-02
  - AN-V4-01

must_haves:
  truths:
    - "Competitors page renders with Tailwind utility classes and zero style= attributes"
    - "Gap analysis page renders with Tailwind utility classes and zero style= attributes"
    - "All existing functionality preserved: HTMX delete, compare modal, detect modal, gap import, proposals, groups"
  artifacts:
    - path: "app/templates/competitors/index.html"
      provides: "Tailwind-migrated competitors page"
      contains: "class=\"bg-white"
    - path: "app/templates/gap/index.html"
      provides: "Tailwind-migrated gap analysis page"
      contains: "class=\"bg-white"
  key_links:
    - from: "app/templates/competitors/index.html"
      to: "/competitors/sites/{site_id}/compare"
      via: "fetch in comparePositions()"
      pattern: "comparePositions"
    - from: "app/templates/competitors/index.html"
      to: "hx-delete"
      via: "HTMX delete on competitor row"
      pattern: "hx-delete"
    - from: "app/templates/gap/index.html"
      to: "/gap/{SITE_ID}/detect"
      via: "fetch in detectGaps()"
      pattern: "detectGaps"
---

<objective>
Migrate competitors/index.html (130 lines) and gap/index.html (227 lines) to pure Tailwind CSS — replacing all inline style= attributes with Tailwind utility classes while preserving all functional elements (HTMX delete, compare modal, detect modal, gap keyword table, proposals table, group management).

Purpose: Simplest two analytics templates migrated first, establishing the pattern for the section.
Output: Two fully Tailwind-migrated templates with zero style= attributes.
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP-v4.md
@.planning/phases/v4-05-section-analytics/v4-05-section-CONTEXT.md

<interfaces>
<!-- Tailwind patterns established in v4-02/v4-03/v4-04 -->

Table pattern:
- Wrapper: class="bg-white rounded-lg shadow-sm border border-gray-200 p-4"
- Table: class="min-w-full divide-y divide-gray-200"
- Thead: class="bg-gray-50"
- Th: class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
- Tbody: class="bg-white divide-y divide-gray-100"
- Tr hover: class="hover:bg-gray-50"

Button patterns:
- Primary: class="inline-flex items-center px-3 py-1.5 bg-indigo-600 text-white text-sm font-medium rounded-md hover:bg-indigo-700"
- Small: class="px-2 py-1 text-xs font-medium rounded"
- Danger: bg-red-800 text-white hover:bg-red-700
- Colored: bg-{color}-600 text-white hover:bg-{color}-700

Badge pattern:
- class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-{color}-100 text-{color}-700"

Form input pattern:
- class="px-2 py-1 border border-gray-300 rounded text-sm bg-white focus:ring-indigo-500 focus:border-indigo-500"

Modal pattern (from v4-04):
- Overlay: class="hidden fixed inset-0 bg-black/50 z-[100] justify-center items-center"
- Body: class="bg-white rounded-lg p-8 max-w-[900px] w-[95%] max-h-[85vh] overflow-y-auto relative"
- Close: class="absolute top-2 right-4 bg-transparent border-none text-2xl cursor-pointer"
- Show JS: classList.remove('hidden'); classList.add('flex')
- Hide JS: classList.add('hidden'); classList.remove('flex')
- Backdrop click: if (e.target === this) { this.classList.add('hidden'); this.classList.remove('flex'); }

Colors: indigo=primary, emerald=success, red=danger, sky=info, amber=warning, violet=accent, gray=neutral
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate competitors/index.html to Tailwind</name>
  <files>app/templates/competitors/index.html</files>
  <read_first>
    - app/templates/competitors/index.html (current state — 130 lines, 30 inline styles)
    - app/templates/positions/index.html (Tailwind reference pattern from v4-04 — check modal pattern)
  </read_first>
  <action>
Rewrite app/templates/competitors/index.html replacing ALL style= attributes with Tailwind utility classes per D-04. Keep extends "base.html", block content structure intact.

Specific migrations:

**Main card and header (lines 4-13):**
1. Outer card: replace `class="card" style="margin-bottom:1.5rem"` with `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4 mb-6"`
2. Header flex div: replace `style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1rem"` with `class="flex justify-between items-center mb-4"`
3. h2: replace `style="margin:0; display:inline"` with `class="m-0 inline text-lg font-semibold text-gray-800"`
4. Count span: replace `style="color:#6b7280; margin-left:1rem"` with `class="text-gray-500 ml-4"`
5. Button group div: replace `style="display:flex; gap:.5rem"` with `class="flex gap-2"`
6. Auto-Detect button: replace `style="background:#7c3aed;color:white"` with `class="btn btn-sm px-2 py-1 text-xs font-medium rounded bg-violet-600 text-white hover:bg-violet-700"`

**Add competitor form (line 16-22):**
7. Form: replace `style="display:flex; gap:.5rem; margin-bottom:1.5rem"` with `class="flex gap-2 mb-6 flex-wrap"`
8. Domain input: replace `style="flex:1; min-width:200px; padding:.4rem .75rem; border:1px solid #d1d5db; border-radius:4px; font-size:.875rem"` with `class="flex-1 min-w-[200px] px-3 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"`
9. Name input: replace `style="width:200px; padding:.4rem .75rem; border:1px solid #d1d5db; border-radius:4px; font-size:.875rem"` with `class="w-[200px] px-3 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"`

**Table (lines 25-50):**
10. Table: add `class="min-w-full divide-y divide-gray-200 text-sm"`
11. Thead: add `class="bg-gray-50"`
12. Th elements: add `class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"`
13. Tbody: add `class="bg-white divide-y divide-gray-100"`
14. Domain td: replace `style="font-weight:500"` with `class="font-medium"`
15. Notes td: replace `style="font-size:.85rem; color:#6b7280; max-width:200px; overflow:hidden; text-overflow:ellipsis"` with `class="text-sm text-gray-500 max-w-[200px] overflow-hidden text-ellipsis"`
16. Actions td: replace `style="display:flex; gap:.25rem"` with `class="flex gap-1"`
17. Compare button: replace `style="background:#2563eb;color:white"` with `class="btn btn-sm px-2 py-1 text-xs font-medium rounded bg-blue-600 text-white hover:bg-blue-700"`
18. Delete button: replace `style="background:#991b1b;color:white"` with `class="btn btn-sm px-2 py-1 text-xs font-medium rounded bg-red-800 text-white hover:bg-red-700"` — preserve hx-delete, hx-target, hx-swap, hx-confirm

**Empty state (line 52):**
19. Replace `style="color:#6b7280"` with `class="text-gray-500"`

**Compare modal (lines 57-63):**
20. Overlay div: replace `style="display:none; position:fixed; top:0; left:0; right:0; bottom:0; background:rgba(0,0,0,.5); z-index:100; justify-content:center; align-items:center"` with `class="hidden fixed inset-0 bg-black/50 z-[100] justify-center items-center"` (keep id="compare-modal")
21. Modal body: replace `style="background:white; border-radius:8px; padding:2rem; max-width:900px; width:95%; max-height:85vh; overflow-y:auto; position:relative"` with `class="bg-white rounded-lg p-8 max-w-[900px] w-[95%] max-h-[85vh] overflow-y-auto relative"`
22. Close button: replace `style="position:absolute; top:.5rem; right:1rem; background:none; border:none; font-size:1.5rem; cursor:pointer"` with `class="absolute top-2 right-4 bg-transparent border-none text-2xl cursor-pointer"` — update onclick to use classList pattern
23. Title h3: replace `style="margin:0 0 1rem"` with `class="m-0 mb-4"`
24. Result div: replace `style="color:#6b7280"` with `class="text-gray-500"`

**Detect modal (lines 66-72):**
25. Same modal pattern as compare modal but with `id="detect-modal"`, `max-w-[700px]`, `w-[90%]`, `max-h-[80vh]`

**JavaScript section (lines 74-129):**
26. In comparePositions(): replace `document.getElementById('compare-modal').style.display = 'flex'` with:
    ```
    const modal = document.getElementById('compare-modal');
    modal.classList.remove('hidden'); modal.classList.add('flex');
    ```
27. In JS delta spans: replace `style="color:#dc2626"` with `class="text-red-600"`, `style="color:#059669"` with `class="text-emerald-600"`, `style="color:#6b7280"` with `class="text-gray-500"`
28. In detectCompetitors(): replace `style.display = 'flex'` with classList pattern
29. In detect result JS: replace `style="font-weight:500"` with `class="font-medium"`, replace `style="font-size:.85rem;color:#6b7280;margin-top:1rem"` with `class="text-sm text-gray-500 mt-4"`
30. Backdrop click listeners (line 127-128): replace `this.style.display = 'none'` with `this.classList.add('hidden'); this.classList.remove('flex')`
31. Close button onclicks in both modals: replace `style.display='none'` with classList pattern:
    `onclick="const m=document.getElementById('compare-modal'); m.classList.add('hidden'); m.classList.remove('flex')"`
    Same for detect-modal.

Preserve ALL hx-delete, hx-target, hx-swap, hx-confirm attributes exactly.
Preserve ALL Jinja2 template variables and control flow.
Preserve ALL fetch URLs and JS function logic.
  </action>
  <verify>
    <automated>cd /projects/test && grep -c 'style=' app/templates/competitors/index.html | grep -q '^0$' && echo "PASS: zero inline styles" || echo "FAIL: inline styles remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'style=' app/templates/competitors/index.html` returns 0
    - `grep -c 'class="' app/templates/competitors/index.html` returns >= 15
    - `grep 'hx-delete' app/templates/competitors/index.html` returns at least 1 match
    - `grep 'hx-confirm' app/templates/competitors/index.html` returns at least 1 match
    - `grep 'comparePositions' app/templates/competitors/index.html` returns at least 2 matches
    - `grep 'detectCompetitors' app/templates/competitors/index.html` returns at least 2 matches
    - `grep 'classList' app/templates/competitors/index.html` returns at least 4 matches (modal show/hide)
    - `grep 'hidden fixed inset-0' app/templates/competitors/index.html` returns 2 matches (two modals)
    - `grep 'extends "base.html"' app/templates/competitors/index.html` returns 1 match
  </acceptance_criteria>
  <done>competitors/index.html has zero style= attributes, all Tailwind utility classes, both modals use classList toggle pattern, HTMX delete preserved, all JS functions preserved</done>
</task>

<task type="auto">
  <name>Task 2: Migrate gap/index.html to Tailwind</name>
  <files>app/templates/gap/index.html</files>
  <read_first>
    - app/templates/gap/index.html (current state — 227 lines, 60 inline styles)
    - app/templates/competitors/index.html (just-migrated reference for Tailwind patterns)
  </read_first>
  <action>
Rewrite app/templates/gap/index.html replacing ALL style= attributes with Tailwind utility classes per D-04. Keep extends "base.html", block content structure intact.

Specific migrations:

**Page header (lines 4-7):**
1. Header flex: replace `style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1.5rem"` with `class="flex justify-between items-center mb-6"`
2. h2: replace `style="margin:0"` with `class="m-0 text-lg font-semibold text-gray-800"`

**Import section card (lines 10-37):**
3. Card div: replace `class="card" style="margin-bottom:1.5rem"` with `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4 mb-6"`
4. h3: replace `style="margin:0 0 1rem"` with `class="m-0 mb-4 text-base font-semibold"`
5. Flex container: replace `style="display:flex; gap:2rem; flex-wrap:wrap"` with `class="flex gap-8 flex-wrap"`
6. Left column: replace `style="flex:1; min-width:250px"` with `class="flex-1 min-w-[250px]"`
7. h4 (session): replace `style="margin:0 0 .5rem; font-size:.9rem"` with `class="m-0 mb-2 text-sm font-medium"`
8. Session select: replace `style="width:100%; padding:.4rem .5rem; border:1px solid #d1d5db; border-radius:4px; margin-bottom:.5rem"` with `class="w-full px-2 py-1.5 border border-gray-300 rounded mb-2 text-sm focus:ring-indigo-500 focus:border-indigo-500"`
9. Domain input: same pattern as select — `class="w-full px-2 py-1.5 border border-gray-300 rounded mb-2 text-sm focus:ring-indigo-500 focus:border-indigo-500"`
10. Detect button: replace `style="background:#4f46e5; color:white"` with `class="btn px-3 py-1.5 text-sm font-medium rounded bg-indigo-600 text-white hover:bg-indigo-700"`
11. Right column (file import): same flex-1 min-w pattern
12. h4 (file): same as session h4
13. File description p: replace `style="font-size:.75rem; color:#6b7280; margin:0 0 .5rem"` with `class="text-xs text-gray-500 m-0 mb-2"`
14. Import domain input: same class as session domain input
15. File input: replace `style="margin-bottom:.5rem"` with `class="mb-2 text-sm"`
16. Upload button: replace `style="background:#059669; color:white"` with `class="btn px-3 py-1.5 text-sm font-medium rounded bg-emerald-600 text-white hover:bg-emerald-700"`
17. Import status span: replace `style="font-size:.85rem; margin-top:.5rem; display:block"` with `class="text-sm mt-2 block"`

**Summary cards (lines 40-53):**
18. Grid container: replace `style="display:grid; grid-template-columns:repeat(auto-fit, minmax(160px, 1fr)); gap:1rem; margin-bottom:1.5rem"` with `class="grid grid-cols-[repeat(auto-fit,minmax(160px,1fr))] gap-4 mb-6"`
19. Each summary card: replace `class="card" style="text-align:center; padding:1rem"` with `class="bg-white rounded-lg shadow-sm border border-gray-200 text-center p-4"`
20. Gap count value: replace `style="font-size:1.75rem; font-weight:700; color:#dc2626"` with `class="text-3xl font-bold text-red-600"`
21. Gap count label: replace `style="color:#6b7280; font-size:.85rem"` with `class="text-gray-500 text-sm"`
22. Avg score value: replace color `#2563eb` with `class="text-3xl font-bold text-blue-600"`
23. Proposals value: replace color `#059669` with `class="text-3xl font-bold text-emerald-600"`

**Gap keywords table section (lines 56-109):**
24. Card: replace `class="card" style="margin-bottom:1.5rem"` with `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4 mb-6"`
25. Header flex: replace inline styles with `class="flex justify-between items-center mb-3 flex-wrap gap-2"`
26. h3: replace `style="margin:0"` with `class="m-0 text-base font-semibold"`
27. Filter group div: replace `style="display:flex; gap:.5rem; align-items:center"` with `class="flex gap-2 items-center"`
28. Group select: replace inline styles with `class="px-2 py-1 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"`
29. Min score input: replace inline styles with `class="w-[120px] px-2 py-1 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"`
30. Toolbar div: replace `style="display:flex; gap:.5rem; margin-bottom:.75rem; flex-wrap:wrap"` with `class="flex gap-2 mb-3 flex-wrap items-center"`
31. Select-all label: replace `style="font-size:.85rem"` with `class="text-sm"`
32. Create group button: replace `style="font-size:.75rem"` with `class="btn btn-sm text-xs"`
33. Assign group select: replace inline styles with `class="text-xs px-1 py-0.5 border border-gray-300 rounded"`
34. Move button: replace `style="font-size:.75rem"` with `class="btn btn-sm text-xs"`
35. Create proposals button: replace `style="font-size:.75rem; background:#4f46e5; color:white"` with `class="btn btn-sm text-xs bg-indigo-600 text-white hover:bg-indigo-700"`
36. Table wrapper: replace `style="overflow-x:auto; max-height:400px; overflow-y:auto"` with `class="overflow-x-auto max-h-[400px] overflow-y-auto"`
37. Table: replace `style="font-size:.85rem"` with `class="min-w-full divide-y divide-gray-200 text-sm"`, add thead class="bg-gray-50", th classes, tbody class
38. Potential score td: replace `style="font-weight:600; {% if ... %}color:#059669{% elif ... %}color:#2563eb{% else %}color:#6b7280{% endif %}"` with `class="font-semibold {% if k.potential_score and k.potential_score >= 500 %}text-emerald-600{% elif k.potential_score and k.potential_score >= 100 %}text-blue-600{% else %}text-gray-500{% endif %}"`
39. Source badge: replace `style="background:#f3f4f6;color:#6b7280;font-size:.7rem"` with `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-gray-100 text-gray-500"`
40. Score formula tooltip span: replace `style="cursor:help; color:#6b7280; font-size:.75rem; border:1px solid #d1d5db; border-radius:50%; padding:0 4px"` with `class="cursor-help text-gray-500 text-xs border border-gray-300 rounded-full px-1"`
41. Empty state: replace `style="padding:1rem; color:#6b7280; text-align:center"` with `class="p-4 text-gray-500 text-center"`

**Groups section (lines 113-125):**
42. Details card: replace `class="card" style="margin-bottom:1.5rem"` with `class="bg-white rounded-lg shadow-sm border border-gray-200 mb-6"`
43. Summary: replace `style="cursor:pointer; font-weight:600; padding:.75rem"` with `class="cursor-pointer font-semibold p-3"`
44. Inner div: replace `style="padding:0 .75rem .75rem"` with `class="px-3 pb-3"`
45. Group row: replace `style="display:flex; justify-content:space-between; align-items:center; padding:.4rem 0; border-bottom:1px solid #e5e7eb"` with `class="flex justify-between items-center py-1 border-b border-gray-200"`
46. Delete group button: replace `style="font-size:.7rem; background:#991b1b; color:white"` with `class="btn btn-sm text-xs bg-red-800 text-white hover:bg-red-700"`

**Proposals section (lines 128-159):**
47. Card: replace `class="card"` with `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4"`
48. h3: replace `style="margin:0 0 1rem"` with `class="m-0 mb-4 text-base font-semibold"`
49. Table: replace `style="font-size:.85rem"` with `class="min-w-full divide-y divide-gray-200 text-sm"` + thead/tbody classes
50. Pending badge: replace `style="background:#fef3c7;color:#d97706"` with `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-amber-100 text-amber-700"`
51. Approved badge: replace `style="background:#d1fae5;color:#059669"` with `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-emerald-100 text-emerald-700"`
52. Rejected badge: replace `style="background:#f3f4f6;color:#6b7280"` with `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-gray-100 text-gray-500"`
53. Approve button: replace `style="font-size:.7rem; background:#059669; color:white"` with `class="btn btn-sm text-xs bg-emerald-600 text-white hover:bg-emerald-700"`
54. Reject button: replace `style="font-size:.7rem"` with `class="btn btn-sm text-xs"`
55. Proposals empty state: replace `style="color:#6b7280; text-align:center"` with `class="text-gray-500 text-center"`

**JavaScript section (lines 161-226):**
56. In detectGaps() status span: replace `style="color:#059669"` with `class="text-emerald-600"`
57. In importFile() status span: replace `style="color:#059669"` with `class="text-emerald-600"`

Preserve ALL Jinja2 template variables and control flow.
Preserve ALL fetch URLs, JS function logic, form handling.
  </action>
  <verify>
    <automated>cd /projects/test && grep -c 'style=' app/templates/gap/index.html | grep -q '^0$' && echo "PASS: zero inline styles" || echo "FAIL: inline styles remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'style=' app/templates/gap/index.html` returns 0
    - `grep -c 'class="' app/templates/gap/index.html` returns >= 30
    - `grep 'detectGaps' app/templates/gap/index.html` returns at least 2 matches
    - `grep 'importFile' app/templates/gap/index.html` returns at least 2 matches
    - `grep 'createProposals' app/templates/gap/index.html` returns at least 2 matches
    - `grep 'approveProposal' app/templates/gap/index.html` returns at least 1 match
    - `grep 'bg-white rounded-lg' app/templates/gap/index.html` returns at least 3 matches (multiple cards)
    - `grep 'bg-amber-100' app/templates/gap/index.html` returns at least 1 match (pending badge)
    - `grep 'bg-emerald-100' app/templates/gap/index.html` returns at least 1 match (approved badge)
    - `grep 'extends "base.html"' app/templates/gap/index.html` returns 1 match
  </acceptance_criteria>
  <done>gap/index.html has zero style= attributes, all Tailwind utility classes, gap detection + import + proposals + groups all preserved, badge colors use Tailwind classes</done>
</task>

</tasks>

<verification>
- `grep -rc 'style=' app/templates/competitors/index.html` returns 0
- `grep -rc 'style=' app/templates/gap/index.html` returns 0
- Both templates still extend base.html
- All HTMX attributes (hx-delete, hx-target, hx-swap, hx-confirm) preserved in competitors
- All JS functions preserved: comparePositions, detectCompetitors, detectGaps, importFile, createProposals, approveProposal, rejectProposal
</verification>

<success_criteria>
Competitors and Gap analysis pages render with pure Tailwind CSS. Zero inline style= attributes. All interactive features (HTMX delete, modals with classList toggle, gap detection, file import, proposals, group management) preserved exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/v4-05-section-analytics/v4-05-01-SUMMARY.md`
</output>
