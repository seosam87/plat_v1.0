---
phase: 17-in-app-notifications
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - app/tasks/crawl_tasks.py
  - app/tasks/position_tasks.py
  - app/tasks/client_report_tasks.py
  - app/tasks/audit_tasks.py
  - app/tasks/suggest_tasks.py
  - app/tasks/llm_tasks.py
  - app/services/change_monitoring_service.py
  - tests/tasks/test_notify_wiring.py
autonomous: true
requirements: [NOTIF-02]
must_haves:
  truths:
    - "Crawl completion calls notify() with kind='crawl.completed' when user_id is available"
    - "Position check completion calls notify() with kind='position_check.completed' when user_id is available"
    - "Client PDF completion calls notify() with kind='client_pdf.ready' when user_id is available"
    - "Audit completion calls notify() with kind='audit.completed' when user_id is available"
    - "Keyword suggest completion calls notify() with kind='keyword_suggest.ready' when user_id is available"
    - "LLM brief completion calls notify() with kind='llm_brief.ready' when user_id is available"
    - "Monitoring alert dispatcher calls notify() with kind='monitoring.alert' when user_id is available"
    - "Tasks without a user_id arg skip in-app notify silently (debug log only); Telegram still fires"
    - "Telegram dispatch is unchanged (additive only)"
    - "Beat digest tasks (report_tasks.py) are explicitly out of scope — no notify() there"
  artifacts:
    - path: "app/tasks/crawl_tasks.py"
      provides: "notify() call at task finalization"
      pattern: "from app.services.notifications import notify"
    - path: "app/services/change_monitoring_service.py"
      provides: "monitoring alert notify() call inside dispatch_immediate_alerts()"
      pattern: "from app.services.notifications import notify"
  key_links:
    - from: "app/tasks/*.py"
      to: "app.services.notifications.notify"
      via: "import + call at end of happy path and exception handler"
      pattern: "await notify\\("
    - from: "app/services/change_monitoring_service.py"
      to: "app.services.notifications.notify"
      via: "call inside dispatch_immediate_alerts loop"
      pattern: "notify\\("
---

<objective>
Wire `notify()` calls into the Celery tasks listed in CONTEXT.md so completion events surface in the in-app feed (additive to Telegram).

Purpose: Satisfies NOTIF-02 (feed lists crawl/position_check/client_pdf/audit/keyword_suggest/llm_brief/monitoring events).
Output: 6 Celery task files + monitoring dispatcher (`change_monitoring_service.py`) updated; integration tests verify notifications are inserted when a user scope exists and are skipped cleanly when it does not.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/17-in-app-notifications/17-CONTEXT.md
@app/tasks/crawl_tasks.py
@app/tasks/position_tasks.py
@app/tasks/client_report_tasks.py
@app/tasks/audit_tasks.py
@app/tasks/suggest_tasks.py
@app/tasks/llm_tasks.py
@app/services/change_monitoring_service.py
</context>

<interfaces>
From Plan 17-01 (app/services/notifications.py):
```python
async def notify(
    db: AsyncSession,
    user_id: UUID,
    kind: str,
    title: str,
    body: str,
    link_url: str,
    site_id: UUID | None = None,
    severity: Literal["info", "warning", "error"] = "info",
) -> Notification
```

Per revised D-02 (see 17-CONTEXT.md):
- If the task signature accepts a `user_id` arg → pass it through to `notify()`.
- If the task has no `user_id` (most current tasks) → skip the in-app notify and emit a debug log `"no user scope; skipping in-app notification"`. Telegram/email paths still fire as today.
- The `Site` model has NO `owner_id` column — do NOT attempt any Site-based fallback. Adding `owner_id` to Site is out of scope for Phase 17.

Reality of the existing code (verified against master):
- `app/tasks/position_tasks.py::_send_drop_alerts()` is where Telegram position-drop alerts fire — notify() goes alongside.
- `app/services/change_monitoring_service.py::dispatch_immediate_alerts()` is the real monitoring-alert dispatcher (NOT `app/services/monitoring_alerts.py`, which does not exist).
- `app/tasks/report_tasks.py` contains ONLY `send_morning_digest` and `send_weekly_summary_report` — scheduled Beat tasks with no user scope. These are EXPLICITLY out of scope for in-app notify (Telegram/email only). Do NOT modify report_tasks.py.
- `app/tasks/client_report_tasks.py` is the user-triggered PDF path and IS in scope.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire notify() into 6 Celery task files + change_monitoring_service dispatcher</name>
  <files>app/tasks/crawl_tasks.py, app/tasks/position_tasks.py, app/tasks/client_report_tasks.py, app/tasks/audit_tasks.py, app/tasks/suggest_tasks.py, app/tasks/llm_tasks.py, app/services/change_monitoring_service.py</files>
  <read_first>
    - app/tasks/crawl_tasks.py — find finalization point + existing Telegram dispatch; notify() goes alongside
    - app/tasks/position_tasks.py — specifically `_send_drop_alerts()` (where Telegram alerts fire) AND the top-level `check_positions` happy-path/exception paths
    - app/tasks/client_report_tasks.py — `generate_client_pdf` happy path and failure path
    - app/tasks/audit_tasks.py
    - app/tasks/suggest_tasks.py
    - app/tasks/llm_tasks.py
    - app/services/change_monitoring_service.py — specifically `dispatch_immediate_alerts()` (the real monitoring alert dispatcher; `app/services/monitoring_alerts.py` does NOT exist)
    - For each file, identify: does the task signature have a `user_id` arg? Where is the happy-path completion? Where is the exception handler?
  </read_first>
  <action>
    For EACH of the 6 task files below, add `await notify(...)` at the END of the happy path AND in the exception handler, alongside the existing Telegram dispatch (additive, do not remove Telegram).

    User scoping (revised D-02 — authoritative):
      - If the task signature has a `user_id` arg → use it directly.
      - If no `user_id` arg → SKIP the in-app notify entirely and log `logger.debug("no user scope; skipping in-app notification", task="<task_name>", kind="<kind>")`. Telegram still fires as before.
      - DO NOT touch the Site model. DO NOT reference `Site.owner_id` — it does not exist.

    Russian titles per project memory. Kind / title / body / link / severity table:

    | File | Happy kind | Title | Body template | link_url | Severity | Failure kind | Failure severity |
    |------|-----------|-------|---------------|----------|----------|--------------|------------------|
    | crawl_tasks.py | crawl.completed | "Краул завершён" | "Сайт {site.domain}: обработано {n} страниц" | /sites/{site_id}/crawl | info | crawl.failed | error |
    | position_tasks.py | position_check.completed | "Проверка позиций завершена" | "Сайт {site.domain}: проверено {n} ключей" | /sites/{site_id}/positions | info | position_check.failed | error |
    | client_report_tasks.py | client_pdf.ready | "Клиентский PDF готов" | "Инструкции для {site.domain} готовы" | /client-reports/{report_id} | info | client_pdf.failed | error |
    | audit_tasks.py | audit.completed | "Аудит завершён" | "Сайт {site.domain}: аудит закончен" | /sites/{site_id}/audit | info | audit.failed | error |
    | suggest_tasks.py | keyword_suggest.ready | "Подсказки ключей готовы" | "Seed «{seed}»: получено {n} подсказок" | /keyword-suggest/{job_id} | info | keyword_suggest.failed | error |
    | llm_tasks.py | llm_brief.ready | "AI-бриф готов" | "Бриф для {topic} сгенерирован" | /llm-briefs/{brief_id} | info | llm_brief.failed | error |

    Additional wiring inside position_tasks.py: `_send_drop_alerts()` also calls `notify(kind="position_drop", severity="warning", ...)` IF a user_id is in scope at that call site; if not, skip silently (this helper currently takes only `site_id` — so in practice it will skip. Document in the code comment that drop alerts remain Telegram-only until a future phase plumbs user_id through).

    Pattern (use AsyncSession from existing task DB scope where tasks are async; for sync tasks use `asyncio.run()` on a small async wrapper, matching the pattern already in `client_report_tasks.py::generate_client_pdf`):
    ```python
    from app.services.notifications import notify
    ...
    # at end of happy path
    if user_id is not None:
        await notify(
            db=session,
            user_id=user_id,
            kind="crawl.completed",
            title="Краул завершён",
            body=f"Сайт {site.domain}: обработано {pages_count} страниц",
            link_url=f"/sites/{site.id}/crawl",
            site_id=site.id,
            severity="info",
        )
    else:
        logger.debug("no user scope; skipping in-app notification", task="crawl", kind="crawl.completed")
    # exception handler — same shape, kind=*.failed, severity="error"
    ```

    Monitoring alert dispatcher — `app/services/change_monitoring_service.py::dispatch_immediate_alerts()`:
      - Currently sends Telegram for error-severity alerts. ADDITIONALLY call `notify(kind="monitoring.alert", severity=<alert.severity>, ...)` inside the same loop.
      - Title: "Алерт мониторинга"; body: f"{site_name}: {change_type} — {details}"; link_url: `/monitoring`; site_id: from the alert.
      - `dispatch_immediate_alerts()` currently has NO user_id in scope — so per revised D-02, emit `logger.debug("no user scope; skipping in-app notification", dispatcher="change_monitoring_service", kind="monitoring.alert")` once per call and do NOT call notify(). Leave a `TODO(Phase 18)` comment: "Plumb user_id via ChangeAlertRule.owner_id when monitoring gains per-user rules."
      - This preserves the additive, non-breaking invariant while documenting the gap honestly.

    DO NOT modify `app/tasks/report_tasks.py` — its tasks are Beat-scheduled digests with no user scope and are explicitly out of scope for Phase 17.
    DO NOT touch any Telegram code paths. notify() is purely additive.
  </action>
  <verify>
    <automated>cd /projects/test && bash -c 'IMPORTS=$(grep -l "from app.services.notifications import notify" app/tasks/crawl_tasks.py app/tasks/position_tasks.py app/tasks/client_report_tasks.py app/tasks/audit_tasks.py app/tasks/suggest_tasks.py app/tasks/llm_tasks.py app/services/change_monitoring_service.py | wc -l); CALLS=$(grep -c "notify(" app/tasks/crawl_tasks.py app/tasks/position_tasks.py app/tasks/client_report_tasks.py app/tasks/audit_tasks.py app/tasks/suggest_tasks.py app/tasks/llm_tasks.py | awk -F: "{sum+=\$2} END {print sum}"); REPORT_UNTOUCHED=$(grep -c "notify(" app/tasks/report_tasks.py 2>/dev/null || echo 0); echo "imports=$IMPORTS calls=$CALLS report_tasks=$REPORT_UNTOUCHED"; test "$IMPORTS" = "7" && test "$CALLS" -ge "12" && test "$REPORT_UNTOUCHED" = "0"'</automated>
  </verify>
  <done>All 6 task files + change_monitoring_service.py import notify; happy path + exception handler in each of the 6 task files calls notify() when user_id is in scope and logs a debug skip otherwise; change_monitoring_service.py emits a debug skip + TODO (no user_id plumbed yet); report_tasks.py untouched; Telegram dispatch unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for notify() wiring</name>
  <files>tests/tasks/test_notify_wiring.py</files>
  <read_first>
    - tests/tasks/ for existing Celery task test patterns (mocking Telegram, factory-boy site/user)
    - Plan 17-01 test files for AsyncSession test fixture
  </read_first>
  <action>
    Create `tests/tasks/test_notify_wiring.py` with:

    Happy-path tests (one per kind, tasks that DO accept user_id):
      - test_crawl_completed_creates_notification
      - test_position_check_completed_creates_notification
      - test_client_pdf_ready_creates_notification
      - test_audit_completed_creates_notification
      - test_keyword_suggest_ready_creates_notification
      - test_llm_brief_ready_creates_notification

    Each test:
      1. Seed user + site (factory-boy)
      2. Mock Telegram dispatch (respx or unittest.mock — must NOT make real calls)
      3. Invoke task function directly (not via Celery worker) passing user_id + site_id
      4. Assert exactly one Notification row with expected kind, user_id, site_id, severity, link_url containing site_id

    Failure-path test:
      - test_crawl_failed_creates_error_notification (raise inside task, assert kind='crawl.failed' severity='error')

    Skip-on-no-scope tests (CRITICAL — these pin the revised D-02 behaviour):
      - test_crawl_task_without_user_id_skips_inapp_but_telegram_fires — invoke with user_id=None, assert zero Notification rows, assert Telegram mock was called
      - test_monitoring_dispatch_skips_inapp — call `dispatch_immediate_alerts()` with error alerts, assert zero Notification rows, assert Telegram mock was called, assert debug log emitted
  </action>
  <verify>
    <automated>cd /projects/test && pytest tests/tasks/test_notify_wiring.py -x</automated>
  </verify>
  <done>All wiring tests pass; failure-path + skip-on-no-scope (including monitoring dispatcher) covered.</done>
</task>

</tasks>

<verification>
- `pytest tests/tasks/test_notify_wiring.py -x` green
- `grep -c "await notify(" app/tasks/crawl_tasks.py app/tasks/position_tasks.py app/tasks/client_report_tasks.py app/tasks/audit_tasks.py app/tasks/suggest_tasks.py app/tasks/llm_tasks.py | awk -F: '{sum+=$2} END {print sum}'` shows ≥ 12 (6 tasks × 2 paths minimum)
- `grep -c "notify(" app/tasks/report_tasks.py` returns 0 (digest tasks untouched)
- Telegram code untouched: `git diff --stat app/tasks/ app/services/change_monitoring_service.py` shows no removed lines from telegram/send_message functions
- `grep -n "owner_id" app/models/site.py` returns nothing (we did not add it)
</verification>

<success_criteria>
NOTIF-02 satisfied for the tasks that have a user scope: feed shows crawl/position_check/client_pdf/audit/keyword_suggest/llm_brief events. Tasks without a user scope (monitoring dispatch today, Beat digests always) skip in-app silently while Telegram continues. Telegram remains additive.
</success_criteria>

<output>
Create `.planning/phases/17-in-app-notifications/17-03-SUMMARY.md` after completion.
</output>
