---
phase: 17-in-app-notifications
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - app/tasks/crawl_tasks.py
  - app/tasks/position_tasks.py
  - app/tasks/report_tasks.py
  - app/tasks/client_report_tasks.py
  - app/tasks/audit_tasks.py
  - app/tasks/suggest_tasks.py
  - app/tasks/llm_tasks.py
  - app/services/monitoring_alerts.py
  - tests/tasks/test_notify_wiring.py
autonomous: true
requirements: [NOTIF-02]
must_haves:
  truths:
    - "Crawl completion calls notify() with kind='crawl.completed'"
    - "Position check completion calls notify() with kind='position_check.completed'"
    - "PDF report completion calls notify() with kind='pdf.ready'"
    - "Client PDF completion calls notify() with kind='client_pdf.ready'"
    - "Audit completion calls notify() with kind='audit.completed'"
    - "Keyword suggest completion calls notify() with kind='keyword_suggest.ready'"
    - "LLM brief completion calls notify() with kind='llm_brief.ready'"
    - "Monitoring alerts call notify() with kind='monitoring.alert' and severity='warning' or 'error'"
    - "Telegram dispatch is unchanged (additive only)"
  artifacts:
    - path: "app/tasks/crawl_tasks.py"
      provides: "notify() call at task finalization"
      pattern: "from app.services.notifications import notify"
  key_links:
    - from: "app/tasks/*.py"
      to: "app.services.notifications.notify"
      via: "import + call at end of happy path and exception handler"
      pattern: "await notify\\("
---

<objective>
Wire `notify()` calls into all Celery tasks listed in CONTEXT.md so completion events surface in the in-app feed (additive to Telegram).

Purpose: Satisfies NOTIF-02 (feed lists crawl/position_check/pdf/monitoring events).
Output: 7 Celery task files + monitoring dispatcher updated; integration tests verify notifications are inserted.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/17-in-app-notifications/17-CONTEXT.md
@app/tasks/crawl_tasks.py
@app/tasks/position_tasks.py
@app/tasks/report_tasks.py
@app/tasks/client_report_tasks.py
@app/tasks/audit_tasks.py
@app/tasks/suggest_tasks.py
@app/tasks/llm_tasks.py
</context>

<interfaces>
From Plan 17-01 (app/services/notifications.py):
```python
async def notify(
    db: AsyncSession,
    user_id: UUID,
    kind: str,
    title: str,
    body: str,
    link_url: str,
    site_id: UUID | None = None,
    severity: Literal["info", "warning", "error"] = "info",
) -> Notification
```

Per D-02: `user_id` = the user who triggered the task. For scheduled tasks (Beat) with no initiator, fall back to `Site.owner_id`. If neither exists, skip the in-app notify (Telegram still fires).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire notify() into 7 Celery tasks + monitoring alert dispatcher</name>
  <files>app/tasks/crawl_tasks.py, app/tasks/position_tasks.py, app/tasks/report_tasks.py, app/tasks/client_report_tasks.py, app/tasks/audit_tasks.py, app/tasks/suggest_tasks.py, app/tasks/llm_tasks.py, app/services/monitoring_alerts.py</files>
  <read_first>
    - app/tasks/crawl_tasks.py (find finalization point + Telegram dispatch — notify() goes alongside, NOT replacing)
    - app/tasks/position_tasks.py
    - app/tasks/report_tasks.py
    - app/tasks/client_report_tasks.py
    - app/tasks/audit_tasks.py
    - app/tasks/suggest_tasks.py
    - app/tasks/llm_tasks.py
    - Search for monitoring alert dispatcher: `grep -rn "telegram" app/services/ app/tasks/ | grep -i "alert\\|monitor"` to locate it (likely `app/services/monitoring_alerts.py` or inside a tasks file)
    - Each task to identify: how user_id is available (task arg? pulled from Site.owner_id?), where the existing happy-path completion is, where the exception handler is
  </read_first>
  <action>
    For EACH of the 7 task files below, at the END of the happy path AND in the exception handler, add an `await notify(...)` call alongside the existing Telegram dispatch (additive, do not remove Telegram).

    User scoping (D-02):
      - If task accepts `user_id` arg → use it
      - Else if task has `site_id` → load Site, use `site.owner_id`
      - Else → skip in-app notify, log debug "no user scope; skipping in-app notification"

    Use this kind/title/body/link/severity table (Russian titles per project memory):

    | File | Happy kind | Title | Body template | link_url | Severity | Failure kind | Failure severity |
    |------|-----------|-------|---------------|----------|----------|--------------|------------------|
    | crawl_tasks.py | crawl.completed | "Краул завершён" | "Сайт {site.domain}: обработано {n} страниц" | /sites/{site_id}/crawl | info | crawl.failed | error |
    | position_tasks.py | position_check.completed | "Проверка позиций завершена" | "Сайт {site.domain}: проверено {n} ключей" | /sites/{site_id}/positions | info | position_check.failed | error |
    | report_tasks.py | pdf.ready | "PDF-отчёт готов" | "Отчёт по {site.domain} готов к скачиванию" | /reports/{report_id} | info | pdf.failed | error |
    | client_report_tasks.py | client_pdf.ready | "Клиентский PDF готов" | "Инструкции для {site.domain} готовы" | /client-reports/{report_id} | info | client_pdf.failed | error |
    | audit_tasks.py | audit.completed | "Аудит завершён" | "Сайт {site.domain}: аудит закончен" | /sites/{site_id}/audit | info | audit.failed | error |
    | suggest_tasks.py | keyword_suggest.ready | "Подсказки ключей готовы" | "Seed «{seed}»: получено {n} подсказок" | /keyword-suggest/{job_id} | info | keyword_suggest.failed | error |
    | llm_tasks.py | llm_brief.ready | "AI-бриф готов" | "Бриф для {topic} сгенерирован" | /llm-briefs/{brief_id} | info | llm_brief.failed | error |

    Pattern (use AsyncSession from existing task DB scope):
    ```python
    from app.services.notifications import notify
    ...
    # at end of happy path
    if user_id is not None:
        await notify(
            db=session,
            user_id=user_id,
            kind="crawl.completed",
            title="Краул завершён",
            body=f"Сайт {site.domain}: обработано {pages_count} страниц",
            link_url=f"/sites/{site.id}/crawl",
            site_id=site.id,
            severity="info",
        )
    # in exception handler — same shape, kind=*.failed, severity="error"
    ```

    Monitoring alert dispatcher (`app/services/monitoring_alerts.py` — locate via grep above):
      - At every place that currently sends a Telegram alert, ALSO call `notify(kind="monitoring.alert", severity="warning" or "error" depending on alert level)`.
      - Title: "Алерт мониторинга", body: alert message, link_url: `/monitoring`, site_id: relevant site if any.

    DO NOT touch any Telegram code paths. notify() is purely additive.
  </action>
  <verify>
    <automated>cd /projects/test && grep -l "from app.services.notifications import notify" app/tasks/crawl_tasks.py app/tasks/position_tasks.py app/tasks/report_tasks.py app/tasks/client_report_tasks.py app/tasks/audit_tasks.py app/tasks/suggest_tasks.py app/tasks/llm_tasks.py | wc -l | grep -q 7</automated>
  </verify>
  <done>All 7 task files import notify; happy path + exception handler in each calls notify() with the kind from the table; Telegram dispatch unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for notify() wiring</name>
  <files>tests/tasks/test_notify_wiring.py</files>
  <read_first>
    - tests/tasks/ for existing Celery task test patterns (mocking Telegram, factory-boy site/user)
    - Plan 17-01 test files for AsyncSession test fixture
  </read_first>
  <action>
    Create `tests/tasks/test_notify_wiring.py` with one test per kind (happy path):
      - test_crawl_completed_creates_notification
      - test_position_check_completed_creates_notification
      - test_pdf_ready_creates_notification
      - test_client_pdf_ready_creates_notification
      - test_audit_completed_creates_notification
      - test_keyword_suggest_ready_creates_notification
      - test_llm_brief_ready_creates_notification
      - test_monitoring_alert_creates_notification (severity warning + error variants)

    Each test:
      1. Seed user + site (factory-boy)
      2. Mock Telegram dispatch (respx or unittest.mock — must NOT make real calls)
      3. Invoke task function directly (not via Celery worker) with user_id + site_id
      4. Assert exactly one Notification row exists with the expected kind, user_id, site_id, severity, and link_url containing the site_id

    Plus 2 failure-path tests:
      - test_crawl_failed_creates_error_notification (raise inside task, assert kind='crawl.failed' severity='error')
      - test_no_user_scope_skips_inapp (call task with user_id=None and site without owner_id, assert NO notification row but Telegram still called)
  </action>
  <verify>
    <automated>cd /projects/test && pytest tests/tasks/test_notify_wiring.py -x</automated>
  </verify>
  <done>All wiring tests pass; failure-path + skip-on-no-scope covered.</done>
</task>

</tasks>

<verification>
- `pytest tests/tasks/test_notify_wiring.py -x` green
- `grep -c "await notify(" app/tasks/*.py` shows ≥ 14 (7 tasks × 2 paths)
- Telegram code untouched: `git diff --stat app/tasks/` shows no removed lines from telegram functions
</verification>

<success_criteria>
NOTIF-02 satisfied: feed shows crawl/position_check/pdf/client_pdf/audit/keyword_suggest/llm_brief/monitoring events. Telegram remains additive.
</success_criteria>

<output>
Create `.planning/phases/17-in-app-notifications/17-03-SUMMARY.md` after completion.
</output>
