---
phase: 21-site-audit-intake
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - app/templates/sites/index.html
  - app/templates/sites/detail.html
  - app/main.py
autonomous: true
requirements:
  - INTAKE-05
must_haves:
  truths:
    - "Site list page shows intake status column with green checkmark for complete and gray dash for absent/draft"
    - "Site detail page shows intake badge and link to intake form"
    - "Intake badge uses existing badge-connected/badge-unknown CSS classes"
  artifacts:
    - path: "app/templates/sites/index.html"
      provides: "Intake status column in site list table"
      contains: "intake"
    - path: "app/templates/sites/detail.html"
      provides: "Intake badge and link on site detail page"
      contains: "intake"
  key_links:
    - from: "app/main.py (ui_site_list)"
      to: "app/services/intake_service.py"
      via: "get_intake_statuses_for_sites batch query"
      pattern: "intake_statuses"
    - from: "app/main.py (ui_site_overview)"
      to: "app/services/intake_service.py"
      via: "get_or_create_intake query"
      pattern: "intake"
---

<objective>
Integrate intake status badges into the existing site list and site detail pages.

Purpose: Make intake completion visible at a glance (INTAKE-05, D-13) — users see which sites have completed intake directly from the site list and detail pages.
Output: Site list shows intake status column; site detail shows intake badge with link to form.
</objective>

<execution_context>
@/opt/seo-platform/.claude/get-shit-done/workflows/execute-plan.md
@/opt/seo-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-site-audit-intake/21-CONTEXT.md
@.planning/phases/21-site-audit-intake/21-UI-SPEC.md
@.planning/phases/21-site-audit-intake/21-01-SUMMARY.md

<interfaces>
From app/services/intake_service.py (Plan 01):
```python
async def get_or_create_intake(db: AsyncSession, *, site_id: uuid.UUID) -> SiteIntake
async def get_intake_statuses_for_sites(db: AsyncSession, *, site_ids: list[uuid.UUID]) -> dict[uuid.UUID, str]
```

From app/models/site_intake.py:
```python
class IntakeStatus(str, PyEnum):
    draft = "draft"
    complete = "complete"
```

From app/templates/sites/index.html (site list table headers, line 13-27):
```html
<table class="min-w-full divide-y divide-gray-200" data-filterable>
  <thead class="bg-gray-50">
    <tr>
      <th>Name</th> <th>Keywords</th> <th>Crawls</th> <th>Tasks</th>
      <th>URL</th> <th>Group</th> <th>WP Username</th> <th>Status</th>
      <th>Schedule</th> <th>Active</th> <th>Actions</th>
    </tr>
  </thead>
```

From app/templates/sites/detail.html (client badge area, line 14-34):
```html
<div style="margin-bottom:1rem;display:flex;align-items:center;gap:0.75rem;">
  <span style="font-size:0.85rem;color:#6b7280;font-weight:600;">Клиент:</span>
  {% if client %} ... {% else %} ... {% endif %}
</div>
```

From app/main.py ui_site_list function (~line 396):
- Renders sites/index.html with context: sites, site_metrics, site_groups, schedules

From app/main.py ui_site_overview function (~line 2193):
- Renders sites/detail.html with context: site, health, keyword_count, crawl_count, task_count, client, all_clients
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add intake status to site list page</name>
  <files>app/templates/sites/index.html, app/main.py</files>
  <read_first>
    - app/templates/sites/index.html (full file — table structure, existing columns, site_metrics dict pattern)
    - app/main.py (search for "ui_site_list" or the GET /ui/sites endpoint that renders sites/index.html — need to find where site_metrics is passed to add intake_statuses)
    - app/services/intake_service.py (get_intake_statuses_for_sites function signature)
    - .planning/phases/21-site-audit-intake/21-UI-SPEC.md (badge specs, site list entry point)
  </read_first>
  <action>
    **Step 1: Update the site list endpoint in app/main.py.**

    Find the endpoint that renders `sites/index.html` (likely the GET `/ui/sites` handler). It currently passes `site_metrics`, `site_groups`, `schedules` to the template. Add intake status prefetch:

    Add import at top of function (or at module level):
    ```python
    from app.services import intake_service
    ```

    After the existing site list query, add:
    ```python
    # Intake status prefetch (avoid N+1, per RESEARCH.md pitfall 3)
    intake_statuses = {}
    if sites:
        intake_statuses = await intake_service.get_intake_statuses_for_sites(
            db, site_ids=[s.id for s in sites]
        )
    ```

    Add `"intake_statuses": intake_statuses` to the template context dict.

    **Step 2: Update `app/templates/sites/index.html`.**

    Add a new column header after the "Status" column (or before "Actions"):
    ```html
    <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Intake</th>
    ```

    Add a corresponding table cell in the `{% for site in sites %}` loop, after the Status cell:
    ```html
    <td class="px-3 py-2 text-sm text-center">
      {% set intake_status = intake_statuses.get(site.id) %}
      {% if intake_status == "complete" %}
        <a href="/ui/sites/{{ site.id }}/intake" title="Анкета заполнена" style="color:#059669;text-decoration:none;">
          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>
        </a>
      {% else %}
        <a href="/ui/sites/{{ site.id }}/intake" title="Открыть intake" style="color:#9ca3af;text-decoration:none;">—</a>
      {% endif %}
    </td>
    ```

    This shows a green checkmark (linked) for complete intake and a gray dash (linked) for draft/absent.
  </action>
  <verify>
    <automated>cd /opt/seo-platform && grep -c "intake_statuses" app/templates/sites/index.html && grep -c "intake_statuses" app/main.py && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `app/templates/sites/index.html` contains `<th` with text "Intake" (case-insensitive)
    - Template references `intake_statuses.get(site.id)` to determine status
    - Complete status shows green checkmark SVG with color `#059669`
    - Draft/absent shows gray dash with color `#9ca3af`
    - Both states link to `/ui/sites/{{ site.id }}/intake`
    - `app/main.py` site list endpoint imports and calls `intake_service.get_intake_statuses_for_sites`
    - Template context includes `intake_statuses` key
  </acceptance_criteria>
  <done>Site list page shows Intake column with green checkmark for complete and gray dash for draft/absent</done>
</task>

<task type="auto">
  <name>Task 2: Add intake badge and link to site detail page</name>
  <files>app/templates/sites/detail.html, app/main.py</files>
  <read_first>
    - app/templates/sites/detail.html (full file — client badge area near line 14-34, stats row, quick actions)
    - app/main.py (ui_site_overview function at ~line 2193 — context variables passed, client fetching pattern)
    - app/services/intake_service.py (get_or_create_intake function)
    - .planning/phases/21-site-audit-intake/21-UI-SPEC.md (site detail entry point spec)
    - .planning/phases/21-site-audit-intake/21-CONTEXT.md (D-13: badge in site detail)
  </read_first>
  <action>
    **Step 1: Update ui_site_overview in app/main.py** (~line 2193).

    Add intake data fetch after the client fetch block:
    ```python
    from app.services import intake_service as _intake_svc
    from app.models.site_intake import IntakeStatus

    intake = await _intake_svc.get_or_create_intake(db, site_id=sid)
    ```

    Note: Using get_or_create means the intake record is created on first visit to site detail too. This is acceptable per D-16 (implicit creation). If this is not desired, use a SELECT-only query instead:
    ```python
    from app.models.site_intake import SiteIntake, IntakeStatus
    from sqlalchemy import select as sa_select
    intake_result = await db.execute(
        sa_select(SiteIntake).where(SiteIntake.site_id == sid)
    )
    intake = intake_result.scalar_one_or_none()
    ```

    Prefer the SELECT-only approach for the detail page (don't auto-create on detail view — only on intake form visit).

    Add `"intake": intake` to the template context dict.

    **Step 2: Update `app/templates/sites/detail.html`.**

    Add intake badge row after the client badge section (after the `</div>` that closes the client badge area, around line 34). Insert:

    ```html
    <!-- Intake badge (D-13) -->
    <div style="margin-bottom:1rem;display:flex;align-items:center;gap:0.75rem;">
      <span style="font-size:0.85rem;color:#6b7280;font-weight:600;">Intake:</span>
      {% if intake and intake.status.value == "complete" %}
        <a href="/ui/sites/{{ site.id }}/intake" style="display:inline-flex;align-items:center;gap:0.3rem;padding:0.2rem 0.6rem;background:#d1fae5;color:#065f46;border-radius:4px;font-size:0.85rem;font-weight:500;text-decoration:none;">
          Анкета заполнена
        </a>
      {% elif intake and intake.status.value == "draft" %}
        <a href="/ui/sites/{{ site.id }}/intake" class="badge badge-unknown" style="font-size:0.8rem;text-decoration:none;">
          Анкета не заполнена
        </a>
      {% else %}
        <a href="/ui/sites/{{ site.id }}/intake" style="color:#4f46e5;font-size:0.85rem;text-decoration:none;">
          Открыть intake
        </a>
      {% endif %}
    </div>
    ```

    This provides:
    - Complete: green badge "Анкета заполнена" linking to intake form
    - Draft: gray badge "Анкета не заполнена" linking to intake form
    - No intake: accent-colored link "Открыть intake"
  </action>
  <verify>
    <automated>cd /opt/seo-platform && grep -c "Анкета заполнена" app/templates/sites/detail.html && grep -c "intake" app/templates/sites/detail.html && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `app/templates/sites/detail.html` contains "Intake:" label with font-weight:600
    - Shows "Анкета заполнена" badge with green background (#d1fae5) when intake is complete
    - Shows "Анкета не заполнена" badge (badge-unknown) when intake is draft
    - Shows "Открыть intake" link when no intake exists
    - All three states link to `/ui/sites/{{ site.id }}/intake`
    - `app/main.py` ui_site_overview function passes `intake` to template context
    - Intake is fetched via SELECT (not get_or_create) to avoid auto-creating on detail view
  </acceptance_criteria>
  <done>Site detail page shows intake badge near client badge, with link to intake form in all states</done>
</task>

</tasks>

<verification>
1. Navigate to `/ui/sites` — "Intake" column visible in table header
2. Sites with complete intake show green checkmark; others show gray dash
3. Navigate to `/ui/sites/{id}` — intake badge visible below client badge
4. Click intake badge/link — navigates to `/ui/sites/{id}/intake`
</verification>

<success_criteria>
- Site list table has Intake column with visual status indicators
- Site detail page shows intake badge matching the 3 states (complete/draft/absent)
- All indicators link to the intake form page
- No N+1 queries on site list (batch prefetch used)
</success_criteria>

<output>
After completion, create `.planning/phases/21-site-audit-intake/21-03-SUMMARY.md`
</output>
