---
phase: v4-03-section-sites
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/crawl/history.html
  - app/templates/crawl/feed.html
  - app/templates/crawl/feed_rows.html
autonomous: true
requirements:
  - SITE-V4-02

must_haves:
  truths:
    - "User can view crawl history for the selected site with Tailwind-styled table"
    - "User can trigger a crawl from the crawl history page via a button"
    - "User can view change feed with filter buttons, all styled with Tailwind"
    - "Feed rows partial works correctly with HTMX swap (hx-target tbody)"
  artifacts:
    - path: "app/templates/crawl/history.html"
      provides: "Tailwind crawl history page with Start Crawl button"
      contains: "Start Crawl"
    - path: "app/templates/crawl/feed.html"
      provides: "Tailwind change feed page with filter buttons"
      contains: "bg-white rounded-lg"
    - path: "app/templates/crawl/feed_rows.html"
      provides: "Tailwind-styled feed rows partial for HTMX"
      contains: "px-3 py-2"
  key_links:
    - from: "app/templates/crawl/history.html"
      to: "/sites/{site_id}/crawl"
      via: "HTMX hx-post for crawl trigger"
      pattern: "hx-post.*crawl"
    - from: "app/templates/crawl/feed.html"
      to: "app/templates/crawl/feed_rows.html"
      via: "Jinja2 include + HTMX swap"
      pattern: "include.*feed_rows"
---

<objective>
Migrate crawl history and change feed templates from inline styles to Tailwind CSS. Add a "Start Crawl" button to the crawl history page (per D-11, since the detail page that previously had this button is being removed).

Purpose: Fulfills SITE-V4-02 (crawl history and schedule accessible within Сайты section) and completes the Tailwind migration for crawl templates (D-10, D-11, D-12).
Output: Three rewritten templates using Tailwind CSS with preserved HTMX functionality.
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v4.md
@.planning/phases/v4-03-section-sites/v4-03-section-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/routers/crawl.py (~line 179):
```python
@router.get("/ui/sites/{site_id}/crawls", response_class=HTMLResponse)
async def ui_site_crawl_history(site_id, request, db):
    # Returns: {"site": site, "jobs": jobs}
    # site = Site model instance (has .id, .name, .url)
    # jobs = list of CrawlJob (has .id, .status.value, .pages_crawled, .started_at, .finished_at)

@router.get("/ui/crawls/{crawl_job_id}", response_class=HTMLResponse)
async def ui_crawl_feed(crawl_job_id, filter="all", request=None, db=...):
    # Returns: {"job": job, "page_rows": [...], "filter": filter}
    # job = CrawlJob instance (has .id, .site_id, .status.value, .pages_crawled)
    # page_rows = [{"page": Page, "snap": PageSnapshot|None}, ...]
    # filter = "all"|"seo_changed"|"content_changed"|"new_pages"|"status_changed"
```

From app/routers/sites.py (~line 199):
```python
@router.post("/{site_id}/crawl", status_code=202)
async def trigger_crawl(site_id: uuid.UUID, use_playwright: bool = False, ...):
    # Existing crawl trigger endpoint — returns {"task_id": ..., "site_id": ..., "mode": ...}
```

Existing Tailwind pattern from app/templates/dashboard/index.html:
- Card: `bg-white rounded-lg shadow-sm border border-gray-200 p-4`
- Table header: `bg-gray-50`, `px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase`
- Badge connected: `bg-emerald-50 text-emerald-700 px-1.5 py-0.5 rounded text-xs font-semibold`
- Badge failed: `bg-red-50 text-red-700 px-1.5 py-0.5 rounded text-xs font-semibold`
- Badge unknown: `bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded text-xs font-semibold`
- Button primary: `px-3 py-1.5 bg-indigo-600 text-white text-sm font-medium rounded-md hover:bg-indigo-700`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite crawl/history.html to Tailwind + add Start Crawl button</name>
  <files>app/templates/crawl/history.html</files>
  <read_first>
    - app/templates/crawl/history.html (current template with inline styles)
    - app/templates/dashboard/index.html (Tailwind reference pattern)
    - app/routers/crawl.py (ui_site_crawl_history handler, context variables: site, jobs)
    - app/routers/sites.py (trigger_crawl endpoint at POST /sites/{site_id}/crawl)
    - app/templates/sites/detail.html (Start Crawl button pattern to extract — line 57-58)
  </read_first>
  <action>
    Rewrite app/templates/crawl/history.html entirely with Tailwind classes per D-10.

    Replace the entire template with:
    ```html
    {% extends "base.html" %}
    {% block title %}Краулы — {{ site.name }}{% endblock %}
    {% block content %}
    <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-4">
      <div class="flex justify-between items-center mb-4">
        <div>
          <h2 class="text-lg font-semibold text-gray-800">История краулов</h2>
          <p class="text-sm text-gray-500 mt-1">
            <a href="{{ site.url }}" target="_blank" class="text-indigo-600 hover:underline">{{ site.name }}</a>
          </p>
        </div>
        <div class="flex gap-2">
          <button class="inline-flex items-center px-3 py-1.5 bg-emerald-600 text-white text-sm font-medium rounded-md hover:bg-emerald-700"
                  hx-post="/sites/{{ site.id }}/crawl"
                  hx-swap="none"
                  onclick="this.textContent='Запуск...'; this.disabled=true"
                  hx-on::after-request="this.textContent='Запустить краул'; this.disabled=false">
            Запустить краул
          </button>
          <a href="/ui/sites" class="inline-flex items-center px-3 py-1.5 bg-gray-100 text-gray-700 text-sm font-medium rounded-md hover:bg-gray-200">
            &larr; Все сайты
          </a>
        </div>
      </div>

      {% if jobs %}
      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200">
          <thead class="bg-gray-50">
            <tr>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase">Started</th>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase">Status</th>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase">Pages</th>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase">Finished</th>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase">Actions</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-gray-100">
            {% for job in jobs %}
            <tr class="hover:bg-gray-50">
              <td class="px-3 py-2 text-sm text-gray-700">{{ job.started_at.strftime("%Y-%m-%d %H:%M") if job.started_at else "—" }}</td>
              <td class="px-3 py-2">
                {% if job.status.value == 'done' %}
                <span class="inline-flex items-center bg-emerald-50 text-emerald-700 px-1.5 py-0.5 rounded text-xs font-semibold">{{ job.status.value }}</span>
                {% elif job.status.value == 'running' %}
                <span class="inline-flex items-center bg-amber-50 text-amber-700 px-1.5 py-0.5 rounded text-xs font-semibold">{{ job.status.value }}</span>
                {% else %}
                <span class="inline-flex items-center bg-red-50 text-red-700 px-1.5 py-0.5 rounded text-xs font-semibold">{{ job.status.value }}</span>
                {% endif %}
              </td>
              <td class="px-3 py-2 text-sm text-gray-700 font-medium">{{ job.pages_crawled }}</td>
              <td class="px-3 py-2 text-sm text-gray-700">{{ job.finished_at.strftime("%Y-%m-%d %H:%M") if job.finished_at else "—" }}</td>
              <td class="px-3 py-2">
                <a href="/ui/crawls/{{ job.id }}" class="inline-flex items-center px-2 py-1 bg-indigo-50 text-indigo-700 text-xs font-medium rounded hover:bg-indigo-100">View Changes</a>
              </td>
            </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
      {% else %}
      <p class="text-gray-400 text-sm mt-2">No crawl jobs yet for this site.</p>
      {% endif %}
    </div>
    {% endblock %}
    ```

    Key changes (per D-10, D-11):
    - All inline styles replaced with Tailwind classes
    - "Запустить краул" button added with `hx-post="/sites/{{ site.id }}/crawl"` and `hx-swap="none"` (D-11) — uses the existing trigger_crawl endpoint in sites router
    - Badge colors use the v4 palette (emerald for done, amber for running, red for failed)
    - Back link changed to "Все сайты" pointing to /ui/sites
    - Zero `style=` attributes
  </action>
  <verify>
    <automated>cd /projects/test && python -c "
import pathlib
content = pathlib.Path('app/templates/crawl/history.html').read_text()
assert 'style=' not in content, 'Found inline style= in history.html'
assert 'hx-post=\"/sites/{{ site.id }}/crawl\"' in content or 'hx-post' in content, 'Missing crawl trigger button'
assert 'bg-white rounded-lg' in content, 'Missing Tailwind card pattern'
assert 'divide-y divide-gray' in content, 'Missing Tailwind table dividers'
assert 'Запустить краул' in content or 'Start Crawl' in content, 'Missing start crawl button text'
print('history.html validation passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - app/templates/crawl/history.html contains NO `style=` attributes
    - app/templates/crawl/history.html contains `hx-post` with `/crawl` endpoint for the start crawl button
    - app/templates/crawl/history.html contains `bg-white rounded-lg shadow-sm` Tailwind card pattern
    - app/templates/crawl/history.html contains `min-w-full divide-y` table pattern
    - app/templates/crawl/history.html contains crawl trigger button text
  </acceptance_criteria>
  <done>
    Crawl history page fully migrated to Tailwind with working "Запустить краул" button that triggers the existing crawl endpoint via HTMX.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite crawl/feed.html and feed_rows.html to Tailwind</name>
  <files>app/templates/crawl/feed.html, app/templates/crawl/feed_rows.html</files>
  <read_first>
    - app/templates/crawl/feed.html (current template with inline styles)
    - app/templates/crawl/feed_rows.html (current HTMX partial)
    - app/routers/crawl.py (ui_crawl_feed handler, context variables: job, page_rows, filter)
    - app/templates/dashboard/index.html (Tailwind reference pattern)
  </read_first>
  <action>
    **1. Rewrite app/templates/crawl/feed.html with Tailwind (per D-10):**

    Replace the entire template:
    ```html
    {% extends "base.html" %}
    {% block title %}Change Feed — Crawl {{ job.id }}{% endblock %}
    {% block content %}
    <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-4">
      <div class="flex justify-between items-center mb-4">
        <div>
          <h2 class="text-lg font-semibold text-gray-800">Change Feed</h2>
          <p class="text-sm text-gray-500 mt-1">
            Job {{ job.id }} &middot;
            {{ job.status.value }} &middot;
            {{ job.pages_crawled }} pages
          </p>
        </div>
        <a href="/ui/sites/{{ job.site_id }}/crawls"
           class="inline-flex items-center px-3 py-1.5 bg-gray-100 text-gray-700 text-sm font-medium rounded-md hover:bg-gray-200">
          &larr; История краулов
        </a>
      </div>

      <!-- Filter buttons -->
      <div class="flex gap-2 flex-wrap mb-4">
        {% for f in ["all", "seo_changed", "content_changed", "new_pages", "status_changed"] %}
        <button
          class="px-3 py-1.5 text-sm font-medium rounded-md {% if filter == f %}bg-indigo-600 text-white{% else %}bg-gray-100 text-gray-700 hover:bg-gray-200{% endif %}"
          hx-get="/ui/crawls/{{ job.id }}?filter={{ f }}"
          hx-target="#pages-tbody"
          hx-swap="innerHTML"
          hx-push-url="false"
        >{{ f.replace("_", " ").title() }}</button>
        {% endfor %}
      </div>

      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200">
          <thead class="bg-gray-50">
            <tr>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase">URL</th>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase">Title</th>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase">Status</th>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase">Type</th>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase">Changes</th>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase">Flags</th>
            </tr>
          </thead>
          <tbody id="pages-tbody" class="divide-y divide-gray-100">
            {% include "crawl/feed_rows.html" %}
          </tbody>
        </table>
      </div>
    </div>
    {% endblock %}
    ```

    Key changes:
    - All inline styles replaced with Tailwind
    - Filter buttons use `bg-indigo-600 text-white` for active, `bg-gray-100 text-gray-700` for inactive
    - HTMX attributes preserved exactly: `hx-get`, `hx-target="#pages-tbody"`, `hx-swap="innerHTML"`, `hx-push-url="false"`
    - Back link points to `/ui/sites/{{ job.site_id }}/crawls` with "История краулов" text
    - Zero `style=` attributes

    **2. Rewrite app/templates/crawl/feed_rows.html with Tailwind (per D-12):**

    Replace the entire partial:
    ```html
    {% for row in page_rows %}
    {% set page = row.page %}
    {% set snap = row.snap %}
    {% set diff = snap.diff_data if snap else None %}
    <tr class="hover:bg-gray-50">
      <td class="px-3 py-2 text-sm max-w-xs truncate">
        <a href="{{ page.url }}" target="_blank" class="text-indigo-600 hover:underline">{{ page.url }}</a>
      </td>
      <td class="px-3 py-2 text-sm text-gray-700">{{ page.title or "—" }}</td>
      <td class="px-3 py-2">
        {% if page.http_status %}
          {% if page.http_status == 200 %}
          <span class="inline-flex items-center bg-emerald-50 text-emerald-700 px-1.5 py-0.5 rounded text-xs font-semibold">{{ page.http_status }}</span>
          {% elif page.http_status >= 400 %}
          <span class="inline-flex items-center bg-red-50 text-red-700 px-1.5 py-0.5 rounded text-xs font-semibold">{{ page.http_status }}</span>
          {% else %}
          <span class="inline-flex items-center bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded text-xs font-semibold">{{ page.http_status }}</span>
          {% endif %}
        {% else %}—{% endif %}
      </td>
      <td class="px-3 py-2">
        <span class="inline-flex items-center bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded text-xs font-semibold">{{ page.page_type.value }}</span>
      </td>
      <td class="px-3 py-2">
        {% if diff %}
          {% for field, change in diff.items() %}
          <span class="inline-flex items-center bg-red-50 text-red-700 px-1.5 py-0.5 rounded text-xs font-semibold mr-1" title="{{ field }}: {{ change.old }} → {{ change.new }}">
            {{ field }}
          </span>
          {% endfor %}
        {% elif snap and snap.diff_data is none %}
          <span class="inline-flex items-center bg-emerald-50 text-emerald-700 px-1.5 py-0.5 rounded text-xs font-semibold">new</span>
        {% else %}
          —
        {% endif %}
      </td>
      <td class="px-3 py-2">
        {% if page.has_schema %}<span class="inline-flex items-center bg-emerald-50 text-emerald-700 px-1.5 py-0.5 rounded text-xs font-semibold mr-1" title="Schema.org">S</span>{% endif %}
        {% if page.has_toc %}<span class="inline-flex items-center bg-emerald-50 text-emerald-700 px-1.5 py-0.5 rounded text-xs font-semibold mr-1" title="Table of Contents">TOC</span>{% endif %}
        {% if page.has_noindex %}<span class="inline-flex items-center bg-red-50 text-red-700 px-1.5 py-0.5 rounded text-xs font-semibold" title="noindex">NI</span>{% endif %}
      </td>
    </tr>
    {% else %}
    <tr><td colspan="6" class="px-3 py-4 text-gray-400 text-sm text-center">No pages match this filter.</td></tr>
    {% endfor %}
    ```

    Key changes:
    - All inline styles replaced with Tailwind classes
    - URL cell uses `max-w-xs truncate` instead of `max-width:300px;word-break:break-all`
    - Badge classes use the consistent v4 palette
    - HTMX swap pattern preserved — this partial is loaded into `#pages-tbody` by feed.html filter buttons
    - Zero `style=` attributes
  </action>
  <verify>
    <automated>cd /projects/test && python -c "
import pathlib
feed = pathlib.Path('app/templates/crawl/feed.html').read_text()
rows = pathlib.Path('app/templates/crawl/feed_rows.html').read_text()
assert 'style=' not in feed, 'Found inline style= in feed.html'
assert 'style=' not in rows, 'Found inline style= in feed_rows.html'
assert 'hx-get' in feed, 'Missing HTMX filter buttons in feed.html'
assert 'hx-target=\"#pages-tbody\"' in feed, 'Missing HTMX target in feed.html'
assert 'hx-swap=\"innerHTML\"' in feed, 'Missing HTMX swap in feed.html'
assert 'include \"crawl/feed_rows.html\"' in feed, 'Missing feed_rows include'
assert 'bg-white rounded-lg' in feed, 'Missing Tailwind card pattern in feed.html'
assert 'px-3 py-2' in rows, 'Missing Tailwind cell padding in feed_rows.html'
assert 'bg-emerald-50' in rows, 'Missing Tailwind badge color in feed_rows.html'
print('feed.html and feed_rows.html validation passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - app/templates/crawl/feed.html contains NO `style=` attributes
    - app/templates/crawl/feed.html contains `hx-get`, `hx-target="#pages-tbody"`, `hx-swap="innerHTML"` for filter buttons
    - app/templates/crawl/feed.html contains `include "crawl/feed_rows.html"` for initial row rendering
    - app/templates/crawl/feed.html contains `bg-indigo-600 text-white` for active filter button
    - app/templates/crawl/feed_rows.html contains NO `style=` attributes
    - app/templates/crawl/feed_rows.html contains `px-3 py-2` cell classes
    - app/templates/crawl/feed_rows.html contains `bg-emerald-50 text-emerald-700` badge pattern
    - app/templates/crawl/feed_rows.html contains the Jinja2 `{% for row in page_rows %}` loop
  </acceptance_criteria>
  <done>
    Change feed page and feed rows partial fully migrated to Tailwind. All HTMX interactions (filter buttons, tbody swap) preserved. Zero inline styles.
  </done>
</task>

</tasks>

<verification>
1. `grep -c 'style=' app/templates/crawl/history.html app/templates/crawl/feed.html app/templates/crawl/feed_rows.html` — all return 0
2. `grep 'hx-post.*crawl' app/templates/crawl/history.html` — confirms crawl trigger button
3. `grep 'hx-get.*filter' app/templates/crawl/feed.html` — confirms filter buttons preserved
4. `grep 'include.*feed_rows' app/templates/crawl/feed.html` — confirms partial include
5. `grep 'pages-tbody' app/templates/crawl/feed.html app/templates/crawl/feed_rows.html` — confirms HTMX target consistency
</verification>

<success_criteria>
- Crawl history page renders with Tailwind styling and a working "Запустить краул" button
- Change feed page renders with Tailwind styling and working filter buttons
- Feed rows partial uses Tailwind classes and works correctly with HTMX tbody swap
- All three templates have zero inline style= attributes
- All existing HTMX interactions preserved (filter buttons, crawl trigger)
</success_criteria>

<output>
After completion, create `.planning/phases/v4-03-section-sites/v4-03-02-SUMMARY.md`
</output>
