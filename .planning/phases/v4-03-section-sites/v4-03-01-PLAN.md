---
phase: v4-03-section-sites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/navigation.py
  - app/main.py
  - app/templates/sites/index.html
  - app/templates/sites/detail.html
  - app/templates/sites/schedule.html
autonomous: true
requirements:
  - SITE-V4-01
  - SITE-V4-03

must_haves:
  truths:
    - "Sidebar Сайты section has 3 children: Список сайтов, Краулы, Расписание"
    - "Sites-detail child is removed from NAV_SECTIONS"
    - "Site list table shows keyword count, crawl count, and open tasks count per row"
    - "Site list uses Tailwind classes instead of inline styles"
    - "Navigating to /ui/sites/{site_id}/detail redirects to /ui/sites"
    - "Schedule page exists at /ui/sites/{site_id}/schedule with crawl and position schedule controls"
  artifacts:
    - path: "app/navigation.py"
      provides: "Updated NAV_SECTIONS with 3 children for sites section"
      contains: "sites-schedule"
    - path: "app/templates/sites/index.html"
      provides: "Tailwind-based site list with metrics columns"
      contains: "grid"
    - path: "app/templates/sites/schedule.html"
      provides: "Schedule management page for a site"
      contains: "Crawl Schedule"
    - path: "app/main.py"
      provides: "Schedule page route + detail redirect"
      contains: "ui_site_schedule"
  key_links:
    - from: "app/navigation.py"
      to: "app/templates/components/sidebar.html"
      via: "NAV_SECTIONS sites children"
      pattern: "sites-schedule"
    - from: "app/main.py"
      to: "app/templates/sites/schedule.html"
      via: "TemplateResponse"
      pattern: "sites/schedule.html"
    - from: "app/templates/sites/index.html"
      to: "app/main.py ui_sites handler"
      via: "template context variables"
      pattern: "site_metrics"
---

<objective>
Update sidebar navigation for the Сайты section, redesign the site list page with Tailwind and inline metrics, create a new schedule management page, and remove the standalone site detail page.

Purpose: Fulfills SITE-V4-01 (site management via sidebar sub-items) and SITE-V4-03 (detail page removal with functionality redistribution).
Output: Updated navigation config, Tailwind site list with metrics, new schedule page, detail page redirect.
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v4.md
@.planning/STATE.md
@.planning/phases/v4-03-section-sites/v4-03-section-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/navigation.py:
```python
NAV_SECTIONS = [
    # ... sections list with id, label, icon, url, admin_only, children
    {
        "id": "sites",
        "label": "Сайты",
        "icon": "globe-alt",
        "url": None,
        "admin_only": False,
        "children": [
            {"id": "sites-list", "label": "Список сайтов", "url": "/ui/sites"},
            {"id": "sites-detail", "label": "Детали сайта", "url": "/ui/sites/{site_id}/detail"},
            {"id": "sites-crawls", "label": "Краулы", "url": "/ui/sites/{site_id}/crawls"},
        ],
    },
]
```

From app/template_engine.py:
```python
templates = _NavAwareTemplates(_jinja_templates)
# Usage: templates.TemplateResponse(request, "template.html", {context_dict})
# Auto-injects: nav_sections, selected_site_id, active_section, active_child, breadcrumb_*
```

From app/main.py (site detail handler ~line 2104):
```python
@app.get("/ui/sites/{site_id}/detail", response_class=HTMLResponse)
async def ui_site_detail(site_id: str, request: Request, db: AsyncSession = Depends(get_db)):
    # Fetches keyword_count, crawl_count, task_count, schedules, recent_crawls, recent_tasks
```

From app/services/schedule_service.py:
```python
async def get_schedule(db, site_id) -> Schedule | None
async def get_position_schedule(db, site_id) -> Schedule | None
async def upsert_schedule(db, site_id, schedule_type) -> Schedule
async def get_all_schedules(db) -> list[Schedule]
```

From app/models/schedule.py:
```python
class ScheduleType(str, Enum):
    manual = "manual"
    daily = "daily"
    weekly = "weekly"
    every_12h = "every_12h"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update NAV_SECTIONS + site list redesign with metrics + schedule page route</name>
  <files>app/navigation.py, app/main.py, app/templates/sites/index.html, app/templates/sites/schedule.html</files>
  <read_first>
    - app/navigation.py (current NAV_SECTIONS definition)
    - app/main.py (ui_sites handler at ~line 173, ui_site_detail at ~line 2104, ui_update_schedule at ~line 344)
    - app/templates/sites/index.html (current site list template)
    - app/templates/sites/detail.html (metrics and schedules to extract)
    - app/templates/dashboard/index.html (v4 Tailwind reference pattern)
    - app/template_engine.py (template context auto-injection)
    - app/services/schedule_service.py (get_schedule, get_position_schedule, upsert_schedule signatures)
    - app/models/schedule.py (ScheduleType enum values)
  </read_first>
  <action>
    **1. Update NAV_SECTIONS in app/navigation.py (per D-01, D-02):**

    Replace the "sites" section children from:
    ```python
    {"id": "sites-list", "label": "Список сайтов", "url": "/ui/sites"},
    {"id": "sites-detail", "label": "Детали сайта", "url": "/ui/sites/{site_id}/detail"},
    {"id": "sites-crawls", "label": "Краулы", "url": "/ui/sites/{site_id}/crawls"},
    ```
    To:
    ```python
    {"id": "sites-list", "label": "Список сайтов", "url": "/ui/sites"},
    {"id": "sites-crawls", "label": "Краулы", "url": "/ui/sites/{site_id}/crawls"},
    {"id": "sites-schedule", "label": "Расписание", "url": "/ui/sites/{site_id}/schedule"},
    ```
    Remove sites-detail entirely. Add sites-schedule as third child.

    **2. Redesign app/templates/sites/index.html to Tailwind (per D-04, D-05, D-07):**

    Rewrite the template using explicit Tailwind classes (same pattern as dashboard/index.html). Keep table layout. Keep existing columns (Name, URL, Group, WP Username, Status, Schedule, Active, Actions). Add 3 new columns after Name:
    - Keywords (integer count, text-indigo-600 font-bold)
    - Crawls (integer count, text-emerald-600 font-bold)
    - Tasks (integer count, text-amber-600 font-bold)

    Table structure:
    ```html
    <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-4">
      <div class="flex justify-between items-center mb-4">
        <h2 class="text-lg font-semibold text-gray-800">WordPress Sites</h2>
        <a href="/ui/sites/new" class="inline-flex items-center px-3 py-1.5 bg-indigo-600 text-white text-sm font-medium rounded-md hover:bg-indigo-700">+ Add Site</a>
      </div>
      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200">
          <thead class="bg-gray-50">
            <tr>
              <th class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase">Name</th>
              <th ...>Keywords</th>
              <th ...>Crawls</th>
              <th ...>Tasks</th>
              <th ...>URL</th>
              <th ...>Group</th>
              <th ...>Status</th>
              <th ...>Schedule</th>
              <th ...>Active</th>
              <th ...>Actions</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-gray-100">
            {% for site in sites %}
            <tr class="hover:bg-gray-50">
              <!-- Name links to /ui/sites NOT detail -->
              <td class="px-3 py-2"><span class="font-medium text-indigo-600">{{ site.name }}</span></td>
              <td class="px-3 py-2 text-sm text-indigo-600 font-bold">{{ site_metrics[site.id|string].keywords }}</td>
              <td class="px-3 py-2 text-sm text-emerald-600 font-bold">{{ site_metrics[site.id|string].crawls }}</td>
              <td class="px-3 py-2 text-sm text-amber-600 font-bold">{{ site_metrics[site.id|string].tasks }}</td>
              ...
            </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
    </div>
    ```

    Remove all inline `style=` attributes. Remove the `<style>` block at the bottom. Use Tailwind utility classes for schedule select, badges, buttons.

    Badge styles: connection status uses `bg-emerald-50 text-emerald-700` for connected, `bg-red-50 text-red-700` for failed, `bg-gray-100 text-gray-600` for unknown. Group badge: `bg-sky-50 text-sky-700`.

    Action buttons: use Tailwind `px-2 py-1 text-xs font-medium rounded` with appropriate bg colors. Keep all existing HTMX attributes (hx-post, hx-patch, hx-delete, hx-target, hx-swap, hx-confirm) exactly as they are.

    Site name should NOT link to detail page anymore — just display as text with `font-medium text-indigo-600` (per D-06, detail is removed).

    **3. Update ui_sites handler in app/main.py (~line 173):**

    After fetching sites, compute per-site metrics. Add after the existing `site_groups` logic:
    ```python
    # Compute per-site metrics for index table (D-04, D-07)
    from app.services.keyword_service import count_keywords
    from app.models.crawl import CrawlJob
    from app.models.task import SeoTask, TaskStatus
    from sqlalchemy import select as sa_select, func
    import uuid as _uuid

    site_metrics = {}
    for site_obj in sites:
        sid = site_obj.id
        kw_count = await count_keywords(db, sid)
        crawl_count = (await db.execute(
            sa_select(func.count()).select_from(CrawlJob).where(CrawlJob.site_id == sid)
        )).scalar() or 0
        task_count = (await db.execute(
            sa_select(func.count()).select_from(SeoTask).where(
                SeoTask.site_id == sid,
                SeoTask.status.in_([TaskStatus.open, TaskStatus.assigned, TaskStatus.in_progress]),
            )
        )).scalar() or 0
        site_metrics[str(sid)] = {"keywords": kw_count, "crawls": crawl_count, "tasks": task_count}
    ```

    Pass `site_metrics` to the template context:
    ```python
    return templates.TemplateResponse(
        request, "sites/index.html",
        {"sites": sites, "schedules": schedules, "site_groups": site_groups, "site_metrics": site_metrics}
    )
    ```

    **4. Create schedule page at app/templates/sites/schedule.html (per D-01, D-09):**

    Create a per-site schedule management page. Pattern: show selected site name, crawl schedule dropdown, position check schedule dropdown. Use the same HTMX pattern from detail.html schedules section but with Tailwind styling.

    Template structure:
    ```html
    {% extends "base.html" %}
    {% block title %}Расписание — {{ site.name }}{% endblock %}
    {% block content %}
    <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
      <div class="flex justify-between items-center mb-6">
        <div>
          <h2 class="text-lg font-semibold text-gray-800">Расписание</h2>
          <p class="text-sm text-gray-500 mt-1">
            <a href="{{ site.url }}" target="_blank" class="text-indigo-600 hover:underline">{{ site.name }}</a>
          </p>
        </div>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <!-- Crawl Schedule -->
        <div class="bg-gray-50 rounded-lg p-4 border border-gray-200">
          <label class="block text-sm font-medium text-gray-700 mb-2">Расписание краулов</label>
          <select hx-put="/ui/sites/{{ site.id }}/schedule"
                  hx-target="#crawl-schedule-result" hx-swap="innerHTML"
                  hx-vals='js:{"schedule_type": event.target.value}'
                  name="schedule_type"
                  class="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:ring-indigo-500 focus:border-indigo-500">
            {% for opt in ["manual", "daily", "weekly"] %}
            <option value="{{ opt }}" {% if crawl_schedule == opt %}selected{% endif %}>{{ opt | capitalize }}</option>
            {% endfor %}
          </select>
          <span id="crawl-schedule-result" class="text-xs text-emerald-600 mt-1 block"></span>
        </div>

        <!-- Position Check Schedule -->
        <div class="bg-gray-50 rounded-lg p-4 border border-gray-200">
          <label class="block text-sm font-medium text-gray-700 mb-2">Расписание проверки позиций</label>
          <select id="pos-schedule-select"
                  class="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:ring-indigo-500 focus:border-indigo-500"
                  onchange="updatePosSchedule(this.value)">
            {% for opt in ["manual", "daily", "weekly", "every_12h"] %}
            <option value="{{ opt }}" {% if position_schedule == opt %}selected{% endif %}>{{ opt | replace('_', ' ') | capitalize }}</option>
            {% endfor %}
          </select>
          <span id="pos-schedule-result" class="text-xs text-emerald-600 mt-1 block"></span>
        </div>
      </div>
    </div>

    <script>
    async function updatePosSchedule(value) {
      const resp = await fetch('/positions/sites/{{ site.id }}/schedule', {
        method: 'PUT',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({schedule_type: value})
      });
      const el = document.getElementById('pos-schedule-result');
      if (resp.ok) {
        el.innerHTML = '<span class="text-emerald-600">Сохранено</span>';
      } else {
        el.innerHTML = '<span class="text-red-600">Ошибка</span>';
      }
      setTimeout(() => el.innerHTML = '', 3000);
    }
    </script>
    {% endblock %}
    ```

    **5. Add schedule page route in app/main.py:**

    Add a new route handler (near the site-related routes, after ui_update_schedule):
    ```python
    @app.get("/ui/sites/{site_id}/schedule", response_class=HTMLResponse)
    async def ui_site_schedule(site_id: str, request: Request, db: AsyncSession = Depends(get_db)) -> HTMLResponse:
        import uuid as _uuid
        from app.services.schedule_service import get_schedule, get_position_schedule
        site = await get_site(db, _uuid.UUID(site_id))
        if not site:
            return HTMLResponse("Site not found", status_code=404)
        crawl_sched = await get_schedule(db, _uuid.UUID(site_id))
        pos_sched = await get_position_schedule(db, _uuid.UUID(site_id))
        return templates.TemplateResponse(request, "sites/schedule.html", {
            "site": {"id": str(site.id), "name": site.name, "url": site.url},
            "crawl_schedule": crawl_sched.schedule_type.value if crawl_sched else "manual",
            "position_schedule": pos_sched.schedule_type.value if pos_sched else "manual",
        })
    ```

    **6. Replace detail page route with redirect (per D-06, D-08):**

    Replace the `ui_site_detail` handler (~line 2104) with a simple 301 redirect:
    ```python
    @app.get("/ui/sites/{site_id}/detail", response_class=HTMLResponse)
    async def ui_site_detail(site_id: str, request: Request, db: AsyncSession = Depends(get_db)):
        return RedirectResponse(url="/ui/sites", status_code=301)
    ```

    Do NOT delete detail.html yet — just remove the route's rendering. The template file can be cleaned up separately.

    **7. Update dashboard/index.html sites_overview links:**

    In dashboard/index.html, the Sites Overview table links to `/ui/sites/{{ s.id }}/detail` — change this to `/ui/sites` since detail page is removed:
    ```html
    <a href="/ui/sites" class="font-semibold text-indigo-600 hover:underline">{{ s.name }}</a>
    ```
  </action>
  <verify>
    <automated>cd /projects/test && python -c "
from app.navigation import NAV_SECTIONS, resolve_nav_context
sites_section = [s for s in NAV_SECTIONS if s['id'] == 'sites'][0]
children_ids = [c['id'] for c in sites_section['children']]
assert 'sites-list' in children_ids, 'Missing sites-list'
assert 'sites-crawls' in children_ids, 'Missing sites-crawls'
assert 'sites-schedule' in children_ids, 'Missing sites-schedule'
assert 'sites-detail' not in children_ids, 'sites-detail should be removed'
assert len(children_ids) == 3, f'Expected 3 children, got {len(children_ids)}'
# Test resolve_nav_context for schedule page
ctx = resolve_nav_context('/ui/sites/abc-123/schedule')
assert ctx['active_section'] == 'sites', f'Wrong section: {ctx}'
print('NAV_SECTIONS validation passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - app/navigation.py NAV_SECTIONS sites children are exactly: sites-list, sites-crawls, sites-schedule
    - app/navigation.py does NOT contain "sites-detail"
    - app/templates/sites/index.html contains "site_metrics" template variable references
    - app/templates/sites/index.html contains "bg-white rounded-lg shadow-sm" (Tailwind card pattern)
    - app/templates/sites/index.html does NOT contain any 'style=' attributes
    - app/templates/sites/schedule.html exists and contains "Расписание"
    - app/templates/sites/schedule.html contains "hx-put" for HTMX schedule update
    - app/main.py contains "def ui_site_schedule"
    - app/main.py contains "site_metrics" in the ui_sites handler
    - app/main.py ui_site_detail returns RedirectResponse with status_code=301
    - app/templates/dashboard/index.html does NOT contain "/detail" links
  </acceptance_criteria>
  <done>
    Sidebar shows 3 children for Сайты (Список, Краулы, Расписание). Site list page uses Tailwind with keyword/crawl/task counts per row. Schedule page renders at /ui/sites/{site_id}/schedule with both schedule controls. Old detail URL 301-redirects to /ui/sites. Dashboard site links updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Site-dependent placeholder for Краулы and Расписание when no site selected</name>
  <files>app/routers/crawl.py</files>
  <read_first>
    - app/routers/crawl.py (ui_site_crawl_history handler at ~line 179)
    - app/main.py (ui_site_schedule handler just created in Task 1)
    - app/template_engine.py (selected_site_id injection pattern)
  </read_first>
  <action>
    Per D-03: Краулы and Расписание are site-dependent. When the user clicks these sidebar items without a site selected (site_id in URL is placeholder "#"), they should see a placeholder message.

    The sidebar already resolves `{site_id}` to `#` and sets `disabled: True` when no site is selected (see build_sidebar_sections in navigation.py). The sidebar template renders disabled items as non-clickable. So the placeholder behavior is already handled by the existing sidebar logic.

    However, verify that the crawl history route and schedule route handle edge cases gracefully. If a user somehow navigates to `/ui/sites//crawls` or an invalid UUID, the existing 404 handling in crawl.py line 191 (`raise HTTPException(status_code=404)`) covers it.

    No code changes needed for this task — the existing sidebar disabled-link mechanism from v4-01 handles the D-03 requirement. Document this in verification.
  </action>
  <verify>
    <automated>cd /projects/test && python -c "
from app.navigation import build_sidebar_sections
# When no site selected, site-dependent URLs should be '#' and disabled
sections = build_sidebar_sections(None, True)
sites_section = [s for s in sections if s['id'] == 'sites'][0]
crawls_child = [c for c in sites_section['children'] if c['id'] == 'sites-crawls'][0]
schedule_child = [c for c in sites_section['children'] if c['id'] == 'sites-schedule'][0]
assert crawls_child['url'] == '#', f'Crawls should be # when no site: {crawls_child[\"url\"]}'
assert crawls_child['disabled'] == True, 'Crawls should be disabled when no site'
assert schedule_child['url'] == '#', f'Schedule should be # when no site: {schedule_child[\"url\"]}'
assert schedule_child['disabled'] == True, 'Schedule should be disabled when no site'
# When site IS selected, URLs should be resolved
sections2 = build_sidebar_sections('abc-123', True)
sites2 = [s for s in sections2 if s['id'] == 'sites'][0]
crawls2 = [c for c in sites2['children'] if c['id'] == 'sites-crawls'][0]
schedule2 = [c for c in sites2['children'] if c['id'] == 'sites-schedule'][0]
assert 'abc-123' in crawls2['url'], f'Crawls URL should contain site_id: {crawls2[\"url\"]}'
assert 'abc-123' in schedule2['url'], f'Schedule URL should contain site_id: {schedule2[\"url\"]}'
print('Site-dependent placeholder validation passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - build_sidebar_sections(None, True) returns sites-crawls and sites-schedule with url="#" and disabled=True
    - build_sidebar_sections("some-uuid", True) returns sites-crawls and sites-schedule with resolved URLs containing the UUID
  </acceptance_criteria>
  <done>
    Краулы and Расписание sidebar items show as disabled with "#" href when no site is selected, and resolve to proper URLs when a site is selected. No separate placeholder page needed — sidebar disabled state handles it.
  </done>
</task>

</tasks>

<verification>
1. `python -c "from app.navigation import NAV_SECTIONS; ..."` — validates NAV_SECTIONS structure
2. `grep -c 'style=' app/templates/sites/index.html` returns 0 (no inline styles)
3. `grep 'site_metrics' app/templates/sites/index.html` finds metric references
4. `grep 'sites/schedule.html' app/main.py` confirms route exists
5. `grep 'RedirectResponse' app/main.py` near ui_site_detail confirms redirect
6. `grep '/detail' app/templates/dashboard/index.html` returns no matches
</verification>

<success_criteria>
- Sidebar Сайты section shows exactly 3 children: Список сайтов, Краулы, Расписание
- Site list page renders with Tailwind styling and per-site keyword/crawl/task metrics
- Schedule page accessible at /ui/sites/{site_id}/schedule with working schedule controls
- Old /ui/sites/{site_id}/detail URL returns 301 redirect to /ui/sites
- Site-dependent sidebar items disabled when no site selected
</success_criteria>

<output>
After completion, create `.planning/phases/v4-03-section-sites/v4-03-01-SUMMARY.md`
</output>
