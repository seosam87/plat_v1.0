---
phase: v4-07-settings-section
plan: 02
type: execute
wave: 2
depends_on: ["v4-07-01"]
files_modified:
  - app/templates/admin/users.html
  - app/templates/admin/groups.html
  - app/templates/admin/proxy.html
  - app/templates/admin/partials/proxy_row.html
  - app/templates/admin/partials/proxy_section.html
autonomous: true
requirements:
  - CFG-V4-01

must_haves:
  truths:
    - "Users page renders with Tailwind cards, tables, modals — zero style= attributes"
    - "Groups page renders with Tailwind cards, tables, modals — zero style= attributes"
    - "Proxy page renders proxy table, add form, credential widgets — zero style= attributes"
    - "All HTMX interactions (user create/edit/deactivate, group CRUD, proxy add/delete/check) still function"
    - "Modals use classList toggle (hidden/flex), not style.display"
  artifacts:
    - path: "app/templates/admin/users.html"
      provides: "Users management page with Tailwind CSS"
      contains: "bg-white rounded-lg shadow-sm"
    - path: "app/templates/admin/groups.html"
      provides: "Groups management page with Tailwind CSS"
      contains: "bg-white rounded-lg shadow-sm"
    - path: "app/templates/admin/proxy.html"
      provides: "Proxy management page (split from settings.html)"
      contains: "bg-white rounded-lg shadow-sm"
    - path: "app/templates/admin/partials/proxy_row.html"
      provides: "Proxy table row partial with Tailwind"
      contains: "px-4 py-3"
    - path: "app/templates/admin/partials/proxy_section.html"
      provides: "Proxy section HTMX partial with Tailwind"
      contains: "min-w-full divide-y"
  key_links:
    - from: "app/templates/admin/proxy.html"
      to: "/admin/proxies"
      via: "HTMX form targets for proxy CRUD"
      pattern: "hx-post=\"/admin/proxies\""
    - from: "app/templates/admin/users.html"
      to: "/ui/admin/users"
      via: "HTMX form targets for user CRUD"
      pattern: "hx-post"
---

<objective>
Migrate users.html, groups.html to Tailwind CSS, and create the new proxy.html (from the proxy portion of settings.html) with Tailwind. Also migrate proxy partials. This covers the larger, more complex Settings templates.

Purpose: Complete Tailwind migration for user management, group management, and proxy management pages.
Output: 3 full templates + 2 partials with zero style= attributes, all using established Tailwind patterns.
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v4-07-settings-section/v4-07-CONTEXT.md
@.planning/phases/v4-07-settings-section/v4-07-RESEARCH.md
@.planning/phases/v4-07-settings-section/v4-07-01-SUMMARY.md

<interfaces>
<!-- Tailwind patterns established across v4-02 through v4-06 -->

Card container:
```html
<div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6">
```

Table:
```html
<div class="overflow-x-auto">
  <table class="min-w-full divide-y divide-gray-200 text-sm">
    <thead class="bg-gray-50">
      <tr><th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Col</th></tr>
    </thead>
    <tbody class="bg-white divide-y divide-gray-100">
      <tr><td class="px-4 py-3 text-gray-700">Val</td></tr>
    </tbody>
  </table>
</div>
```

Modal (classList toggle):
```html
<div id="modal-id" class="hidden fixed inset-0 bg-black/50 z-50 items-center justify-center">
  <div class="bg-white rounded-lg shadow-xl p-6 max-w-md w-full mx-4">...</div>
</div>
<script>
// Open: el.classList.remove('hidden'); el.classList.add('flex');
// Close: el.classList.add('hidden'); el.classList.remove('flex');
</script>
```

Badges:
```html
<span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-amber-100 text-amber-800">admin</span>
<span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-emerald-100 text-emerald-700">Active</span>
<span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-700">Inactive</span>
```

Form inputs:
```html
<label class="block text-sm font-medium text-gray-700 mb-1">Label</label>
<input type="text" name="field" required class="block w-full rounded-md border border-gray-300 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500">
```

Buttons:
```html
<button class="bg-indigo-600 hover:bg-indigo-700 text-white text-sm font-medium px-4 py-2 rounded-md">Primary</button>
<button class="bg-red-500 hover:bg-red-600 text-white text-xs font-medium px-3 py-1.5 rounded">Delete</button>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate users.html and groups.html to Tailwind CSS</name>
  <files>app/templates/admin/users.html, app/templates/admin/groups.html</files>
  <read_first>
    - app/templates/admin/users.html
    - app/templates/admin/groups.html
    - app/templates/admin/settings.html (for reference of current style patterns)
  </read_first>
  <action>
**users.html (191 lines, 53 style= attributes):**

Rewrite the entire template keeping the same `{% extends "base.html" %}` structure and all Jinja2 logic/variables/HTMX attributes intact. Replace every `style=` attribute with Tailwind utility classes:

1. Page header: Replace `style="display:flex; justify-content:space-between"` with `class="flex justify-between items-center mb-6"`
2. User table: Replace inline-styled `<table>` with the standard Tailwind table pattern (min-w-full divide-y divide-gray-200)
3. Role badges: Use the role badge Tailwind classes — `bg-amber-100 text-amber-800` for admin, `bg-blue-100 text-blue-800` for manager, `bg-gray-100 text-gray-600` for client
4. Status badges: `bg-emerald-100 text-emerald-700` for active, `bg-red-100 text-red-700` for inactive
5. Action buttons in table rows: small Tailwind buttons (`bg-indigo-600 hover:bg-indigo-700 text-white text-xs font-medium px-3 py-1.5 rounded`)
6. Add User modal: Replace `style.display` JS with classList toggle pattern. Modal container: `class="hidden fixed inset-0 bg-black/50 z-50 items-center justify-center"`. Open: `classList.remove('hidden'); classList.add('flex')`. Close: `classList.add('hidden'); classList.remove('flex')`.
7. Edit User modal: Same classList pattern
8. All form inputs inside modals: Tailwind form classes (`block w-full rounded-md border border-gray-300 px-3 py-2 text-sm`)
9. Card wrapper: `bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6`
10. Group assignment checkboxes/badges in user edit: Tailwind styled

**CRITICAL:** Preserve ALL Jinja2 template variables ({{ user.email }}, {{ user.role.value }}, {% for user in users %}, etc.), ALL hx-* attributes, ALL form action URLs, ALL onclick handlers (converted to classList pattern). Do not change any backend interaction — only visual styling.

**groups.html (167 lines, 48 style= attributes):**

Same approach — rewrite keeping all Jinja2/HTMX intact:

1. Page header with "Создать группу" button: flex layout with Tailwind
2. Groups table: standard Tailwind table pattern
3. Group name, site count, user count columns
4. Action buttons (edit, delete): small Tailwind buttons
5. Create Group modal: classList toggle pattern (hidden/flex)
6. Edit Group modal: classList toggle pattern
7. Site checkboxes in group forms: Tailwind styled
8. User assignment section in group edit: Tailwind styled
9. Card wrapper: `bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6`

**CRITICAL for both templates:**
- Zero `style=` attributes in final output
- Zero `.style.display` in JavaScript — use classList only
- Keep all existing `.btn`, `.btn-sm`, `.btn-primary` class references REPLACED with explicit Tailwind classes (do not keep the old CSS classes)
- Keep all `.badge` class references REPLACED with inline Tailwind badge classes
  </action>
  <verify>
    <automated>cd /projects/test && echo "=== users.html style= count ===" && grep -c 'style=' app/templates/admin/users.html || echo "0" && echo "=== groups.html style= count ===" && grep -c 'style=' app/templates/admin/groups.html || echo "0" && echo "=== users.html style.display count ===" && grep -c 'style\.display' app/templates/admin/users.html || echo "0" && echo "=== groups.html style.display count ===" && grep -c 'style\.display' app/templates/admin/groups.html || echo "0" && echo "=== users.html classList check ===" && grep -c 'classList' app/templates/admin/users.html && echo "=== groups.html classList check ===" && grep -c 'classList' app/templates/admin/groups.html && echo "=== Tailwind card check ===" && grep -c 'bg-white rounded-lg shadow-sm' app/templates/admin/users.html && grep -c 'bg-white rounded-lg shadow-sm' app/templates/admin/groups.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'style=' app/templates/admin/users.html` returns 0
    - `grep -c 'style=' app/templates/admin/groups.html` returns 0
    - `grep -c 'style\.display' app/templates/admin/users.html` returns 0
    - `grep -c 'style\.display' app/templates/admin/groups.html` returns 0
    - `grep -c 'classList' app/templates/admin/users.html` returns 2+ (modal open/close)
    - `grep -c 'classList' app/templates/admin/groups.html` returns 2+ (modal open/close)
    - `grep 'bg-white rounded-lg shadow-sm' app/templates/admin/users.html` matches
    - `grep 'bg-white rounded-lg shadow-sm' app/templates/admin/groups.html` matches
    - `grep 'min-w-full divide-y' app/templates/admin/users.html` matches (table pattern)
    - `grep 'min-w-full divide-y' app/templates/admin/groups.html` matches (table pattern)
    - `grep 'extends "base.html"' app/templates/admin/users.html` matches (layout inheritance preserved)
    - All Jinja2 for loops, if conditions, and variable references preserved
    - All hx-post, hx-put, hx-delete, hx-target, hx-swap attributes preserved
  </acceptance_criteria>
  <done>users.html and groups.html fully migrated to Tailwind with zero style= attributes, classList modal pattern, and all HTMX/Jinja2 logic preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Create proxy.html from settings.html split + migrate proxy partials</name>
  <files>app/templates/admin/proxy.html, app/templates/admin/partials/proxy_row.html, app/templates/admin/partials/proxy_section.html</files>
  <read_first>
    - app/templates/admin/settings.html (lines 77-199 = proxy section to extract)
    - app/templates/admin/partials/proxy_row.html
    - app/templates/admin/partials/proxy_section.html
  </read_first>
  <action>
**Create app/templates/admin/proxy.html (NEW file, per D-01 and D-09):**

Extract the proxy management portion from settings.html (lines 77-199) into a new standalone template. This includes:
- The proxy table with check-all button (lines 77-131)
- The service credentials grid: XMLProxy, rucaptcha, anticaptcha widgets (lines 134-199)

Structure:
```html
{% extends "base.html" %}
{% block title %}Прокси — Настройки{% endblock %}
{% block content %}
<!-- Proxy Management -->
<div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6">
  <div class="flex justify-between items-center mb-4">
    <h2 class="text-lg font-semibold text-gray-800">Прокси</h2>
    <div class="flex items-center gap-2">
      <span id="check-spinner" class="htmx-indicator text-sm text-gray-500">Checking...</span>
      <button hx-post="/admin/proxies/check-all"
              hx-target="#proxy-section"
              hx-swap="innerHTML"
              hx-indicator="#check-spinner"
              class="bg-indigo-600 hover:bg-indigo-700 text-white text-xs font-medium px-3 py-1.5 rounded">
        Проверить все
      </button>
    </div>
  </div>

  <div id="proxy-section">
    <!-- Proxy table with Tailwind styling -->
    <div class="overflow-x-auto mb-4">
      <table class="min-w-full divide-y divide-gray-200 text-sm">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">URL</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Type</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Response Time</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Last Checked</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
          </tr>
        </thead>
        <tbody id="proxy-table-body" class="bg-white divide-y divide-gray-100">
          {% for p in proxies %}
            {% include "admin/partials/proxy_row.html" %}
          {% endfor %}
        </tbody>
      </table>
    </div>

    <!-- Add proxy form -->
    <form hx-post="/admin/proxies"
          hx-target="#proxy-table-body"
          hx-swap="beforeend"
          class="flex gap-3 items-end flex-wrap mb-4">
      <div>
        <label class="block text-sm font-medium text-gray-700 mb-1">Proxy URL</label>
        <input type="text" name="url" required placeholder="http://user:pass@host:port"
               class="block rounded-md border border-gray-300 px-3 py-2 text-sm min-w-[280px] focus:outline-none focus:ring-2 focus:ring-indigo-500">
      </div>
      <div>
        <label class="block text-sm font-medium text-gray-700 mb-1">Type</label>
        <select name="proxy_type"
                class="block rounded-md border border-gray-300 px-3 py-2 text-sm bg-white focus:outline-none focus:ring-2 focus:ring-indigo-500">
          <option value="http">HTTP</option>
          <option value="socks5">SOCKS5</option>
        </select>
      </div>
      <button type="submit" class="bg-indigo-600 hover:bg-indigo-700 text-white text-xs font-medium px-3 py-1.5 rounded">Добавить</button>
    </form>
  </div>
</div>

<!-- Service Credentials grid -->
<div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
  <!-- XMLProxy widget -->
  <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-4">
    <h3 class="text-base font-semibold text-gray-800 mb-3">XMLProxy.ru</h3>
    <form hx-post="/admin/proxies/credentials/xmlproxy" class="mb-3">
      ... (login + api key fields with Tailwind form classes)
      <button type="submit" class="bg-indigo-600 hover:bg-indigo-700 text-white text-xs font-medium px-3 py-1.5 rounded">Сохранить</button>
    </form>
    <div id="xmlproxy-balance" hx-get="/admin/proxies/xmlproxy-balance" hx-trigger="load" hx-swap="innerHTML" class="text-sm text-gray-500">Loading...</div>
  </div>
  <!-- rucaptcha widget (same pattern) -->
  <!-- anticaptcha widget (same pattern) -->
</div>
{% endblock %}
```

**CRITICAL:** All hx-post targets remain `/admin/proxies/*` (NOT `/ui/admin/proxies/*`). These are backend API routes in proxy_admin.py and must NOT change (per Research Pitfall 3).

**Migrate proxy_row.html partial:**

Read the current file and replace all `style=` with Tailwind classes. Each `<tr>` row should use:
- `<td class="px-4 py-3 text-gray-700">` for cells
- Status badges: `bg-emerald-100 text-emerald-700` for active, `bg-red-100 text-red-700` for dead, `bg-gray-100 text-gray-500` for unknown
- Action buttons: small Tailwind buttons for check/delete

Preserve all hx-put, hx-delete, hx-post, hx-target, hx-swap attributes exactly as-is.

**Migrate proxy_section.html partial:**

This partial is returned as HTMX response body. Replace all `style=` with Tailwind classes matching the proxy table pattern in proxy.html. Must use same table structure (min-w-full divide-y) and same add-proxy form pattern.
  </action>
  <verify>
    <automated>cd /projects/test && echo "=== proxy.html exists ===" && test -f app/templates/admin/proxy.html && echo "YES" || echo "NO" && echo "=== proxy.html style= count ===" && grep -c 'style=' app/templates/admin/proxy.html || echo "0" && echo "=== proxy_row style= count ===" && grep -c 'style=' app/templates/admin/partials/proxy_row.html || echo "0" && echo "=== proxy_section style= count ===" && grep -c 'style=' app/templates/admin/partials/proxy_section.html || echo "0" && echo "=== HTMX targets preserved ===" && grep -c 'hx-post="/admin/proxies' app/templates/admin/proxy.html && echo "=== Tailwind card check ===" && grep -c 'bg-white rounded-lg shadow-sm' app/templates/admin/proxy.html</automated>
  </verify>
  <acceptance_criteria>
    - File `app/templates/admin/proxy.html` exists
    - `grep -c 'style=' app/templates/admin/proxy.html` returns 0
    - `grep -c 'style=' app/templates/admin/partials/proxy_row.html` returns 0
    - `grep -c 'style=' app/templates/admin/partials/proxy_section.html` returns 0
    - `grep 'hx-post="/admin/proxies"' app/templates/admin/proxy.html` matches (HTMX target NOT /ui/admin/)
    - `grep 'hx-post="/admin/proxies/check-all"' app/templates/admin/proxy.html` matches
    - `grep 'hx-post="/admin/proxies/credentials/xmlproxy"' app/templates/admin/proxy.html` matches
    - `grep 'hx-post="/admin/proxies/credentials/rucaptcha"' app/templates/admin/proxy.html` matches
    - `grep 'bg-white rounded-lg shadow-sm' app/templates/admin/proxy.html` matches
    - `grep 'min-w-full divide-y' app/templates/admin/proxy.html` matches
    - `grep 'extends "base.html"' app/templates/admin/proxy.html` matches
    - proxy_row.html preserves hx-delete and hx-post for individual proxy actions
    - proxy_section.html has min-w-full divide-y table pattern
  </acceptance_criteria>
  <done>New proxy.html created from settings.html proxy section with full Tailwind styling. Both proxy partials migrated to Tailwind. Zero style= attributes. All HTMX endpoints preserved unchanged.</done>
</task>

</tasks>

<verification>
1. All 5 template files have zero `style=` attributes
2. All 5 template files have zero `style.display` in JS
3. All HTMX form targets point to correct backend routes (/admin/proxies/*, NOT /ui/admin/*)
4. All Jinja2 template inheritance, loops, conditionals preserved
5. Modal interactions use classList toggle pattern
</verification>

<success_criteria>
- users.html, groups.html fully migrated to Tailwind (zero style=)
- New proxy.html created with Tailwind from settings.html proxy section
- proxy_row.html and proxy_section.html partials migrated to Tailwind
- All HTMX interactions preserved
- All modal JS uses classList, not style.display
</success_criteria>

<output>
After completion, create `.planning/phases/v4-07-settings-section/v4-07-02-SUMMARY.md`
</output>
