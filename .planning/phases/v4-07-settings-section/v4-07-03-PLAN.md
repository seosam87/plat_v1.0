---
phase: v4-07-settings-section
plan: 03
type: execute
wave: 2
depends_on: ["v4-07-01"]
files_modified:
  - app/templates/admin/parameters.html
  - app/templates/admin/issues.html
  - app/templates/admin/audit.html
  - app/templates/datasources/index.html
autonomous: true
requirements:
  - CFG-V4-01

must_haves:
  truths:
    - "Parameters page renders crawler/SERP settings and integration status with Tailwind — zero style= attributes"
    - "Issues page renders platform issues table with status badges — zero style= attributes"
    - "Audit log page renders filtered log entries — zero style= attributes"
    - "Data sources page renders integration status and SERP usage — zero style= attributes (except style=width:X% for progress bar)"
    - "All existing HTMX interactions and form submissions still function"
  artifacts:
    - path: "app/templates/admin/parameters.html"
      provides: "Parameters page (split from settings.html) with Tailwind CSS"
      contains: "bg-white rounded-lg shadow-sm"
    - path: "app/templates/admin/issues.html"
      provides: "Platform issues page with Tailwind CSS"
      contains: "bg-white rounded-lg shadow-sm"
    - path: "app/templates/admin/audit.html"
      provides: "Audit log page with Tailwind CSS"
      contains: "min-w-full divide-y"
    - path: "app/templates/datasources/index.html"
      provides: "Data sources page with Tailwind CSS"
      contains: "bg-white rounded-lg shadow-sm"
  key_links:
    - from: "app/main.py"
      to: "app/templates/admin/parameters.html"
      via: "ui_admin_parameters handler renders this template"
      pattern: "admin/parameters.html"
---

<objective>
Create the new parameters.html (from the parameters portion of settings.html) and migrate issues.html, audit.html, and datasources/index.html to Tailwind CSS. These are the smaller Settings templates.

Purpose: Complete the Tailwind migration for all remaining Settings section pages.
Output: 4 templates with zero style= attributes (except the permitted dynamic width exception in datasources).
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v4-07-settings-section/v4-07-CONTEXT.md
@.planning/phases/v4-07-settings-section/v4-07-RESEARCH.md
@.planning/phases/v4-07-settings-section/v4-07-01-SUMMARY.md

<interfaces>
<!-- Tailwind patterns — same as plan 02 -->

Card: `<div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6">`

Table:
```html
<div class="overflow-x-auto">
  <table class="min-w-full divide-y divide-gray-200 text-sm">
    <thead class="bg-gray-50">
      <tr><th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Col</th></tr>
    </thead>
    <tbody class="bg-white divide-y divide-gray-100">
      <tr><td class="px-4 py-3 text-gray-700">Val</td></tr>
    </tbody>
  </table>
</div>
```

Status badges:
```html
<span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-emerald-100 text-emerald-700">configured</span>
<span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-500">not set</span>
<span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-700">open</span>
<span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-amber-100 text-amber-800">in progress</span>
<span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-emerald-100 text-emerald-700">resolved</span>
```

Info grid card:
```html
<div class="border border-gray-200 rounded-lg p-4">
  <div class="text-sm text-gray-500 mb-1">Label</div>
  <div class="font-semibold text-gray-800">Value</div>
</div>
```

From app/main.py (after plan 01):
```python
# ui_admin_parameters passes: {"settings": settings_data}
# settings_data keys: crawler_delay_ms, crawler_max_pages, serp_max_daily, serp_delay_ms,
#                     gsc_configured, yandex_configured, dataforseo_configured
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create parameters.html and migrate issues.html to Tailwind</name>
  <files>app/templates/admin/parameters.html, app/templates/admin/issues.html</files>
  <read_first>
    - app/templates/admin/settings.html (lines 1-75 = parameters section to extract)
    - app/templates/admin/issues.html
  </read_first>
  <action>
**Create app/templates/admin/parameters.html (NEW file, per D-01 and D-09):**

Extract the parameters/settings portion from settings.html (lines 1-75) into a new standalone template with Tailwind CSS. This includes:
- Crawler settings grid (default mode, delay, max pages)
- SERP parser settings grid (daily limit, delay)
- External integrations status grid (GSC, Yandex, DataForSEO)
- Env-var note paragraph

Structure:
```html
{% extends "base.html" %}
{% block title %}Параметры — Настройки{% endblock %}
{% block content %}
<div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6">
  <h2 class="text-lg font-semibold text-gray-800 mb-6">Параметры платформы</h2>

  <!-- Crawler Settings -->
  <h3 class="text-base font-semibold text-gray-700 mb-4">Crawler</h3>
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mb-8">
    <div class="border border-gray-200 rounded-lg p-4">
      <div class="text-sm text-gray-500 mb-1">Default Mode</div>
      <div class="font-semibold text-gray-800">httpx + BeautifulSoup</div>
      <div class="text-xs text-gray-400 mt-1">Playwright доступен через API: use_playwright=true</div>
    </div>
    <div class="border border-gray-200 rounded-lg p-4">
      <div class="text-sm text-gray-500 mb-1">Delay between pages</div>
      <div class="font-semibold text-gray-800">{{ settings.crawler_delay_ms }} ms</div>
    </div>
    <div class="border border-gray-200 rounded-lg p-4">
      <div class="text-sm text-gray-500 mb-1">Max pages per crawl</div>
      <div class="font-semibold text-gray-800">{{ settings.crawler_max_pages }}</div>
    </div>
  </div>

  <!-- SERP Parser Settings -->
  <h3 class="text-base font-semibold text-gray-700 mb-4">SERP Parser</h3>
  <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 mb-8">
    <div class="border border-gray-200 rounded-lg p-4">
      <div class="text-sm text-gray-500 mb-1">Daily request limit</div>
      <div class="font-semibold text-gray-800">{{ settings.serp_max_daily }}</div>
    </div>
    <div class="border border-gray-200 rounded-lg p-4">
      <div class="text-sm text-gray-500 mb-1">Delay between requests</div>
      <div class="font-semibold text-gray-800">{{ settings.serp_delay_ms }} ms</div>
    </div>
  </div>

  <!-- External Integrations -->
  <h3 class="text-base font-semibold text-gray-700 mb-4">External Integrations</h3>
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mb-8">
    <div class="border border-gray-200 rounded-lg p-4">
      <div class="flex justify-between items-center">
        <span class="text-sm text-gray-700">Google Search Console</span>
        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium {% if settings.gsc_configured %}bg-emerald-100 text-emerald-700{% else %}bg-gray-100 text-gray-500{% endif %}">
          {{ 'configured' if settings.gsc_configured else 'not set' }}
        </span>
      </div>
    </div>
    <!-- Same pattern for Yandex Webmaster and DataForSEO -->
  </div>

  <p class="text-sm text-gray-400">Настройки задаются через переменные окружения (.env). Для изменения значений обновите .env и перезапустите сервис.</p>
</div>
{% endblock %}
```

All Jinja2 variables from the original settings.html parameters section must be preserved: `{{ settings.crawler_delay_ms }}`, `{{ settings.crawler_max_pages }}`, `{{ settings.serp_max_daily }}`, `{{ settings.serp_delay_ms }}`, `{{ settings.gsc_configured }}`, `{{ settings.yandex_configured }}`, `{{ settings.dataforseo_configured }}`.

**Migrate app/templates/admin/issues.html (86 lines, 21 style= attributes):**

Rewrite keeping all Jinja2/HTMX intact. Replace every `style=` with Tailwind:

1. Card wrapper: `bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6`
2. Page header: `flex justify-between items-center mb-6` with h2 and "Создать задачу" button
3. Issues table: standard Tailwind table (min-w-full divide-y)
4. Status badges using issue status colors:
   - open: `bg-red-100 text-red-700`
   - in_progress: `bg-amber-100 text-amber-800`
   - resolved: `bg-emerald-100 text-emerald-700`
5. Action buttons: small Tailwind buttons
6. Create issue form/modal: Tailwind form classes, classList toggle if modal exists
7. Preserve ALL hx-post, hx-target, hx-swap attributes

Zero `style=` attributes in final output. Zero `style.display` in JS (use classList if modals exist).
  </action>
  <verify>
    <automated>cd /projects/test && echo "=== parameters.html exists ===" && test -f app/templates/admin/parameters.html && echo "YES" || echo "NO" && echo "=== parameters.html style= count ===" && grep -c 'style=' app/templates/admin/parameters.html || echo "0" && echo "=== issues.html style= count ===" && grep -c 'style=' app/templates/admin/issues.html || echo "0" && echo "=== parameters.html Jinja2 vars ===" && grep -c 'settings\.' app/templates/admin/parameters.html && echo "=== Tailwind card check ===" && grep -c 'bg-white rounded-lg shadow-sm' app/templates/admin/parameters.html && grep -c 'bg-white rounded-lg shadow-sm' app/templates/admin/issues.html</automated>
  </verify>
  <acceptance_criteria>
    - File `app/templates/admin/parameters.html` exists
    - `grep -c 'style=' app/templates/admin/parameters.html` returns 0
    - `grep -c 'style=' app/templates/admin/issues.html` returns 0
    - `grep 'settings.crawler_delay_ms' app/templates/admin/parameters.html` matches
    - `grep 'settings.crawler_max_pages' app/templates/admin/parameters.html` matches
    - `grep 'settings.serp_max_daily' app/templates/admin/parameters.html` matches
    - `grep 'settings.gsc_configured' app/templates/admin/parameters.html` matches
    - `grep 'bg-white rounded-lg shadow-sm' app/templates/admin/parameters.html` matches
    - `grep 'bg-white rounded-lg shadow-sm' app/templates/admin/issues.html` matches
    - `grep 'extends "base.html"' app/templates/admin/parameters.html` matches
    - issues.html preserves all hx-post and form action attributes
    - issues.html has Tailwind status badges (bg-red-100, bg-amber-100, bg-emerald-100)
  </acceptance_criteria>
  <done>New parameters.html created from settings.html parameters section with Tailwind. Issues.html fully migrated to Tailwind. Zero style= attributes in both files.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate audit.html and datasources/index.html to Tailwind</name>
  <files>app/templates/admin/audit.html, app/templates/datasources/index.html</files>
  <read_first>
    - app/templates/admin/audit.html
    - app/templates/datasources/index.html
  </read_first>
  <action>
**Migrate app/templates/admin/audit.html (64 lines, 14 style= attributes):**

Rewrite keeping all Jinja2/HTMX intact:

1. Card wrapper: `bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6`
2. Page header with h2 "Журнал аудита"
3. Filter controls (action dropdown, user dropdown): Tailwind form select classes (`block rounded-md border border-gray-300 px-3 py-2 text-sm bg-white`)
4. Audit log table: standard Tailwind table (min-w-full divide-y divide-gray-200)
5. Table columns: timestamp, user, action, entity — all with Tailwind td classes
6. Pagination if present: Tailwind styled
7. Preserve all query parameter handling, filter form submissions

Zero `style=` attributes in final output.

**Migrate app/templates/datasources/index.html (66 lines, 22 style= attributes):**

Rewrite keeping all Jinja2/HTMX intact:

1. Card wrapper: `bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6`
2. Page header with h2 "Источники данных"
3. Integration status cards: each data source (GSC, Yandex Webmaster, DataForSEO, Playwright SERP) shown in a grid card with:
   - Source name
   - Status badge: `bg-emerald-100 text-emerald-700` for configured, `bg-gray-100 text-gray-500` for not set
4. SERP usage section with progress bar

**IMPORTANT exception for datasources/index.html:** The SERP usage progress bar uses `style="width:{{ pct|round(1) }}%"` for dynamic Jinja2 percentage. This ONE `style=width:X%` is the permitted exception per established project convention (STATE.md: "Distribution bar dynamic widths kept as style=width:X% — only permitted style= exception for dynamic Jinja2 calculations"). Keep this single style= attribute. All OTHER style= attributes must be replaced with Tailwind classes.

The progress bar itself should use Tailwind for everything except the dynamic width:
```html
<div class="w-full bg-gray-200 rounded-full h-2.5">
  <div class="bg-indigo-600 h-2.5 rounded-full" style="width:{{ pct|round(1) }}%"></div>
</div>
```

Zero `style=` attributes except the one permitted progress bar width.
  </action>
  <verify>
    <automated>cd /projects/test && echo "=== audit.html style= count ===" && grep -c 'style=' app/templates/admin/audit.html || echo "0" && echo "=== datasources style= count (expect 1 for progress bar) ===" && grep -c 'style=' app/templates/datasources/index.html && echo "=== datasources permitted style= ===" && grep 'style=' app/templates/datasources/index.html && echo "=== Tailwind card check ===" && grep -c 'bg-white rounded-lg shadow-sm' app/templates/admin/audit.html && grep -c 'bg-white rounded-lg shadow-sm' app/templates/datasources/index.html && echo "=== Table check ===" && grep -c 'min-w-full divide-y' app/templates/admin/audit.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'style=' app/templates/admin/audit.html` returns 0
    - `grep -c 'style=' app/templates/datasources/index.html` returns 1 (only the progress bar width)
    - `grep 'style=' app/templates/datasources/index.html` shows only `style="width:{{ pct` pattern
    - `grep 'bg-white rounded-lg shadow-sm' app/templates/admin/audit.html` matches
    - `grep 'bg-white rounded-lg shadow-sm' app/templates/datasources/index.html` matches
    - `grep 'min-w-full divide-y' app/templates/admin/audit.html` matches (table)
    - `grep 'extends "base.html"' app/templates/admin/audit.html` matches
    - `grep 'extends "base.html"' app/templates/datasources/index.html` matches
    - All Jinja2 loops and conditionals preserved in both files
    - All filter form submissions and HTMX attributes preserved in audit.html
  </acceptance_criteria>
  <done>audit.html fully migrated to Tailwind (zero style=). datasources/index.html migrated to Tailwind with only the permitted progress bar style=width exception. All functionality preserved.</done>
</task>

</tasks>

<verification>
1. All 4 template files have zero unauthorized `style=` attributes
2. datasources/index.html has exactly 1 `style=` (the progress bar width — permitted exception)
3. All Jinja2 template variables and logic preserved
4. All HTMX interactions preserved
5. New parameters.html renders all 7 settings variables from the handler context
</verification>

<success_criteria>
- parameters.html created with Tailwind from settings.html parameters section
- issues.html fully migrated to Tailwind
- audit.html fully migrated to Tailwind
- datasources/index.html migrated to Tailwind (1 permitted style= exception)
- All existing functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/v4-07-settings-section/v4-07-03-SUMMARY.md`
</output>
