---
phase: v4-09-fix-runtime-route-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/sites/schedule.html
  - app/templates/analytics/index.html
  - app/templates/audit/index.html
  - app/templates/architecture/index.html
  - app/templates/bulk/index.html
  - app/templates/clusters/index.html
  - app/templates/gap/index.html
  - app/templates/intent/index.html
  - app/templates/keywords/index.html
  - app/templates/metrika/index.html
  - app/templates/monitoring/index.html
  - app/templates/pipeline/publish.html
  - app/templates/positions/index.html
  - app/templates/traffic_analysis/index.html
  - app/templates/sites/edit.html
  - app/templates/admin/settings.html
autonomous: true
gap_closure: true
requirements:
  - SITE-V4-02
  - SITE-V4-03

must_haves:
  truths:
    - "Crawl schedule form submits without 405 error"
    - "Back-to-site links in all templates navigate to /ui/sites without 405"
    - "Orphaned admin/settings.html no longer exists"
  artifacts:
    - path: "app/templates/sites/schedule.html"
      provides: "Crawl schedule form with correct hx-post method"
      contains: "hx-post"
    - path: "app/templates/analytics/index.html"
      provides: "Back-to-site links pointing to /ui/sites"
      contains: 'href="/ui/sites"'
  key_links:
    - from: "app/templates/sites/schedule.html"
      to: "@app.post /ui/sites/{site_id}/schedule"
      via: "hx-post attribute"
      pattern: 'hx-post="/ui/sites/.*schedule"'
    - from: "all 14 templates"
      to: "GET /ui/sites"
      via: "href attribute"
      pattern: 'href="/ui/sites"'
---

<objective>
Fix 2 runtime-broken routes found by milestone audit and delete orphaned file.

Purpose: Close SITE-V4-02 (crawl schedule 405) and SITE-V4-03 (back-to-site navigation 405) gaps from v1.0 milestone audit.
Output: All 16 files fixed or deleted; zero 405 errors on schedule save and back-to-site navigation.
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix crawl schedule hx-put to hx-post and fix back-to-site links in all 14 templates</name>
  <files>
    app/templates/sites/schedule.html
    app/templates/analytics/index.html
    app/templates/audit/index.html
    app/templates/architecture/index.html
    app/templates/bulk/index.html
    app/templates/clusters/index.html
    app/templates/gap/index.html
    app/templates/intent/index.html
    app/templates/keywords/index.html
    app/templates/metrika/index.html
    app/templates/monitoring/index.html
    app/templates/pipeline/publish.html
    app/templates/positions/index.html
    app/templates/traffic_analysis/index.html
    app/templates/sites/edit.html
  </files>
  <read_first>
    app/templates/sites/schedule.html
    app/templates/analytics/index.html
    app/templates/audit/index.html
    app/templates/architecture/index.html
    app/templates/bulk/index.html
    app/templates/clusters/index.html
    app/templates/gap/index.html
    app/templates/intent/index.html
    app/templates/keywords/index.html
    app/templates/metrika/index.html
    app/templates/monitoring/index.html
    app/templates/pipeline/publish.html
    app/templates/positions/index.html
    app/templates/traffic_analysis/index.html
    app/templates/sites/edit.html
  </read_first>
  <action>
    **Gap 1 fix (SITE-V4-02):** In `app/templates/sites/schedule.html` line 19, change:
    ```
    hx-put="/ui/sites/{{ site.id }}/schedule"
    ```
    to:
    ```
    hx-post="/ui/sites/{{ site.id }}/schedule"
    ```
    The backend route is `@app.post("/ui/sites/{site_id}/schedule")` — the HTMX attribute must match.

    **Gap 2 fix (SITE-V4-03):** In all 14 templates listed below, change every occurrence of `href="/ui/sites/{{ site.id }}"` or `href="/ui/sites/{{ site_id }}"` to `href="/ui/sites"`. The old URL resolves to a DELETE-only endpoint which returns 405 on GET. The correct destination is the sites list page at `/ui/sites`.

    Files and their specific changes:
    1. `analytics/index.html` — 2 occurrences: breadcrumb line 4 and button line 16
    2. `audit/index.html` — 1 occurrence: button line 12
    3. `architecture/index.html` — 1 occurrence: button line 6
    4. `bulk/index.html` — 1 occurrence: button line 6
    5. `clusters/index.html` — 1 occurrence: breadcrumb line 4 (uses `{{ site_id }}`)
    6. `gap/index.html` — 1 occurrence: button line 6
    7. `intent/index.html` — 1 occurrence: breadcrumb line 7 (uses `{{ site_id }}`)
    8. `keywords/index.html` — 1 occurrence: breadcrumb line 4 (uses `{{ site_id }}`)
    9. `metrika/index.html` — 1 occurrence: breadcrumb line 4
    10. `monitoring/index.html` — 1 occurrence: button line 6
    11. `pipeline/publish.html` — 1 occurrence: breadcrumb line 4
    12. `positions/index.html` — 1 occurrence: breadcrumb line 4 (uses `{{ site_id }}`)
    13. `traffic_analysis/index.html` — 1 occurrence: button line 6
    14. `sites/edit.html` — 1 occurrence: Cancel button line 56
  </action>
  <verify>
    <automated>cd /projects/test && grep -rn 'hx-put.*schedule' app/templates/sites/schedule.html | wc -l | xargs test 0 -eq && grep -rn 'hx-post.*schedule' app/templates/sites/schedule.html | head -1 && echo "--- back-to-site check ---" && grep -rn 'href="/ui/sites/{{ site.id }}"' app/templates/ | wc -l | xargs test 0 -eq && grep -rn 'href="/ui/sites/{{ site_id }}"' app/templates/ | wc -l | xargs test 0 -eq && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'hx-put' app/templates/sites/schedule.html` returns zero matches
    - `grep 'hx-post.*schedule' app/templates/sites/schedule.html` returns exactly 1 match
    - `grep -r 'href="/ui/sites/{{ site.id }}"' app/templates/` returns zero matches
    - `grep -r 'href="/ui/sites/{{ site_id }}"' app/templates/` returns zero matches
    - All 14 templates contain `href="/ui/sites"` where the old back-to-site link was
  </acceptance_criteria>
  <done>Schedule form uses hx-post (not hx-put). All 14 templates link to /ui/sites (not /ui/sites/{id}). Zero 405 errors on these interactions.</done>
</task>

<task type="auto">
  <name>Task 2: Delete orphaned admin/settings.html</name>
  <files>app/templates/admin/settings.html</files>
  <read_first>
    app/templates/admin/settings.html
  </read_first>
  <action>
    Delete `app/templates/admin/settings.html`. This file was superseded by `proxy.html` + `parameters.html` during Phase v4-07 but was never removed. It contains 74 inline style= attributes and is unreachable — the old `/ui/admin/settings` route now returns 301 redirect to `/ui/admin/parameters` (per Phase v4-07-01 decision).

    Verify no other template or Python code references `admin/settings.html` as a Jinja2 template name before deleting:
    ```
    grep -r "admin/settings.html" app/
    ```
    If zero references found, delete the file with `rm app/templates/admin/settings.html`.
  </action>
  <verify>
    <automated>cd /projects/test && test ! -f app/templates/admin/settings.html && echo "DELETED OK" && grep -r "admin/settings.html" app/ | wc -l | xargs test 0 -eq && echo "NO REFERENCES"</automated>
  </verify>
  <acceptance_criteria>
    - `ls app/templates/admin/settings.html` returns "No such file or directory"
    - `grep -r "admin/settings.html" app/` returns zero matches
  </acceptance_criteria>
  <done>Orphaned admin/settings.html deleted. No dangling references in codebase.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'hx-put' app/templates/sites/schedule.html` returns 0 matches
2. `grep -rn 'hx-post.*schedule' app/templates/sites/schedule.html` returns 1 match
3. `grep -rn 'href="/ui/sites/{{ site.id }}"' app/templates/` returns 0 matches
4. `grep -rn 'href="/ui/sites/{{ site_id }}"' app/templates/` returns 0 matches
5. `test ! -f app/templates/admin/settings.html` succeeds
6. `grep -r "admin/settings.html" app/` returns 0 matches
</verification>

<success_criteria>
- SITE-V4-02 satisfied: crawl schedule form submits via hx-post matching @app.post route
- SITE-V4-03 satisfied: all back-to-site links point to /ui/sites (GET-accessible list page)
- Orphaned settings.html removed from codebase
- Zero new functionality introduced — strictly bug fixes and cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/v4-09-fix-runtime-route-gaps/v4-09-01-SUMMARY.md`
</output>
