---
phase: v4-06-section-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/projects/index.html
  - app/templates/projects/kanban.html
  - app/templates/projects/plan.html
autonomous: true
requirements:
  - CNT-V4-02

must_haves:
  truths:
    - "Projects list page renders with Tailwind utility classes and zero style= attributes"
    - "Kanban board renders with Tailwind utility classes and zero style= attributes"
    - "Content plan page renders with Tailwind utility classes and zero style= attributes"
    - "All existing functionality preserved: new project form toggle, HTMX status transitions on Kanban cards, HTMX create-draft button, comment form"
  artifacts:
    - path: "app/templates/projects/index.html"
      provides: "Tailwind-migrated projects list"
      contains: "class=\"bg-white"
    - path: "app/templates/projects/kanban.html"
      provides: "Tailwind-migrated kanban board"
      contains: "grid-cols-5"
    - path: "app/templates/projects/plan.html"
      provides: "Tailwind-migrated content plan"
      contains: "class=\"min-w-full"
  key_links:
    - from: "app/templates/projects/kanban.html"
      to: "/projects/tasks/{task.id}"
      via: "HTMX hx-patch for status transitions"
      pattern: "hx-patch"
    - from: "app/templates/projects/plan.html"
      to: "/projects/plan/{item.id}/create-draft"
      via: "HTMX hx-post for draft creation"
      pattern: "hx-post"
    - from: "app/templates/projects/index.html"
      to: "/ui/projects/new"
      via: "POST form for new project"
      pattern: "action=\"/ui/projects/new\""
---

<objective>
Migrate the three project-domain templates (projects/index.html 80 lines, projects/kanban.html 78 lines, projects/plan.html 49 lines) to pure Tailwind CSS. Replace all inline style= attributes with Tailwind utility classes. Preserve all HTMX interactions (Kanban status transitions, create-draft button), form toggle for new project, and comment form.

Purpose: Complete CNT-V4-02 requirement — projects, kanban, and content plan accessible via sidebar with consistent Tailwind styling.
Output: Three fully Tailwind-migrated templates with zero style= attributes.
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP-v4.md
@.planning/phases/v4-06-section-content/v4-06-CONTEXT.md

<interfaces>
<!-- Tailwind patterns established in v4-02..v4-05 -->
Badge palette: bg-emerald-100/text-emerald-700, bg-red-100/text-red-700, bg-sky-100/text-sky-700, bg-amber-100/text-amber-700
Form inputs: w-full px-2 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500
Buttons: px-3 py-1.5 text-sm font-medium rounded bg-indigo-600 text-white hover:bg-indigo-700
Stat cards: bg-white rounded-lg shadow-sm border border-gray-200 text-center p-4
Tables: min-w-full divide-y divide-gray-200
Modals: hidden div + classList toggle (hidden/flex), never style.display
Form show/hide: classList toggle (hidden), never style.display

<!-- Kanban column background colors (Tailwind equivalents) -->
Open (#f3f4f6) = bg-gray-100
Assigned (#eff6ff) = bg-blue-50
In Progress (#fef3c7) = bg-amber-100
Review (#ede9fe) = bg-violet-100
Done (#d1fae5) = bg-emerald-100

<!-- Badge mapping for project status -->
active = bg-emerald-100 text-emerald-700
completed = bg-gray-100 text-gray-700
other = bg-red-100 text-red-700

<!-- Badge mapping for task types -->
page_404, lost_indexation = bg-red-100 text-red-700
manual = bg-gray-100 text-gray-700
other = bg-emerald-100 text-emerald-700

<!-- Badge mapping for content plan status -->
published = bg-emerald-100 text-emerald-700
writing = bg-amber-100 text-amber-700
other = bg-red-100 text-red-700
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tailwind migration of projects/index.html and projects/plan.html</name>
  <files>app/templates/projects/index.html, app/templates/projects/plan.html</files>
  <read_first>
    - app/templates/projects/index.html (current state — 80 lines, inline styles)
    - app/templates/projects/plan.html (current state — 49 lines, inline styles)
    - app/templates/positions/index.html (reference Tailwind pattern from v4-04)
  </read_first>
  <action>
Migrate projects/index.html (80 lines) to pure Tailwind:

1. Header bar: Replace `style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1.5rem"` with `class="flex justify-between items-center mb-6"`. The h2 gets `class="text-lg font-semibold text-gray-800 m-0"`.

2. New Project button: Replace `onclick="document.getElementById('new-project-form').style.display='block'"` with `onclick="document.getElementById('new-project-form').classList.remove('hidden')"`. Per D-04 and v4-04 pattern — classList toggle, never style.display.

3. New project form: Replace `style="display:none; ..."` with `class="hidden mb-6 p-4 bg-gray-50 rounded-lg"`. Inner flex wrapper: `class="flex gap-3 flex-wrap items-end"`. Each form field label: `class="block text-sm text-gray-500 mb-1"`. Each input: `class="w-full px-2 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"`. Select same pattern. Cancel button: `onclick="this.closest('form').classList.add('hidden')"`.

4. Table: Add `class="min-w-full divide-y divide-gray-200"` to table element. Status badges: replace `.badge` + `.badge-connected`/`.badge-failed`/`.badge-unknown` with inline Tailwind:
   - active: `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-emerald-100 text-emerald-700"`
   - completed: `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-gray-100 text-gray-700"`
   - else: `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-red-100 text-red-700"`

5. Task counts: Replace `style="color:#059669"` with `class="text-emerald-600"`, `style="color:#2563eb"` with `class="text-blue-600"`.

6. Date column: Replace `style="color:#6b7280; font-size:.85rem"` with `class="text-gray-500 text-sm"`.

7. Action buttons: Keep existing btn/btn-sm/btn-primary classes. Replace `style="margin-left:.3rem"` with `class="ml-1"`. Pipeline button: replace `style="margin-left:.3rem; background:#7c3aed; color:white"` with `class="ml-1 bg-violet-600 text-white hover:bg-violet-700"`.

8. Empty state: Replace `style="color:#6b7280"` with `class="text-gray-500"`.

Migrate projects/plan.html (49 lines) to pure Tailwind:

1. Header: `class="flex justify-between items-center mb-6"`, h2: `class="text-lg font-semibold text-gray-800 m-0"`.

2. Table: `class="min-w-full divide-y divide-gray-200"`. Status badges:
   - published: `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-emerald-100 text-emerald-700"`
   - writing: `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-amber-100 text-amber-700"`
   - else: `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-red-100 text-red-700"`

3. Empty state: `class="text-gray-500"`.

4. Preserve HTMX: `hx-post="/projects/plan/{{ item.id }}/create-draft"` and `hx-swap="outerHTML"` unchanged.

CRITICAL: Zero style= attributes in final output. All visual styling via Tailwind classes only.
  </action>
  <verify>
    <automated>grep -c 'style=' app/templates/projects/index.html app/templates/projects/plan.html | grep -v ':0$' | wc -l | grep -q '^0$' && echo "PASS: zero style= attributes" || echo "FAIL: style= attributes remain"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'style=' app/templates/projects/index.html returns 0
    - grep -c 'style=' app/templates/projects/plan.html returns 0
    - grep 'hx-patch\|hx-post\|hx-swap' app/templates/projects/plan.html returns at least 1 match (HTMX preserved)
    - grep 'classList' app/templates/projects/index.html returns at least 1 match (JS uses classList not style.display)
    - grep 'bg-emerald-100' app/templates/projects/index.html returns at least 1 match (Tailwind badges)
    - grep 'min-w-full' app/templates/projects/index.html returns at least 1 match (Tailwind table)
    - grep 'min-w-full' app/templates/projects/plan.html returns at least 1 match (Tailwind table)
  </acceptance_criteria>
  <done>Both templates render with pure Tailwind classes, zero style= attributes, all existing functionality preserved (form toggle via classList, HTMX create-draft, badges with Tailwind colors)</done>
</task>

<task type="auto">
  <name>Task 2: Tailwind migration of projects/kanban.html</name>
  <files>app/templates/projects/kanban.html</files>
  <read_first>
    - app/templates/projects/kanban.html (current state — 78 lines, inline styles, 5-column grid)
    - app/templates/positions/index.html (reference Tailwind pattern from v4-04)
  </read_first>
  <action>
Migrate projects/kanban.html (78 lines) to pure Tailwind:

1. Header card: `class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6"`. h2: `class="text-lg font-semibold text-gray-800 m-0 mb-6"`.

2. Kanban grid: Replace `style="display:grid; grid-template-columns:repeat(5, 1fr); gap:.75rem; overflow-x:auto"` with `class="grid grid-cols-5 gap-3 overflow-x-auto"`.

3. Column containers: Replace inline `style="background:{{ col_color }}; ..."` with Jinja2 conditional Tailwind classes. Update the for loop to use Tailwind bg classes instead of hex colors:
   ```
   {% for col_status, col_label, col_bg in [
     ("open", "Open", "bg-gray-100"),
     ("assigned", "Assigned", "bg-blue-50"),
     ("in_progress", "In Progress", "bg-amber-100"),
     ("review", "Review", "bg-violet-100"),
     ("resolved", "Done", "bg-emerald-100")
   ] %}
   <div class="{{ col_bg }} rounded-lg p-3 min-h-[200px]">
   ```
   Column header h3: Replace `style="margin:0 0 .75rem; color:#6b7280; font-size:.8rem; text-transform:uppercase"` with `class="m-0 mb-3 text-gray-500 text-xs uppercase"`.

4. Task cards: Replace `style="background:white; border:1px solid #e5e7eb; border-radius:6px; padding:.6rem; margin-bottom:.5rem"` with `class="bg-white border border-gray-200 rounded-md p-2.5 mb-2"`.
   - Title: `class="font-medium text-sm mb-1"` (replaces inline style)
   - Task type badge: Replace `.badge` + conditional class with Tailwind conditionals:
     - page_404/lost_indexation: `class="inline-flex items-center px-1.5 py-0.5 rounded text-[0.65rem] font-medium bg-red-100 text-red-700"`
     - manual: `class="inline-flex items-center px-1.5 py-0.5 rounded text-[0.65rem] font-medium bg-gray-100 text-gray-700"`
     - else: `class="inline-flex items-center px-1.5 py-0.5 rounded text-[0.65rem] font-medium bg-emerald-100 text-emerald-700"`
   - Due date: `class="text-[0.7rem] text-gray-500 ml-1"` (replaces inline style)
   - Button row: `class="mt-1.5 flex gap-1 flex-wrap"` (replaces inline style)

5. Status transition buttons: Replace all inline styles with Tailwind classes:
   - Assign (blue): `class="px-2 py-0.5 text-[0.7rem] font-medium rounded bg-blue-500 text-white hover:bg-blue-600"`
   - Start (amber): `class="px-2 py-0.5 text-[0.7rem] font-medium rounded bg-amber-600 text-white hover:bg-amber-700"`
   - To Review (violet): `class="px-2 py-0.5 text-[0.7rem] font-medium rounded bg-violet-600 text-white hover:bg-violet-700"`
   - Done (green): `class="px-2 py-0.5 text-[0.7rem] font-medium rounded bg-emerald-600 text-white hover:bg-emerald-700"`
   All HTMX attributes (hx-patch, hx-vals, hx-swap) preserved exactly as-is.

6. Comments section: card wrapper `class="bg-white rounded-lg shadow-sm border border-gray-200 p-6"`. h3: `class="text-base font-semibold text-gray-800 m-0 mb-4"`.
   - Comment items: Replace `style="border-bottom:1px solid #e5e7eb; padding:.75rem 0"` with `class="border-b border-gray-200 py-3"`.
   - Username: keep `<strong>`. Timestamp: `class="text-gray-500 text-xs ml-2"`.
   - Comment text: `class="mt-1 mb-0"`.
   - Empty state: `class="text-gray-500"`.
   - Comment form: `class="mt-4 flex gap-2"`. Input: `class="flex-1 px-2 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"`.

CRITICAL: Zero style= attributes. Column backgrounds use Tailwind class variable from for loop, not inline style.
  </action>
  <verify>
    <automated>grep -c 'style=' app/templates/projects/kanban.html | grep -q '^0$' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'style=' app/templates/projects/kanban.html returns 0
    - grep 'hx-patch' app/templates/projects/kanban.html returns at least 4 matches (one per non-resolved column)
    - grep 'hx-vals' app/templates/projects/kanban.html returns at least 4 matches
    - grep 'grid-cols-5' app/templates/projects/kanban.html returns 1 match
    - grep 'bg-gray-100\|bg-blue-50\|bg-amber-100\|bg-violet-100\|bg-emerald-100' app/templates/projects/kanban.html returns at least 5 matches
    - grep 'hx-post="/projects/' app/templates/projects/kanban.html returns at least 1 match (comment form)
  </acceptance_criteria>
  <done>Kanban board renders with pure Tailwind classes, 5-column grid layout with Tailwind bg colors, all HTMX status transitions preserved, comment form functional, zero style= attributes</done>
</task>

</tasks>

<verification>
Run for all three templates:
```bash
for f in app/templates/projects/index.html app/templates/projects/kanban.html app/templates/projects/plan.html; do
  count=$(grep -c 'style=' "$f" 2>/dev/null || echo 0)
  echo "$f: style= count = $count"
done
```
All must return 0.

Check HTMX preservation:
```bash
grep -l 'hx-patch\|hx-post\|hx-swap' app/templates/projects/*.html
```
Must include kanban.html and plan.html.
</verification>

<success_criteria>
- All three project templates have zero style= attributes
- Kanban 5-column grid uses Tailwind grid-cols-5 with Tailwind bg color classes
- All HTMX interactions preserved (kanban status transitions, content plan create-draft, comment form)
- New project form uses classList.remove('hidden') / classList.add('hidden') for show/hide
- Badge colors follow established palette (emerald/red/amber/gray/violet)
</success_criteria>

<output>
After completion, create `.planning/phases/v4-06-section-content/v4-06-01-SUMMARY.md`
</output>
