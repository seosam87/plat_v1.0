---
phase: v4-06-section-content
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/monitoring/index.html
  - app/templates/audit/index.html
autonomous: true
requirements:
  - CNT-V4-01
  - CNT-V4-03

must_haves:
  truths:
    - "Change Monitoring page renders with Tailwind utility classes and zero style= attributes"
    - "Content Audit page renders with Tailwind utility classes and zero style= attributes"
    - "All existing functionality preserved: alert rule editing, digest schedule, severity filter, audit table filter, content type dropdown, CTA editor, schema modal, check toggles"
  artifacts:
    - path: "app/templates/monitoring/index.html"
      provides: "Tailwind-migrated monitoring page"
      contains: "class=\"bg-white"
    - path: "app/templates/audit/index.html"
      provides: "Tailwind-migrated content audit page"
      contains: "class=\"bg-white"
  key_links:
    - from: "app/templates/monitoring/index.html"
      to: "/monitoring/rules/{r.id}"
      via: "fetch PUT for rule updates"
      pattern: "updateRule"
    - from: "app/templates/monitoring/index.html"
      to: "/monitoring/{site.id}/digest-schedule"
      via: "fetch PUT for digest schedule"
      pattern: "saveDigestSchedule"
    - from: "app/templates/audit/index.html"
      to: "/audit/{site.id}/run"
      via: "HTMX hx-post for audit run"
      pattern: "hx-post"
    - from: "app/templates/audit/index.html"
      to: "editTemplate"
      via: "JS function opening schema modal"
      pattern: "editTemplate"
    - from: "app/templates/audit/index.html"
      to: "filterTable"
      via: "JS client-side table filtering"
      pattern: "filterTable"
---

<objective>
Migrate monitoring/index.html (162 lines) and audit/index.html (300 lines) to pure Tailwind CSS. Replace all inline style= attributes with Tailwind utility classes. Preserve all JS interactions (alert rule editing, digest schedule, severity filter, audit table filtering, content type dropdowns, CTA editor, schema template modal, check toggles).

Purpose: Complete CNT-V4-01 (Content Audit, Pipeline) and CNT-V4-03 (Monitoring) requirements — all content pages accessible via sidebar with consistent Tailwind styling.
Output: Two fully Tailwind-migrated templates with zero style= attributes.
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP-v4.md
@.planning/phases/v4-06-section-content/v4-06-CONTEXT.md

<interfaces>
<!-- Tailwind patterns established in v4-02..v4-05 -->
Badge palette: bg-emerald-100/text-emerald-700, bg-red-100/text-red-700, bg-sky-100/text-sky-700, bg-amber-100/text-amber-700
Form inputs: w-full px-2 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500
Buttons: px-3 py-1.5 text-sm font-medium rounded bg-indigo-600 text-white hover:bg-indigo-700
Stat cards: bg-white rounded-lg shadow-sm border border-gray-200 text-center p-4
Tables: min-w-full divide-y divide-gray-200
Modals: hidden div + classList toggle (hidden/flex), never style.display

<!-- Monitoring severity badge mapping -->
error/Критичный: bg-red-100 text-red-700
warning/Предупреждение: bg-amber-100 text-amber-700
info/Информация: bg-gray-100 text-gray-700

<!-- Audit check result icons (text colors) -->
pass (checkmark): text-emerald-600 font-bold
fail (cross): text-red-600 font-bold
warning (triangle): text-amber-500 font-bold
none (dash): text-gray-300

<!-- Audit check_defs applies_to badges -->
informational: bg-blue-100 text-blue-600
commercial: bg-amber-100 text-amber-600
all: bg-gray-100 text-gray-500

<!-- Audit check_defs severity -->
error: text-red-600 font-semibold
warning: text-amber-500

<!-- Schema modal pattern (classList hidden/flex toggle) -->
Backdrop: hidden fixed inset-0 bg-black/50 z-50 justify-center items-center
Content: bg-white rounded-lg p-6 w-[600px] max-h-[80vh] overflow-y-auto
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tailwind migration of monitoring/index.html</name>
  <files>app/templates/monitoring/index.html</files>
  <read_first>
    - app/templates/monitoring/index.html (current state — 162 lines, inline styles)
    - app/templates/analytics/index.html (reference multi-section Tailwind from v4-05)
  </read_first>
  <action>
Migrate monitoring/index.html (162 lines) to pure Tailwind:

1. Header bar: Replace `style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1.5rem"` with `class="flex justify-between items-center mb-6"`. h2: `class="text-lg font-semibold text-gray-800 m-0"`. Back link: keep btn btn-sm classes.

2. Summary stat cards: Replace `style="display:grid; grid-template-columns:repeat(auto-fit, minmax(160px, 1fr)); gap:1rem; margin-bottom:1.5rem"` with `class="grid grid-cols-1 sm:grid-cols-3 gap-4 mb-6"`.
   Each card: Replace `class="card" style="text-align:center; padding:1rem"` with `class="bg-white rounded-lg shadow-sm border border-gray-200 text-center p-4"`.
   - Error count: `class="text-3xl font-bold text-red-600"` (replaces `style="font-size:1.75rem; font-weight:700; color:#dc2626"`)
   - Warning count: `class="text-3xl font-bold text-amber-500"` (replaces color:#f59e0b)
   - Total count: `class="text-3xl font-bold text-blue-600"` (replaces color:#2563eb)
   - Card labels: `class="text-gray-500 text-sm"` (replaces inline style)

3. Alert rules card: Replace `class="card" style="margin-bottom:1.5rem"` with `class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6"`. h3: `class="text-base font-semibold text-gray-800 m-0 mb-4"`. Table: `class="min-w-full divide-y divide-gray-200 text-sm"`.
   - Severity select: `class="text-xs px-2 py-1 border border-gray-300 rounded focus:ring-indigo-500 focus:border-indigo-500"`. Remove inline style= on options (color on option elements is not reliable cross-browser anyway — the select itself is sufficient).

4. Digest schedule card: Replace with `class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6"`. h3: `class="text-base font-semibold text-gray-800 m-0 mb-4"`.
   - Controls row: `class="flex gap-4 flex-wrap items-center mb-4"`. Label wrapper: `class="flex items-center gap-2"`.
   - Select: `class="px-2 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"`.
   - Number inputs: `class="w-16 px-2 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"`.
   - UTC label: `class="text-gray-500 text-sm"`.
   - Button row: `class="flex gap-2"`.
   - Save button: `class="px-3 py-1.5 text-sm font-medium rounded bg-indigo-600 text-white hover:bg-indigo-700"` (replaces background:#4f46e5)
   - Send now button: `class="px-3 py-1.5 text-sm font-medium rounded bg-emerald-600 text-white hover:bg-emerald-700"` (replaces background:#059669)
   - Status span: `class="text-sm ml-2"`.

5. Alert history card: Replace with `class="bg-white rounded-lg shadow-sm border border-gray-200 p-6"`.
   - Header flex: `class="flex justify-between items-center mb-4"`. h3: `class="text-base font-semibold text-gray-800 m-0"`.
   - Filter select: `class="px-2 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"`.
   - Table: `class="min-w-full divide-y divide-gray-200 text-sm"`.
   - Date cell: `class="whitespace-nowrap"` (replaces inline style).
   - Page URL cell: `class="max-w-[250px] overflow-hidden text-ellipsis whitespace-nowrap"` (replaces inline style).
   - Severity badges:
     - error: `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-red-100 text-red-700"`
     - warning: `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-amber-100 text-amber-700"`
     - info: `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-gray-100 text-gray-700"`
   - Details cell: `class="max-w-[200px] overflow-hidden text-ellipsis whitespace-nowrap"` (replaces inline style).
   - Empty state: `class="p-4 text-gray-500 text-center"`.

6. JS updates for saveDigestSchedule/sendDigestNow: Change innerHTML status messages from inline style spans to Tailwind class spans:
   - Success: `'<span class="text-emerald-600">Сохранено</span>'` (replaces `style="color:#059669"`)
   - Error: `'<span class="text-red-600">Ошибка</span>'` (replaces `style="color:#dc2626"`)
   - Digest sent: `'<span class="text-emerald-600">Дайджест отправлен</span>'`

7. filterAlerts() JS: Change `row.style.display = ...` to classList toggle:
   ```javascript
   function filterAlerts() {
     const sev = document.getElementById('severity-filter').value;
     document.querySelectorAll('.alert-row').forEach(row => {
       if (sev === 'all' || row.dataset.severity === sev) {
         row.classList.remove('hidden');
       } else {
         row.classList.add('hidden');
       }
     });
   }
   ```
   Note: For `<tr>` elements, using hidden class on a table row works with Tailwind's `[hidden] { display: none }` utility, but to be safe, use the `classList.add('hidden')` approach which sets `display: none` via Tailwind. If the table row display needs to be `table-row`, change `<tr>` to include a base class and use a CSS approach. Simplest: keep row.style.display for `<tr>` elements since Tailwind hidden on `<tr>` can break table layout. EXCEPTION: table row filtering uses `row.style.display` — this is the only permitted style= exception for dynamic JS on `<tr>` elements (same pattern used in v4-04 positions/index.html filterTable). The template HTML itself must have zero style= attributes.

CRITICAL: Zero style= attributes in the Jinja2 template HTML. JS-generated innerHTML uses Tailwind classes. JS row.style.display on `<tr>` filtering is permitted (runtime-only, not in template source).
  </action>
  <verify>
    <automated>grep -c 'style=' app/templates/monitoring/index.html | grep -q '^0$' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'style=' app/templates/monitoring/index.html returns 0
    - grep 'updateRule' app/templates/monitoring/index.html returns at least 2 matches (function def + onchange calls)
    - grep 'saveDigestSchedule' app/templates/monitoring/index.html returns at least 2 matches (function def + onclick)
    - grep 'filterAlerts' app/templates/monitoring/index.html returns at least 2 matches (function def + onchange)
    - grep 'bg-red-100\|bg-amber-100\|bg-gray-100' app/templates/monitoring/index.html returns at least 3 matches (severity badges)
    - grep 'bg-white rounded-lg shadow-sm' app/templates/monitoring/index.html returns at least 3 matches (stat cards + sections)
  </acceptance_criteria>
  <done>Monitoring page renders with pure Tailwind, stat cards styled, alert rules and digest schedule forms functional, severity filter works, zero style= attributes in template HTML</done>
</task>

<task type="auto">
  <name>Task 2: Tailwind migration of audit/index.html</name>
  <files>app/templates/audit/index.html</files>
  <read_first>
    - app/templates/audit/index.html (current state — 300 lines, inline styles, modal, complex table)
    - app/templates/positions/index.html (reference complex Tailwind template from v4-04)
    - app/templates/intent/index.html (reference modal + classList pattern from v4-04)
  </read_first>
  <action>
Migrate audit/index.html (300 lines) to pure Tailwind. This is the most complex template in this phase.

1. Header bar: Replace `style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1.5rem"` with `class="flex justify-between items-center mb-6"`. h2: `class="text-lg font-semibold text-gray-800 m-0"`.
   - Run audit button: `class="px-3 py-1.5 text-sm font-medium rounded bg-indigo-600 text-white hover:bg-indigo-700"` (replaces `style="background:#4f46e5;color:white"`). Keep hx-post and onclick behavior.
   - Button group: `class="flex gap-2"`.

2. Summary stat cards: Replace grid style with `class="grid grid-cols-2 sm:grid-cols-4 gap-4 mb-6"`.
   Each card: `class="bg-white rounded-lg shadow-sm border border-gray-200 text-center p-4"` (replaces `class="card" style="..."`).
   - Total pages: `class="text-3xl font-bold text-blue-600"` (replaces color:#2563eb)
   - With issues: `class="text-3xl font-bold text-red-600"` (replaces color:#dc2626)
   - All pass: `class="text-3xl font-bold text-emerald-600"` (replaces color:#059669)
   - Unchecked: `class="text-3xl font-bold text-gray-500"` (replaces color:#6b7280)
   - Labels: `class="text-gray-500 text-sm"`

3. Filters card: Replace `class="card" style="margin-bottom:1.5rem; padding:1rem"` with `class="bg-white rounded-lg shadow-sm border border-gray-200 p-4 mb-6"`.
   - Filter row: `class="flex gap-4 flex-wrap items-center"`
   - Search input: `class="flex-1 min-w-[200px] px-2 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"` (replaces inline style)
   - Type filter select: `class="px-2 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"` (replaces inline style)
   - Status filter select: same pattern

4. Pages table card: Replace `class="card" style="margin-bottom:1.5rem; overflow-x:auto"` with `class="bg-white rounded-lg shadow-sm border border-gray-200 mb-6 overflow-x-auto"`.
   - Table: `id="audit-table" class="min-w-full divide-y divide-gray-200"`
   - Header th cells with text-align:center: `class="text-center"`
   - URL cell: `class="max-w-[300px] overflow-hidden text-ellipsis whitespace-nowrap"` (replaces inline style)
   - URL link: `class="text-sm text-blue-600 hover:text-blue-800"` (replaces `style="font-size:.85rem"`)
   - Title subtitle: `class="text-gray-500 text-xs"` (replaces inline style)
   - Content type select: `class="text-xs px-1.5 py-1 border border-gray-300 rounded focus:ring-indigo-500 focus:border-indigo-500"` (replaces inline style)

5. Check result icons in the for loop: Replace inline style spans with Tailwind class spans:
   - pass: `<span class="text-emerald-600 font-bold">&#10003;</span>` (replaces `style="color:#059669; font-weight:700"`)
   - fail: `<span class="text-red-600 font-bold">&#10007;</span>` (replaces `style="color:#dc2626; font-weight:700"`)
   - warning: `<span class="text-amber-500 font-bold">&#9888;</span>` (replaces `style="color:#f59e0b; font-weight:700"`)
   - none: `<span class="text-gray-300">&mdash;</span>` (replaces `style="color:#d1d5db"`)

6. Single audit button: `class="px-2 py-1 text-xs font-medium rounded"` (replaces `style="font-size:.75rem"`)

7. Empty state: `class="p-4 text-gray-500 text-center"` (replaces inline style)

8. Check definitions details: Replace `class="card" style="margin-bottom:1.5rem"` with `class="bg-white rounded-lg shadow-sm border border-gray-200 mb-6"`.
   - Summary: `class="cursor-pointer font-semibold p-3"`
   - Inner div: `class="px-3 pb-3"`
   - Table: `class="min-w-full divide-y divide-gray-200 text-sm"`
   - applies_to badges:
     - informational: `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-blue-100 text-blue-600"`
     - commercial: `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-amber-100 text-amber-600"`
     - all: `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-gray-100 text-gray-500"`
   - Severity:
     - error: `class="text-red-600 font-semibold"` (replaces inline style)
     - warning: `class="text-amber-500"` (replaces inline style)
   - Auto-fixable yes: `class="text-emerald-600"` (replaces inline style)

9. CTA template card: Replace with `class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6"`. h3: `class="text-base font-semibold text-gray-800 m-0 mb-3"`. Description: `class="text-sm text-gray-500 mb-2"`.
   - Textarea: `class="w-full font-mono text-sm p-2 border border-gray-300 rounded resize-y"` (replaces inline style)
   - Save CTA button: `class="mt-2 px-3 py-1.5 text-sm font-medium rounded bg-emerald-600 text-white hover:bg-emerald-700"` (replaces `style="margin-top:.5rem; background:#059669; color:white"`)
   - Status span: `class="ml-2 text-sm"`

10. Schema templates card: Replace with `class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6"`. h3 and description same as CTA.
    - Table: `class="min-w-full divide-y divide-gray-200 text-sm"`
    - Source badges:
      - default: `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-gray-100 text-gray-500"`
      - custom: `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-blue-100 text-blue-600"`
    - Edit button: `class="px-2 py-1 text-xs font-medium rounded"`
    - Delete button: `class="px-2 py-1 text-xs font-medium rounded bg-red-800 text-white hover:bg-red-900"` (replaces `style="font-size:.75rem; background:#991b1b; color:white"`)

11. Schema edit modal: Replace entire inline-styled modal with classList-based Tailwind:
    - Backdrop: `id="schema-modal" class="hidden fixed inset-0 bg-black/50 z-50 justify-center items-center"`
    - Content: `class="bg-white rounded-lg p-6 w-[600px] max-h-[80vh] overflow-y-auto"`
    - h3: `class="text-base font-semibold m-0 mb-4"`
    - Labels: `class="text-sm text-gray-500"`
    - Name input: `class="w-full px-2 py-1.5 border border-gray-300 rounded text-sm mb-3 focus:ring-indigo-500 focus:border-indigo-500"`
    - JSON textarea: `class="w-full font-mono text-sm p-2 border border-gray-300 rounded resize-y mb-3"`
    - Button row: `class="flex gap-2"`
    - Save: `class="px-3 py-1.5 text-sm font-medium rounded bg-indigo-600 text-white hover:bg-indigo-700"`
    - Cancel: `class="px-3 py-1.5 text-sm font-medium rounded border border-gray-300 text-gray-700 hover:bg-gray-50"`

12. JS updates:
    - editTemplate(): Change `document.getElementById('schema-modal').style.display = 'flex'` to `document.getElementById('schema-modal').classList.remove('hidden'); document.getElementById('schema-modal').classList.add('flex')`
    - closeModal(): Change `style.display = 'none'` to `classList.add('hidden'); classList.remove('flex')`
    - saveCta() status innerHTML: Use `class="text-emerald-600"` and `class="text-red-600"` instead of inline style spans
    - filterTable(): For `<tr>` row filtering, use `row.style.display` (same permitted exception as monitoring — runtime JS only, not in template HTML)

CRITICAL: Zero style= attributes in template HTML. All Jinja2 output uses Tailwind classes. JS innerHTML uses Tailwind classes. Only row.style.display in filterTable() JS is permitted (runtime-only).
  </action>
  <verify>
    <automated>grep -c 'style=' app/templates/audit/index.html | grep -q '^0$' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'style=' app/templates/audit/index.html returns 0
    - grep 'hx-post' app/templates/audit/index.html returns at least 1 match (run audit button)
    - grep 'filterTable' app/templates/audit/index.html returns at least 3 matches (function def + 2 onchange/oninput)
    - grep 'editTemplate' app/templates/audit/index.html returns at least 2 matches (function def + onclick calls)
    - grep 'classList' app/templates/audit/index.html returns at least 2 matches (modal show/hide)
    - grep 'schema-modal' app/templates/audit/index.html returns at least 3 matches
    - grep 'bg-emerald-600\|text-emerald-600' app/templates/audit/index.html returns at least 2 matches
    - grep 'text-red-600' app/templates/audit/index.html returns at least 2 matches
    - grep 'min-w-full' app/templates/audit/index.html returns at least 2 matches (multiple tables)
    - grep 'bg-white rounded-lg shadow-sm' app/templates/audit/index.html returns at least 4 matches (multiple card sections)
  </acceptance_criteria>
  <done>Content audit page renders with pure Tailwind, all sections styled (stats, filters, table, check defs, CTA, schema templates), modal uses classList toggle, all JS interactions preserved, zero style= attributes in template HTML</done>
</task>

</tasks>

<verification>
Run for both templates:
```bash
for f in app/templates/monitoring/index.html app/templates/audit/index.html; do
  count=$(grep -c 'style=' "$f" 2>/dev/null || echo 0)
  echo "$f: style= count = $count"
done
```
All must return 0.

Check all 7 templates across the phase are migrated:
```bash
for f in app/templates/projects/index.html app/templates/projects/kanban.html app/templates/projects/plan.html app/templates/pipeline/jobs.html app/templates/pipeline/publish.html app/templates/monitoring/index.html app/templates/audit/index.html; do
  count=$(grep -c 'style=' "$f" 2>/dev/null || echo 0)
  echo "$f: style= count = $count"
done
```
All must return 0.
</verification>

<success_criteria>
- Both templates have zero style= attributes in template HTML
- Content audit stat cards, filters, table, check definitions, CTA editor, schema templates, and schema modal all use Tailwind classes
- Monitoring stat cards, alert rules, digest schedule, and alert history all use Tailwind classes
- Schema modal uses classList toggle (hidden/flex)
- All JS interactions preserved (filterTable, editTemplate, closeModal, saveCta, saveTemplate, deleteTemplate, updateRule, saveDigestSchedule, sendDigestNow, filterAlerts)
- JS-generated innerHTML uses Tailwind classes not inline styles
</success_criteria>

<output>
After completion, create `.planning/phases/v4-06-section-content/v4-06-03-SUMMARY.md`
</output>
