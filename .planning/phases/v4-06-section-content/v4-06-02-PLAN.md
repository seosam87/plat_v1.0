---
phase: v4-06-section-content
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/pipeline/jobs.html
  - app/templates/pipeline/publish.html
autonomous: true
requirements:
  - CNT-V4-01

must_haves:
  truths:
    - "Pipeline Jobs page renders with Tailwind utility classes and zero style= attributes"
    - "DOCX Publisher page renders with Tailwind utility classes and zero style= attributes"
    - "All existing functionality preserved: HTMX approve/reject/rollback, bulk actions, diff modal, upload form, tab viewer, publish form"
  artifacts:
    - path: "app/templates/pipeline/jobs.html"
      provides: "Tailwind-migrated pipeline jobs page"
      contains: "class=\"bg-white"
    - path: "app/templates/pipeline/publish.html"
      provides: "Tailwind-migrated DOCX publisher page"
      contains: "class=\"bg-white"
  key_links:
    - from: "app/templates/pipeline/jobs.html"
      to: "/pipeline/jobs/{job.id}/approve"
      via: "HTMX hx-post for job approval"
      pattern: "hx-post"
    - from: "app/templates/pipeline/jobs.html"
      to: "showDiff"
      via: "JS function opening diff modal"
      pattern: "showDiff"
    - from: "app/templates/pipeline/publish.html"
      to: "/ui/content-publish/{site.id}/upload"
      via: "form POST for DOCX upload"
      pattern: "action=\"/ui/content-publish"
    - from: "app/templates/pipeline/publish.html"
      to: "showTab"
      via: "JS tab switcher"
      pattern: "showTab"
---

<objective>
Migrate pipeline/jobs.html (133 lines) and pipeline/publish.html (133 lines) to pure Tailwind CSS. Replace all inline style= attributes with Tailwind utility classes. Preserve all HTMX interactions (approve/reject/rollback buttons), diff modal, bulk actions, upload form, tab viewer (Rendered/HTML/Schema), and publish form.

Purpose: Complete CNT-V4-01 requirement — WP Pipeline and DOCX Publisher accessible via sidebar with consistent Tailwind styling.
Output: Two fully Tailwind-migrated templates with zero style= attributes.
</objective>

<execution_context>
@/projects/test/.claude/get-shit-done/workflows/execute-plan.md
@/projects/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP-v4.md
@.planning/phases/v4-06-section-content/v4-06-CONTEXT.md

<interfaces>
<!-- Tailwind patterns established in v4-02..v4-05 -->
Badge palette: bg-emerald-100/text-emerald-700, bg-red-100/text-red-700, bg-sky-100/text-sky-700, bg-amber-100/text-amber-700
Form inputs: w-full px-2 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500
Buttons: px-3 py-1.5 text-sm font-medium rounded bg-indigo-600 text-white hover:bg-indigo-700
Tables: min-w-full divide-y divide-gray-200
Modals: hidden div + classList toggle (hidden/flex), never style.display

<!-- Badge mapping for job status -->
pushed = bg-emerald-100 text-emerald-700
awaiting_approval = bg-amber-100 text-amber-700
failed = bg-red-100 text-red-700
rolled_back / other = bg-gray-100 text-gray-700

<!-- Badge mapping for publish job status -->
pushed = bg-emerald-100 text-emerald-700
failed = bg-red-100 text-red-700
other = bg-gray-100 text-gray-700

<!-- Tab pattern (use classList toggle, not style.display) -->
Active tab: border-b-2 border-indigo-600 text-indigo-600 font-semibold bg-white
Inactive tab: text-gray-500 bg-gray-50 hover:text-gray-700

<!-- Diff modal pattern (classList hidden/flex toggle) -->
Backdrop: hidden fixed inset-0 bg-black/50 z-50 justify-center items-center (toggled to flex)
Content: bg-white rounded-lg p-8 max-w-4xl w-[95%] max-h-[85vh] overflow-y-auto relative

<!-- Diff line coloring exception: innerHTML-generated spans use Tailwind classes -->
Added line: class="text-emerald-600 bg-emerald-50"
Removed line: class="text-red-600 bg-red-50"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tailwind migration of pipeline/jobs.html</name>
  <files>app/templates/pipeline/jobs.html</files>
  <read_first>
    - app/templates/pipeline/jobs.html (current state — 133 lines, inline styles, diff modal)
    - app/templates/positions/index.html (reference Tailwind pattern from v4-04)
    - app/templates/intent/index.html (reference modal pattern from v4-04)
  </read_first>
  <action>
Migrate pipeline/jobs.html (133 lines) to pure Tailwind:

1. Header bar: Replace `style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1.5rem"` with `class="flex justify-between items-center mb-6"`. h2: `class="text-lg font-semibold text-gray-800 m-0"`.

2. Bulk action buttons: Replace inline styles:
   - Approve All: `class="px-3 py-1.5 text-sm font-medium rounded bg-emerald-600 text-white hover:bg-emerald-700"` (replaces `style="background:#059669;color:white"`)
   - Reject All: `class="px-3 py-1.5 text-sm font-medium rounded bg-red-600 text-white hover:bg-red-700"` (replaces `style="background:#dc2626;color:white"`)
   - Button container: `class="flex gap-2"`

3. Table: Add `class="min-w-full divide-y divide-gray-200"`. URL cell: Replace `style="max-width:300px; overflow:hidden; text-overflow:ellipsis; white-space:nowrap"` with `class="max-w-[300px] overflow-hidden text-ellipsis whitespace-nowrap"`.

4. Status badges: Replace `.badge` + conditional class with Tailwind conditionals:
   - pushed: `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-emerald-100 text-emerald-700"`
   - awaiting_approval: `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-amber-100 text-amber-700"`
   - failed: `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-red-100 text-red-700"`
   - rolled_back/other: `class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-gray-100 text-gray-700"`

5. Action buttons in rows:
   - Cell: Replace `style="display:flex; gap:.25rem; flex-wrap:wrap"` with `class="flex gap-1 flex-wrap"`
   - Approve: `class="px-2 py-1 text-xs font-medium rounded bg-emerald-600 text-white hover:bg-emerald-700"` (replaces inline style)
   - Reject: `class="px-2 py-1 text-xs font-medium rounded bg-red-600 text-white hover:bg-red-700"`
   - Rollback: `class="px-2 py-1 text-xs font-medium rounded bg-amber-600 text-white hover:bg-amber-700"`
   - History link: `class="px-2 py-1 text-xs rounded"` (replaces `style="font-size:.75rem"`)
   All HTMX attributes (hx-post, hx-swap, hx-confirm) preserved exactly.

6. Diff changes link: Replace `style="color:#2563eb"` with `class="text-blue-600 hover:text-blue-800"`.

7. Empty state: Replace `style="color:#6b7280"` with `class="text-gray-500"`.

8. Diff modal: Replace entire inline-styled modal with classList-based Tailwind modal:
   - Backdrop: `id="diff-modal" class="hidden fixed inset-0 bg-black/50 z-50 justify-center items-center"` (toggled to flex by JS)
   - Content div: `class="bg-white rounded-lg p-8 max-w-4xl w-[95%] max-h-[85vh] overflow-y-auto relative"`
   - Close button: `class="absolute top-2 right-4 bg-transparent border-none text-2xl cursor-pointer text-gray-500 hover:text-gray-800"` with `onclick="document.getElementById('diff-modal').classList.add('hidden'); document.getElementById('diff-modal').classList.remove('flex')"`
   - h3: `class="text-base font-semibold m-0 mb-4"`
   - Pre block: `class="bg-gray-50 p-4 rounded text-xs overflow-x-auto whitespace-pre-wrap leading-relaxed"`

9. JS updates:
   - showDiff(): Change `document.getElementById('diff-modal').style.display = 'flex'` to `document.getElementById('diff-modal').classList.remove('hidden'); document.getElementById('diff-modal').classList.add('flex')`
   - Diff line coloring in innerHTML: Replace `style="color:#059669;background:#ecfdf5"` with `class="text-emerald-600 bg-emerald-50"` and `style="color:#dc2626;background:#fef2f2"` with `class="text-red-600 bg-red-50"`
   - Backdrop click handler: Change `this.style.display = 'none'` to `this.classList.add('hidden'); this.classList.remove('flex')`

CRITICAL: Zero style= attributes in final output.
  </action>
  <verify>
    <automated>grep -c 'style=' app/templates/pipeline/jobs.html | grep -q '^0$' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'style=' app/templates/pipeline/jobs.html returns 0
    - grep 'hx-post' app/templates/pipeline/jobs.html returns at least 3 matches (approve, reject, rollback, batch)
    - grep 'hx-confirm' app/templates/pipeline/jobs.html returns at least 1 match (rollback confirm)
    - grep 'classList' app/templates/pipeline/jobs.html returns at least 3 matches (modal show/hide)
    - grep 'diff-modal' app/templates/pipeline/jobs.html returns at least 2 matches
    - grep 'bg-emerald-100\|bg-red-100\|bg-amber-100' app/templates/pipeline/jobs.html returns at least 3 matches (badges)
    - grep 'min-w-full' app/templates/pipeline/jobs.html returns at least 1 match
  </acceptance_criteria>
  <done>Pipeline jobs page renders with pure Tailwind, diff modal uses classList toggle, all HTMX approve/reject/rollback/batch interactions preserved, zero style= attributes</done>
</task>

<task type="auto">
  <name>Task 2: Tailwind migration of pipeline/publish.html</name>
  <files>app/templates/pipeline/publish.html</files>
  <read_first>
    - app/templates/pipeline/publish.html (current state — 133 lines, inline styles, tabs)
    - app/templates/positions/index.html (reference Tailwind pattern)
  </read_first>
  <action>
Migrate pipeline/publish.html (133 lines) to pure Tailwind:

1. Main card wrapper: `class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6"`. h2: `class="text-lg font-semibold text-gray-800 m-0 mb-4"`. Description p: `class="text-gray-500 mb-6"`.

2. Upload form: Replace `style="display:flex;gap:1rem;align-items:end;flex-wrap:wrap;margin-bottom:1.5rem;padding:1.5rem;background:#f9fafb;border-radius:8px;border:1px dashed #d1d5db"` with `class="flex gap-4 items-end flex-wrap mb-6 p-6 bg-gray-50 rounded-lg border border-dashed border-gray-300"`.
   - Labels: `class="block text-sm text-gray-500 mb-1"`
   - Text inputs: `class="px-2 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"`
   - Selects: `class="px-2 py-1.5 border border-gray-300 rounded text-sm focus:ring-indigo-500 focus:border-indigo-500"`

3. Error div: Replace `style="color:#dc2626;background:#fef2f2;padding:.8rem;border-radius:6px;margin-bottom:1.5rem"` with `class="text-red-600 bg-red-50 p-3 rounded-md mb-6"`.

4. Preview section: Card wrapper `class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6"`.
   - Header flex: `class="flex justify-between items-center mb-4"`. h3: `class="text-base font-semibold m-0"`.
   - Publish button: `class="px-3 py-1.5 text-sm font-medium rounded bg-emerald-600 text-white hover:bg-emerald-700"`
   - Stats row: `class="flex gap-6 mb-4 text-sm text-gray-500"`. Colored strongs: `class="text-emerald-600"` for Added, `class="text-red-600"` for No headings.

5. Tab bar: Replace inline styles with Tailwind + classList-based JS:
   - Container: `class="flex gap-0 mb-4 border-b-2 border-gray-200"`
   - Active tab (rendered by default): `class="tab-btn px-4 py-2 text-sm rounded-t border-b-2 border-indigo-600 -mb-0.5 bg-white text-indigo-600 font-semibold cursor-pointer"`
   - Inactive tabs: `class="tab-btn px-4 py-2 text-sm rounded-t bg-gray-50 text-gray-500 -mb-0.5 cursor-pointer hover:text-gray-700"`

6. Tab panels:
   - Rendered: Replace `style="max-height:600px;overflow-y:auto;padding:1rem;border:1px solid #e5e7eb;border-radius:4px;background:white;line-height:1.7"` with `class="max-h-[600px] overflow-y-auto p-4 border border-gray-200 rounded bg-white leading-relaxed"`
   - HTML source: Replace `style="display:none;max-height:600px;overflow-y:auto"` with `class="hidden max-h-[600px] overflow-y-auto"`. Pre: `class="bg-gray-900 text-gray-200 p-4 rounded text-xs whitespace-pre-wrap break-all"`
   - Schema: Same hidden pattern. Pre: `class="bg-gray-900 text-gray-200 p-4 rounded text-sm whitespace-pre-wrap"`

7. showTab() JS: Rewrite to use classList instead of style properties:
   ```javascript
   function showTab(name) {
     ['rendered','html','schema'].forEach(t => {
       const panel = document.getElementById('panel-'+t);
       const btn = document.getElementById('tab-'+t);
       if (t===name) {
         panel.classList.remove('hidden');
         btn.className = 'tab-btn px-4 py-2 text-sm rounded-t border-b-2 border-indigo-600 -mb-0.5 bg-white text-indigo-600 font-semibold cursor-pointer';
       } else {
         panel.classList.add('hidden');
         btn.className = 'tab-btn px-4 py-2 text-sm rounded-t bg-gray-50 text-gray-500 -mb-0.5 cursor-pointer hover:text-gray-700';
       }
     });
   }
   ```

8. Recent jobs table: Card wrapper `class="bg-white rounded-lg shadow-sm border border-gray-200 p-6"`. Table: `class="min-w-full divide-y divide-gray-200"`.
   - Status badges same palette as jobs.html above.
   - TOC check/cross: `class="text-emerald-600"` and `class="text-red-600"`.
   - Date: `class="text-sm text-gray-500"`.
   - View button: `class="px-2 py-1 text-xs rounded"`.
   - Post type: `class="text-sm"`.

CRITICAL: Zero style= attributes in final output.
  </action>
  <verify>
    <automated>grep -c 'style=' app/templates/pipeline/publish.html | grep -q '^0$' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'style=' app/templates/pipeline/publish.html returns 0
    - grep 'showTab' app/templates/pipeline/publish.html returns at least 3 matches (function + 2 onclick calls)
    - grep 'classList' app/templates/pipeline/publish.html returns at least 2 matches (tab panel show/hide)
    - grep 'action="/ui/content-publish' app/templates/pipeline/publish.html returns at least 1 match (upload form preserved)
    - grep 'method="post"' app/templates/pipeline/publish.html returns at least 1 match (publish form preserved)
    - grep 'bg-gray-900' app/templates/pipeline/publish.html returns at least 2 matches (code preview panels)
    - grep 'border-indigo-600' app/templates/pipeline/publish.html returns at least 1 match (active tab)
  </acceptance_criteria>
  <done>DOCX Publisher renders with pure Tailwind, tab viewer uses classList for show/hide, upload and publish forms preserved, recent jobs table styled, zero style= attributes</done>
</task>

</tasks>

<verification>
Run for both templates:
```bash
for f in app/templates/pipeline/jobs.html app/templates/pipeline/publish.html; do
  count=$(grep -c 'style=' "$f" 2>/dev/null || echo 0)
  echo "$f: style= count = $count"
done
```
All must return 0.

Check HTMX preservation:
```bash
grep -c 'hx-post\|hx-swap\|hx-confirm' app/templates/pipeline/jobs.html
```
Must be >= 5.

Check form preservation:
```bash
grep 'action=' app/templates/pipeline/publish.html
```
Must include upload and publish form actions.
</verification>

<success_criteria>
- Both pipeline templates have zero style= attributes
- Diff modal uses classList toggle (hidden/flex), not style.display
- Tab viewer uses classList for panel show/hide and class swapping for active tab
- All HTMX interactions preserved (approve, reject, rollback, batch run)
- Upload form with file input and publish form preserved
- Diff line coloring uses Tailwind classes in innerHTML, not inline styles
</success_criteria>

<output>
After completion, create `.planning/phases/v4-06-section-content/v4-06-02-SUMMARY.md`
</output>
