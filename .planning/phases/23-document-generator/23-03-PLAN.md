---
phase: 23-document-generator
plan: 03
type: execute
wave: 3
depends_on: ["23-02"]
files_modified: []
autonomous: false
requirements: [DOC-01, DOC-02, DOC-03, DOC-04, DOC-05]

must_haves:
  truths:
    - "User can generate a PDF from template + client + site and see polling status"
    - "User can download the generated PDF"
    - "User can view documents list with type and date filters"
    - "User can send document via Telegram or Email with confirm dialog"
    - "User can regenerate a document creating a new version"
    - "Document type badges show correctly for proposal, audit_report, brief"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete document generator feature: generation, polling, download, filtering, sending, regeneration, and deletion.

Purpose: Confirm all DOC-01 through DOC-05 requirements work end-to-end in the running application.
Output: Verification sign-off
</objective>

<execution_context>
@/opt/seo-platform/.claude/get-shit-done/workflows/execute-plan.md
@/opt/seo-platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-document-generator/23-CONTEXT.md
@.planning/phases/23-document-generator/23-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete document generator feature</name>
  <files>none</files>
  <action>
Present the human verification checklist to the user. This is a human-verify checkpoint — no code changes. The user will test the following flows in the running application:

1. Navigate to any client detail page and verify "Документы" tab exists
2. Test document generation flow (template + site selection, Celery task, HTMX polling)
3. Test PDF download
4. Test document list filtering by type and date
5. Test send via Telegram/Email with confirm dialogs
6. Test regeneration (new version creation)
7. Test deletion with confirm dialog
8. Verify access control (require_manager_or_above)

Provide the user with the verification steps from the how-to-verify section below.
  </action>
  <what-built>
    Complete document generation pipeline: GeneratedDocument model with 3-version cap, Celery PDF generation via subprocess WeasyPrint, document listing with type/date filters, PDF download, send via Telegram/Email with confirm dialogs, regeneration, deletion. All accessible from the "Документы" tab in client detail page.
  </what-built>
  <how-to-verify>
    1. Navigate to any client detail page: `/ui/crm/clients/{id}`
    2. Click the "Документы" tab — verify it appears alongside Сайты, Контакты, Взаимодействия, Информация
    3. Verify empty state shows: "Документов пока нет" with description text
    4. Select a template from dropdown, select a site (required), click "Создать документ"
    5. Verify generation status card appears: "Генерируется..." with spinner
    6. Wait for status to change to "PDF готов" (should take less than 30 seconds)
    7. Verify document appears in the table with correct type badge, site, date, and "Готов" status
    8. Click "Скачать" — verify PDF downloads with correct filename
    9. Open the PDF — verify it contains rendered template content with client/site data
    10. Click "Отправить" dropdown — verify Email and Telegram options appear
    11. Click Email option — verify confirm dialog: "Отправить документ на {email} через Email?"
    12. Click "Перегенерировать" — verify confirm dialog and new generation starts
    13. Test type filter: select "Предложение" — verify only proposal documents shown
    14. Test date filter — verify filtering works
    15. Click "Удалить" on a document — verify confirm dialog: "Удалить документ? Это действие необратимо."
    16. Verify access control: non-manager user cannot see the documents tab or generate documents
  </how-to-verify>
  <read_first>
    .planning/phases/23-document-generator/23-CONTEXT.md,
    .planning/phases/23-document-generator/23-UI-SPEC.md
  </read_first>
  <verify>Human approves all 16 verification steps</verify>
  <acceptance_criteria>
    - Documents tab visible in client detail page
    - PDF generation completes via Celery task
    - PDF download works with correct content
    - Document list shows type badges and status indicators
    - Send dropdown with Email/Telegram options and confirm dialogs works
    - Regeneration creates new version
    - Type and date filters filter the document list
    - Delete with confirmation removes the document
  </acceptance_criteria>
  <done>User types "approved" confirming all DOC-01 through DOC-05 requirements work correctly</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
All DOC-01 through DOC-05 requirements verified by human testing.
</verification>

<success_criteria>
Human confirms: PDF generation, download, listing with filters, sending, regeneration, and deletion all work correctly in the running application.
</success_criteria>

<output>
After completion, create `.planning/phases/23-document-generator/23-03-SUMMARY.md`
</output>
