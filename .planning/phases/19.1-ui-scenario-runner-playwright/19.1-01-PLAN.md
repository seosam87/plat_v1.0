---
phase: 19.1-ui-scenario-runner-playwright
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /opt/seo-platform/tests/fixtures/smoke_seed.py
  - /opt/seo-platform/tests/fixtures/scenario_runner/__init__.py
  - /opt/seo-platform/scenarios/.gitkeep
  - /opt/seo-platform/pyproject.toml
  - /opt/seo-platform/.gitignore
autonomous: true
requirements: []  # Wave 0 foundation — no SCN req fully closed here, enables all downstream
must_haves:
  truths:
    - "seed_core and seed_extended are importable as public names from tests.fixtures.smoke_seed"
    - "tests/fixtures/scenario_runner/ package exists and is importable"
    - "scenarios/ directory exists at repo root and is picked up by pytest collection"
    - "playwright>=1.47 and pyyaml>=6.0 are declared in project dev/test deps"
    - "artifacts/ and storage_state.json files are gitignored"
  artifacts:
    - path: "/opt/seo-platform/tests/fixtures/smoke_seed.py"
      provides: "seed_core, seed_extended (public), SMOKE_IDS"
    - path: "/opt/seo-platform/tests/fixtures/scenario_runner/__init__.py"
      provides: "package marker"
    - path: "/opt/seo-platform/scenarios/.gitkeep"
      provides: "scenarios directory committed"
  key_links:
    - from: "scenario_runner/seed.py (plan 03)"
      to: "tests.fixtures.smoke_seed.seed_core/seed_extended"
      via: "public import after underscore rename"
---

<objective>
Wave 0 foundations for Phase 19.1: refactor smoke_seed.py underscore-private helpers to public names so the out-of-process live-stack seed (plan 03) can import them; scaffold scenario_runner package + scenarios/ directory; add Playwright + PyYAML to test deps; gitignore runner artifacts.

Purpose: Every downstream plan imports from or writes into these paths; they must exist before waves 1+ can run.
Output: Refactored smoke_seed, new package + dir scaffolding, updated pyproject + gitignore.
</objective>

<context>
@.planning/phases/19.1-ui-scenario-runner-playwright/19.1-CONTEXT.md
@.planning/phases/19.1-ui-scenario-runner-playwright/19.1-RESEARCH.md
@/opt/seo-platform/tests/fixtures/smoke_seed.py

<interfaces>
Current (to be refactored):
- async def _seed_core(session: AsyncSession) -> None
- async def _seed_extended(session: AsyncSession) -> None
Call sites inside smoke_seed.py fixture: `_seed_core(session)`, `_seed_extended(session)`.
Phase 15.1 does NOT import these directly (only the `smoke_seed` fixture), so the rename is internal-only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename smoke_seed helpers to public + scaffold scenario_runner package</name>
  <files>
    /opt/seo-platform/tests/fixtures/smoke_seed.py,
    /opt/seo-platform/tests/fixtures/scenario_runner/__init__.py,
    /opt/seo-platform/scenarios/.gitkeep
  </files>
  <action>
    1. In /opt/seo-platform/tests/fixtures/smoke_seed.py: rename `async def _seed_core` -> `async def seed_core` and `async def _seed_extended` -> `async def seed_extended`. Update the two call sites inside the `smoke_seed` fixture body (lines ~373-374) accordingly. Leave all other logic untouched. Add module docstring note: "seed_core / seed_extended are public so tests.fixtures.scenario_runner.seed can reuse them against the live stack (Phase 19.1)."
    2. Create /opt/seo-platform/tests/fixtures/scenario_runner/__init__.py with a one-line docstring: `"""Phase 19.1 YAML-driven Playwright scenario runner (pytest plugin)."""`
    3. Create /opt/seo-platform/scenarios/.gitkeep (empty file) so the scenarios/ directory exists at repo root for the pytest collector (plan 02) and P0 YAMLs (plan 05).

    Do NOT touch the fixture's SAVEPOINT machinery, TEST_DATABASE_URL, or SMOKE_IDS dict — only the two function names and their internal calls.
  </action>
  <verify>
    <automated>cd /opt/seo-platform && python -c "from tests.fixtures.smoke_seed import seed_core, seed_extended, SMOKE_IDS; print('ok')" &amp;&amp; test -f tests/fixtures/scenario_runner/__init__.py &amp;&amp; test -d scenarios</automated>
  </verify>
  <done>Public `seed_core` and `seed_extended` importable; Phase 15.1 smoke fixture still calls them internally; scenario_runner package + scenarios/ dir exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright + PyYAML to deps and gitignore runner artifacts</name>
  <files>
    /opt/seo-platform/pyproject.toml,
    /opt/seo-platform/.gitignore
  </files>
  <action>
    1. In /opt/seo-platform/pyproject.toml: under the existing test dep group (or dev deps — whichever already holds pytest/pytest-asyncio), add `playwright>=1.47,<2.0` and `pyyaml>=6.0`. If a dedicated `[dependency-groups.test]` or `[project.optional-dependencies].test` exists, add there. If only dev extras exist, add to that group. Do NOT create a new group if one already exists for tests.
    2. In /opt/seo-platform/.gitignore: append a `# Phase 19.1 scenario runner` section with: `artifacts/`, `**/storage_state.json`, `scenarios/__pycache__/`. De-dupe if any already present.
    3. Do NOT run `playwright install chromium` here — that happens inside the CI tester container (plan 04), which already has Chromium via the Microsoft base image. Local dev invocation is documented in plan 05's README.
  </action>
  <verify>
    <automated>cd /opt/seo-platform &amp;&amp; grep -q 'playwright' pyproject.toml &amp;&amp; grep -q 'pyyaml' pyproject.toml &amp;&amp; grep -q '^artifacts/' .gitignore &amp;&amp; grep -q 'storage_state.json' .gitignore</automated>
  </verify>
  <done>pyproject declares playwright>=1.47 and pyyaml>=6.0 in the test dep group; .gitignore excludes artifacts/ and storage_state.json.</done>
</task>

</tasks>

<success_criteria>
- `from tests.fixtures.smoke_seed import seed_core, seed_extended, SMOKE_IDS` succeeds
- Phase 15.1 smoke tests still pass (the rename is purely cosmetic for external importers)
- `tests/fixtures/scenario_runner/` package + `scenarios/` dir exist
- playwright and pyyaml in pyproject test deps
- artifacts/ and storage_state.json gitignored
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-ui-scenario-runner-playwright/19.1-01-SUMMARY.md`.
</output>
