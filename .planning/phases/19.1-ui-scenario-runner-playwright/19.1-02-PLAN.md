---
phase: 19.1-ui-scenario-runner-playwright
plan: 02
type: execute
wave: 2
depends_on: ["19.1-01"]
files_modified:
  - /opt/seo-platform/tests/fixtures/scenario_runner/schema.py
  - /opt/seo-platform/tests/fixtures/scenario_runner/collector.py
  - /opt/seo-platform/tests/fixtures/scenario_runner/executor.py
  - /opt/seo-platform/tests/fixtures/scenario_runner/test_schema.py
  - /opt/seo-platform/tests/fixtures/scenario_runner/test_collector.py
autonomous: true
requirements: [SCN-01, SCN-02]
must_haves:
  truths:
    - "A scenarios/*.yaml file is discovered as a pytest item (visible in `pytest --collect-only`)"
    - "A scenario with step `op: open` / `click` / `fill` / `wait_for` / `expect_text` / `expect_status` validates successfully via Pydantic v2"
    - "A scenario with reserved step `op: say` / `highlight` / `wait_for_click` also validates successfully (schema accepts reserved types)"
    - "A scenario with unknown `op` value fails validation with a discriminated-union error that names the offending step index"
    - "Executor's step dispatch skips reserved step types with a WARNING log (not a failure)"
  artifacts:
    - path: "/opt/seo-platform/tests/fixtures/scenario_runner/schema.py"
      provides: "Scenario model + Step discriminated union (9 step types: 6 P0 + 3 reserved)"
    - path: "/opt/seo-platform/tests/fixtures/scenario_runner/collector.py"
      provides: "pytest_collect_file hook, ScenarioFile, ScenarioItem"
    - path: "/opt/seo-platform/tests/fixtures/scenario_runner/executor.py"
      provides: "async run_scenario(page, scenario) step-dispatch skeleton (real Playwright ops wired in plan 03)"
  key_links:
    - from: "pytest_collect_file"
      to: "Scenario.model_validate"
      via: "yaml.safe_load -> pydantic v2"
    - from: "ScenarioItem.runtest"
      to: "asyncio.run(run_scenario(page, scenario))"
      via: "getfixturevalue('scenario_page') — fixture defined in plan 03"
---

<objective>
Build the schema + collector + executor skeleton. This is the static, browser-free foundation: Pydantic v2 discriminated union over step types, pytest custom collector that turns YAML files into pytest items, and an async executor whose dispatch table covers the 6 P0 step types and skips the 3 reserved 19.2 types with a warning.

Purpose: Separating this from Playwright runtime (plan 03) keeps the schema + collector pure-Python and unit-testable without browser launch. Once green, plan 03 can plug in real page operations.
Output: schema.py + collector.py + executor.py + unit tests for schema and collector.
</objective>

<context>
@.planning/phases/19.1-ui-scenario-runner-playwright/19.1-CONTEXT.md
@.planning/phases/19.1-ui-scenario-runner-playwright/19.1-RESEARCH.md

Research provides full code skeletons:
- Schema: RESEARCH.md Pattern 4 (Pydantic v2 discriminated union) — copy verbatim, do NOT redesign.
- Collector: RESEARCH.md Pattern 1 (`pytest_collect_file` + `ScenarioFile` + `ScenarioItem`) — copy verbatim.
- Executor dispatch: RESEARCH.md Pattern 3 wait_for helper + Pitfall 6 reserved-type skip-with-warning pattern.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pydantic v2 Scenario + Step discriminated union</name>
  <files>
    /opt/seo-platform/tests/fixtures/scenario_runner/schema.py,
    /opt/seo-platform/tests/fixtures/scenario_runner/test_schema.py
  </files>
  <behavior>
    - Valid P0 scenario (all 6 step types: open, click, fill, wait_for, expect_text, expect_status) validates without error.
    - Valid scenario with reserved types (say, highlight, wait_for_click) mixed in validates without error.
    - Unknown step type (`op: clik`) raises pydantic ValidationError naming the discriminator field and listing allowed values.
    - WaitForStep default: state="visible", timeout=30000 when omitted.
    - Scenario.steps is a list[Step] length >= 1.
  </behavior>
  <action>
    Create schema.py copying the discriminated-union skeleton from RESEARCH.md Pattern 4 verbatim (9 Step classes: OpenStep, ClickStep, FillStep, WaitForStep, ExpectTextStep, ExpectStatusStep, SayStep, HighlightStep, WaitForClickStep; `Step = Annotated[Union[...], Field(discriminator="op")]`; `Scenario(BaseModel)` with name, description, steps). Use `from __future__ import annotations` and `model_config = ConfigDict(extra="forbid")` on each Step to catch typos in YAML.

    Create test_schema.py with pytest tests matching the <behavior> block. Use inline dict fixtures passed to `Scenario.model_validate`. One test per behavior bullet.
  </action>
  <verify>
    <automated>cd /opt/seo-platform &amp;&amp; python -m pytest tests/fixtures/scenario_runner/test_schema.py -x -v</automated>
  </verify>
  <done>All schema tests pass; `Scenario.model_validate` accepts the 9 step types and rejects unknown ops with a clear error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pytest custom collector for scenarios/*.yaml</name>
  <files>
    /opt/seo-platform/tests/fixtures/scenario_runner/collector.py,
    /opt/seo-platform/tests/fixtures/scenario_runner/test_collector.py,
    /opt/seo-platform/scenarios/_test_fixture.yaml
  </files>
  <behavior>
    - `pytest --collect-only scenarios/` lists one ScenarioItem per YAML file under scenarios/.
    - A malformed YAML file (bad `op` value) produces a collection error that names the file and step index.
    - ScenarioItem exposes `scenario` attribute typed as `Scenario`.
    - Files outside `scenarios/` with `.yaml` extension are NOT collected (parent dir gate).
  </behavior>
  <action>
    1. Create collector.py copying RESEARCH.md Pattern 1 skeleton verbatim: `pytest_collect_file` hook (gate: `file_path.parent.name == "scenarios"` AND suffix `.yaml`), `ScenarioFile(pytest.File)` with `collect()` yielding `ScenarioItem.from_parent(self, name=scenario.name, scenario=scenario)`, `ScenarioItem(pytest.Item)` with `runtest`, `repr_failure`, `reportinfo`. For now, `runtest` calls `asyncio.run(run_scenario(None, self.scenario))` (executor handles None page by no-op for reserved-only scenarios used in tests; real page wiring happens in plan 03).
    2. Create `scenarios/_test_fixture.yaml` containing a minimal scenario with only reserved step types (so it can run via `runtest` without a real page):
       ```yaml
       name: collector_smoke
       description: collector unit-test fixture — reserved steps only
       steps:
         - op: say
           text: "collector works"
       ```
       **FORWARD NOTE:** the fixture YAML committed under `scenarios/` is TEMPORARY — plan 19.1-05 Task 0 will refactor test_collector.py to write its fixture inline via pytester `tmp_path` and delete `scenarios/_test_fixture.yaml` BEFORE the P0 scenarios land, so that the final `scenarios/` directory contains only real P0 YAMLs. Do not build any other tests or plans that depend on this file existing past plan 19.1-05 Task 0.
    3. Create test_collector.py using pytest's `pytester` fixture (or subprocess `pytest --collect-only`) to assert the fixture yaml is discovered as a ScenarioItem and that a malformed inline yaml produces a collection error. Register the collector plugin via `pytest_plugins = ["tests.fixtures.scenario_runner.collector"]` inside the pytester tmp conftest.
    4. Register the collector globally: append `pytest_plugins = ["tests.fixtures.scenario_runner.collector"]` to /opt/seo-platform/tests/conftest.py if not already present (check first — do not duplicate).
  </action>
  <verify>
    <automated>cd /opt/seo-platform &amp;&amp; python -m pytest tests/fixtures/scenario_runner/test_collector.py -x -v &amp;&amp; python -m pytest --collect-only scenarios/ 2>&amp;1 | grep -q 'collector_smoke'</automated>
  </verify>
  <done>pytest collects scenarios/_test_fixture.yaml as a ScenarioItem; malformed YAML fails collection with a named error; collector plugin registered in top-level conftest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Executor skeleton with reserved-type skip-with-warning</name>
  <files>
    /opt/seo-platform/tests/fixtures/scenario_runner/executor.py,
    /opt/seo-platform/tests/fixtures/scenario_runner/test_executor.py
  </files>
  <behavior>
    - `run_scenario(None, scenario_with_only_reserved_steps)` completes without error and emits one WARNING log per reserved step (via loguru).
    - `run_scenario(None, scenario_with_p0_step)` raises NotImplementedError (real ops wired in plan 03) — placeholder so plan 03 knows where to hook in.
    - Dispatch table explicitly enumerates all 9 step types; unknown `op` would be unreachable because pydantic already rejected it at parse time.
  </behavior>
  <action>
    Create executor.py with:
    ```python
    from loguru import logger
    from .schema import Scenario, SayStep, HighlightStep, WaitForClickStep

    RESERVED_OPS = {"say", "highlight", "wait_for_click"}

    async def run_scenario(page, scenario: Scenario) -> None:
        for idx, step in enumerate(scenario.steps):
            if step.op in RESERVED_OPS:
                logger.warning(
                    f"scenario={scenario.name} step[{idx}] op='{step.op}' "
                    f"reserved for 19.2 tour player — skipping"
                )
                continue
            # P0 step dispatch wired in plan 03 (Playwright runtime)
            raise NotImplementedError(
                f"step op='{step.op}' not yet wired — see plan 19.1-03"
            )
    ```
    Create test_executor.py with two tests matching the <behavior> block. Use `caplog` or loguru's testing hook to assert the warning was logged.
  </action>
  <verify>
    <automated>cd /opt/seo-platform &amp;&amp; python -m pytest tests/fixtures/scenario_runner/test_executor.py -x -v</automated>
  </verify>
  <done>Executor skeleton skips reserved steps with a warning, raises NotImplementedError on P0 steps (pending plan 03 wiring); tests green.</done>
</task>

</tasks>

<success_criteria>
- `pytest tests/fixtures/scenario_runner/ -x` is fully green (schema + collector + executor unit tests)
- `pytest --collect-only scenarios/` lists `collector_smoke` from the test fixture YAML
- Collector plugin registered in /opt/seo-platform/tests/conftest.py
- SCN-01 (collector) and SCN-02 (schema with reserved types) are satisfied at the unit-test level
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-ui-scenario-runner-playwright/19.1-02-SUMMARY.md`.
</output>
