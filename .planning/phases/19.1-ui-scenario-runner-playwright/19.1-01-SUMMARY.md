---
phase: 19.1-ui-scenario-runner-playwright
plan: 01
subsystem: test-infra
tags: [playwright, scenario-runner, foundation, wave-0]
requires: []
provides:
  - "tests.fixtures.smoke_seed.seed_core (public)"
  - "tests.fixtures.smoke_seed.seed_extended (public)"
  - "tests.fixtures.scenario_runner package"
  - "scenarios/ directory"
  - "playwright>=1.47, pyyaml>=6.0 test deps"
affects:
  - "/opt/seo-platform/tests/fixtures/smoke_seed.py"
tech-stack:
  added: [playwright>=1.47, pyyaml>=6.0]
  patterns: []
key-files:
  created:
    - /opt/seo-platform/tests/fixtures/scenario_runner/__init__.py
    - /opt/seo-platform/scenarios/.gitkeep
  modified:
    - /opt/seo-platform/tests/fixtures/smoke_seed.py
    - /opt/seo-platform/pyproject.toml
    - /opt/seo-platform/.gitignore
decisions: []
metrics:
  duration_min: 3
  completed: 2026-04-08
  tasks: 2
  files: 5
---

# Phase 19.1 Plan 01: Wave 0 Foundations Summary

**One-liner:** Public seed_core/seed_extended helpers, scenario_runner package scaffold, and Playwright+PyYAML test deps prepare the repo for the YAML scenario runner.

## What Shipped

- Renamed `_seed_core` / `_seed_extended` → public `seed_core` / `seed_extended` in `tests/fixtures/smoke_seed.py`; updated both internal call sites inside the `smoke_seed` fixture. Docstring note added explaining the public rename is for Phase 19.1 reuse against the live stack.
- Created `tests/fixtures/scenario_runner/__init__.py` package marker with one-line docstring.
- Created `scenarios/.gitkeep` so the repo-root scenarios directory is committed and discoverable by the future pytest collector (plan 02) and P0 YAML scenarios (plan 05).
- Added `[dependency-groups].test` in `pyproject.toml` declaring `playwright>=1.47,<2.0` and `pyyaml>=6.0` (no existing test dep group existed; new group created).
- Gitignored `artifacts/`, `**/storage_state.json`, and `scenarios/__pycache__/` under a dedicated "Phase 19.1 scenario runner" section of `.gitignore`.

## Verification

- `python -c "from tests.fixtures.smoke_seed import seed_core, seed_extended, SMOKE_IDS"` → ok
- `tests/fixtures/scenario_runner/__init__.py` exists
- `scenarios/` directory exists
- `pyproject.toml` contains `playwright` and `pyyaml`
- `.gitignore` contains `artifacts/` and `storage_state.json`

All automated verifies from both tasks passed.

## Commits

- `06cfb0d` refactor(19.1-01): public seed_core/seed_extended + scenario_runner scaffold
- `74de73b` chore(19.1-01): add playwright+pyyaml test deps, gitignore runner artifacts

Note: commits landed in the `/opt/seo-platform` repo (application repo). Planning artifacts live in `/projects/test`.

## Deviations from Plan

None — plan executed exactly as written. The pyproject.toml had no pre-existing test dep group, so a new `[dependency-groups].test` group was created (permitted by the plan's instructions which only forbid duplicate groups).

## Self-Check: PASSED

- FOUND: /opt/seo-platform/tests/fixtures/smoke_seed.py (modified)
- FOUND: /opt/seo-platform/tests/fixtures/scenario_runner/__init__.py
- FOUND: /opt/seo-platform/scenarios/.gitkeep
- FOUND: /opt/seo-platform/pyproject.toml (modified)
- FOUND: /opt/seo-platform/.gitignore (modified)
- FOUND commit: 06cfb0d
- FOUND commit: 74de73b
