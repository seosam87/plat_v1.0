---
phase: 19.1-ui-scenario-runner-playwright
plan: 05
type: execute
wave: 5
depends_on: ["19.1-04"]
files_modified:
  - /opt/seo-platform/tests/fixtures/scenario_runner/test_collector.py
  - /opt/seo-platform/scenarios/_test_fixture.yaml
  - /opt/seo-platform/scenarios/01-suggest-to-results.yaml
  - /opt/seo-platform/scenarios/02-site-form-submit.yaml
  - /opt/seo-platform/scenarios/README.md
autonomous: true
requirements: [SCN-08, SCN-09, SCN-10]
must_haves:
  truths:
    - "scenarios/01-suggest-to-results.yaml exercises the HTMX polling pattern: submit suggest job -> wait for result rows -> assert content"
    - "scenarios/02-site-form-submit.yaml exercises a synchronous form submit: fill create-site form -> submit -> assert new row visible"
    - "Both scenarios pass end-to-end via `bash scripts/run-scenarios-ci.sh`"
    - "scenarios/ contains ONLY real P0 scenarios — no committed test fixture YAMLs; test_collector.py writes its fixture inline via pytester tmp_path"
    - "scenarios/README.md documents: schema overview, step types, reserved 19.2 types, local run command, CI entrypoint, how 19.2 tour player consumes the same files"
    - "On intentional failure (forced selector typo), the runner writes artifacts/scenarios/{name}/failure.png and trace.zip"
  artifacts:
    - path: "/opt/seo-platform/scenarios/01-suggest-to-results.yaml"
      provides: "P0 scenario 1 — HTMX polling"
    - path: "/opt/seo-platform/scenarios/02-site-form-submit.yaml"
      provides: "P0 scenario 2 — form submit"
    - path: "/opt/seo-platform/scenarios/README.md"
      provides: "User-facing runner documentation"
  key_links:
    - from: "scenarios/01-suggest-to-results.yaml"
      to: "seeded smoke_admin + suggest endpoint"
      via: "storage_state auth + HTTP form submit"
    - from: "scenarios/*.yaml"
      to: "19.2 tour player (future)"
      via: "shared schema with reserved step types documented in README"
---

<objective>
Ship the 2 P0 scenarios that validate the runner end-to-end in CI, plus the README that makes the YAML schema a public contract for the 19.2 tour player. This is the phase-gate plan: if it's green, the runner delivers its promised value; if red, there's a concrete failing scenario to debug.

Purpose: Everything before this plan is infrastructure; this plan is the proof that the infrastructure works.
Output: 2 YAMLs, 1 README, 1 green CI run, clean scenarios/ directory (no leftover test fixtures).
</objective>

<context>
@.planning/phases/19.1-ui-scenario-runner-playwright/19.1-CONTEXT.md
@.planning/phases/19.1-ui-scenario-runner-playwright/19.1-RESEARCH.md
@/opt/seo-platform/tests/fixtures/scenario_runner/schema.py
@/opt/seo-platform/tests/fixtures/scenario_runner/executor.py
@/opt/seo-platform/tests/fixtures/scenario_runner/test_collector.py

Scenario 1 wait pattern is specified in RESEARCH.md Pattern 3 (the suggest→results example with `hx-trigger="every 2s"` polling — timeout 60s on `#suggest-results tr.result-row >> nth=0`).

Actual route paths and form field names must be discovered from the app templates at planning/execution time — the planner should NOT guess. Executor inspects:
- `/opt/seo-platform/app/templates/suggest/` (or wherever suggest UI lives — grep for `hx-post` + `suggest`)
- `/opt/seo-platform/app/templates/sites/` for the create-site form

Auth: storage_state.json is generated by the `storage_state` fixture (plan 03) using username=`smoke_admin`, password=`smoke-password` per smoke_seed.py SMOKE_IDS. Login URL = `/ui/login` per Phase 15.1 SMOKE_SKIP entry.
</context>

<tasks>

<task type="auto">
  <name>Task 0: Refactor test_collector.py to inline tmp_path fixture, then delete _test_fixture.yaml</name>
  <files>
    /opt/seo-platform/tests/fixtures/scenario_runner/test_collector.py,
    /opt/seo-platform/scenarios/_test_fixture.yaml
  </files>
  <action>
    Plan 19.1-02 Task 2 committed `scenarios/_test_fixture.yaml` so the collector test had something to discover. Before the real P0 YAMLs land (Task 1), we MUST remove that stub — but simply deleting it would break test_collector.py. Refactor order is therefore strict:

    1. Open `/opt/seo-platform/tests/fixtures/scenario_runner/test_collector.py`. Rewrite the collector-discovery test to use pytest's `pytester` fixture and write the fixture YAML INLINE to `tmp_path` (or the pytester-managed scenarios dir) at test runtime. The body should look like:

       ```python
       def test_collector_discovers_scenario(pytester):
           pytester.makefile(
               ".yaml",
               **{
                   "scenarios/_inline_fixture": (
                       "name: collector_smoke\n"
                       "description: inline collector unit-test fixture\n"
                       "steps:\n"
                       "  - op: say\n"
                       "    text: \"collector works\"\n"
                   )
               },
           )
           pytester.makeconftest(
               'pytest_plugins = ["tests.fixtures.scenario_runner.collector"]'
           )
           result = pytester.runpytest("--collect-only", "scenarios/")
           result.stdout.fnmatch_lines(["*collector_smoke*"])
       ```

       The malformed-YAML test should do the same pattern: write a file with `op: clik` inline and assert the collection error names the file + step index.

       No test in this file may reference `scenarios/_test_fixture.yaml` or any path under the repo's real `scenarios/` directory.

    2. Run the rewritten tests and confirm green BEFORE deleting the stub:
       `cd /opt/seo-platform && python -m pytest tests/fixtures/scenario_runner/test_collector.py -x -v`

    3. Delete `/opt/seo-platform/scenarios/_test_fixture.yaml`.

    4. Re-run test_collector.py to confirm it is still green AFTER deletion:
       `cd /opt/seo-platform && python -m pytest tests/fixtures/scenario_runner/test_collector.py -x -v`

    5. Confirm no lingering references anywhere in the repo:
       `grep -rn _test_fixture /opt/seo-platform/scenarios/ /opt/seo-platform/tests/` — MUST return nothing.
  </action>
  <verify>
    <automated>cd /opt/seo-platform &amp;&amp; python -m pytest tests/fixtures/scenario_runner/test_collector.py -x -v &amp;&amp; ! test -e scenarios/_test_fixture.yaml &amp;&amp; ! grep -rn _test_fixture scenarios/ tests/</automated>
  </verify>
  <done>test_collector.py writes its fixture YAML inline via pytester tmp_path; `scenarios/_test_fixture.yaml` deleted; `grep -r _test_fixture scenarios/ tests/` returns nothing; collector tests still green.</done>
</task>

<task type="auto">
  <name>Task 1: Write P0 scenario YAMLs</name>
  <files>
    /opt/seo-platform/scenarios/01-suggest-to-results.yaml,
    /opt/seo-platform/scenarios/02-site-form-submit.yaml
  </files>
  <action>
    (Task 0 has already removed `scenarios/_test_fixture.yaml` and rewired test_collector.py — do NOT attempt to delete it again here.)

    1. Discover actual route + selector details:
       - `grep -rn 'hx-post' /opt/seo-platform/app/templates/ | grep -i suggest` to find the suggest form route and input name
       - Find the results container id: `grep -rn 'suggest-results\|id="suggest' /opt/seo-platform/app/templates/`
       - `grep -rn 'hx-post\|action=' /opt/seo-platform/app/templates/sites/ | head` for the create-site form
       - Log findings in-line as YAML comments so future editors understand why selectors were chosen

    2. Create scenarios/01-suggest-to-results.yaml:
       ```yaml
       name: suggest-to-results
       description: Submit a suggest job and wait for HTMX polling to render result rows
       steps:
         - op: open
           url: "/ui/suggest"                      # adjust per discovery
         - op: fill
           target: "label=Seed phrase"             # or CSS fallback if label missing
           value: "smoke seed"
         - op: click
           target: 'role=button[name="Run suggest"]'
         - op: wait_for
           target: "#suggest-results tr.result-row >> nth=0"
           state: visible
           timeout: 60000
         - op: expect_text
           target: "#suggest-results"
           contains: "smoke seed"
       ```
       Note: the seeded `SuggestJob` row (id 5555...) already has `status=complete` and `seed="smoke seed"` per smoke_seed.py — the scenario may not need to trigger a NEW job; it could just navigate to the suggest results page for the existing job. Planner/executor should choose based on what the suggest UI actually exposes. DOCUMENT the choice in a YAML comment. The HTMX polling pattern is exercised either way if the results page uses `hx-trigger="load, every Ns"`.

    3. Create scenarios/02-site-form-submit.yaml:
       ```yaml
       name: site-form-submit
       description: Create a new site via the create-site form and assert it renders in the list
       steps:
         - op: open
           url: "/ui/sites/new"                    # adjust per discovery
         - op: fill
           target: "label=Name"
           value: "Scenario Smoke Site"
         - op: fill
           target: "label=URL"
           value: "https://scenario-smoke.example.com"
         - op: click
           target: 'role=button[name="Create"]'
         - op: wait_for
           target: 'text=Scenario Smoke Site'
           state: visible
           timeout: 10000
       ```
       The seeded `smoke_admin` has admin role so this should succeed. Side-effect: a new Site row committed to the dev DB — acceptable since the scenario runs against the dev/CI DB, not prod.

    4. Run the full CI pipeline locally to confirm both scenarios pass:
       `bash /opt/seo-platform/scripts/run-scenarios-ci.sh`
  </action>
  <verify>
    <automated>cd /opt/seo-platform &amp;&amp; bash scripts/run-scenarios-ci.sh</automated>
  </verify>
  <done>Both P0 scenarios pass green in the CI tester container.</done>
</task>

<task type="auto">
  <name>Task 2: scenarios/README.md</name>
  <files>
    /opt/seo-platform/scenarios/README.md
  </files>
  <action>
    Write scenarios/README.md (Russian primary per user preference, English technical terms OK) covering:

    1. **Назначение** — YAML scenario runner, one source of truth for Phase 19.1 tests and Phase 19.2 tour player
    2. **Схема YAML** — `name`, `description`, `steps: list[Step]`; reference to `tests/fixtures/scenario_runner/schema.py` as the authoritative definition
    3. **Step типы (P0)** — open / click / fill / wait_for / expect_text / expect_status, with one-line description + minimal example for each
    4. **Зарезервированные step типы** — say / highlight / wait_for_click: принимаются схемой, runner 19.1 пропускает с WARNING, будут использованы 19.2 tour player. Не меняйте их синтаксис в будущем без обновления обоих runners.
    5. **Локатор-синтаксис target:** — `role=button[name="..."]`, `text=...`, `label=...`, `testid=...`, `#css` / `.css` fallback
    6. **Локальный запуск** —
       - Против уже запущенного стека: `BASE_URL=http://localhost:8000 pytest scenarios/ -v`
       - Полный CI pipeline: `bash scripts/run-scenarios-ci.sh`
    7. **Артефакты при падении** — `artifacts/scenarios/{scenario_name}/failure.png` + `trace.zip`; открыть trace: `playwright show-trace artifacts/scenarios/<name>/trace.zip`
    8. **Как добавить сценарий** — create `scenarios/NN-name.yaml`, pytest collects automatically, no registration
    9. **Связь с Phase 19.2** — один и тот же файл загружается frontend tour player; добавляйте `say` / `highlight` / `wait_for_click` там, где нужны tour-шаги, они не сломают test runner
  </action>
  <verify>
    <automated>test -f /opt/seo-platform/scenarios/README.md &amp;&amp; grep -q 'wait_for_click' /opt/seo-platform/scenarios/README.md &amp;&amp; grep -q 'run-scenarios-ci.sh' /opt/seo-platform/scenarios/README.md</automated>
  </verify>
  <done>README documents schema, step types (P0 + reserved), locator syntax, local + CI run commands, artifact paths, and 19.2 handoff contract.</done>
</task>

</tasks>

<success_criteria>
- `bash scripts/run-scenarios-ci.sh` exits 0 with both P0 scenarios passing
- SCN-08 / SCN-09 / SCN-10 satisfied
- Manually forcing a selector typo in 01-suggest-to-results.yaml produces `artifacts/scenarios/suggest-to-results/failure.png` + `trace.zip` (verify then revert the typo)
- Phase 19.1 phase-gate met: runner exists, 2 P0 scenarios pass in CI, YAML contract documented for 19.2 tour player
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-ui-scenario-runner-playwright/19.1-05-SUMMARY.md` and update ROADMAP.md Phase 19.1 entry to `[x]`.
</output>
